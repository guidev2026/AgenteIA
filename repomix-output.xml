This file is a merged representation of the entire codebase, combined into a single document by Repomix.
The content has been processed where content has been compressed (code blocks are separated by ⋮---- delimiter).

================================================================
File Summary
================================================================

Purpose:
--------
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.

File Format:
------------
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  a. A separator line (================)
  b. The file path (File: path/to/file)
  c. Another separator line
  d. The full contents of the file
  e. A blank line

Usage Guidelines:
-----------------
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.

Notes:
------
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Content has been compressed - code blocks are separated by ⋮---- delimiter
- Files are sorted by Git change count (files with more changes are at the bottom)


================================================================
Directory Structure
================================================================
packages/
  core/
    src/
      cli/
        strategies/
          ChatStrategy.ts
          index.ts
          ReActStrategy.ts
          StreamStrategy.ts
        commands.ts
        index.ts
      core/
        rag/
          graph/
            ASTRelationshipExtractor.ts
            GraphBuilder.ts
            GraphRAGManager.ts
            IGraphQuery.ts
            IGraphStore.ts
            index.ts
            IRelationshipExtractor.ts
            JsonGraphStore.ts
            types.ts
          ASTChunkerService.ts
          Chunker.ts
          Embedder.ts
          IASTParser.ts
          IChunker.ts
          index.ts
          RAGManager.ts
          ReActLoop.ts
          Retriever.ts
          TypescriptASTAdapter.ts
          VectorStore.ts
        AppContext.ts
        ASTEditor.ts
        astUtils.ts
        CommandExecutor.ts
        ErrorJournal.ts
        FileReader.ts
        IContextCompressor.ts
        index.ts
        ProviderFactory.ts
        Reflector.ts
        SearchReplaceEditor.ts
        SessionManager.ts
        SessionStore.ts
        StatefulCompressor.ts
        TokenEstimator.ts
        ToolRegistry.ts
      providers/
        index.ts
        OllamaHttpClient.ts
        OllamaProvider.ts
        types.ts
      validation/
        JsonValidator.ts
      index.ts
    tests/
      integration/
        editing-pipeline.test.ts
      unit/
        cli/
          ChatStrategy.test.ts
          commands.test.ts
        core/
          ASTEditor.test.ts
          CommandExecutor.test.ts
          ErrorJournal.test.ts
          IContextCompressor.test.ts
          Reflector.test.ts
          SearchReplaceEditor.test.ts
          SessionManager.test.ts
          SessionStore.test.ts
          StatefulCompressor.test.ts
          TokenEstimator.test.ts
          ToolRegistry.test.ts
        graph/
          ASTRelationshipExtractor.test.ts
          GraphBuilder.test.ts
          GraphRAGManager.test.ts
          JsonGraphStore.test.ts
        providers/
          OllamaProvider.test.ts
        rag/
          ASTChunkerService.test.ts
          Chunker.test.ts
          ReActLoop.test.ts
          Retriever.test.ts
          TypescriptASTAdapter.test.ts
        validation/
          JsonValidator.test.ts
    package.json
    repomix-output.xml
    tsconfig.json
    tsconfig.tsbuildinfo
    vitest.config.ts
  shell/
    electron/
      dev-runner.ts
      main.ts
      preload.ts
    src/
      App.tsx
      main.tsx
      vite-env.d.ts
    index.html
    package.json
    repomix-output.xml
    tsconfig.electron.json
    tsconfig.json
    vite.config.ts
.clinerules
.gitignore
DOCUMENTACAO_PROJETO.md
package.json

================================================================
Files
================================================================

================
File: packages/core/repomix-output.xml
================
This file is a merged representation of the entire codebase, combined into a single document by Repomix.
The content has been processed where content has been compressed (code blocks are separated by ⋮---- delimiter).

================================================================
File Summary
================================================================

Purpose:
--------
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.

File Format:
------------
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  a. A separator line (================)
  b. The file path (File: path/to/file)
  c. Another separator line
  d. The full contents of the file
  e. A blank line

Usage Guidelines:
-----------------
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.

Notes:
------
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Content has been compressed - code blocks are separated by ⋮---- delimiter
- Files are sorted by Git change count (files with more changes are at the bottom)


================================================================
Directory Structure
================================================================
src/
  cli/
    strategies/
      ChatStrategy.ts
      index.ts
      ReActStrategy.ts
      StreamStrategy.ts
    commands.ts
    index.ts
  core/
    rag/
      graph/
        ASTRelationshipExtractor.ts
        GraphBuilder.ts
        GraphRAGManager.ts
        IGraphQuery.ts
        IGraphStore.ts
        index.ts
        IRelationshipExtractor.ts
        JsonGraphStore.ts
        types.ts
      ASTChunkerService.ts
      Chunker.ts
      Embedder.ts
      IASTParser.ts
      IChunker.ts
      index.ts
      RAGManager.ts
      ReActLoop.ts
      Retriever.ts
      TypescriptASTAdapter.ts
      VectorStore.ts
    AppContext.ts
    ASTEditor.ts
    astUtils.ts
    CommandExecutor.ts
    ErrorJournal.ts
    FileReader.ts
    IContextCompressor.ts
    index.ts
    ProviderFactory.ts
    Reflector.ts
    SearchReplaceEditor.ts
    SessionManager.ts
    SessionStore.ts
    StatefulCompressor.ts
    TokenEstimator.ts
    ToolRegistry.ts
  providers/
    index.ts
    OllamaHttpClient.ts
    OllamaProvider.ts
    types.ts
  validation/
    JsonValidator.ts
  index.ts
tests/
  integration/
    editing-pipeline.test.ts
  unit/
    cli/
      ChatStrategy.test.ts
      commands.test.ts
    core/
      ASTEditor.test.ts
      CommandExecutor.test.ts
      ErrorJournal.test.ts
      IContextCompressor.test.ts
      Reflector.test.ts
      SearchReplaceEditor.test.ts
      SessionManager.test.ts
      SessionStore.test.ts
      StatefulCompressor.test.ts
      TokenEstimator.test.ts
      ToolRegistry.test.ts
    graph/
      ASTRelationshipExtractor.test.ts
      GraphBuilder.test.ts
      GraphRAGManager.test.ts
      JsonGraphStore.test.ts
    providers/
      OllamaProvider.test.ts
    rag/
      ASTChunkerService.test.ts
      Chunker.test.ts
      ReActLoop.test.ts
      Retriever.test.ts
      TypescriptASTAdapter.test.ts
    validation/
      JsonValidator.test.ts
package.json
tsconfig.json
tsconfig.tsbuildinfo
vitest.config.ts

================================================================
Files
================================================================

================
File: src/cli/strategies/ChatStrategy.ts
================
/**
 * ChatStrategy — Interface Strategy para modos de chat no CLI.
 *
 * SRP: Cada estratégia encapsula um pipeline de chat diferente:
 *   - StreamStrategy: streaming direto (sem ReAct)
 *   - ReActStrategy: ReAct loop (com ou sem streaming pós-resposta)
 *
 * OCP: Novos modos de chat são adicionados criando novas classes
 * que implementam ChatStrategy, sem modificar as existentes.
 */
⋮----
import { AppContext, ReActLoop, RAGManager } from '../../core';
import type { IProvider } from '../../providers/types';
⋮----
/** Contexto compartilhado entre todas as estratégias de chat */
export interface ChatContext {
  app: AppContext;
  prompt: string;
  streamMode: boolean;
  noThink: boolean;
  /** Se true, aplica Reflector (self-correction) pós-resposta */
  reflectMode: boolean;
}
⋮----
/** Se true, aplica Reflector (self-correction) pós-resposta */
⋮----
/** Interface base para todas as estratégias de chat */
export interface ChatStrategy {
  /** Executa o pipeline de chat e retorna a resposta final formatada */
  execute(ctx: ChatContext): Promise<string>;
}
⋮----
/** Executa o pipeline de chat e retorna a resposta final formatada */
execute(ctx: ChatContext): Promise<string>;
⋮----
/**
 * Constrói o system prompt completo (tool definitions + RAG + JSON rules).
 *
 * Extraída para função pura (SRP) — testável isoladamente.
 *
 * @param app     AppContext (provider, toolRegistry, commandExecutor, etc.)
 * @param prompt  Prompt do usuário
 * @param jsonMode Se true, adiciona regras de resposta em formato JSON
 * @param ragDir  Diretório RAG opcional
 * @param skipPromptSuffix Se true, não adiciona o sufixo "Pergunta do usuário" —
 *                         útil quando o prompt já está no histórico da sessão
 * @returns System prompt completo
 */
export async function buildSystemPrompt(
  app: AppContext,
  prompt: string,
  jsonMode: boolean,
  ragDir?: string,
  skipPromptSuffix?: boolean
): Promise<string>
⋮----
// ── RAG (Retrieval-Augmented Generation) ──
⋮----
// ── System Prompt ──
⋮----
// ── WORKFLOW DE ENGENHARIA OBRIGATÓRIO (sempre presente) ──
⋮----
// ── Bloco condicional de workflow de edição ──
⋮----
// Injeta contexto RAG no system prompt, se houver
⋮----
// Se --json estiver ativo, usa formato tool_call / final_response

================
File: src/cli/strategies/index.ts
================


================
File: src/cli/strategies/ReActStrategy.ts
================
/**
 * ReActStrategy — Estratégia de chat com ReAct Loop (Reasoning + Acting).
 *
 * Fluxo:
 *   1. buildSystemPrompt() → system prompt completo (com RAG se --rag)
 *   2. Delega para ReActLoop.execute() — loop de raciocínio com ferramentas
 *   3. Se --stream + streamChat disponível: faz streaming da resposta final
 *   4. Se --no-think: suprime o indicador "🤔 Pensando..."
 *
 * SRP: Responsabilidade única — gerenciar o pipeline ReAct.
 * OCP: Não precisa ser modificada para novos modos de chat.
 */
⋮----
import type { ChatStrategy, ChatContext } from './ChatStrategy';
import { buildSystemPrompt } from './ChatStrategy';
import type { ICritiqueProvider } from '../../providers/types';
⋮----
/** Type guard: verifica se provider implementa ICritiqueProvider em runtime */
function isCritiqueProvider(p: unknown): p is ICritiqueProvider
import { ReActLoop, CommandExecutor, Reflector, ErrorJournal, StatefulCompressor } from '../../core';
⋮----
export class ReActStrategy implements ChatStrategy
⋮----
async execute(ctx: ChatContext): Promise<string>
⋮----
// Obtém histórico da sessão ativa (vazio se não houver sessão)
⋮----
// Constrói o system prompt completo (RAG + rules)
// skipPromptSuffix=true quando há histórico, pois o prompt já está lá
⋮----
history.length > 0 // skipPromptSuffix
⋮----
// Cria o Reflector (self-correction) se --reflect estiver ativo
// Type guard verifica se provider implementa ICritiqueProvider em runtime
⋮----
// ErrorJournal: persistência de erros de reflexão (TASK 6)
// Usado internamente para registrar correções; exposto com --verbose
⋮----
// Indicador visual de reflexão (vai para stderr para não poluir stdout)
⋮----
// Cria o ReActLoop com dependências injetadas
// - jsonMode = true: usa toolRegistry (JSON tool calls)
// - jsonMode = false: usa commandExecutor (texto ACTION/FINAL_ANSWER)
// StatefulCompressor: compressão de contexto para evitar estouro de tokens
⋮----
reflector, // injeta o Reflector (undefined se --reflect não ativo)
compressor // injeta o compressor de contexto
⋮----
// Indicador visual de "pensamento" durante o ReAct
⋮----
// Prepara opções de streaming (efeito "máquina de escrever")
⋮----
// Executa o loop ReAct com histórico da sessão multi-turn
⋮----
history, // histórico real da sessão (multi-turn)
⋮----
// ── REGISTRO NO ERRORJOURNAL ──
// Persiste erros de reflexão para auditoria (TASK 6)
⋮----
// ── INDICADOR PÓS-REACT ──
// Mostra iterações e status de correção no stderr (não polui stdout)
⋮----
// Flag --verbose: estatísticas detalhadas do ErrorJournal (TASK 7)
⋮----
// Propaga CorrectionStatus (TASK 4)
⋮----
// ── RESPOSTA FINAL ──
// Se --stream está ativo, o streaming já foi feito inline pelo ReActLoop
// via callback onToken. A resposta final é retornada normalmente.
// Inclui badge de correção se aplicável
⋮----
// Streaming já foi feito pelo ReActLoop durante a execução.
// Só precisamos do \n final e do badge de correção no stderr
⋮----
return ''; // Já escrevemos os tokens no stdout via onToken
⋮----
// Modo normal (sem streaming): retorna a resposta formatada

================
File: src/cli/strategies/StreamStrategy.ts
================
/**
 * StreamStrategy — Estratégia de chat com streaming direto (efeito máquina de escrever).
 *
 * Fluxo:
 *   1. buildSystemPrompt() → system prompt completo (com RAG se --rag)
 *   2. Itera sobre provider.streamChat() escrevendo tokens no stdout
 *   3. Se streamChat não estiver disponível ou falhar, faz fallback para chat()
 *
 * SRP: Responsabilidade única — gerenciar o pipeline de streaming puro.
 * OCP: Não precisa ser modificada para novos modos de chat.
 */
⋮----
import type { ChatStrategy, ChatContext } from './ChatStrategy';
import { buildSystemPrompt } from './ChatStrategy';
⋮----
export class StreamStrategy implements ChatStrategy
⋮----
async execute(ctx: ChatContext): Promise<string>
⋮----
// Validação: streaming direto não é compatível com --json
⋮----
// Obtém o histórico multi-turn da sessão ativa
⋮----
// Constrói o system prompt completo (RAG + tools)
// skipPromptSuffix=true quando há histórico, pois o prompt já está lá
⋮----
// Injeta histórico multi-turn no prompt, se houver
// Formato: [USER]/[ASSISTANT] blocks para o modelo entender o contexto
⋮----
// Se não houver histórico, usa systemPrompt direto (já tem o sufixo "Pergunta do usuário")
// Se houver histórico, o buildSystemPrompt pulou o sufixo, mas a mensagem do usuário
// já foi adicionada ao histórico pelo commands.ts ANTES de chamar execute().
// Portanto, o historyBlock já contém a pergunta — adicioná-la novamente duplicaria.
⋮----
// Indica o modelo no início da resposta
⋮----
// Se streamMode está desativado, ou streamChat não está disponível,
// faz uma chamada normal e escreve de uma vez
⋮----
// Streaming real (efeito máquina de escrever)
⋮----
// Fallback para chat normal se streaming falhar

================
File: src/cli/commands.ts
================
/**
 * commands.ts — Roteador de comandos CLI com Injeção de Dependência (DIP).
 *
 * Fluxo:
 *   parseArgs (index.ts) → CliArgs → buildConfig() → AppContext
 *   ↓
 *   runCommand() usa AppContext para acessar:
 *     - fileReader, commandExecutor, provider, toolRegistry
 *
 * DIP: Nenhum comando instancia diretamente FileReader, OllamaProvider, etc.
 *       Tudo é injetado via AppContext → ProviderFactory → interfaces.
 */
⋮----
import { AppContext } from '../core';
import { StreamStrategy, ReActStrategy } from './strategies';
⋮----
/**
 * Estrutura que representa os argumentos parseados da linha de comando.
 *
 * Exemplo: `soberano chat "Explique SOLID" --model phi3:3b`
 *   command = "chat"
 *   args    = ["Explique SOLID"]
 *   flags   = { model: "phi3:3b" }
 *
 * Flags booleanas:
 *   --json     → ativa tool_call/final_response em JSON
 *   --stream   → ativa streaming SSE (efeito máquina de escrever)
 *   --no-think → desativa o indicador "pensando..." (útil para scripting)
 *   --reflect  → ativa Reflector (auto-revisão) na resposta final
 */
export interface CliArgs {
  command: string;
  args: string[];
  flags: Record<string, string | boolean>;
}
⋮----
/**
 * Constrói a configuração do AppContext a partir dos argumentos CLI parseados.
 *
 * DIP (Dependency Inversion Principle):
 * - AppContext gerencia a criação e injeção de TODAS as dependências.
 * - O comando não instancia diretamente providers, readers ou executors.
 * - Basta mudar a config para trocar de provider ou config.
 *
 * Flags de sessão:
 *   --session <id>    → carrega uma sessão existente pelo UUID
 *   --new-session     → força a criação de uma nova sessão (ignora --session)
 */
function buildConfig(parsed: CliArgs): import('../core').AppContextConfig
⋮----
/**
 * Cria um novo AppContext a partir dos argumentos parseados.
 *
 * Cada chamada de comando recebe um AppContext limpo (sem cache de execuções
 * anteriores). Isso garante que flags como --json, --stream, --model sejam
 * respeitadas independentemente da ordem de execução.
 *
 * @param parsed Argumentos parseados da linha de comando
 * @returns AppContext configurado e inicializado
 */
async function createContext(parsed: CliArgs): Promise<AppContext>
⋮----
/**
 * Função central de roteamento da CLI.
 *
 * Fluxo geral de dados:
 * ┌──────────┐    CliArgs     ┌──────────────────┐    comando     ┌─────────────────┐
 * │  index.ts│ ── parse ──▶  │  runCommand()     │ ── switch ──▶ │  FileReader /   │
 * │ (argv)   │              │                    │              │  CmdExecutor /  │
 * └──────────┘              └──────────────────┘              │  OllamaProvider │
 *                                    │                        └─────────────────┘
 *                                    │ string (output)
 *                                    ▼
 *                              ┌──────────┐
 *                              │  console │
 *                              └──────────┘
 *
 * @param parsed Argumentos parseados da linha de comando
 * @returns String formatada para exibição no terminal
 */
export async function runCommand(parsed: CliArgs): Promise<string>
⋮----
/**
     * Comando: read <file>
     * Exemplo: soberano read package.json
     */
⋮----
/**
     * Comando: dir <path>
     * Exemplo: soberano dir src
     */
⋮----
/**
     * Comando: search <dir> <pattern>
     * Exemplo: soberano search src "export"
     */
⋮----
/**
     * Comando: exec <cmd>
     * Exemplo: soberano exec "ls -la"
     *
     * ⚠️ Segurança: o comando é dividido em executável + argumentos,
     * e o CommandExecutor usa spawn com shell:false, prevenindo injeção.
     */
⋮----
/**
     * Comando: chat <prompt>
     *
     * Pipeline com suporte a dois modos:
     *   - StreamStrategy: streaming direto (efeito máquina de escrever, sem ReAct)
     *   - ReActStrategy: ReAct Loop (com ou sem streaming pós-resposta)
     *
     * OCP (Open-Closed Principle): Novos modos de chat são adicionados criando
     * novas classes que implementam ChatStrategy, sem modificar este case.
     *
     * Flags:
     *   --stream   → ativa streaming SSE (efeito máquina de escrever)
     *   --json     → ativa tool_call/final_response em JSON (força ReAct)
     *   --no-think → desativa o indicador "pensando..." (útil para scripting)
     *
     * Exemplos:
     *   soberano chat "Explique SOLID" --model phi3:3b --stream
     *   soberano chat "Qual o conteúdo do package.json?" --json
     *   soberano chat "Liste os arquivos" --stream --no-think
     */
⋮----
// Adiciona a mensagem do usuário ao histórico da sessão
⋮----
// Estratégia: decide qual pipeline executar
// - streaming direto: quando --stream está ativo, --json NÃO está, e provider suporta
// - ReAct: quando --json está ativo OU streaming não é suportado
⋮----
// Adiciona a resposta do assistente ao histórico e persiste
// Apenas salva se houver conteúdo real (evita sessões fantasmas)
⋮----
/**
     * Comando: sessions
     * Exemplo: soberano sessions
     *
     * Lista todas as sessões de conversa salvas, ordenadas por
     * última atualização (mais recentes primeiro).
     */
⋮----
/** Fallback: comando não reconhecido */

================
File: src/cli/index.ts
================
import { runCommand, CliArgs } from './commands';
⋮----
/**
 * Parser simples de argumentos de linha de comando.
 *
 * Fluxo de parsing:
 * ┌────────────┐                         ┌─────────────────┐
 * │ process    │  Ex: ['ts-node',        │  parseArgs()    │
 * │ .argv      │       'chat',           │                 │
 * │            │       'Explique SOLID', │  ┌─ positional ─▶ ['chat', 'Explique SOLID']
 * │            │       '--model',        │  │               
 * │            │       'phi3:3b']        │  └─ flags ──────▶ { model: 'phi3:3b' }
 * └────────────┘                         └──────┬──────────┘
 *                                               │
 *                                     ┌─────────▼──────────┐
 *                                     │ CliArgs {           │
 *                                     │   command: 'chat',  │
 *                                     │   args: ['Explique  │
 *                                     │          SOLID'],   │
 *                                     │   flags: {          │
 *                                     │     model:'phi3:3b' │
 *                                     │   }                 │
 *                                     │ }                   │
 *                                     └─────────────────────┘
 *
 * Convenção:
 *   - Tudo que começa com -- é flag
 *   - --flag valor → flags.flag = valor (consome o próximo token)
 *   - --flag (sem valor seguinte) → flags.flag = true (booleano)
 *   - Todo o resto é argumento posicional
 *   - O primeiro argumento posicional é o nome do comando
 *
 * @param raw Array cru de strings (process.argv)
 * @returns Objeto estruturado com comando, argumentos e flags
 */
function parseArgs(raw: string[]): CliArgs
⋮----
// raw = ['ts-node', 'src/cli/index.ts', 'read', 'file.txt', '--model', 'tinyllama:1b']
⋮----
// Descarta os dois primeiros (interpretador + script)
// Ex: ['ts-node', 'src/cli/index.ts', ...] → sliced = ['read', 'file.txt', ...]
⋮----
// Remove o prefixo '--' para obter o nome da flag
const key = token.slice(2); // '--model' → 'model'
⋮----
// Se o próximo token existe e não é outra flag, é o valor desta flag
⋮----
i++; // Consome o valor (avança um token extra)
⋮----
// Flag booleana (ex: --verbose)
⋮----
// Argumento posicional (não começa com --)
⋮----
// Primeiro posicional = comando; se ausente, default é 'help'
⋮----
// Demais posicionais = argumentos do comando
⋮----
/**
 * Entrypoint principal da CLI Soberano-Core.
 *
 * Fluxo completo da aplicação (do terminal ao output):
 *
 * Terminal
 *    │
 *    │ $ npm run dev -- chat "Explique SOLID" --model phi3:3b
 *    ▼
 * process.argv
 *    │
 *    ▼
 * parseArgs() ──▶ CliArgs { command, args, flags }
 *    │
 *    ├── command === 'help' ? ──▶ Exibe tela de ajuda e encerra
 *    │
 *    ▼
 * runCommand(parsed) ──▶ Promise<string>
 *    │
 *    ├── Sucesso ──▶ console.log(output)
 *    │
 *    └── Erro ──────▶ console.error() + process.exit(1)
 *
 * Tratamento de erros:
 *   - Erros de uso (args faltando) → mensagem + exit code 1
 *   - Erros de sistema (arquivo não encontrado) → mensagem + exit code 1
 *   - Erros de rede (Ollama offline) → mensagem + exit code 1
 */
async function main(): Promise<void>
⋮----
// Comando help: exibe documentação e encerra sem erro
⋮----
// Executa o comando e exibe o resultado
⋮----
// Qualquer erro é exibido no stderr e o processo termina com código 1
⋮----
// Executa a função principal

================
File: src/core/rag/graph/ASTRelationshipExtractor.ts
================
/**
 * ASTRelationshipExtractor: Extrai relações semânticas do código fonte via AST.
 *
 * Responsabilidade Única (SRP):
 * - Consumir IASTParser (já existente) para obter ASTNode[]
 * - Detectar CALLS, IMPORTS, EXTENDS, IMPLEMENTS, CONTAINS, BELONGS_TO
 * - Gerar GraphNode para cada símbolo
 * - Gerar GraphEdge para cada relação descoberta
 *
 * DIP: Depende de IASTParser (abstração), não de TypescriptASTAdapter (concreto).
 *
 * Validações:
 * - Ignora chamadas a built-ins (console, process, require, import)
 * - Ignora auto-referências
 * - Limite de 200 arestas por arquivo
 */
⋮----
import type { IASTParser } from '../IASTParser';
import type { IRelationshipExtractor, ExtractionResult } from './IRelationshipExtractor';
import type { GraphNode, GraphEdge } from './types';
import { hashId } from './JsonGraphStore';
import type { IChunker } from '../IChunker';
⋮----
// Built-ins do Node.js/JS que não devem gerar arestas
⋮----
// Extensões suportadas para extração
⋮----
export class ASTRelationshipExtractor implements IRelationshipExtractor
⋮----
constructor(astParser: IASTParser, chunkIdMapper?: IChunker)
⋮----
extract(filePath: string, source: string): ExtractionResult
⋮----
// Valida se o arquivo é suportado
⋮----
// Se código vazio, retorna vazio
⋮----
// Se não conseguiu parsear, retorna vazio
⋮----
// 1. Nó do arquivo
⋮----
// 2. Nós dos símbolos (funções, classes, métodos, interfaces)
⋮----
// Pré-calcula chunkIds se chunkIdMapper estiver disponível
⋮----
// Determina chunkId: se temos o mapeamento, usa a startLine do símbolo
⋮----
// Procura o chunk que contém a startLine do símbolo
⋮----
// Fallback: usa o índice do nó como parte do chunkId (formato: path#indice)
⋮----
// Aresta BELONGS_TO: símbolo → arquivo
⋮----
// Aresta CONTAINS: arquivo → símbolo (inverso)
⋮----
// 3. Extrair relações a partir do texto de cada nó
⋮----
// CALLS: detectar chamadas de função
⋮----
// Ignorar built-ins
⋮----
// Ignorar auto-chamadas
⋮----
// Verificar se a chamada é para um símbolo conhecido no mesmo arquivo
⋮----
// Não criar aresta para si mesmo
⋮----
// Método de objeto (ex: this.save()) ou lib nativa — ignorar
⋮----
// IMPORTS: extrair importações
⋮----
// Ignorar módulos core do Node
⋮----
// Criar nó para o módulo (se não existir)
⋮----
// EXTENDS: herança de classe
⋮----
// IMPLEMENTS: implementação de interface
⋮----
// REFERENCES: referências a símbolos sem chamada explícita
⋮----
// Pré-compila todas as regexes de nome para evitar new RegExp() no loop duplo
⋮----
// Pular auto-referência
⋮----
// Verificar se o nome aparece no texto do nó (excluindo chamadas já detectadas)
⋮----
// Verificar se não é uma chamada (já coberta por CALLS)
⋮----
// Verificar se já existe aresta CALLS entre os mesmos nós
⋮----
/**
   * Verifica se a extensão do arquivo é suportada para extração.
   */
private isSupportedFile(filePath: string): boolean
⋮----
/**
   * Mapeia kind do AST para tipo do GraphNode.
   */
private mapKindToType(kind: string): GraphNode['type']
⋮----
/**
   * Constrói um mapa de linha inicial → chunkId usando o chunkIdMapper (IChunker).
   * Útil para que os GraphNode tenham chunkId preenchido, populando nodesByChunkId.
   */
private buildChunkIdMap(source: string, filePath: string): Map<number, string>
⋮----
// Mapeia cada linha do chunk para o chunkId
⋮----
/**
   * Encontra o chunkId que contém uma determinada linha.
   * Busca exata primeiro, depois a linha mais próxima anterior.
   */
private findChunkIdForLine(chunkIdByLine: Map<number, string>, line: number): string | undefined
⋮----
// Fallback: encontra a linha anterior mais próxima que tem chunkId

================
File: src/core/rag/graph/GraphBuilder.ts
================
/**
 * GraphBuilder: Orquestrador de construção do KnowledgeGraph.
 *
 * Responsabilidade Única (SRP):
 * - Percorrer diretório de código fonte
 * - Para cada arquivo .ts/.js, delegar extração ao IRelationshipExtractor
 * - Acumular todos os nós e arestas
 * - Persistir o grafo via IGraphStore
 *
 * DIP: Depende de IRelationshipExtractor e IGraphStore (abstrações).
 */
⋮----
import { FileReader } from '../../FileReader';
import type { IRelationshipExtractor } from './IRelationshipExtractor';
import type { IGraphStore } from './IGraphStore';
import type { KnowledgeGraph, GraphNode, GraphEdge } from './types';
⋮----
// Extensões de código-fonte relevantes para o grafo
⋮----
export interface GraphBuildOptions {
  /** Diretório raiz para indexar */
  rootDir: string;
  /** Máximo de arquivos a processar (default 200) */
  maxFiles?: number;
  /** Versão do grafo (default 1) */
  version?: number;
}
⋮----
/** Diretório raiz para indexar */
⋮----
/** Máximo de arquivos a processar (default 200) */
⋮----
/** Versão do grafo (default 1) */
⋮----
export interface GraphBuildResult {
  /** Total de nós no grafo final */
  totalNodes: number;
  /** Total de arestas no grafo final */
  totalEdges: number;
  /** Número de arquivos processados */
  filesProcessed: number;
  /** Se o grafo foi recriado do zero ou atualizado incrementalmente */
  rebuilt: boolean;
}
⋮----
/** Total de nós no grafo final */
⋮----
/** Total de arestas no grafo final */
⋮----
/** Número de arquivos processados */
⋮----
/** Se o grafo foi recriado do zero ou atualizado incrementalmente */
⋮----
export class GraphBuilder
⋮----
constructor(
    fileReader: FileReader,
    relationshipExtractor: IRelationshipExtractor,
    graphStore: IGraphStore
)
⋮----
/**
   * Constrói ou atualiza o KnowledgeGraph para o diretório informado.
   *
   * Estratégia incremental:
   * - Carrega grafo existente (se houver)
   * - Processa apenas arquivos modificados (por mtime)
   * - Remove nós/arestas de arquivos removidos
   * - Mescla com grafo existente
   */
async build(options: GraphBuildOptions): Promise<GraphBuildResult>
⋮----
// 1. Carrega grafo existente (se houver)
⋮----
// 2. Coleta arquivos fonte
⋮----
// 3. Determina arquivos modificados e removidos
⋮----
continue; // não modificado
⋮----
// Arquivos que existiam antes mas não existem mais → remover
⋮----
// Se não há nada para processar, retorna estado atual
⋮----
// 4. Processa arquivos modificados/novos
⋮----
// Helper para chave única de aresta (dedup)
const edgeKey = (e: GraphEdge): string => `$
⋮----
// Pré-carrega nós/arestas existentes (exceto de arquivos removidos)
⋮----
// Verificar se a aresta envolve algum nó de arquivo removido
⋮----
// Preserva mtimes de arquivos não modificados
⋮----
// Processa arquivos modificados — paralelizado
⋮----
// Remove nós/arestas antigos deste arquivo (se estava no grafo anterior)
⋮----
// Remove arestas conectadas a nós antigos
⋮----
// Adiciona novos nós/arestas
⋮----
// 5. Monta grafo final
⋮----
// 6. Persiste
⋮----
/**
   * Coleta arquivos fonte recursivamente.
   */
private async collectSourceFiles(
    rootDir: string,
    maxFiles: number
): Promise<SourceFile[]>
⋮----
const walk = async (dir: string): Promise<void> =>
⋮----
// Paraleliza fs.stat em todas as entradas do diretório
⋮----
interface SourceFile {
  fullPath: string;
  relPath: string;
  mtime: number;
}

================
File: src/core/rag/graph/GraphRAGManager.ts
================
/**
 * GraphRAGManager: Busca híbrida que combina similaridade vetorial (RAG clássico)
 * com navegação em grafo de conhecimento (GraphRAG).
 *
 * Responsabilidade Única (SRP):
 * - Receber uma consulta de busca
 * - Executar retrieve vetorial (via Retriever)
 * - Expandir os top-N resultados com vizinhança no grafo (via IGraphQuery)
 * - Mesclar e ranquear resultados combinados
 * - Formatar resultado para prompt
 *
 * DIP: Depende de Retriever e IGraphQuery (abstrações).
 * OCP: Novas estratégias de ranqueamento podem ser adicionadas sem modificar esta classe.
 */
⋮----
import { Retriever, type SearchMatch } from '../Retriever';
import type { IGraphQuery, NeighborNode, ExpandOptions } from './IGraphQuery';
import type { ChunkEntry } from '../VectorStore';
⋮----
/**
 * Opções de busca híbrida.
 */
export interface GraphSearchOptions {
  /** Número de resultados vetoriais iniciais (default 5) */
  topK?: number;
  /** Profundidade de expansão no grafo (default 2) */
  graphDepth?: number;
  /** Tipos de aresta para filtrar expansão (default: todos) */
  edgeTypes?: ExpandOptions['edgeTypes'];
  /** Peso da relevância textual vs estrutural (0..1, default 0.7) */
  textWeight?: number;
}
⋮----
/** Número de resultados vetoriais iniciais (default 5) */
⋮----
/** Profundidade de expansão no grafo (default 2) */
⋮----
/** Tipos de aresta para filtrar expansão (default: todos) */
⋮----
/** Peso da relevância textual vs estrutural (0..1, default 0.7) */
⋮----
/**
 * Resultado da busca híbrida.
 */
export interface GraphSearchResult {
  /** Resultados combinados e ranqueados */
  matches: SearchMatch[];
  /** Nós do grafo visitados durante a expansão */
  visitedNodes: NeighborNode[];
  /** Total de chunks antes da expansão */
  baseChunks: number;
  /** Total de chunks após expansão */
  expandedChunks: number;
}
⋮----
/** Resultados combinados e ranqueados */
⋮----
/** Nós do grafo visitados durante a expansão */
⋮----
/** Total de chunks antes da expansão */
⋮----
/** Total de chunks após expansão */
⋮----
export class GraphRAGManager
⋮----
constructor(retriever: Retriever, graphQuery: IGraphQuery)
⋮----
/**
   * Busca híbrida: similaridade vetorial + expansão em grafo.
   *
   * Algoritmo:
   * 1. Retrieve vetorial (top-K chunks por similaridade de cosseno)
   * 2. Para cada chunk nos top resultados, encontra nó do grafo via chunk.id
   * 3. Expande vizinhança de cada nó (profundidade configurável)
   * 4. Coleta filePaths dos nós vizinhos
   * 5. Adiciona chunks dos arquivos vizinhos ao resultado (se não duplicados)
   * 6. Re-ranqueia por score combinado (textual * textWeight + estrutural * (1-textWeight))
   * 7. Retorna top-K final
   */
async search(
    queryEmbedding: number[],
    chunks: ChunkEntry[],
    options: GraphSearchOptions = {}
): Promise<GraphSearchResult>
⋮----
// 1. Retrieve vetorial
⋮----
// 2. Para cada match, encontra nó no grafo e expande
⋮----
// Expande vizinhança
⋮----
// 3. Adiciona chunks dos arquivos vizinhos (não duplicados)
⋮----
let structuralScore = 0.3; // score base para arquivo vizinho
⋮----
// Verifica se o nó está nos vizinhos expandidos
⋮----
// Score inversamente proporcional à distância
⋮----
// 4. Mescla e re-ranqueia
⋮----
// Matches vetoriais mantêm seu score original * textWeight
⋮----
// Matches estruturais recebem (1 - textWeight) * structuralScore
⋮----
// Ordena por score descendente
⋮----
/**
   * Formata resultado híbrido para inclusão no prompt.
   * Inclui contexto textual e informações estruturais do grafo.
   */
formatGraphContext(result: GraphSearchResult): string
⋮----
// Resultados textuais
⋮----
// Informações estruturais

================
File: src/core/rag/graph/IGraphQuery.ts
================
/**
 * IGraphQuery: Interface para consultas no KnowledgeGraph (DIP, CQRS).
 *
 * Responsabilidade Única (SRP): Definir o contrato de consulta (read-only)
 * do grafo de conhecimento. Separada de IGraphStore (Command) por ISP.
 *
 * A implementação concreta (JsonGraphStore) implementa ambas as interfaces
 * mas o consumidor (GraphRAGManager) depende apenas desta interface
 * para não ter acesso acidental a operações de escrita.
 */
⋮----
import type { GraphNode, RelationshipType } from './types';
⋮----
/**
 * Nó vizinho com metadados da relação.
 */
export interface NeighborNode extends GraphNode {
  /** Tipo da aresta que conecta ao nó de origem */
  edgeType: RelationshipType;
  /** Distância no grafo a partir do nó de origem (1 = direto) */
  distance: number;
}
⋮----
/** Tipo da aresta que conecta ao nó de origem */
⋮----
/** Distância no grafo a partir do nó de origem (1 = direto) */
⋮----
/**
 * Opções para expansão de vizinhança no grafo.
 */
export interface ExpandOptions {
  /** Profundidade da BFS (1 = apenas vizinhos diretos). Default: 1 */
  depth: number;
  /** Filtro opcional por tipos de aresta (se omitido, retorna todos) */
  edgeTypes?: RelationshipType[];
}
⋮----
/** Profundidade da BFS (1 = apenas vizinhos diretos). Default: 1 */
⋮----
/** Filtro opcional por tipos de aresta (se omitido, retorna todos) */
⋮----
export interface IGraphQuery {
  /**
   * Busca um nó do grafo pelo ID do chunk correspondente.
   *
   * @param chunkId ID do ChunkEntry (ex: "src/core/RAGManager.ts#3")
   * @returns GraphNode ou null se não encontrado
   */
  getNodeByChunkId(chunkId: string): GraphNode | null;

  /**
   * Expande a vizinhança de um nó usando BFS limitada por profundidade.
   *
   * @param nodeId ID do nó de origem
   * @param options Opções de expansão (profundidade, filtro de arestas)
   * @returns Array de nós vizinhos (sem incluir o nó de origem)
   */
  expandNeighborhood(nodeId: string, options: ExpandOptions): NeighborNode[];

  /**
   * Retorna todos os nós pertencentes a um arquivo.
   *
   * @param filePath Caminho relativo do arquivo
   * @returns Array de nós do arquivo (vazio se nenhum)
   */
  getNodesByFile(filePath: string): GraphNode[];
}
⋮----
/**
   * Busca um nó do grafo pelo ID do chunk correspondente.
   *
   * @param chunkId ID do ChunkEntry (ex: "src/core/RAGManager.ts#3")
   * @returns GraphNode ou null se não encontrado
   */
getNodeByChunkId(chunkId: string): GraphNode | null;
⋮----
/**
   * Expande a vizinhança de um nó usando BFS limitada por profundidade.
   *
   * @param nodeId ID do nó de origem
   * @param options Opções de expansão (profundidade, filtro de arestas)
   * @returns Array de nós vizinhos (sem incluir o nó de origem)
   */
expandNeighborhood(nodeId: string, options: ExpandOptions): NeighborNode[];
⋮----
/**
   * Retorna todos os nós pertencentes a um arquivo.
   *
   * @param filePath Caminho relativo do arquivo
   * @returns Array de nós do arquivo (vazio se nenhum)
   */
getNodesByFile(filePath: string): GraphNode[];

================
File: src/core/rag/graph/IGraphStore.ts
================
/**
 * IGraphStore: Interface para persistência do KnowledgeGraph (DIP).
 *
 * Responsabilidade Única (SRP): Definir o contrato de armazenamento
 * do grafo de conhecimento. A implementação concreta (JsonGraphStore)
 * cuida da serialização em disco.
 *
 * ISP: Métodos segregados — CRUD básico de nós/arestas, sem lógica de busca.
 * A busca fica em IGraphQuery.
 */
⋮----
import type { KnowledgeGraph, GraphNode, GraphEdge } from './types';
⋮----
export interface IGraphStore {
  /**
   * Persiste o grafo completo em disco.
   * Substitui completamente o arquivo anterior.
   */
  save(graph: KnowledgeGraph): Promise<void>;

  /**
   * Carrega o grafo do disco.
   * @returns KnowledgeGraph ou null se o arquivo não existir.
   */
  load(): Promise<KnowledgeGraph | null>;

  /**
   * Insere ou atualiza nós no grafo.
   * Usa `id` como chave: se o nó já existe, substitui (merge).
   */
  upsertNodes(nodes: GraphNode[]): Promise<void>;

  /**
   * Insere ou atualiza arestas no grafo.
   * Usa a chave composta `from|to|type` para deduplicação.
   */
  upsertEdges(edges: GraphEdge[]): Promise<void>;

  /**
   * Remove o arquivo de grafo do disco.
   * Usado para resetar o cache.
   */
  clear(): Promise<void>;
}
⋮----
/**
   * Persiste o grafo completo em disco.
   * Substitui completamente o arquivo anterior.
   */
save(graph: KnowledgeGraph): Promise<void>;
⋮----
/**
   * Carrega o grafo do disco.
   * @returns KnowledgeGraph ou null se o arquivo não existir.
   */
load(): Promise<KnowledgeGraph | null>;
⋮----
/**
   * Insere ou atualiza nós no grafo.
   * Usa `id` como chave: se o nó já existe, substitui (merge).
   */
upsertNodes(nodes: GraphNode[]): Promise<void>;
⋮----
/**
   * Insere ou atualiza arestas no grafo.
   * Usa a chave composta `from|to|type` para deduplicação.
   */
upsertEdges(edges: GraphEdge[]): Promise<void>;
⋮----
/**
   * Remove o arquivo de grafo do disco.
   * Usado para resetar o cache.
   */
clear(): Promise<void>;

================
File: src/core/rag/graph/index.ts
================
/**
 * GraphRAG — barrel exports
 */

================
File: src/core/rag/graph/IRelationshipExtractor.ts
================
/**
 * IRelationshipExtractor: Interface para extração de relações entre símbolos (DIP).
 *
 * Responsabilidade Única (SRP): Extrair relações semânticas (arestas)
 * de um arquivo de código fonte. A implementação concreta
 * (ASTRelationshipExtractor) analisa o AST para descobrir chamadas,
 * heranças, imports, etc.
 *
 * ISP: Segregada do armazenamento — esta interface não sabe onde
 * o grafo é persistido.
 */
⋮----
import type { GraphNode, GraphEdge } from './types';
⋮----
/**
 * Resultado da extração de relações de um único arquivo.
 * Contém apenas os novos nós e arestas descobertos.
 */
export interface ExtractionResult {
  /** Novos nós descobertos (funções, classes, interfaces) */
  nodes: GraphNode[];
  /** Novas arestas descobertas (CALLS, IMPORTS, EXTENDS, etc.) */
  edges: GraphEdge[];
}
⋮----
/** Novos nós descobertos (funções, classes, interfaces) */
⋮----
/** Novas arestas descobertas (CALLS, IMPORTS, EXTENDS, etc.) */
⋮----
export interface IRelationshipExtractor {
  /**
   * Extrai relações semânticas do código fonte de um arquivo.
   *
   * @param filePath Caminho relativo do arquivo (usado para namespacing dos IDs)
   * @param source Conteúdo completo do arquivo
   * @returns ExtractionResult com os nós e arestas descobertos.
   *          Retorna `{ nodes: [], edges: [] }` se o código for inválido
   *          ou se o arquivo não for suportado.
   */
  extract(filePath: string, source: string): ExtractionResult;
}
⋮----
/**
   * Extrai relações semânticas do código fonte de um arquivo.
   *
   * @param filePath Caminho relativo do arquivo (usado para namespacing dos IDs)
   * @param source Conteúdo completo do arquivo
   * @returns ExtractionResult com os nós e arestas descobertos.
   *          Retorna `{ nodes: [], edges: [] }` se o código for inválido
   *          ou se o arquivo não for suportado.
   */
extract(filePath: string, source: string): ExtractionResult;

================
File: src/core/rag/graph/JsonGraphStore.ts
================
/**
 * JsonGraphStore: Implementação concreta de IGraphStore + IGraphQuery
 * usando JSON em disco (SRP, DIP, CQRS).
 *
 * Responsabilidade Única:
 * - Persistir o KnowledgeGraph em .soberano/graph.json
 * - Manter índice de adjacência em memória para O(1) em consultas
 * - Implementar IGraphStore (escrita) e IGraphQuery (leitura)
 *
 * Soberania Local: 100% node:fs/promises. Zero dependências externas.
 */
⋮----
import type { KnowledgeGraph, GraphNode, GraphEdge, RelationshipType } from './types';
import type { IGraphStore } from './IGraphStore';
import type { IGraphQuery, NeighborNode, ExpandOptions } from './IGraphQuery';
⋮----
/**
 * Índice de adjacência: nodeId → lista de arestas que saem do nó.
 * Construído uma vez no load() para consultas O(1).
 */
interface AdjacencyIndex {
  edgesBySource: Map<string, GraphEdge[]>;
  nodesById: Map<string, GraphNode>;
  nodesByChunkId: Map<string, GraphNode>;
  nodesByFile: Map<string, GraphNode[]>;
}
⋮----
export class JsonGraphStore implements IGraphStore, IGraphQuery
⋮----
constructor(rootDir: string)
⋮----
// =========================================================================
// IGraphStore — Escrita
// =========================================================================
⋮----
async save(graph: KnowledgeGraph): Promise<void>
⋮----
async load(): Promise<KnowledgeGraph | null>
⋮----
async upsertNodes(nodes: GraphNode[]): Promise<void>
⋮----
async upsertEdges(edges: GraphEdge[]): Promise<void>
⋮----
const edgeKey = (e: GraphEdge) => `$
⋮----
async clear(): Promise<void>
⋮----
// Arquivo não existe — ignorar
⋮----
// =========================================================================
// IGraphQuery — Leitura
// =========================================================================
⋮----
getNodeByChunkId(chunkId: string): GraphNode | null
⋮----
expandNeighborhood(nodeId: string, options: ExpandOptions): NeighborNode[]
⋮----
getNodesByFile(filePath: string): GraphNode[]
⋮----
// =========================================================================
// Privados
// =========================================================================
⋮----
private getGraphPath(): string
⋮----
private async getOrCreateGraph(): Promise<KnowledgeGraph>
⋮----
/**
   * Constrói índices de adjacência a partir do grafo carregado.
   * Executado uma vez no load()/save().
   */
private buildAdjacency(graph: KnowledgeGraph): void
⋮----
// Indexar nós
⋮----
// Indexar arestas (direcionais, apenas saída)
⋮----
/**
 * Utilitário para gerar IDs hash determinísticos.
 * Usa SHA-256 truncado para 12 caracteres hexadecimais.
 */
export function hashId(input: string): string

================
File: src/core/rag/graph/types.ts
================
/**
 * GraphRAG Local — Tipos de domínio do KnowledgeGraph.
 *
 * Define as estruturas de dados para representar o grafo de conhecimento
 * que mapeia relações entre chunks, funções, classes, interfaces e arquivos.
 *
 * ISP: Interfaces segregadas — cada tipo tem uma responsabilidade única.
 */
⋮----
/**
 * Relação semântica entre dois nós do grafo.
 */
export type RelationshipType =
  | 'CALLS'         // Função A chama Função B (detectado via AST: CallExpression)
  | 'IMPORTS'       // Arquivo X importa Módulo Y (AST: ImportDeclaration)
  | 'EXTENDS'       // Classe X herda de Classe Y (AST: HeritageClause)
  | 'IMPLEMENTS'    // Classe X implementa Interface Y (AST: HeritageClause)
  | 'BELONGS_TO'    // Chunk/Função pertence a um Arquivo
  | 'CONTAINS'      // Arquivo/Classe contém Função/Método (inverso de BELONGS_TO)
  | 'REFERENCES';   // Referência genérica (fallback para export/require)
⋮----
| 'CALLS'         // Função A chama Função B (detectado via AST: CallExpression)
| 'IMPORTS'       // Arquivo X importa Módulo Y (AST: ImportDeclaration)
| 'EXTENDS'       // Classe X herda de Classe Y (AST: HeritageClause)
| 'IMPLEMENTS'    // Classe X implementa Interface Y (AST: HeritageClause)
| 'BELONGS_TO'    // Chunk/Função pertence a um Arquivo
| 'CONTAINS'      // Arquivo/Classe contém Função/Método (inverso de BELONGS_TO)
| 'REFERENCES';   // Referência genérica (fallback para export/require)
⋮----
/**
 * Nó do grafo de conhecimento.
 * Representa um símbolo do código fonte (função, classe, chunk, arquivo).
 */
export interface GraphNode {
  /** Identificador único do nó (hash determinístico) */
  id: string;
  /** Nome legível — ex: "RAGManager.ensureIndex()" */
  label: string;
  /** Tipo do nó */
  type: 'chunk' | 'function' | 'method' | 'class' | 'interface' | 'file';
  /** ID do ChunkEntry correspondente (apenas para type='chunk') */
  chunkId?: string;
  /** Caminho relativo do arquivo de origem */
  filePath: string;
  /** Linha inicial no arquivo (1-based) */
  startLine: number;
  /** Linha final no arquivo (1-based) */
  endLine: number;
}
⋮----
/** Identificador único do nó (hash determinístico) */
⋮----
/** Nome legível — ex: "RAGManager.ensureIndex()" */
⋮----
/** Tipo do nó */
⋮----
/** ID do ChunkEntry correspondente (apenas para type='chunk') */
⋮----
/** Caminho relativo do arquivo de origem */
⋮----
/** Linha inicial no arquivo (1-based) */
⋮----
/** Linha final no arquivo (1-based) */
⋮----
/**
 * Aresta direcionada entre dois nós do grafo.
 */
export interface GraphEdge {
  /** ID do nó de origem */
  from: string;
  /** ID do nó de destino */
  to: string;
  /** Tipo da relação semântica */
  type: RelationshipType;
  /** Metadados opcionais — ex: { lineNumber: 42 } */
  metadata?: Record<string, unknown>;
}
⋮----
/** ID do nó de origem */
⋮----
/** ID do nó de destino */
⋮----
/** Tipo da relação semântica */
⋮----
/** Metadados opcionais — ex: { lineNumber: 42 } */
⋮----
/**
 * Grafo de conhecimento completo.
 * Persistido em .soberano/graph.json.
 */
export interface KnowledgeGraph {
  /** Versão da estrutura do grafo (para migrações futuras) */
  version: number;
  /** Todos os nós do grafo */
  nodes: GraphNode[];
  /** Todas as arestas do grafo */
  edges: GraphEdge[];
  /** Mapeamento de filePath → mtimeMs para detecção incremental */
  fileMtimes: Record<string, number>;
  /** Timestamp ISO de criação */
  createdAt: string;
  /** Timestamp ISO da última atualização */
  updatedAt: string;
}
⋮----
/** Versão da estrutura do grafo (para migrações futuras) */
⋮----
/** Todos os nós do grafo */
⋮----
/** Todas as arestas do grafo */
⋮----
/** Mapeamento de filePath → mtimeMs para detecção incremental */
⋮----
/** Timestamp ISO de criação */
⋮----
/** Timestamp ISO da última atualização */

================
File: src/core/rag/ASTChunkerService.ts
================
/**
 * ASTChunkerService: Chunking estrutural baseado em AST (SRP, DIP).
 *
 * Responsabilidade Única:
 * - Decidir se usa AST ou fallback textual baseado na extensão do arquivo
 * - Delegar parse para IASTParser (abstração — DIP)
 * - Transformar ASTNode[] em ChunkResult[] com respeito a limites de tamanho
 * - Subdividir nós grandes (ex: classes com muitos métodos) preservando
 *   blocos sintáticos completos
 *
 * Depende de IASTParser (interface) e IChunker (interface para fallback).
 * NÃO depende de nenhuma implementação concreta de parser.
 */
⋮----
import type { IChunker, ChunkResult } from './IChunker';
import type { IASTParser, ASTNode } from './IASTParser';
⋮----
const MAX_CHUNK_SIZE = 2000;         // caracteres máximos por chunk
const MAX_CHUNKS_PER_FILE = 50;      // limite máximo de chunks por arquivo
⋮----
// Extensões que suportam parseamento AST
⋮----
export class ASTChunkerService implements IChunker
⋮----
constructor(
⋮----
/**
   * Divide o conteúdo usando AST para arquivos de código,
   * ou fallback textual para os demais formatos.
   *
   * @param content Conteúdo do arquivo
   * @param filePath Caminho (opcional — usado para detectar extensão)
   * @returns Array de chunks semanticamente coesos
   */
chunk(content: string, filePath?: string): ChunkResult[]
⋮----
// Se não tem filePath ou não é arquivo de código, usa fallback textual
⋮----
// Tenta parse AST
⋮----
// Se o parse falhou (código inválido), usa fallback
⋮----
// Converte nós AST em chunks, respeitando limites
⋮----
/**
   * Verifica se a extensão do arquivo é de código-processável por AST.
   */
private isCodeFile(filePath: string): boolean
⋮----
/**
   * Extrai extensão do arquivo (incluindo o ponto).
   */
private getExtension(filePath: string): string
⋮----
// Pega a extensão: ".ts", ".js", etc.
⋮----
/**
   * Converte nós AST em ChunkResult[] com as regras:
   * 1. Nó pequeno (≤ MAX_CHUNK_SIZE) → 1 chunk
   * 2. Nó grande do tipo ClassDeclaration → subdivide por métodos
   * 3. Outros nós grandes → chunk por linhas respeitando MAX_CHUNK_SIZE
   * 4. Respeita MAX_CHUNKS_PER_FILE
   */
private nodesToChunks(nodes: ASTNode[], _source: string): ChunkResult[]
⋮----
// Para classes grandes: chunk da classe + chunk de cada método
⋮----
// Para outros nós grandes: divide por linhas
⋮----
/**
   * Subdivide uma classe grande: primeiro a assinatura da classe,
   * depois cada método como chunk independente.
   */
private splitLargeClass(node: ASTNode, chunks: ChunkResult[]): void
⋮----
// Extrai a assinatura da classe (até a primeira {)
⋮----
// Encontra métodos dentro do texto da classe usando regex simples
⋮----
// Acha o fim do método contando chaves aninhadas
⋮----
/**
   * Subdivide um nó grande que não é classe
   * em chunks menores respeitando MAX_CHUNK_SIZE.
   */
private splitLargeNode(node: ASTNode, chunks: ChunkResult[]): void

================
File: src/core/rag/Chunker.ts
================
/**
 * Chunker: Responsável apenas por dividir textos em chunks textuais (SRP).
 *
 * Estratégias de chunking:
 * - Por parágrafo (\n\n): divisão semântica padrão
 * - Por sentença (. ? !): fallback para parágrafos longos
 * - Com overlap entre chunks consecutivos
 *
 * Implementa IChunker — funciona como fallback textual para o ASTChunkerService.
 * É uma função pura — sem estado, sem I/O. Ideal para testes.
 */
⋮----
import type { IChunker, ChunkResult } from './IChunker';
⋮----
const MAX_CHUNK_SIZE = 2000;        // caracteres (~500 tokens)
const OVERLAP = 200;                 // sobreposição entre chunks
const MAX_CHUNKS_PER_FILE = 50;     // limite por arquivo
⋮----
export class Chunker implements IChunker
⋮----
/**
   * Divide o conteúdo de um arquivo em chunks semanticamente coerentes.
   *
   * Algoritmo:
   * 1. Divide o texto em parágrafos (separados por \n\n)
   * 2. Se o parágrafo cabe em MAX_CHUNK_SIZE, usa como chunk
   * 3. Se não, divide por sentenças
   * 4. Aplica overlap de OVERLAP caracteres entre chunks consecutivos
   * 5. Respeita MAX_CHUNKS_PER_FILE
   *
   * @param content Conteúdo completo do arquivo
   * @param _filePath Caminho do arquivo (ignorado — chunking sempre textual)
   * @returns Array de chunks com metadados de linha
   */
chunk(content: string, _filePath?: string): ChunkResult[]
⋮----
// Se o parágrafo cabe em um chunk
⋮----
currentLine += paraLines.length + 1; // +1 pelo \n\n
⋮----
// Parágrafo muito grande: divide por sentenças
⋮----
// Overlap: mantém os últimos OVERLAP caracteres

================
File: src/core/rag/Embedder.ts
================
/**
 * Embedder: Camada de abstração sobre o provider de embeddings (SRP).
 *
 * Responsabilidade Única:
 * - Recebe um IEmbedProvider e gerencia a chamada de embedding
 * - Controla keep_alive para gerenciamento de RAM
 * - Trata erros de rede/timeout silenciosamente
 *
 * Não faz chunking, não busca no índice, não formata contexto.
 * Apenas embeddings.
 */
⋮----
import type { IEmbedProvider } from '../../providers/types';
⋮----
export class Embedder
⋮----
constructor(provider: IEmbedProvider, defaultModel: string = 'all-minilm')
⋮----
/**
   * Gera embedding para um texto.
   *
   * @param text Texto a ser embedado
   * @param model Nome do modelo (usa default se omitido)
   * @param keepAlive Tempo de keep_alive (padrão "30s" para indexação)
   * @returns Vetor de floats 384-dim
   */
async embed(text: string, model?: string, keepAlive: string = '30s'): Promise<number[]>
⋮----
/**
   * Descarrega o modelo de embedding da RAM.
   * Usado após indexação para liberar recursos (12GB RAM limitada).
   */
async unload(): Promise<void>
⋮----
// Não falha se o descarregamento não funcionar

================
File: src/core/rag/IASTParser.ts
================
/**
 * IASTParser: Interface para parseamento de AST (DIP).
 *
 * Permite que o ASTChunkerService dependa de uma abstração em vez de
 * uma implementação concreta de parser AST. O Adapter concreto
 * (TypescriptASTAdapter) isola a dependência do compilador TypeScript.
 */
⋮----
/**
 * Nó AST extraído do código fonte.
 */
export interface ASTNode {
  /** Nome do nó: nome da função, classe, interface, etc. */
  name: string;
  /** Tipo do nó: 'FunctionDeclaration' | 'ClassDeclaration' | 'MethodDeclaration' | ... */
  kind: string;
  /** Código fonte completo do nó (assinatura + corpo) */
  text: string;
  /** Linha inicial (1-based) */
  startLine: number;
  /** Linha final (1-based) */
  endLine: number;
}
⋮----
/** Nome do nó: nome da função, classe, interface, etc. */
⋮----
/** Tipo do nó: 'FunctionDeclaration' | 'ClassDeclaration' | 'MethodDeclaration' | ... */
⋮----
/** Código fonte completo do nó (assinatura + corpo) */
⋮----
/** Linha inicial (1-based) */
⋮----
/** Linha final (1-based) */
⋮----
export interface IASTParser {
  /**
   * Extrai nós semânticos (funções, classes, etc.) do código fonte.
   *
   * @param source Código fonte TypeScript/JavaScript completo
   * @returns Array de nós AST ordenados por startLine.
   *          Retorna array vazio se o código não for TypeScript/JavaScript válido.
   */
  parse(source: string): ASTNode[];
}
⋮----
/**
   * Extrai nós semânticos (funções, classes, etc.) do código fonte.
   *
   * @param source Código fonte TypeScript/JavaScript completo
   * @returns Array de nós AST ordenados por startLine.
   *          Retorna array vazio se o código não for TypeScript/JavaScript válido.
   */
parse(source: string): ASTNode[];

================
File: src/core/rag/IChunker.ts
================
/**
 * IChunker: Interface para o contrato de chunking (DIP).
 *
 * Permite que o RAGManager dependa de uma abstração em vez de
 * uma implementação concreta de chunking. Suporta tanto chunking
 * textual (por parágrafo/sentença) quanto estrutural (AST-aware).
 *
 * O parâmetro opcional `filePath` permite que implementações
 * adaptem a estratégia com base na extensão do arquivo.
 */
⋮----
export interface ChunkResult {
  text: string;
  startLine: number;
  endLine: number;
}
⋮----
export interface IChunker {
  /**
   * Divide o conteúdo em chunks semanticamente coerentes.
   *
   * @param content Conteúdo completo do arquivo
   * @param filePath Caminho do arquivo (opcional, usado para detectar
   *                 extensão e adaptar estratégia de chunking)
   * @returns Array de chunks com metadados de linha
   */
  chunk(content: string, filePath?: string): ChunkResult[];
}
⋮----
/**
   * Divide o conteúdo em chunks semanticamente coerentes.
   *
   * @param content Conteúdo completo do arquivo
   * @param filePath Caminho do arquivo (opcional, usado para detectar
   *                 extensão e adaptar estratégia de chunking)
   * @returns Array de chunks com metadados de linha
   */
chunk(content: string, filePath?: string): ChunkResult[];

================
File: src/core/rag/index.ts
================


================
File: src/core/rag/RAGManager.ts
================
/**
 * RAGManager: Orquestrador do pipeline RAG + GraphRAG (SRP — orquestração pura).
 *
 * Responsabilidade Única: Coordenar as etapas do pipeline RAG:
 * 1. Coletar arquivos texto do diretório
 * 2. Chunking (delega para Chunker)
 * 3. Embedding (delega para Embedder)
 * 4. Cache em disco (delega para VectorStore)
 * 5. Busca semântica (delega para Retriever)
 * 6. Construção do KnowledgeGraph (delega para GraphBuilder)
 *
 * Esta classe NÃO implementa chunking, NÃO gera embeddings,
 * NÃO salva arquivos, NÃO constrói grafos — ela apenas coordena as peças.
 */
⋮----
import { FileReader } from '../FileReader';
import { Chunker } from './Chunker';
import type { IChunker } from './IChunker';
import { Embedder } from './Embedder';
import { VectorStore } from './VectorStore';
import { Retriever } from './Retriever';
import type { SearchMatch } from './Retriever';
import type { ChunkEntry } from './VectorStore';
import type { IEmbedProvider } from '../../providers/types';
import { GraphBuilder } from './graph/GraphBuilder';
import type { IGraphStore } from './graph/IGraphStore';
import type { IGraphQuery } from './graph/IGraphQuery';
import type { IRelationshipExtractor } from './graph/IRelationshipExtractor';
import { ASTRelationshipExtractor } from './graph/ASTRelationshipExtractor';
import { JsonGraphStore } from './graph/JsonGraphStore';
import { TypescriptASTAdapter } from './TypescriptASTAdapter';
import { ASTChunkerService } from './ASTChunkerService';
import { GraphRAGManager } from './graph/GraphRAGManager';
⋮----
// Extensões de arquivos texto para indexar
⋮----
export class RAGManager
⋮----
constructor(
    fileReader: FileReader,
    chunker: IChunker,
    embedder: Embedder,
    vectorStore: VectorStore,
    retriever: Retriever,
    graphBuilder?: GraphBuilder | null,
    graphRAGManager?: GraphRAGManager | null
)
⋮----
/**
   * Factory method: cria RAGManager completo com instâncias default.
   * Útil para consumo externo (ex: CLI) onde não se quer instanciar
   * cada dependência manualmente.
   *
   * @param fileReader FileReader para ler arquivos
   * @param embedProvider Provider de embeddings (ex: OllamaProvider)
   * @param graphStorePath Caminho para persistir o grafo (opcional, ex: ".soberano/graph.json")
   */
static create(
    fileReader: FileReader,
    embedProvider: IEmbedProvider,
    chunker?: IChunker,
    graphStorePath?: string
): RAGManager
⋮----
// Default: ASTChunkerService com fallback textual
⋮----
/**
   * Verifica se um arquivo é texto (vs binário) baseado nos primeiros bytes.
   */
private isTextContent(content: string): boolean
⋮----
/**
   * Coleta arquivos texto de um diretório recursivamente.
   */
private async collectTextFiles(
    rootDir: string,
    maxFiles: number = 200
): Promise<string[]>
⋮----
const walk = async (dir: string): Promise<void> =>
⋮----
// Paraleliza fs.stat em todas as entradas do diretório
⋮----
/**
   * Garante que o índice existe para o diretório informado.
   * Se o cache estiver atualizado, reutiliza.
   * Indexa apenas arquivos novos/modificados.
   */
async ensureIndex(rootDir: string): Promise<void>
⋮----
// 1. Coleta arquivos
⋮----
// 2. Monta lista com DOCUMENTACAO_PROJETO.md sempre primeiro
⋮----
// 3. Verifica quais precisam ser (re)indexados — paralelizado
⋮----
// Se não há nada para processar, usa índice existente
⋮----
// 4. Indexa arquivos novos/modificados — paralelizado com allSettled
⋮----
// Gera embeddings em paralelo para cada chunk deste arquivo
⋮----
// resultados rejected (erro de leitura/embed) são ignorados
⋮----
// 5. Descarrega modelo de embedding
⋮----
// 6. Mescla com índice existente
⋮----
// Paraleliza fs.stat para todos os arquivos processados
⋮----
// 7. Salva índice
⋮----
// 8. Constrói/atualiza o KnowledgeGraph (se habilitado)
⋮----
/**
   * Busca os chunks mais similares à consulta.
   * Se o GraphRAGManager estiver disponível, enriquece os resultados
   * com expansão de vizinhança no grafo de conhecimento.
   */
async retrieve(query: string, rootDir: string): Promise<SearchMatch[]>
⋮----
// Carrega índice
⋮----
// Busca vetorial base
⋮----
// Busca híbrida: vetorial + grafo de conhecimento
⋮----
/**
   * Formata os resultados em contexto.
   */
formatContext(matches: SearchMatch[]): string

================
File: src/core/rag/ReActLoop.ts
================
/**
 * ReActLoop: Execução do ciclo Reasoning + Acting (SRP).
 *
 * Responsabilidade Única: Gerenciar o loop de interação ReAct:
 * 1. Concatena histórico de mensagens em um prompt único
 * 2. Envia para o provider e interpreta ações vs resposta final
 * 3. Se for JSON mode (--json), usa ToolRegistry + formato tool_call/final_response
 * 4. Se for modo texto, usa ACTION/FINAL_ANSWER + CommandExecutor
 * 5. Se streamMode=true, usa provider.streamChat() com callback onToken
 *
 * Compatível com IProvider.chat(request: ChatRequest): ChatResponse
 * e opcionalmente provider.streamChat(request: ChatRequest): AsyncIterable<string>
 * (prompt único, não array de mensagens — formata tudo inline)
 */
⋮----
import type { IProvider } from '../../providers/types';
import type { CommandExecutor } from '../CommandExecutor';
import type { ToolRegistry } from '../ToolRegistry';
import type { Reflector, ReflectionResult, ReflectionError } from '../Reflector';
import { JsonValidator } from '../../validation/JsonValidator';
import { assessCompressionNeed, CompressionTrigger } from '../IContextCompressor';
import type { IContextCompressor } from '../IContextCompressor';
import { TokenEstimator } from '../TokenEstimator';
⋮----
export interface ReActMessage {
  role: 'user' | 'assistant' | 'system';
  content: string;
}
⋮----
export interface ReActResult {
  finalAnswer: string;
  iterations: number;
  /** Status da correção do Reflector */
  correctionStatus?: 'stable' | 'suspicious' | 'rejected';
  /** Erros encontrados pelo Reflector (vazio se não houve reflexão) */
  errors?: ReflectionError[];
  /** Indica se a compressão de contexto foi aplicada no histórico */
  wasCompressed?: boolean;
  /** Taxa de compressão (0.0 a 1.0), presente apenas se wasCompressed for true */
  compressionRatio?: number;
}
⋮----
/** Status da correção do Reflector */
⋮----
/** Erros encontrados pelo Reflector (vazio se não houve reflexão) */
⋮----
/** Indica se a compressão de contexto foi aplicada no histórico */
⋮----
/** Taxa de compressão (0.0 a 1.0), presente apenas se wasCompressed for true */
⋮----
/** Opções de streaming para o ReActLoop */
export interface StreamOptions {
  /** Se true, ativa streaming das respostas do modelo */
  enabled: boolean;
  /**
   * Callback chamado para cada token recebido durante o streaming.
   * Usado para efeito "máquina de escrever" no terminal.
   */
  onToken: (token: string) => void;
}
⋮----
/** Se true, ativa streaming das respostas do modelo */
⋮----
/**
   * Callback chamado para cada token recebido durante o streaming.
   * Usado para efeito "máquina de escrever" no terminal.
   */
⋮----
export class ReActLoop
⋮----
constructor(
    provider: IProvider,
    executor?: CommandExecutor,
    toolRegistry?: ToolRegistry,
    reflector?: Reflector,
    compressor?: IContextCompressor
)
⋮----
/**
   * Concatena o histórico de mensagens em um único prompt texto,
   * no formato esperado pelo Ollama (system + user/assistant turns).
   */
private buildPrompt(
    systemPrompt: string,
    history: ReActMessage[]
): string
⋮----
/**
   * Envia um prompt para o provider e retorna a resposta completa.
   * Se streamMode estiver ativo e o provider suportar streamChat,
   * faz streaming dos tokens + acumula o buffer para o parser.
   */
private async sendPrompt(
    prompt: string,
    model: string,
    temperature: number,
    format?: 'json',
    streamOpts?: StreamOptions
): Promise<string>
⋮----
// Modo streaming: usa streamChat + acumula buffer
⋮----
// Modo normal: usa chat() padrão
⋮----
/**
   * Executa o loop ReAct no modo JSON (tool_call / final_response).
   * Usado quando --json está ativo. Usa ToolRegistry + detecção de loop.
   * Suporta streaming se streamOpts for fornecido.
   */
private async executeJsonMode(
    systemPrompt: string,
    history: ReActMessage[],
    model: string,
    streamOpts?: StreamOptions
): Promise<ReActResult>
⋮----
streamOpts // streaming para todas iterações; sendPrompt decide
⋮----
// Usa JsonValidator para parsear
⋮----
// Se não for JSON, retorna cru como fallback
⋮----
// tool_call → executa e continua
⋮----
// Detecta chamadas repetidas consecutivas (loop infinito)
⋮----
// ── Prevenção de Context Overflow ──
⋮----
// Trunca o toolResult para caber no limite seguro
⋮----
const truncated = toolResult.slice(0, maxToolTokens * 4); // 4 chars ≈ 1 token
⋮----
// final_response → retorna
⋮----
// Formato desconhecido → fallback
⋮----
// Loop esgotado
⋮----
/**
   * Executa o loop ReAct no modo texto (ACTION / FINAL_ANSWER).
   * Formato legado, sem JSON.
   * Suporta streaming se streamOpts for fornecido.
   */
private async executeTextMode(
    systemPrompt: string,
    history: ReActMessage[],
    model: string,
    streamOpts?: StreamOptions
): Promise<ReActResult>
⋮----
streamOpts // streaming para todas iterações; sendPrompt decide
⋮----
/**
   * Aplica o Reflector na resposta final, se disponível e solicitado.
   */
private async applyReflection(
    result: ReActResult,
    model: string,
    reflectFlag: boolean
): Promise<ReActResult>
⋮----
// Só aplica reflexão se:
// 1. A flag --reflect está ativa
// 2. O Reflector foi injetado
// 3. Há uma resposta real (não vazia)
⋮----
/**
   * Ponto de entrada: executa o loop ReAct no modo apropriado.
   *
   * @param systemPrompt Prompt de sistema
   * @param history Histórico de mensagens
   * @param model Modelo (default: tinyllama:1b)
   * @param options.jsonMode Se true, usa JSON mode (tool_call/final_response)
   * @param options.reflect Se true, aplica Reflector pós-resposta (self-correction)
   * @param options.stream Opções de streaming (se undefined, usa chat() normal)
   * @returns Resultado com resposta final e número de iterações
   */
async execute(
    systemPrompt: string,
    history: ReActMessage[],
    model?: string,
    options?: { jsonMode?: boolean; reflect?: boolean; stream?: StreamOptions }
): Promise<ReActResult>
⋮----
// ── Compressão de contexto (pré-buildPrompt) ──
⋮----
// Só adiciona workingMemory ao histórico se tiver conteúdo real
// Isso evita mensagens [SYSTEM]: vazias no SessionStore (sessões fantasmas)
⋮----
// Aplica reflexão após a resposta final (ambos os modos)

================
File: src/core/rag/Retriever.ts
================
/**
 * Retriever: Busca semântica por cosine similarity (SRP).
 *
 * Responsabilidade Única:
 * - Calcular similaridade entre vetores (cosine similarity)
 * - Filtrar por threshold
 * - Ordenar e retornar top-K
 *
 * Função pura — sem estado, sem I/O. Ideal para testes unitários.
 */
⋮----
import type { ChunkEntry } from './VectorStore';
⋮----
export interface SearchMatch {
  chunk: ChunkEntry;
  score: number; // 0.0 a 1.0
}
⋮----
score: number; // 0.0 a 1.0
⋮----
/**
 * Cosine Similarity — implementação nativa TypeScript.
 * cos(θ) = (A·B) / (||A|| × ||B||)
 */
export function cosineSimilarity(a: number[], b: number[]): number
⋮----
export class Retriever
⋮----
/**
   * Busca os chunks mais similares a um vetor de consulta.
   *
   * @param queryEmbedding Vetor da consulta (384-dim)
   * @param chunks Lista de chunks candidatos
   * @returns Top-K matches ordenados por similaridade (decrescente)
   */
retrieve(
    queryEmbedding: number[],
    chunks: ChunkEntry[]
): SearchMatch[]
⋮----
/**
   * Formata os matches em um bloco de contexto para o prompt.
   *
   * @param matches Resultados da busca
   * @param maxChars Limite de caracteres (padrão 4000)
   * @returns String formatada com citação de fonte [arquivo:linha]
   */
formatContext(matches: SearchMatch[], maxChars: number = 4000): string

================
File: src/core/rag/TypescriptASTAdapter.ts
================
/**
 * TypescriptASTAdapter: Adaptador para o compilador TypeScript (DIP).
 *
 * Implementa IASTParser usando a API nativa do TypeScript (ts.createSourceFile).
 * Aproveita o pacote 'typescript' já instalado como devDependency — sem
 * dependências externas adicionais (regra Zero Dependências).
 *
 * Extrai nós semânticos do código fonte:
 * - FunctionDeclaration, ClassDeclaration, MethodDeclaration
 * - InterfaceDeclaration, EnumDeclaration, TypeAliasDeclaration
 * - VariableStatement (const/let/var com arrow functions)
 * - ExportAssignment (export default)
 */
⋮----
import type { IASTParser, ASTNode } from './IASTParser';
import { getNodeName as sharedGetNodeName } from '../astUtils';
⋮----
export class TypescriptASTAdapter implements IASTParser
⋮----
/**
   * Extrai nós semânticos do código fonte TypeScript/JavaScript.
   *
   * Percorre a árvore sintática e extrai declarações de alto nível que
   * representam blocos semânticos completos (funções, classes, etc.).
   *
   * @param source Código fonte completo
   * @returns Array de nós AST ordenados por startLine.
   *          Array vazio se o parse falhar ou código for inválido.
   */
parse(source: string): ASTNode[]
⋮----
// Ordena por startLine
⋮----
/**
   * Percorre recursivamente os nós filhos e extrai os nós semânticos.
   *
   * @param node Nó atual da AST
   * @param source Código fonte original
   * @param sourceFile SourceFile para calcular line/character
   * @param nodes Array acumulador de nós encontrados
   */
private extractNodes(
    node: ts.Node,
    source: string,
    sourceFile: ts.SourceFile,
    nodes: ASTNode[]
): void
⋮----
// Só processa nós de alto nível (statements) exceto para métodos de classe
⋮----
// Percorre filhos — para MethodDeclaration dentro de ClassDeclaration,
// isso captura os métodos como nós independentes
⋮----
/**
   * Verifica se um nó da AST deve ser extraído como chunk semântico.
   */
private shouldExtractNode(node: ts.Node): boolean
⋮----
// Declarações de alto nível
⋮----
// ExportAssignment: export default <expression>
// NOTA: export default class/function gera ClassDeclaration/FunctionDeclaration
// com modificador, NÃO ExportAssignment.
⋮----
// Métodos dentro de classe (MethodDeclaration, Constructor, GetAccessor, SetAccessor)
⋮----
// VariableStatement com arrow function ou declaração relevante
⋮----
/**
   * Extrai o nome do nó.
   * Delega para a função compartilhada em astUtils.ts.
   */
private getNodeName(node: ts.Node, _source: string, sourceFile: ts.SourceFile): string
⋮----
/**
   * Retorna o tipo do nó como string legível.
   */
private getNodeKind(node: ts.Node): string

================
File: src/core/rag/VectorStore.ts
================
/**
 * VectorStore: Persistência do índice de embeddings em disco (SRP).
 *
 * Responsabilidade Única:
 * - Salvar/carregar o índice de chunks + embeddings em JSON
 * - Gerenciar metadados de arquivos (mtime para cache)
 * - Criar diretório de cache se necessário
 *
 * Não gera embeddings, não faz chunking, não busca.
 * Apenas I/O em disco.
 */
⋮----
/**
 * Um chunk de texto com metadados e embedding
 */
export interface ChunkEntry {
  id: string;
  text: string;
  filePath: string;
  startLine: number;
  endLine: number;
  embedding: number[];
  indexedAt: number;
}
⋮----
interface FileMeta {
  mtime: number;
  chunks: number;
}
⋮----
interface IndexStore {
  formatVersion: 1;
  embedModel: string;
  dimensions: number;
  indexedAt: number;
  files: Record<string, FileMeta>;
  chunks: ChunkEntry[];
}
⋮----
export class VectorStore
⋮----
/**
   * Salva o índice em disco.
   * Cria o diretório de cache automaticamente.
   */
async save(rootDir: string, store: IndexStore): Promise<void>
⋮----
/**
   * Carrega o índice do disco.
   * Retorna null se não existir (primeira execução).
   */
async load(rootDir: string): Promise<IndexStore | null>
⋮----
private getCacheDir(rootDir: string): string
⋮----
private getIndexPath(rootDir: string): string

================
File: src/core/AppContext.ts
================
/**
 * AppContext: Container de injeção de dependência (DIP).
 *
 * Dependency Inversion Principle (DIP):
 * - Módulos de ALTO nível (CLI, comandos) NÃO dependem de módulos de BAIXO nível
 *   (OllamaProvider, FileReader concretos).
 * - Ambos dependem de abstrações (IProvider, FileReader, CommandExecutor).
 * - AppContext é o "ponto de cola" onde as implementações concretas são injetadas.
 *
 * Benefícios:
 * 1. Testabilidade: nos testes unitários, podemos passar mocks/stubs
 *    no lugar das implementações reais.
 * 2. Flexibilidade: para trocar de provider (ex: Ollama → OpenRouter),
 *    basta mudar a fábrica em um único lugar.
 * 3. Configuração por flags CLI: os parâmetros (host, porta) são passados
 *    pelo AppContext, não lidos diretamente dentro do comando.
 */
⋮----
import type { IProvider, IEmbedProvider } from '../providers/types';
import { FileReader } from './FileReader';
import { CommandExecutor } from './CommandExecutor';
import { ToolRegistry } from './ToolRegistry';
import { ProviderFactory } from './ProviderFactory';
import type { ProviderConfig } from './ProviderFactory';
import { SessionManager } from './SessionManager';
import { SessionStore } from './SessionStore';
import type { ReActMessage } from './rag/ReActLoop';
import { ASTEditor } from './ASTEditor';
import { SearchReplaceEditor } from './SearchReplaceEditor';
⋮----
export interface AppContextConfig {
  provider: ProviderConfig;
  model?: string;
  jsonMode?: boolean;
  ragDir?: string;
  sessionId?: string;
  newSession?: boolean;
}
⋮----
export class AppContext
⋮----
constructor(config: AppContextConfig)
⋮----
// Utilitários base — sempre os mesmos
⋮----
// Provider via fábrica (OCP) — fácil de estender para novos providers
⋮----
// ToolRegistry — configurado com as tools padrão
⋮----
// Configurações de execução
⋮----
// SessionManager — gerencia histórico da conversa multi-turn
⋮----
// NOTA: initSession NÃO é chamado aqui no construtor para evitar race condition.
// Chame app.initialize() após construir o AppContext para carregar/criar a sessão.
⋮----
/**
   * Inicializa a sessão de conversa (carrega existente ou cria nova).
   *
   * Deve ser chamado após a construção do AppContext, antes de usar o sessionManager.
   * Separar do construtor evita race condition entre initSession() assíncrono
   * e addMessage() síncrono.
   *
   * @param config Configuração original passada no construtor
   */
async initialize(config?: AppContextConfig): Promise<void>
⋮----
if (!sessionId && !newSession) return; // Nada a inicializar
⋮----
private async initSession(sessionId?: string, newSession?: boolean): Promise<void>
⋮----
/**
   * Helper para obter o histórico de mensagens da sessão ativa.
   * Retorna array vazio se não houver sessão ativa.
   */
getSessionHistory(): ReActMessage[]
⋮----
/**
   * Registra as tools padrão disponíveis para o ReAct loop.
   * São injetadas via DI, mas configuradas com as dependências reais.
   */
private setupDefaultTools(): void
⋮----
// Tool: readFile
⋮----
// Tool: readDir
⋮----
// Tool: execute
⋮----
// Tool: editSymbol — edição estrutural de símbolos TypeScript via AST
⋮----
// Tool: searchReplace — edição textual por bloco exato
⋮----
// Tool: writeFile — criação/atualização de ficheiros
⋮----
// Tool: readFileForEdit — leitura com numeração de linhas

================
File: src/core/ASTEditor.ts
================
/**
 * ASTEditor: Edição estrutural de arquivos TypeScript por nome de símbolo.
 *
 * Permite substituir o corpo de uma declaração (função, classe, interface,
 * variável, etc.) em arquivos .ts mantendo o restante do arquivo intacto.
 *
 * Dependências (DIP):
 * - FileReader: leitura do arquivo fonte
 * - fs.writeFile (nativo): escrita do arquivo modificado
 *
 * Uso:
 *   const editor = new ASTEditor(fileReader);
 *   const result = await editor.replaceSymbol('src/foo.ts', 'myFunction', '...new code...');
 */
⋮----
import { FileReader } from './FileReader';
import { findTopLevelSymbol } from './astUtils';
⋮----
/**
 * Resultado de uma operação de substituição de símbolo.
 */
export interface ASTEditResult {
  /** true se a operação foi concluída sem erros */
  success: boolean;
  /** true se o símbolo foi encontrado no arquivo */
  symbolFound: boolean;
  /** Caminho do arquivo alvo */
  filePath: string;
  /** Nome do símbolo procurado */
  symbolName: string;
  /** Mensagem de erro, se houver */
  error?: string;
}
⋮----
/** true se a operação foi concluída sem erros */
⋮----
/** true se o símbolo foi encontrado no arquivo */
⋮----
/** Caminho do arquivo alvo */
⋮----
/** Nome do símbolo procurado */
⋮----
/** Mensagem de erro, se houver */
⋮----
export class ASTEditor
⋮----
/**
   * @param fileReader Instância de FileReader para ler arquivos do disco
   */
constructor(private readonly fileReader: FileReader)
⋮----
/**
   * Substitui o código de um símbolo top-level em um arquivo TypeScript.
   *
   * Pipeline:
   *   1. Lê o arquivo via FileReader.readFile()
   *   2. Parseia com ts.createSourceFile()
   *   3. Busca o nó top-level cujo nome corresponda a symbolName
   *   4. Se encontrado, substitui o trecho (node.getFullStart() a node.getEnd())
   *      por newCode
   *   5. Escreve o resultado de volta no arquivo via fs.writeFile()
   *
   * @param filePath Caminho do arquivo .ts a editar
   * @param symbolName Nome do símbolo top-level a substituir
   * @param newCode Novo código fonte para substituir o símbolo
   * @returns ASTEditResult indicando sucesso/falha
   */
async replaceSymbol(
    filePath: string,
    symbolName: string,
    newCode: string
): Promise<ASTEditResult>
⋮----
// 1. Ler o arquivo
⋮----
// 2. Parsear com TypeScript
⋮----
// 3. Buscar o símbolo top-level
⋮----
// 4. Calcular offsets e construir novo conteúdo
const fullStart = node.getFullStart(); // includes leading comments/whitespace
⋮----
// 5. Validar caminho (Path Traversal) e escrever o arquivo modificado

================
File: src/core/astUtils.ts
================
/**
 * astUtils: Funções utilitárias compartilhadas para manipulação da AST do TypeScript.
 *
 * Extraída do TypescriptASTAdapter para reuso no ASTEditor.
 * Mantida aqui como módulo independente (sem dependências externas além do 'typescript').
 */
⋮----
/**
 * Extrai o nome de um nó da AST (top-level symbol).
 *
 * Lógica idêntica à usada no TypescriptASTAdapter.getNodeName().
 *
 * @param node Nó da AST do TypeScript
 * @param sourceFile SourceFile de referência para obter text spans
 * @returns Nome do símbolo ou '<anonymous>' se não for nomeável
 */
export function getNodeName(node: ts.Node, sourceFile: ts.SourceFile): string
⋮----
// Para VariableStatement, tenta extrair o nome da variável
⋮----
// Para constructor sem nome explícito
⋮----
/**
 * Busca um símbolo top-level pelo nome no SourceFile.
 *
 * Percorre os nós filhos do SourceFile (apenas nível superior) e retorna
 * o primeiro nó cujo nome corresponda exatamente a `symbolName`.
 *
 * @param sourceFile SourceFile parseado
 * @param symbolName Nome do símbolo a buscar
 * @returns O nó encontrado, ou undefined se não existir
 */
export function findTopLevelSymbol(
  sourceFile: ts.SourceFile,
  symbolName: string
): ts.Node | undefined
⋮----
if (found) return; // já encontrou

================
File: src/core/CommandExecutor.ts
================
/**
 * Resultado da execução de um comando shell.
 * Contém as saídas stdout/stderr, código de saída e sinal (se houve).
 */
export interface CommandResult {
  stdout: string;
  stderr: string;
  exitCode: number | null;
  signal: NodeJS.Signals | null;
}
⋮----
/**
 * CommandExecutor: Camada de abstração sobre child_process.spawn do Node.js.
 *
 * Fluxo de dados:
 * CLI ──(cmd+args)──▶ execute() ──(spawn)──▶ processo filho ──(stdout/stderr streams)──▶ CommandResult
 *
 * Diferente de exec() que bufferiza tudo em memória, spawn() trabalha com streams,
 * o que é mais seguro para comandos com saída longa e permite timeout nativo.
 *
 * shell: false → previne injeção de comandos (não passa pelo interpretador shell).
 */
export class CommandExecutor
⋮----
/**
   * Executa um comando de forma segura e assíncrona.
   *
   * Pipeline:
   *   1. cp.spawn(command, args, options) → cria processo filho
   *   2. Escuta stdout (child.stdout.on('data')) → acumula chunks Buffer
   *   3. Escuta stderr (child.stderr.on('data')) → acumula chunks Buffer
   *   4. child.on('error') → rejeita Promise se o spawn falhar
   *   5. child.on('close') → resolve Promise com stdout/stderr/exitCode
   *   6. Timeout (padrão 60s) → encerra o processo automaticamente
   *
   * Segurança:
   *   - shell: false → o comando é executado diretamente, sem interpretador.
   *     Ex: execute('ls', ['-la']) → spawn('ls', ['-la'])
   *     Isso evita vulnerabilidades como injeção de shell (ex: 'file; rm -rf /').
   *   - timeout → evita processos zumbi.
   *
   * @param command  Nome do executável (ex: 'ls', 'git', 'node')
   * @param args     Argumentos do comando como array de strings
   * @param options  Opções: cwd (working directory), timeout (ms)
   * @returns Promise com stdout, stderr, exitCode e signal
   */
execute(
    command: string,
    args: string[] = [],
    options?: { cwd?: string; timeout?: number }
): Promise<CommandResult>
⋮----
// spawn: cria o processo filho sem shell intermediário
⋮----
shell: false, // Sem shell → sem injeção de comandos
timeout: options?.timeout ?? 60_000, // 60 segundos padrão
⋮----
// stdout: saída padrão do processo
⋮----
// stderr: saída de erro do processo (logs, erros, etc.)
⋮----
// 'error': falha ao criar o processo (comando inexistente, permissão, etc.)
⋮----
// 'close': processo terminou (normalmente, por timeout ou por sinal)

================
File: src/core/ErrorJournal.ts
================
/**
 * ErrorJournal: Persistência de erros de reflexão em disco.
 *
 * SRP: Responsabilidade única — salvar e recuperar registros de erros
 * encontrados pelo Reflector durante a auto-correção.
 *
 * Sem dependências externas: usa apenas node:fs/promises nativo.
 * Operações assíncronas para não bloquear o Event Loop.
 *
 * Os registros são salvos em .soberano/error-journal.json na raiz do projeto
 *
 * Formato do arquivo:
 * ```json
 * {
 *   "version": 1,
 *   "entries": [
 *     {
 *       "timestamp": "2026-05-09T10:30:00.000Z",
 *       "model": "llama3.2:1b",
 *       "type": "hallucination",
 *       "description": "Menção a função inexistente 'fs.readJsonSync'",
 *       "correctionStatus": "stable",
 *       "originalLength": 245,
 *       "correctedLength": 260
 *     }
 *   ]
 * }
 * ```
 */
⋮----
import type { CorrectionStatus } from '../providers/types';
⋮----
/** Um único registro de erro no journal */
export interface ErrorJournalEntry {
  timestamp: string;
  model: string;
  type: 'hallucination' | 'syntax' | 'inconsistency' | 'logic';
  description: string;
  correctionStatus: CorrectionStatus;
  originalLength: number;
  correctedLength: number;
}
⋮----
/** Estrutura completa do journal */
export interface ErrorJournalData {
  version: number;
  entries: ErrorJournalEntry[];
}
⋮----
export class ErrorJournal
⋮----
/**
   * @param filePath Caminho do arquivo JSON (opcional, default .soberano/error-journal.json na raiz do projeto)
   * @param maxEntries Número máximo de entradas (default 1000)
   */
constructor(filePath?: string, maxEntries: number = 1000)
⋮----
/**
   * Carrega o journal do disco (assíncrono).
   * Se o arquivo não existir, retorna um journal vazio.
   */
private async load(): Promise<ErrorJournalData>
⋮----
/**
   * Salva o journal no disco (assíncrono).
   */
private async save(data: ErrorJournalData): Promise<void>
⋮----
// Falha silenciosa — o journal é auxiliar, não deve quebrar o fluxo
⋮----
/**
   * Adiciona um entry ao journal e persiste.
   *
   * @param entry Dados do erro a ser registrado
   */
async addEntry(entry: ErrorJournalEntry): Promise<void>
⋮----
// Limita o número de entradas (FIFO)
⋮----
/**
   * Retorna todos os entries do journal (mais recentes primeiro).
   */
async getEntries(): Promise<ErrorJournalEntry[]>
⋮----
/**
   * Retorna os últimos N entries, filtrados por tipo (opcional).
   *
   * @param limit Número máximo de entries
   * @param type Filter por tipo de erro (opcional)
   */
async getRecentEntries(limit: number = 10, type?: string): Promise<ErrorJournalEntry[]>
⋮----
/**
   * Retorna estatísticas agregadas do journal.
   */
async getStats(): Promise<

================
File: src/core/FileReader.ts
================
import type { Dirent } from 'node:fs';
⋮----
/**
 * Resultado de uma busca textual em arquivos.
 * Cada ocorrência contém o caminho do arquivo, número da linha e o texto encontrado.
 */
export interface SearchResult {
  file: string;
  line: number;
  content: string;
}
⋮----
/**
 * FileReader: Camada de abstração sobre o sistema de arquivos do Node.js.
 *
 * Fluxo de dados:
 * CLI ──(path:string)──▶ readFile() ──(buffer)──▶ utf-8 string ──▶ console
 * CLI ──(dir:string)──▶ readDir() ──(Dirent[])──▶ string[] (nomes) ──▶ console
 * CLI ──(root+pattern)──▶ searchFiles() ──(walk recursivo)──▶ SearchResult[] ──▶ console
 *
 * Toda operação é assíncrona (Promise-based) para não bloquear o event loop.
 *
 * Segurança: Todos os métodos que acessam o sistema de arquivos usam
 * `resolveSecurePath` para prevenir Path Traversal (escapar do diretório do projeto).
 */
export class FileReader
⋮----
/**
   * Resolve um caminho de usuário e valida que ele está dentro do diretório do projeto.
   *
   * Segurança contra Path Traversal (SEC-01, SEC-02) e Symlink Attacks (C-1):
   * 1. Resolve o caminho absoluto via path.resolve()
   * 2. Resolve symlinks via fs.promises.realpath() para evitar que atalhos maliciosos
   *    desviem do diretório do projeto (C-1)
   * 3. Também resolve symlinks do rootDir (caso o projeto em si seja um link)
   * 4. Verifica se o caminho real começa com o diretório raiz real do projeto
   * 5. Se estiver fora (ex: ../../.ssh/id_rsa via symlink), lança erro
   *
   * @param userPath Caminho fornecido pelo usuário/agente (absoluto ou relativo)
   * @returns Caminho absoluto seguro dentro do projeto (com symlinks resolvidos)
   * @throws Error se o caminho resolvido estiver fora do diretório do projeto
   */
static async resolveSecurePath(userPath: string): Promise<string>
⋮----
// Tenta resolver symlinks do caminho completo.
// Se o ficheiro não existir (ENOENT), resolve pelo diretório pai
// para ainda validar que está dentro do projeto.
⋮----
// Caminho não existe no disco → resolve pelo diretório pai
⋮----
throw err; // Outro erro (permissão, etc) — relança
⋮----
/**
   * Lê o conteúdo completo de um arquivo em disco.
   *
   * Pipeline:
   *   1. resolveSecurePath() → validação anti Path Traversal
   *   2. fsp.readFile(path, 'utf-8') → buffer binário decodificado como string
   *   3. Retorna a string diretamente
   *   4. Em caso de erro (arquivo inexistente, permissão), lança exceção
   *
   * @param filePath Caminho absoluto ou relativo do arquivo
   * @returns Conteúdo textual do arquivo
   */
async readFile(filePath: string): Promise<string>
⋮----
/**
   * Lista as entradas (arquivos e subdiretórios) de um diretório.
   *
   * Pipeline:
   *   1. resolveSecurePath() → validação anti Path Traversal
   *   2. fsp.readdir(path) → array de nomes (somente strings, sem metadados)
   *   3. Retorna o array diretamente
   *
   * @param dirPath Caminho do diretório
   * @returns Array com os nomes dos arquivos/diretórios dentro de dirPath
   */
async readDir(dirPath: string): Promise<string[]>
⋮----
/**
   * Busca recursiva em arquivos: percorre uma árvore de diretórios e encontra
   * linhas que casam com um padrão (string ou RegExp).
   *
   * Fluxo detalhado da recursão:
   *   1. Converte `pattern` string em RegExp com flag 'g' (global)
   *   2. `walk(dir)` é chamada recursivamente:
   *      a) fsp.readdir(dir, { withFileTypes: true }) → Dirent[] com metadados
   *      b) Para cada entry:
   *         - Se for diretório → walk(entry) (recursão)
   *         - Se for arquivo → lê conteúdo como utf-8, quebra em linhas,
   *           testa cada linha contra o regex
   *   3. Resultados são acumulados no array `results`
   *   4. Para quando atinge `maxResults` (padrão 100) para evitar consumo excessivo
   *
   * Tratamento de erros silencioso:
   *   - Diretórios sem permissão → ignorados (não interrompe a busca)
   *   - Arquivos binários (não UTF-8) → ignorados
   *
   * @param rootDir  Diretório raiz da busca
   * @param pattern  Padrão a buscar (string literal ou regex)
   * @param maxResults  Limite de ocorrências (padrão 100)
   * @returns Array de SearchResult ordenado por profundidade de descoberta
   */
async searchFiles(
    rootDir: string,
    pattern: string | RegExp,
    maxResults: number = 100
): Promise<SearchResult[]>
⋮----
// Converte string em RegExp — cada linha é testada individualmente.
// Sem flag 'g' para evitar falsos negativos por lastIndex (RegExp global mantém estado interno).
⋮----
/**
     * Função interna recursiva que percorre a árvore de diretórios.
     * Captura `results` e `maxResults` do closure externo via closure.
     */
const walk = async (dir: string): Promise<void> =>
⋮----
// Early return: já atingimos o limite de resultados
⋮----
// withFileTypes: true → retorna objetos Dirent em vez de strings,
// permitindo verificar isDirectory() / isFile() sem stat extra
⋮----
return; // Silencia diretórios sem permissão de leitura
⋮----
if (results.length >= maxResults) break; // Early break
⋮----
// Constrói caminho absoluto juntando dir + nome da entrada
⋮----
// Ramo recursivo: desce um nível na árvore
⋮----
// Lê o arquivo inteiro como string UTF-8
⋮----
// Divide em linhas (LF ou CRLF)
⋮----
// Itera cada linha testando o regex
⋮----
line: i + 1,      // Linhas são 1-indexed
content: lines[i].trim(), // Remove espaços extras
⋮----
// Ignora arquivos binários (não decodificáveis como UTF-8)
// ou arquivos sem permissão de leitura
⋮----
// Inicia a recursão a partir do diretório raiz

================
File: src/core/IContextCompressor.ts
================
/**
 * IContextCompressor: Contrato para compressão de contexto (ISP + DIP).
 *
 * Responsabilidade Única: Definir o contrato de compressão de histórico
 * de mensagens ReAct, sem qualquer lógica de I/O.
 *
 * - workingMemory: bloco consolidado do histórico (sumarização/compressão)
 * - keptMessages: últimas N mensagens preservadas intactas (mínimo última resposta do assistente)
 * - compressionRatio: 0.0 (sem compressão) a 1.0 (100% comprimido), para logging
 */
⋮----
import type { ReActMessage } from './rag/ReActLoop';
⋮----
/**
 * Resultado da compressão de contexto.
 */
export interface CompressedContext {
  /** Bloco consolidado com o histórico comprimido/sumarizado */
  workingMemory: string;
  /** Últimas N mensagens preservadas intactas (mínimo última resposta do assistente) */
  keptMessages: ReActMessage[];
  /** Taxa de compressão: 0.0 (sem compressão) a 1.0 (100% comprimido) */
  compressionRatio: number;
}
⋮----
/** Bloco consolidado com o histórico comprimido/sumarizado */
⋮----
/** Últimas N mensagens preservadas intactas (mínimo última resposta do assistente) */
⋮----
/** Taxa de compressão: 0.0 (sem compressão) a 1.0 (100% comprimido) */
⋮----
/**
 * Gatilhos de compressão baseados na porcentagem de uso do contexto.
 */
export enum CompressionTrigger {
  /** Nenhuma compressão necessária */
  NONE = 'NONE',
  /** Compressão suave: >= 70% do contexto usado */
  SOFT = 'SOFT',
  /** Compressão agressiva: >= 85% do contexto usado */
  HARD = 'HARD',
}
⋮----
/** Nenhuma compressão necessária */
⋮----
/** Compressão suave: >= 70% do contexto usado */
⋮----
/** Compressão agressiva: >= 85% do contexto usado */
⋮----
/**
 * Avalia a necessidade de compressão baseada na proporção de tokens estimados
 * versus o limite máximo de contexto do modelo.
 *
 * @param estimatedTokens - Quantidade estimada de tokens no histórico atual
 * @param contextLimit - Limite máximo de tokens de contexto do modelo
 * @returns CompressionTrigger indicando o nível de compressão necessário
 *
 * @example
 * ```ts
 * assessCompressionNeed(690, 1000) // 69% → CompressionTrigger.NONE
 * assessCompressionNeed(700, 1000) // 70% → CompressionTrigger.SOFT
 * assessCompressionNeed(850, 1000) // 85% → CompressionTrigger.HARD
 * ```
 */
export function assessCompressionNeed(
  estimatedTokens: number,
  contextLimit: number,
): CompressionTrigger
⋮----
/**
 * Interface para implementadores do compressor de contexto (DIP).
 */
export interface IContextCompressor {
  /**
   * Comprime o histórico de mensagens ReAct.
   *
   * @param history - Array completo de mensagens do histórico
   * @param model - Identificador do modelo sendo usado (para limites de contexto)
   * @param systemPrompt - Prompt de sistema original
   * @returns Promise com o contexto comprimido
   */
  compress(
    history: ReActMessage[],
    model: string,
    systemPrompt: string,
  ): Promise<CompressedContext>;
}
⋮----
/**
   * Comprime o histórico de mensagens ReAct.
   *
   * @param history - Array completo de mensagens do histórico
   * @param model - Identificador do modelo sendo usado (para limites de contexto)
   * @param systemPrompt - Prompt de sistema original
   * @returns Promise com o contexto comprimido
   */
compress(
    history: ReActMessage[],
    model: string,
    systemPrompt: string,
  ): Promise<CompressedContext>;

================
File: src/core/index.ts
================
/**
 * Core module index — barrel file que expõe toda a API pública do core.
 *
 * Separação clara entre:
 * - Utilitários base (FileReader, CommandExecutor, ToolRegistry)
 * - Fábrica de providers (ProviderFactory)
 * - Pipeline RAG (RAGManager, Chunker, Embedder, VectorStore, Retriever)
 * - Loop ReAct + PromptBuilder (ReActLoop, PromptBuilder)
 * - Validação JSON (JsonValidator)
 */
⋮----
// Utilitários base
⋮----
// Context Compressor (ISP + DIP)
⋮----
// Fábrica de providers (OCP)
⋮----
// Container de injeção de dependência (DIP)
⋮----
// Pipeline RAG
⋮----
// ReAct Loop + Prompt Builder (Strategy pattern)
⋮----
// Reflector (self-correction)
⋮----
// ErrorJournal (persistência de erros de reflexão)
⋮----
// Session Store (Memória Episódica / Multi-turn)
⋮----
// Session Manager (orquestração de sessão ativa)
⋮----
// AST Editor (edição estrutural de código)
⋮----
// SearchReplace Editor (edição por bloco exato)
⋮----
// Validação JSON

================
File: src/core/ProviderFactory.ts
================
/**
 * ProviderFactory: Fábrica de provedores de linguagem (OCP).
 *
 * Open/Closed Principle: Esta classe está ABERTA para extensão
 * (novos provedores podem ser adicionados sem modificar código existente)
 * e FECHADA para modificação (a lógica de seleção é genérica).
 *
 * Para adicionar um novo provider:
 * 1. Crie a classe que implementa IProvider (ex: OpenRouterProvider)
 * 2. Adicione o case no switch ou no map
 * 3. Pronto — nenhuma outra parte do sistema precisa mudar
 */
⋮----
import type { IProvider, IEmbedProvider } from '../providers/types';
import { OllamaProvider } from '../providers/OllamaProvider';
⋮----
export type ProviderType = 'ollama';
⋮----
export interface ProviderConfig {
  type: ProviderType;
  host?: string;
  port?: number;
}
⋮----
export class ProviderFactory
⋮----
/**
   * Cria um provider baseado na configuração.
   *
   * @param config Configuração do provider (tipo, host, porta)
   * @returns Instância de IProvider
   */
static createProvider(config: ProviderConfig): IProvider
⋮----
/**
   * Cria um provider que também suporta embeddings (IEmbedProvider).
   * Útil para garantia em tempo de compilação de que o provider
   * implementa o método embed().
   */
static createEmbedProvider(config: ProviderConfig): IEmbedProvider

================
File: src/core/Reflector.ts
================
/**
 * Reflector: Módulo de auto-correção (Self-Correction Layer).
 *
 * Responsabilidade Única (SRP):
 * - Receber uma resposta gerada pelo modelo
 * - Submetê-la a um sistema de crítica (segunda chamada ao provider)
 * - Retornar a versão corrigida, se necessário
 *
 * O Reflector NÃO modifica o ReActLoop — ele é um step opcional
 * acionado APÓS a resposta final ser obtida.
 *
 * Zero dependências externas — usa apenas ICritiqueProvider injetado.
 * Compatível com recursos limitados (12GB RAM): reusa o mesmo modelo,
 * sem duplicar contexto do histórico ReAct.
 */
⋮----
import type { ICritiqueProvider, CorrectionStatus } from '../providers/types';
⋮----
// ── Interfaces públicas ──
⋮----
/** Erro individual encontrado pelo crítico */
export interface ReflectionError {
  type: 'hallucination' | 'syntax' | 'inconsistency' | 'logic';
  description: string;
}
⋮----
/** Resultado completo da reflexão */
export interface ReflectionResult {
  /** Conteúdo final (corrigido ou original) */
  finalContent: string;
  /** Status da correção ('stable' | 'suspicious' | 'rejected') */
  correctionStatus: CorrectionStatus;
  /** Lista de erros encontrados (vazia se nenhum) */
  errors: ReflectionError[];
}
⋮----
/** Conteúdo final (corrigido ou original) */
⋮----
/** Status da correção ('stable' | 'suspicious' | 'rejected') */
⋮----
/** Lista de erros encontrados (vazia se nenhum) */
⋮----
// ── System Prompt de Crítica ──
⋮----
/**
 * Prompt de crítica otimizado (TASK 9).
 *
 * Otimizações:
 * - Reduzido de ~40 linhas para ~20 linhas (menos tokens consumidos)
 * - Prioriza exemplos concretos vs regras genéricas
 * - Usa verbos imperativos no início de cada seção
 * - Formato JSON simplificado
 * - Removeu seções redundantes (contradições lógicas fundidas com alucinações)
 */
⋮----
/**
 * Reflector: Executa uma chamada de crítica sobre a resposta gerada.
 *
 * Fluxo:
 *   1. Monta prompt de crítica com as tools disponíveis
 *   2. Chama critiqueProvider.critique() (via ICritiqueProvider)
 *   3. Parseia o JSON de retorno
 *   4. Decide se houve correção e retorna ReflectionResult
 *
 * SRP: Apenas reflexão crítica. Não gerencia histórico, não executa ferramentas.
 * LSP: Usa ICritiqueProvider em vez de IProvider — o Reflector não precisa
 *      de chat() genérico, apenas do método critique() especializado.
 */
export class Reflector
⋮----
/**
   * @param critiqueProvider Provider especializado para crítica de respostas
   * @param toolRegistry Registro de ferramentas (para listar tools disponíveis)
   * @throws Error se critiqueProvider não for fornecido
   */
constructor(
    private readonly critiqueProvider: ICritiqueProvider,
    private readonly toolRegistry: { getToolNames(): string[] }
)
⋮----
private readonly toolRegistry:
⋮----
/**
   * Submete a resposta do modelo à crítica e retorna o resultado.
   *
   * @param rawAnswer Resposta original gerada pelo modelo
   * @param model     Nome do modelo (mesmo usado na geração)
   * @returns         Resultado da reflexão (corrigido ou original)
   */
async reflect(rawAnswer: string, model: string): Promise<ReflectionResult>
⋮----
// Se a resposta é vazia ou só espaços, não perde tempo com crítica
⋮----
// Monta o prompt de crítica com a lista de tools disponíveis
⋮----
// Usa critique() em vez de chat() — mais específico e com melhor controle
⋮----
temperature: 0.1, // Baixa temperatura = mais determinístico
⋮----
// ── CIRCUIT BREAKER (TASK 3) ──
// Valida a qualidade da correção antes de aceitá-la
⋮----
// Se a correção é quase idêntica ao original (>90% similaridade),
// o crítico provavelmente gerou um falso positivo
⋮----
finalContent: rawAnswer, // Mantém original
correctionStatus: 'suspicious', // Sinaliza suspeita
errors, // Mantém erros reportados para transparência
⋮----
// Se a correção mudou significativamente (<20% similaridade),
// pode ser alucinação do crítico — descarta a correção
⋮----
finalContent: rawAnswer, // Mantém original
⋮----
// Correção válida: mudança moderada e com erros reportados
⋮----
// Sem erros → mantém original (stable)
⋮----
// Timeout ou erro de conexão → retorna original sem reflexão
⋮----
/**
   * Calcula similaridade entre duas strings usando Dice Coefficient
   * sobre bigramas — leve, rápido e eficaz para detectar mudanças reais.
   *
   * Circuit Breaker: evita que falsos positivos do crítico (hasError=true
   * com mudança mínima) ou alucinações (mudança drástica) contaminem
   * a resposta final.
   *
   * @returns Valor entre 0 (completamente diferente) e 1 (idêntico)
   */
private computeSimilarity(a: string, b: string): number
⋮----
const bigrams = (s: string): Set<string> =>
⋮----
/**
   * Parseia o array de erros do JSON, validando o formato.
   */
private parseErrors(raw: unknown): ReflectionError[]

================
File: src/core/SearchReplaceEditor.ts
================
/**
 * SearchReplaceEditor — Edição por bloco exato com normalização.
 *
 * Estratégia:
 * 1. Lê o arquivo via FileReader
 * 2. Divide o conteúdo e o searchBlock em linhas
 * 3. Normaliza linha a linha (CRLF→LF, trimEnd)
 * 4. Busca um bloco contíguo de linhas normalizadas que case exatamente
 * 5. Se match único: substitui as linhas correspondentes no conteúdo original
 * 6. Se 0 matches: retorna falha (matchCount: 0)
 * 7. Se >1 matches: retorna falha (matchCount: N) sem substituir
 * 8. Escreve o arquivo modificado no disco
 */
⋮----
import { FileReader } from './FileReader';
⋮----
/**
 * Resultado da operação de busca e substituição.
 */
export interface SearchReplaceResult {
  /** true se a substituição foi bem-sucedida */
  success: boolean;
  /** Número de ocorrências do bloco de busca encontradas */
  matchCount: number;
  /** Caminho do arquivo alvo */
  filePath: string;
  /** Mensagem de erro em caso de falha */
  error?: string;
}
⋮----
/** true se a substituição foi bem-sucedida */
⋮----
/** Número de ocorrências do bloco de busca encontradas */
⋮----
/** Caminho do arquivo alvo */
⋮----
/** Mensagem de erro em caso de falha */
⋮----
export class SearchReplaceEditor
⋮----
constructor(fileReader: FileReader)
⋮----
/**
   * Normaliza uma única linha:
   * - Converte \r\n para \n (remove \r do final)
   * - Remove trailing whitespace
   * - Preserva indentação inicial
   */
private normalizeLine(line: string): string
⋮----
/**
   * Normaliza o texto completo para comparação.
   */
private normalize(text: string): string
⋮----
/**
   * Aplica uma substituição de bloco exato no arquivo.
   *
   * A busca é feita no conteúdo normalizado (CRLF→LF, trimEnd por linha),
   * mas a substituição ocorre no conteúdo original, preservando
   * terminações de linha originais e whitespace.
   *
   * @param filePath    Caminho do arquivo a ser editado
   * @param searchBlock Bloco de texto a ser encontrado
   * @param replaceBlock Bloco de texto substituto
   * @returns SearchReplaceResult com o resultado da operação
   */
async apply(
    filePath: string,
    searchBlock: string,
    replaceBlock: string
): Promise<SearchReplaceResult>
⋮----
// 1. Ler o conteúdo original do arquivo
⋮----
// 2. Dividir original e search em linhas
⋮----
// 3. Se searchBlock vazio ou sem linhas, retorna falha
⋮----
// 4. Encontrar todas as ocorrências do bloco normalizado
⋮----
// 5. Validar número de matches
⋮----
// 6. Match único — substituir as linhas no conteúdo original
⋮----
// Constrói o novo conteúdo:
// - Linhas antes do match (preservando originais)
// - Bloco de substituição (texto exato fornecido pelo usuário)
// - Linhas após o match (preservando originais)
⋮----
// 7. Validar caminho (Path Traversal) e escrever o arquivo

================
File: src/core/SessionManager.ts
================
/**
 * SessionManager: Orquestração da sessão de conversa ativa.
 *
 * Responsabilidade Única (SRP):
 * - Gerenciar a sessão ativa (carregar, iniciar nova, adicionar mensagens).
 * - Orquestrar a persistência via ISessionStore.
 * - Fornecer o histórico formatado (ReActMessage[]) para o ReActLoop.
 * - Extrair título automaticamente da primeira mensagem do usuário.
 *
 * DIP (Dependency Inversion Principle):
 * - Depende da abstração ISessionStore, não da implementação concreta.
 * - Pode ser testado com mock de ISessionStore.
 */
⋮----
import { randomUUID } from 'node:crypto';
import type { ISessionStore, Session } from './SessionStore';
import type { ReActMessage } from './rag/ReActLoop';
⋮----
/**
 * SessionManager: interface pública para gerenciar sessões.
 */
export class SessionManager
⋮----
constructor(store: ISessionStore)
⋮----
/**
   * Retorna o ID da sessão ativa, ou null se nenhuma foi iniciada.
   */
getCurrentSessionId(): string | null
⋮----
/**
   * Retorna o título da sessão ativa, ou null.
   */
getCurrentTitle(): string | null
⋮----
/**
   * Indica se há uma sessão ativa com mensagens.
   */
isActive(): boolean
⋮----
/**
   * Retorna cópia das mensagens da sessão ativa.
   * Útil para o ReActLoop consumir como histórico.
   */
getHistory(): ReActMessage[]
⋮----
/**
   * Contagem de mensagens na sessão ativa.
   */
getMessageCount(): number
⋮----
/**
   * Carrega uma sessão existente pelo ID.
   * @param sessionId UUID da sessão a carregar
   * @returns true se a sessão foi carregada, false se não existir
   */
async loadSession(sessionId: string): Promise<boolean>
⋮----
/**
   * Cria uma nova sessão com ID único e timestamp atual.
   * Se já houver uma sessão ativa, ela é salva automaticamente antes.
   */
async newSession(model?: string): Promise<string>
⋮----
// Salva sessão anterior se existir
⋮----
/**
   * Adiciona uma mensagem ao histórico da sessão ativa.
   * Se a sessão não foi iniciada, cria uma automaticamente.
   * Se for a primeira mensagem do usuário, extrai título.
   *
   * @param message Mensagem para adicionar (role + content)
   * @param model Modelo usado (opcional, para metadado)
   */
async addMessage(message: ReActMessage, model?: string): Promise<void>
⋮----
// Garante sessão ativa
⋮----
// Extrai título da primeira mensagem do usuário (máx 80 chars)
⋮----
/**
   * Persiste a sessão ativa em disco via ISessionStore.
   * Chamado automaticamente ao trocar de sessão; também exposto
   * para ser chamado explicitamente após cada turno de conversa.
   */
async flush(): Promise<void>
⋮----
/**
   * Lista todas as sessões disponíveis (delega ao store).
   */
listSessions()
⋮----
/**
   * Deleta uma sessão específica (delega ao store).
   * Se for a sessão ativa, limpa o estado atual.
   */
async deleteSession(sessionId: string): Promise<void>
⋮----
/**
   * Extrai um título legível da primeira pergunta do usuário.
   * Pega as primeiras N palavras, no máximo 80 caracteres.
   */
private extractTitle(content: string): string
⋮----
// Remove quebras de linha e espaços extras
⋮----
// Pega primeiras 8 palavras ou até 80 chars

================
File: src/core/SessionStore.ts
================
/**
 * SessionStore: Persistência de sessões de conversa em disco.
 *
 * Responsabilidade Única (SRP):
 * - Ler, escrever, listar e deletar arquivos de sessão (.json)
 *   no diretório .soberano/sessions/.
 * - Zero conhecimento do que é uma conversa ou ReActLoop.
 * - Usa apenas módulos nativos do Node.js (node:fs/promises).
 *
 * DIP (Dependency Inversion Principle):
 * - A interface ISessionStore é a abstração que o SessionManager
 *   e os testes usam, nunca a classe concreta diretamente.
 */
⋮----
import { mkdir, readFile, writeFile, unlink, readdir } from 'node:fs/promises';
import { join, extname } from 'node:path';
import type { ReActMessage } from './rag/ReActLoop';
⋮----
/**
 * Representa uma sessão de conversa completa, persistida em disco.
 */
export interface Session {
  /** UUID v4 — identificador único da sessão */
  id: string;
  /** ISO 8601 — timestamp de criação */
  createdAt: string;
  /** ISO 8601 — timestamp da última atualização */
  updatedAt: string;
  /** Array de mensagens da conversa (em ordem cronológica) */
  messages: ReActMessage[];
  /** Metadados opcionais (modelo usado, título, etc.) */
  metadata?: {
    /** Modelo utilizado na sessão (ex: "llama3.2:1b") */
    model?: string;
    /** Título auto-extraído da primeira pergunta (máx 80 chars) */
    title?: string;
  };
}
⋮----
/** UUID v4 — identificador único da sessão */
⋮----
/** ISO 8601 — timestamp de criação */
⋮----
/** ISO 8601 — timestamp da última atualização */
⋮----
/** Array de mensagens da conversa (em ordem cronológica) */
⋮----
/** Metadados opcionais (modelo usado, título, etc.) */
⋮----
/** Modelo utilizado na sessão (ex: "llama3.2:1b") */
⋮----
/** Título auto-extraído da primeira pergunta (máx 80 chars) */
⋮----
/**
 * Resumo leve de uma sessão para listagem (sem mensagens).
 */
export interface SessionSummary {
  id: string;
  createdAt: string;
  updatedAt: string;
  messageCount: number;
  title?: string;
}
⋮----
/** Interface para erros com código (Enoent, EACCES, etc.) */
interface FileSystemError extends Error {
  code?: string;
}
⋮----
/**
 * ISessionStore: Abstração para persistência de sessões.
 *
 * DIP (Dependency Inversion Principle):
 * - SessionManager depende desta interface, não da implementação concreta.
 * - Testes podem mockar facilmente.
 */
export interface ISessionStore {
  /** Persiste uma sessão em disco (cria ou sobrescreve) */
  save(session: Session): Promise<void>;
  /** Carrega uma sessão pelo ID. Retorna null se não existir ou corrompida. */
  load(sessionId: string): Promise<Session | null>;
  /** Lista todas as sessões disponíveis (apenas resumo). */
  list(): Promise<SessionSummary[]>;
  /** Remove uma sessão do disco. Idempotente (não erra se não existir). */
  delete(sessionId: string): Promise<void>;
}
⋮----
/** Persiste uma sessão em disco (cria ou sobrescreve) */
save(session: Session): Promise<void>;
/** Carrega uma sessão pelo ID. Retorna null se não existir ou corrompida. */
load(sessionId: string): Promise<Session | null>;
/** Lista todas as sessões disponíveis (apenas resumo). */
list(): Promise<SessionSummary[]>;
/** Remove uma sessão do disco. Idempotente (não erra se não existir). */
delete(sessionId: string): Promise<void>;
⋮----
/**
 * SessionStore: Implementação concreta de ISessionStore.
 *
 * Salva arquivos .json no diretório especificado (padrão: .soberano/sessions).
 * Usa exclusivamente módulos nativos do Node.js.
 */
export class SessionStore implements ISessionStore
⋮----
/**
   * @param baseDir Diretório base para armazenar as sessões.
   *                Padrão: '.soberano/sessions'
   */
constructor(baseDir?: string)
⋮----
/**
   * Retorna o caminho completo para o arquivo de uma sessão.
   */
private sessionPath(sessionId: string): string
⋮----
async save(session: Session): Promise<void>
⋮----
// Garante que o diretório existe
⋮----
// Atualiza o timestamp antes de salvar
⋮----
async load(sessionId: string): Promise<Session | null>
⋮----
// Arquivo não encontrado — retorna null (caso normal)
⋮----
// JSON inválido — retorna null (tolerância a corrupção)
⋮----
// Outros erros (permissão, etc.) — relança
⋮----
async list(): Promise<SessionSummary[]>
⋮----
// Diretório não existe — retorna lista vazia
⋮----
// Filtra apenas arquivos .json e paraleliza a leitura
⋮----
// Arquivos corrompidos ou ausentes são ignorados
⋮----
// Ordena por updatedAt decrescente (mais recentes primeiro)
⋮----
async delete(sessionId: string): Promise<void>
⋮----
// Se o arquivo não existe, resolve silenciosamente (idempotente)

================
File: src/core/StatefulCompressor.ts
================
/**
 * StatefulCompressor: Implementação do IContextCompressor que usa o próprio
 * modelo de linguagem para condensar o histórico (SRP + DIP).
 *
 * Responsabilidade Única: Pegar um histórico longo de mensagens ReAct e
 * pedir ao modelo que extraia três blocos essenciais (objective, completed,
 * critical_rules), descartando o lixo da conversa e preservando o essencial.
 *
 * ISP (Interface Segregation): Depende apenas de IContextCompressor e IProvider.
 * DIP (Dependency Inversion): Recebe IProvider via construtor, não instancia nada.
 */
⋮----
import type { IContextCompressor, CompressedContext } from './IContextCompressor';
import type { IProvider } from '../providers/types';
import type { ReActMessage } from './rag/ReActLoop';
⋮----
/** Número de mensagens finais mantidas intactas após compressão */
⋮----
/**
 * Sistema de prompt usado para instruir o modelo a extrair os três blocos.
 */
⋮----
/**
 * Formata o histórico como texto para enviar ao modelo.
 */
function formatHistoryForCompression(history: ReActMessage[]): string
⋮----
/**
 * Formata o JSON parseado como Markdown estruturado para workingMemory.
 */
function formatWorkingMemory(
  objective: string,
  completed: string,
  criticalRules: string,
): string
⋮----
/**
 * Calcula a taxa de compressão: 0.0 (sem compressão) a 1.0 (100% comprimido).
 *
 * @param originalLength - Tamanho total do histórico original em caracteres
 * @param compressedLength - Tamanho do workingMemory em caracteres
 * @returns Taxa de compressão entre 0.0 e 1.0
 */
function computeCompressionRatio(originalLength: number, compressedLength: number): number
⋮----
// Clamp between 0.0 and 1.0
⋮----
export class StatefulCompressor implements IContextCompressor
⋮----
constructor(provider: IProvider)
⋮----
/**
   * Comprime o histórico de mensagens ReAct usando o modelo de linguagem.
   *
   * @param history - Array completo de mensagens do histórico
   * @param _model - Identificador do modelo sendo usado (não usado diretamente, delegado ao provider)
   * @param _systemPrompt - Prompt de sistema original (usado como contexto adicional)
   * @returns Promise com o contexto comprimido
   */
async compress(
    history: ReActMessage[],
    _model: string,
    _systemPrompt: string,
): Promise<CompressedContext>
⋮----
// Sempre preservar as últimas KEEP_LAST mensagens intactas
⋮----
// Se não há histórico suficiente para comprimir, retorna sem compressão
⋮----
// Prompt completo: system prompt + histórico + instrução de formato
⋮----
// Parse da resposta JSON
⋮----
// Falha no parse JSON — loga erro e retorna sem compressão
⋮----
// Extrair campos (com fallback para string vazia)
⋮----
// Provider lançou exceção (ex: limite de contexto) — loga erro e
// trunca mecanicamente para evitar loop infinito no ReAct.

================
File: src/core/TokenEstimator.ts
================
/**
 * TokenEstimator — Estimador de tokens nativo e sem dependências.
 *
 * Heurística: Math.ceil(text.length / 4) ≈ 4 caracteres por token (média
 * conservadora para modelos Flash como Llama 3.2 e Phi-3).
 *
 * SRP: Responsabilidade única de estimar consumo de tokens.
 * Zero dependências externas.
 */
⋮----
import type { ReActMessage } from './rag/ReActLoop';
⋮----
/**
 * Constante com os limites de contexto conhecidos dos modelos suportados.
 * Chave = nome do modelo (tag), Valor = limite máximo de tokens.
 */
⋮----
/**
 * Limite conservador usado como fallback quando o modelo não está mapeado.
 */
⋮----
export class TokenEstimator
⋮----
/**
   * Estima o número de tokens em um texto usando a heurística Math.ceil(text.length / 4).
   *
   * @param text  Texto de entrada (pode ser vazio).
   * @returns     Número estimado de tokens (0 para string vazia).
   */
static estimate(text: string): number
⋮----
/**
   * Estima o total de tokens de um array de mensagens ReAct.
   * Soma `estimate(role + content)` para cada mensagem.
   *
   * @param messages  Array de mensagens no formato ReAct.
   * @returns         Soma total estimada de tokens.
   */
static estimateMessages(messages: ReActMessage[]): number
⋮----
/**
   * Retorna o limite de contexto (context window) para um modelo específico.
   * Se o modelo não estiver mapeado em DEFAULT_CONTEXT_WINDOWS, retorna
   * 4096 como fallback conservador.
   *
   * @param model  Nome/tag do modelo (ex.: "llama3.2:1b").
   * @returns      Limite máximo de tokens.
   */
static getLimit(model: string): number

================
File: src/core/ToolRegistry.ts
================
/**
 * ToolRegistry: Registro de ferramentas (tools) no formato JSON Schema
 * compatível com Function Calling / Tool Use do Ollama.
 *
 * Arquitetura:
 * ┌─────────────────────────────────────────────────────────┐
 * │                   ToolRegistry                          │
 * │                                                         │
 * │  tools: Map<                                               │
 * │    string,                // nome da tool               │
 * │    {                                                    │
 * │      definition: {...},   // JSON Schema (LLM-readable) │
 * │      handler: Function    // executa a tool de fato     │
 * │    }                                                    │
 * │  >                                                      │
 * │                                                         │
 * │  + register(name, description, paramsSchema, handler)   │
 * │  + getDefinitions(): string   // JSON string p/ prompt  │
 * │  + execute(name, args): Promise    // chama handler     │
 * │  + hasTool(name): boolean                               │
 * │  + getToolNames(): string[]                              │
 * └─────────────────────────────────────────────────────────┘
 *
 * Segurança:
 * - execute() verifica se a tool existe antes de chamar o handler,
 *   prevenindo execução de funções arbitrárias.
 * - Os handlers são registrados explicitamente via register(), nunca
 *   expostos dinamicamente a partir de input do usuário/LLM.
 */
⋮----
/**
 * Definição de uma ferramenta no formato JSON Schema (padrão OpenAI/Function Calling).
 * É o que o LLM recebe para decidir qual tool chamar e com quais argumentos.
 */
export interface ToolDefinition {
  type: 'function';
  function: {
    name: string;
    description: string;
    parameters: {
      type: 'object';
      properties: Record<string, { type: string; description: string }>;
      required: string[];
    };
  };
}
⋮----
/**
 * Entrada completa de uma tool no registro: definição + handler.
 */
interface ToolEntry {
  definition: ToolDefinition;
  handler: (args: Record<string, unknown>) => Promise<string>;
}
⋮----
export class ToolRegistry
⋮----
/**
   * Registra uma nova ferramenta no registro.
   *
   * @param name        Nome único da tool (ex: 'readFile', 'execute')
   * @param description Descrição legível para o LLM
   * @param paramsSchema Schema dos parâmetros: { prop: { type, description } }
   * @param handler     Função que executa a tool. Recebe args e retorna string.
   */
register(
    name: string,
    description: string,
    paramsSchema: Record<string, { type: string; description: string }>,
    handler: (args: Record<string, unknown>) => Promise<string>
): void
⋮----
/**
   * Retorna a lista de definições de todas as tools registradas
   * serializada como JSON string, pronta para ser injetada no system prompt.
   *
   * Formato:
   * [
   *   {
   *     "type": "function",
   *     "function": { "name": "readFile", "description": "...", "parameters": {...} }
   *   },
   *   ...
   * ]
   */
getDefinitions(): string
⋮----
/**
   * Retorna a lista de nomes de tools registradas.
   * Útil para validação e para o system prompt.
   */
getToolNames(): string[]
⋮----
/**
   * Verifica se uma tool com o nome informado existe no registro.
   * Usado para validação de segurança antes de executar.
   */
hasTool(name: string): boolean
⋮----
/**
   * Executa uma tool pelo nome, passando os argumentos.
   *
   * Pipeline de segurança:
   *   1. Verifica se a tool existe no registro
   *   2. Se não existe → lança erro (previne execução arbitrária)
   *   3. Chama o handler registrado com os args fornecidos
   *   4. Retorna o resultado como string
   *
   * @param name Nome da tool a executar
   * @param args Argumentos para o handler (validados pelo JSON Schema no LLM)
   * @returns Resultado da execução como string
   */
async execute(name: string, args: Record<string, unknown>): Promise<string>

================
File: src/providers/index.ts
================


================
File: src/providers/OllamaHttpClient.ts
================
/**
 * OllamaHttpClient: Cliente HTTP de baixo nível para comunicação com a API Ollama.
 *
 * Responsabilidade única (SRP):
 * - Gerenciar conexões TCP, timeouts e parsing de respostas HTTP
 * - Post comum (post) — retorna Promise do JSON parseado
 * - Post com streaming (postStream) — retorna AsyncGenerator de tokens
 *
 * Zero dependências externas — usa exclusivamente node:http.
 */
export class OllamaHttpClient
⋮----
constructor(
⋮----
/**
   * Realiza uma requisição HTTP POST e retorna o corpo parseado como JSON.
   *
   * @param path  Caminho da API (ex: '/api/generate')
   * @param body  Corpo JSON já stringificado
   * @returns Promise que resolve com o objeto JSON parseado
   */
post(path: string, body: string): Promise<any>
⋮----
/**
   * Realiza uma requisição HTTP POST com streaming habilitado e
   * retorna um AsyncGenerator que emite tokens linha por linha.
   *
   * Fluxo:
   * - Conecta via http.request
   * - Aguarda req.on('response') em vez de callback (para suportar async generator)
   * - Processa chunks TCP linha a linha com buffer de linha parcial
   * - Cada linha completa é parseada como JSON e extrai o campo "response"
   * - O loop termina ao receber um objeto com "done": true
   *
   * @param path  Caminho da API (ex: '/api/generate')
   * @param body  Corpo JSON já stringificado (com stream: true)
   * @returns AsyncGenerator que emite strings (tokens individuais)
   */
async *postStream(path: string, body: string): AsyncGenerator<string>
⋮----
// Promise que resolve quando a resposta HTTP começa a chegar
⋮----
// Envia o corpo e finaliza a requisição
⋮----
// Aguarda a resposta HTTP
⋮----
// Verifica status code antes de processar streaming
⋮----
// Buffer que acumula dados até encontrar uma quebra de linha
⋮----
// Processa os chunks TCP conforme chegam
⋮----
// Processa todas as linhas completas no buffer
⋮----
// Linha vazia (ex: keep-alive ou separador SSE) → ignora
⋮----
// Tenta parsear cada linha como JSON individual
⋮----
// Extrai o token de resposta
⋮----
// Se done === true, encerra o streaming
⋮----
// Linha não-JSON (ex: espaços, comentários SSE) → ignora

================
File: src/providers/OllamaProvider.ts
================
import type {
  ChatRequest,
  ChatResponse,
  CritiqueRequest,
  CritiqueResponse,
  EmbedResponse,
  ICritiqueProvider,
  IProvider,
} from './types';
import { OllamaHttpClient } from './OllamaHttpClient';
⋮----
/**
 * OllamaProvider: Implementação concreta do IProvider que se comunica
 * com uma instância local do Ollama via HTTP.
 *
 * Fluxo de dados completo (chat):
 * ┌──────────┐    ChatRequest     ┌──────────────────┐   JSON body    ┌─────────────────┐
 * │  CLI     │ ── {model,        │  OllamaProvider   │ ── POST ──▶  │  Ollama Server  │
 * │ (usuário)│    prompt, temp}──▶│  .chat()          │              │  localhost:11434 │
 * └──────────┘                   │  └─ post()        │ ◀── JSON ──  │  /api/generate   │
 *                                │     └─ http.request│              └─────────────────┘
 *                                └──────────────────┘
 *                                       │
 *                                  ChatResponse
 *                                  {response, model, done}
 *                                       │
 *                                       ▼
 *                                  ┌──────────┐
 *                                  │  console │
 *                                  └──────────┘
 *
 * A classe usa exclusivamente o módulo nativo `node:http` (zero dependências externas).
 * O método `chat()` é a interface pública; `post()` é o método privado que gerencia
 * a conexão TCP, timeout e parsing da resposta.
 */
export class OllamaProvider implements IProvider, ICritiqueProvider
⋮----
/** Host onde o Ollama está rodando (padrão: localhost) */
⋮----
/** Porta da API do Ollama (padrão: 11434) */
⋮----
/**
   * @param host Endereço do servidor Ollama
   * @param port Porta do servidor Ollama
   */
constructor(
    host: string = 'localhost',
    port: number = 11434,
    private readonly http: OllamaHttpClient = new OllamaHttpClient(host, port)
)
⋮----
/**
   * Envia um prompt ao Ollama e retorna a resposta gerada pelo modelo.
   *
   * Pipeline:
   *   1. Constrói o JSON body conforme a API do Ollama:
   *      - model: identificador do modelo (ex: "phi3:3b", "tinyllama:1b")
   *      - prompt: texto do usuário
   *      - stream: false → resposta única, sem streaming SSE
   *      - options.temperature: criatividade (0 = determinístico, 1 = criativo)
   *      - options.num_predict: limite de tokens na resposta
   *   2. Chama this.post('/api/generate', body) → requisição HTTP POST
   *   3. Extrai data.response, data.model, data.done da resposta JSON
   *   4. Retorna ChatResponse padronizado
   *
   * @param request Dados da requisição (modelo, prompt, parâmetros)
   * @returns Resposta do modelo encapsulada em ChatResponse
   */
async chat(request: ChatRequest): Promise<ChatResponse>
⋮----
// Monta o objeto do body de forma incremental para suportar format opcional
⋮----
// temperature: controla aleatoriedade (0-1). Padrão 0.7 se não informado.
⋮----
// num_predict: máximo de tokens a gerar. undefined = sem limite.
⋮----
// Quando format='json', ativa o Grammar Restraint nativo do Ollama
// A API /api/generate aceita "format": "json" para forçar resposta em JSON
⋮----
// Validação de robustez: se o contrato exigia JSON, verifica se a resposta
// do modelo é realmente parseável. Isso previne que alucinações ou falhas
// do modelo cheguem ao CLI como dados inválidos.
⋮----
/**
   * streamChat: Envia um prompt ao Ollama com streaming habilitado e
   * retorna um AsyncIterable que emite cada token individualmente.
   *
   * Pipeline:
   *   1. Constrói o JSON body com stream: true (SSE habilitado)
   *   2. Chama this.postStream('/api/generate', body) → AsyncGenerator
   *   3. Cada token é emitido via yield conforme chega do servidor
   *
   * O cliente (CLI) pode iterar com `for await...of` e escrever os tokens
   * no stdout em tempo real, criando o efeito "máquina de escrever".
   *
   * @param request Dados da requisição (modelo, prompt, parâmetros)
   * @returns AsyncIterable que emite strings (tokens individuais)
   */
async *streamChat(request: ChatRequest): AsyncIterable<string>
⋮----
// Delega para o gerador de streaming que lida com a resposta HTTP
⋮----
/**
   * Gera um embedding (vetor de floats) para um texto usando o modelo all-minilm.
   *
   * Pipeline:
   *   1. Envia POST /api/embed com { model, input, keep_alive }
   *   2. Extrai embeddings[0] do response
   *   3. Retorna o vetor de números
   *
   * O parâmetro keep_alive controla quanto tempo o modelo fica carregado na RAM:
   *   - "0s": descarrega imediatamente (economiza RAM)
   *   - "30s": mantém carregado por 30s (útil para indexação em lote)
   *   - "-1": mantém indefinidamente (não recomendado com 12GB RAM)
   *
   * @param text  O texto a ser embedado
   * @param embedModel  Nome do modelo de embedding (padrão: all-minilm)
   * @param keepAlive  Tempo de vida do modelo na RAM (padrão: "30s")
   * @returns Vetor de floats representando o embedding
   */
async embed(
    text: string,
    embedModel: string = 'all-minilm',
    keepAlive: string = '30s'
): Promise<number[]>
⋮----
/**
   * critique: Submete um prompt de crítica ao modelo com formatação JSON.
   *
   * Diferenças do chat() normal:
   * - stream: false (sempre resposta completa, não streaming)
   * - format: 'json' (força o Ollama a retornar JSON válido)
   * - temperature: 0.1 (baixa para ser mais determinístico)
   * - num_predict: 1024 (limita tokens da crítica)
   *
   * @param request Prompt de crítica + resposta a analisar
   * @returns CritiqueResponse com JSON parseado e rawText
   */
async critique(request: CritiqueRequest): Promise<CritiqueResponse>
⋮----
// Fallback seguro: se o modelo não retornar JSON válido, devolve vazio
⋮----
// post() e postStream() foram extraídos para OllamaHttpClient (SRP)
// Veja src/providers/OllamaHttpClient.ts

================
File: src/providers/types.ts
================
export interface ChatRequest {
  /** Modelo Ollama a ser usado (ex: "tinyllama:1b", "phi3:3b") */
  model: string;
  /** Prompt do usuário */
  prompt: string;
  /** Temperatura para criatividade (0-1), padrão 0.7 */
  temperature?: number;
  /** Máximo de tokens na resposta */
  max_tokens?: number;
  /** Força o modelo a responder em JSON estrito (ativa format: 'json' no Ollama) */
  format?: 'json';
}
⋮----
/** Modelo Ollama a ser usado (ex: "tinyllama:1b", "phi3:3b") */
⋮----
/** Prompt do usuário */
⋮----
/** Temperatura para criatividade (0-1), padrão 0.7 */
⋮----
/** Máximo de tokens na resposta */
⋮----
/** Força o modelo a responder em JSON estrito (ativa format: 'json' no Ollama) */
⋮----
export interface ChatResponse {
  /** Resposta completa gerada pelo modelo */
  response: string;
  /** Modelo utilizado */
  model: string;
  /** Se a resposta foi truncada */
  done: boolean;
}
⋮----
/** Resposta completa gerada pelo modelo */
⋮----
/** Modelo utilizado */
⋮----
/** Se a resposta foi truncada */
⋮----
export interface EmbedResponse {
  /** Array de embeddings gerados (cada input gera um vetor) */
  embeddings: number[][];
}
⋮----
/** Array de embeddings gerados (cada input gera um vetor) */
⋮----
/**
 * IProvider: Contrato base para provedores de linguagem.
 * 
 * LSP (Liskov Substitution): Qualquer classe que implemente IProvider
 * pode ser usada no lugar de OllamaProvider sem quebrar o sistema.
 * 
 * O método embed() é opcional na interface base (para providers
 * que não suportam embeddings, como APIs de chat puro).
 */
export interface IProvider {
  /** Nome do provider para logging */
  readonly name: string;
  /** Envia um prompt e recebe a resposta do modelo */
  chat(request: ChatRequest): Promise<ChatResponse>;
  /**
   * Gera embedding vetorial para um texto.
   * Opcional por padrão — providers que não suportam embedding
   * podem deixar sem implementação.
   */
  embed?(text: string, embedModel?: string, keepAlive?: string): Promise<number[]>;
  /**
   * Streaming: retorna tokens incrementalmente via AsyncIterable.
   * Opcional — providers que não suportam streaming ou modo legado
   * podem deixar sem implementação (fallback para chat()).
   */
  streamChat?(request: ChatRequest): AsyncIterable<string>;
}
⋮----
/** Nome do provider para logging */
⋮----
/** Envia um prompt e recebe a resposta do modelo */
chat(request: ChatRequest): Promise<ChatResponse>;
/**
   * Gera embedding vetorial para um texto.
   * Opcional por padrão — providers que não suportam embedding
   * podem deixar sem implementação.
   */
embed?(text: string, embedModel?: string, keepAlive?: string): Promise<number[]>;
/**
   * Streaming: retorna tokens incrementalmente via AsyncIterable.
   * Opcional — providers que não suportam streaming ou modo legado
   * podem deixar sem implementação (fallback para chat()).
   */
streamChat?(request: ChatRequest): AsyncIterable<string>;
⋮----
/**
 * IEmbedProvider: Extensão de IProvider para provedores que
 * suportam embeddings (RAG, busca semântica, etc.).
 * 
 * Ao contrário de IProvider.embed() que é opcional, esta interface
 * garante que o método existe em tempo de compilação.
 */
export interface IEmbedProvider extends IProvider {
  embed(text: string, embedModel?: string, keepAlive?: string): Promise<number[]>;
}
⋮----
embed(text: string, embedModel?: string, keepAlive?: string): Promise<number[]>;
⋮----
// ── Interfaces de Crítica (Self-Correction) ──
⋮----
export interface CritiqueRequest {
  /** Modelo a ser usado para a crítica */
  model: string;
  /** Prompt completo (system prompt + resposta a analisar) */
  prompt: string;
  /** Temperatura para a crítica (baixa = mais determinístico) */
  temperature?: number;
}
⋮----
/** Modelo a ser usado para a crítica */
⋮----
/** Prompt completo (system prompt + resposta a analisar) */
⋮----
/** Temperatura para a crítica (baixa = mais determinístico) */
⋮----
export interface CritiqueResponse {
  /** JSON parseado da resposta do crítico */
  parsedJson: Record<string, unknown>;
  /** Texto cru antes do parse (para debug/fallback) */
  rawText: string;
}
⋮----
/** JSON parseado da resposta do crítico */
⋮----
/** Texto cru antes do parse (para debug/fallback) */
⋮----
/**
 * Status da correção aplicada pelo Reflector:
 * - 'stable'   → correção confiável, aplicada com sucesso
 * - 'suspicious' → correção aplicada mas com ressalvas (ex: tamanho muito reduzido)
 * - 'rejected'  → correção rejeitada (ex: similaridade muito baixa com original)
 */
export type CorrectionStatus = 'stable' | 'suspicious' | 'rejected';
⋮----
/**
 * ICritiqueProvider: Interface para provedores que suportam crítica de respostas.
 *
 * LSP (Liskov Substitution): Cada provider implementa o método critique()
 * da sua própria forma (Ollama usa format:'json', OpenAI usaria response_format,
 * Anthropic usaria prompt engineering), sem vazar detalhes de implementação
 * para o Reflector.
 *
 * SRP: Responsabilidade única — submeter um prompt de crítica e retornar JSON.
 */
export interface ICritiqueProvider {
  /** Nome do provider para logging */
  readonly name: string;
  /**
   * Submete um prompt de crítica e retorna o JSON parseado.
   *
   * @param request Prompt completo de crítica + resposta a analisar
   * @returns Resposta parseada (parsedJson + rawText para fallback)
   */
  critique(request: CritiqueRequest): Promise<CritiqueResponse>;
}
⋮----
/** Nome do provider para logging */
⋮----
/**
   * Submete um prompt de crítica e retorna o JSON parseado.
   *
   * @param request Prompt completo de crítica + resposta a analisar
   * @returns Resposta parseada (parsedJson + rawText para fallback)
   */
critique(request: CritiqueRequest): Promise<CritiqueResponse>;

================
File: src/validation/JsonValidator.ts
================
/**
 * JsonValidator: Validação de respostas JSON de modelos de linguagem.
 *
 * Responsabilidade Única (SRP): Esta classe existe APENAS para validar
 * se uma string é um JSON válido. Ela não formata prompts, não gerencia
 * conexões HTTP, não lida com provedores de linguagem.
 *
 * Uso típico:
 *   const validator = new JsonValidator();
 *   const parsed = validator.validate(rawResponse);
 *   // Se falhar, lança ValidationError com preview do conteúdo inválido
 */
⋮----
export class ValidationError extends Error
⋮----
constructor(message: string, raw: string)
⋮----
export class JsonValidator
⋮----
/**
   * Valida se uma string é um JSON válido e retorna o objeto parseado.
   *
   * @param raw String bruta recebida do modelo
   * @param contextLabel Rótulo para mensagem de erro (ex: "Ollama response")
   * @returns Objeto parseado
   * @throws ValidationError se o JSON for inválido
   */
validate<T = Record<string, unknown>>(raw: string, contextLabel: string = 'response'): T
⋮----
/**
   * Valida que a resposta do modelo, quando solicitado format:'json',
   * é realmente um JSON válido. Se falhar, retorna null em vez de lançar
   * exceção (útil para fallbacks).
   */
tryValidate<T = Record<string, unknown>>(raw: string): T | null

================
File: src/index.ts
================
/**
 * Root barrel file for @soberano/core package.
 *
 * Re-exports everything from src/core/index.ts so that
 * `import { AppContext, ReActLoop, ... } from '@soberano/core'`
 * resolves correctly to dist/index.js (as declared in package.json).
 */

================
File: tests/integration/editing-pipeline.test.ts
================
/**
 * editing-pipeline.test.ts — Teste de Integração do Pipeline Completo de Edição.
 *
 * ═══════════════════════════════════════════════════════════════════════════
 * AVISO: Este é o ÚNICO conjunto de testes neste projeto que acessa o
 * filesystem real (cria, lê, edita e deleta arquivos em disco).
 * Todos os demais testes usam mocks para FileReader / fs.
 * ═══════════════════════════════════════════════════════════════════════════
 *
 * Pipeline simulado:
 * 1. Cria arquivo TypeScript temporário em tests/integration/.tmp/
 * 2. ASTEditor.replaceSymbol() → substitui função por nome de símbolo
 * 3. Verifica que o arquivo foi alterado corretamente
 * 4. Cria arquivo não-TypeScript (texto) temporário
 * 5. SearchReplaceEditor.apply() → substitui bloco exato
 * 6. Verifica que o arquivo foi alterado corretamente
 * 7. Limpeza: afterEach deleta todos os arquivos temporários
 */
⋮----
import { describe, it, expect, afterEach, beforeAll, afterAll } from 'vitest';
⋮----
import { FileReader } from '../../src/core/FileReader';
import { ASTEditor } from '../../src/core/ASTEditor';
import { SearchReplaceEditor } from '../../src/core/SearchReplaceEditor';
⋮----
// ── Helpers ──────────────────────────────────────────────────────
⋮----
/** Diretório temporário dentro do projeto (respeita FileReader.resolveSecurePath) */
⋮----
/** Cria um arquivo temporário com conteúdo e registra para limpeza */
async function createTempFile(filename: string, content: string): Promise<string>
⋮----
/** Lê arquivo como string */
async function readFile(filePath: string): Promise<string>
⋮----
// ── Setup / Teardown ─────────────────────────────────────────────
⋮----
// Remove diretório temporário e todo o seu conteúdo
⋮----
// Ignora se já foi removido
⋮----
// Ignora se já foi deletado
⋮----
// ── Testes ───────────────────────────────────────────────────────
⋮----
// ──────────────────────────────────────────────────────────────────
// ASTEditor.replaceSymbol() em arquivo TypeScript
// ──────────────────────────────────────────────────────────────────
⋮----
// Substituir a função "add" por uma nova implementação
⋮----
// Verificar conteúdo do arquivo pós-edição
⋮----
// A função 'add' original foi substituída — verificar que o código antigo sumiu
⋮----
// ──────────────────────────────────────────────────────────────────
// SearchReplaceEditor.apply() em arquivo não-TypeScript
// ──────────────────────────────────────────────────────────────────
⋮----
// O erro será sobre bloco não encontrado, não sobre caminho negado
⋮----
// ──────────────────────────────────────────────────────────────────
// Pipeline Completo (TypeScript + texto em sequência)
// ──────────────────────────────────────────────────────────────────
⋮----
// ── Etapa 1: Criar um arquivo .ts com função + comentário ──
⋮----
// ── Etapa 2: ASTEditor.replaceSymbol() em "divide" ──
⋮----
// ── Etapa 3: Verificar alteração AST ──
⋮----
// '// TODO: documentar' era leading trivia de 'divide', então foi substituído junto
⋮----
// ── Etapa 4: SearchReplaceEditor.apply() no mesmo arquivo .ts ──
// NOTA: '// TODO: documentar' foi consumido como leading trivia do símbolo 'divide'
// no passo AST. Vamos substituir o cabeçalho do arquivo que ainda está intacto.
⋮----
// ── Etapa 5: Verificar edições combinadas ──

================
File: tests/unit/cli/ChatStrategy.test.ts
================
/**
 * ChatStrategy.test.ts — Testes para buildSystemPrompt()
 *
 * Cobertura:
 * - Bloco de workflow de edição aparece quando tools de edição estão registradas
 * - Bloco NÃO aparece sem as tools
 * - Bloco tem no máximo 15 linhas
 */
⋮----
import { describe, it, expect, vi } from 'vitest';
import { buildSystemPrompt } from '../../../src/cli/strategies/ChatStrategy';
import type { AppContext } from '../../../src/core/AppContext';
⋮----
// ── Factories ────────────────────────────────────────────────────
⋮----
function makeToolRegistry(toolNames: string[])
⋮----
function makeMockApp(toolRegistry: ReturnType<typeof makeToolRegistry>): AppContext
⋮----
// ── Testes ───────────────────────────────────────────────────────
⋮----
// Extrai o bloco de workflow

================
File: tests/unit/cli/commands.test.ts
================
/**
 * commands.test.ts — Testes unitários para o roteador de comandos CLI.
 *
 * Cobertura:
 * - Comandos individuais: read, dir, search, exec, chat
 * - Modos: normal e stream
 * - Erros: prompt vazio, comando desconhecido, falta de argumentos
 * - Sessões: listagem, addMessage, flush (multi-turn)
 *
 * Arquitetura:
 *   runCommand() recebe CliArgs parseados e usa AppContext.
 *   Para testar, mockamos AppContext com um provider mock que suporta streamChat.
 *
 * IMPORTANTE:
 *   vi.mock() é HOISTED para o topo do arquivo. Tudo que as factories precisam
 *   referenciar deve ser criado via vi.hoisted().
 */
import { describe, it, expect, vi, beforeEach } from 'vitest';
⋮----
// ═══════════════════════════════════════════════════════════════════════
// Estado compartilhado (vi.hoisted → NÃO é hoisted, executado antes)
// ═══════════════════════════════════════════════════════════════════════
⋮----
// ── Objetos mock ──
⋮----
// streamChat como async generator
⋮----
// ── Mock SessionManager ──
⋮----
// ── Classes mock construtíveis ──
class MockCtx
⋮----
get fileReader()
get commandExecutor()
get toolRegistry()
get provider()
get embedProvider()
get model()
get jsonMode()
get ragDir()
get sessionManager()
getSessionHistory()
initialize()
⋮----
class MockStrat
⋮----
class MockReAct
⋮----
// ═══════════════════════════════════════════════════════════════════════
// Mocks (hoisted — podem referenciar vi.hoisted vars)
// ═══════════════════════════════════════════════════════════════════════
⋮----
// ═══════════════════════════════════════════════════════════════════════
// Imports (depois dos mocks)
// ═══════════════════════════════════════════════════════════════════════
⋮----
import { runCommand } from '../../../src/cli/commands';
import type { CliArgs } from '../../../src/cli/commands';
⋮----
// ═══════════════════════════════════════════════════════════════════════
// Testes
// ═══════════════════════════════════════════════════════════════════════
⋮----
// Reseta estado entre testes
⋮----
// Garante streamChat existe (pode ter sido deletado)
⋮----
// ── Helpers ──
function makeArgs(
    command: string,
    args: string[],
    flags: Record<string, string | boolean> = {}
): CliArgs
⋮----
// ── read ──
⋮----
// ── dir ──
⋮----
// ── search ──
⋮----
// ── exec ──
⋮----
// ── chat ──
⋮----
// Reseta contadores dos mocks de sessão entre testes de chat
⋮----
// Remove streamChat dinamicamente
⋮----
// ── sessions ──
⋮----
// ── Comando desconhecido ──

================
File: tests/unit/core/ASTEditor.test.ts
================
/**
 * Tests for ASTEditor — Edição estrutural por nome de símbolo.
 *
 * Cenários:
 * ✅ Sucesso: substituição de função mantém resto do arquivo intacto
 * ✅ Símbolo não encontrado: success: false, symbolFound: false
 * ✅ Arquivo inexistente: success: false, mensagem de erro clara
 * ✅ Substituição de classe, interface, variável, type alias
 * ✅ getFullStart() usado corretamente (preserva comentários/whitespace anteriores)
 * ✅ Path traversal bloqueado: caminhos fora do projeto são rejeitados
 */
⋮----
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { ASTEditor } from '../../../src/core/ASTEditor';
import type { FileReader } from '../../../src/core/FileReader';
import { FileReader as FileReaderImpl } from '../../../src/core/FileReader';
⋮----
// Mock do fs nativo
⋮----
// getFullStart() inclui whitespace/trivia anteriores ao nó,
// então o conteúdo ANTES do nó termina em 'from 'bar';\n'
// e o newCode precisa incluir o whitespace que estava antes
⋮----
// getFullStart() consome o '\n\n' antes de 'function', então a saída fica colada no ';'
⋮----
// getFullStart() consome '// top comment\n\n' junto com o nó
⋮----
// getFullStart() includes leading comments, so they get replaced along with the node
⋮----
// Arquivo não deve ter sido escrito
⋮----
// getFullStart() consome o '\n' antes de 'export default'

================
File: tests/unit/core/CommandExecutor.test.ts
================
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { CommandExecutor } from '../../../src/core/CommandExecutor';
⋮----
/**
 * Testes unitários para CommandExecutor.
 *
 * Mock completo do módulo nativo `node:child_process` → zero execuções reais.
 * Zero consumo de CPU, zero I/O, zero risco de segurança.
 */
⋮----
// Mock dos eventos do child_process
type EventHandler = (...args: any[]) => void;
⋮----
function emit(event: string, ...args: any[])
⋮----
const spawn = (_cmd: string, _args: string[], _opts: any)
⋮----
// Simula dados no stdout
⋮----
// Simula fechamento do processo com exitCode 0
⋮----
// Emite stderr
⋮----
// Fecha com exitCode 1
⋮----
// Emite erro de spawn

================
File: tests/unit/core/ErrorJournal.test.ts
================
/**
 * Testes unitários do ErrorJournal (TASK 6, ARCH-04).
 *
 * Cobertura:
 * - addEntry e persistência assíncrona
 * - getEntries ordenação FIFO reversa
 * - getRecentEntries com filtro
 * - getStats agregação
 * - Carregamento de arquivo corrompido
 */
⋮----
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { ErrorJournal } from '../../../src/core';
⋮----
// Limpa resíduos de testes anteriores
try { fs.rmSync(testDir, { recursive: true }); } catch { /* ok */ }
⋮----
try { fs.rmSync(testDir, { recursive: true }); } catch { /* ok */ }
⋮----
// Verifica que foi persistido em disco
⋮----
expect(entries[0].type).toBe('logic'); // Mais recente primeiro
⋮----
expect(entries).toHaveLength(3); // Apenas os 3 últimos
expect(entries[0].description).toBe('Erro 5'); // Mais recente
expect(entries[2].description).toBe('Erro 3'); // Mais antigo dos 3
⋮----
// Cria arquivo inválido

================
File: tests/unit/core/IContextCompressor.test.ts
================
import { describe, it, expect } from 'vitest';
import { assessCompressionNeed, CompressionTrigger } from '../../../src/core/IContextCompressor';
⋮----
// 69% → NONE
⋮----
// 70% → SOFT
⋮----
// 85% → HARD

================
File: tests/unit/core/Reflector.test.ts
================
/**
 * Reflector.test.ts — Testes unitários para o Reflector (self-correction).
 *
 * Cobertura:
 * 1. Instanciação com critiqueProvider válido
 * 2. Instanciação com critiqueProvider inválido (lança erro)
 * 3. reflect() com resposta correta (stable)
 * 4. reflect() com resposta incorreta (corrected → stable)
 * 5. reflect() com resposta vazia (stable)
 * 6. reflect() com resposta só com espaços (stable)
 * 7. reflect() com JSON inválido do provider (fallback → rejected)
 * 8. reflect() com múltiplos erros
 * 9. Integração com toolRegistry
 * 10. Fallback em caso de erro de rede (rejected)
 * 11. correctedOutput vazio com hasError=true (mantém original)
 */
⋮----
import { describe, it, expect, vi } from 'vitest';
import { Reflector } from '../../../src/core/Reflector';
import type { ICritiqueProvider } from '../../../src/providers/types';
⋮----
// ── Helpers ──
⋮----
/** ToolRegistry mock padrão */
⋮----
/** Cria um critiqueProvider mock que retorna respostas previsíveis */
function createMockCritiqueProvider(
  responses: Record<string, { parsedJson: Record<string, unknown>; rawText: string }>
): ICritiqueProvider
⋮----
// ── Testes ──
⋮----
// eslint-disable-next-line @typescript-eslint/no-explicit-any
⋮----
// eslint-disable-next-line @typescript-eslint/no-explicit-any
⋮----
// Provider que retorna parsedJson vazio (simula JSON inválido)
⋮----
// Fallback: retorna o conteúdo original sem reflexão
⋮----
// hasError=true mas correctedOutput vazio → mantém original

================
File: tests/unit/core/SearchReplaceEditor.test.ts
================
/**
 * Tests for SearchReplaceEditor — Edição por bloco exato com normalização.
 *
 * Cenários (Critério de Aceitação):
 * ✅ Match exato (sucesso): substituição ocorre, arquivo é escrito
 * ✅ Zero matches (falha): matchCount: 0, arquivo não escrito
 * ✅ Múltiplos matches (falha): matchCount: N, mensagem pedindo bloco mais específico
 * ✅ \r\n vs \n (sucesso): normalização trata diferença de CRLF
 * ✅ Trailing whitespace diferente (sucesso): trimEnd normaliza
 * ✅ Indentação diferente (falha): indentação é significativa
 * ✅ Path traversal bloqueado: caminhos fora do projeto são rejeitados
 */
⋮----
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { SearchReplaceEditor } from '../../../src/core/SearchReplaceEditor';
import type { FileReader } from '../../../src/core/FileReader';
⋮----
// Mock do fs nativo
⋮----
// Conteúdo com LF, search com CRLF
⋮----
// A busca encontra 'a\nb\nc' (normalizado), e substitui no original
⋮----
// search tem trailing whitespace em uma linha
⋮----
// search com indentação diferente (3 espaços vs 2)
⋮----
// fs.writeFile will still be called, but with the outside path SearchReplaceEditor
// will catch the error from resolveSecurePath
// We simulate this by checking the path validation in SearchReplaceEditor
⋮----
// Path outside project: should be blocked by resolveSecurePath
⋮----
expect(result.matchCount).toBe(1); // match encontrado, mas escrita falhou

================
File: tests/unit/core/SessionManager.test.ts
================
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { SessionManager } from '../../../src/core/SessionManager';
import type { ISessionStore, Session } from '../../../src/core/SessionStore';
⋮----
// ── Mock do ISessionStore ──
⋮----
function createMockStore(): ISessionStore
⋮----
// ── Estado inicial ──
⋮----
// ── addMessage (criação automática) ──
⋮----
// Título continua sendo o da primeira
⋮----
const longMessage = 'a '.repeat(50); // 100 chars
⋮----
// ── newSession ──
⋮----
expect(store.save).toHaveBeenCalledTimes(0); // flush implícito no newSession
⋮----
// save deve ter sido chamado após newSession (flush)
⋮----
// Acessa internamente via addMessage depois
⋮----
// ── loadSession ──
⋮----
// ── flush ──
⋮----
// ── listSessions ──
⋮----
// ── deleteSession ──

================
File: tests/unit/core/SessionStore.test.ts
================
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { SessionStore, Session } from '../../../src/core/SessionStore';
⋮----
// ── Mocks do node:fs/promises ──
⋮----
// Reseta chamadas e implementações de todos os mocks
⋮----
// ── save() ──
⋮----
// Verifica que mkdir foi chamado com recursive true
⋮----
// Verifica que writeFile foi chamado com o caminho correto
⋮----
// Extrai o JSON passado para writeFile e verifica o conteúdo
⋮----
// updatedAt deve ter sido atualizado (não é mais o original)
⋮----
// ── load() ──
⋮----
// ── list() ──
⋮----
// Deve vir primeiro o mais recente (b)
⋮----
// Isola completamente este teste: redefine o mock
⋮----
// Força a ordem: primeiro invalido.json, depois valido.json
⋮----
// ── delete() ──
⋮----
// Deve resolver sem lançar

================
File: tests/unit/core/StatefulCompressor.test.ts
================
import { describe, it, expect, vi } from 'vitest';
import { StatefulCompressor } from '../../../src/core/StatefulCompressor';
import type { IProvider, ChatRequest, ChatResponse } from '../../../src/providers/types';
import type { ReActMessage } from '../../../src/core/rag/ReActLoop';
⋮----
/**
 * Cria um mock de IProvider que pode ser configurado por teste.
 */
function createMockProvider(
  response?: string,
  shouldThrow = false,
  throwMessage = 'Provider error',
): IProvider
⋮----
/**
 * Cria um array de mensagens ReAct para teste.
 */
function createHistory(count: number): ReActMessage[]
⋮----
// Verificar workingMemory contém os três campos formatados
⋮----
// Verificar keptMessages tem exatamente as últimas 3 mensagens
⋮----
// Verificar compressionRatio é menor que 1.0
⋮----
// Verificar que o provider foi chamado com os parâmetros corretos
⋮----
// Apenas campos não-vazios devem aparecer
⋮----
// Resposta não-JSON do modelo
⋮----
// Deve retornar o histórico completo em keptMessages
⋮----
// workingMemory deve ser vazio
⋮----
// compressionRatio deve ser 1.0 (100% comprimido = falhou)
⋮----
// JSON válido mas sem os campos esperados
⋮----
// Campos vazios → workingMemory com placeholder
⋮----
const history = createHistory(3); // Apenas 3 mensagens
⋮----
// Com 3 mensagens ou menos, histórico vai inteiro pra keptMessages
⋮----
// Provider não deve ser chamado quando não há o que comprimir
⋮----
// Deve retornar apenas as últimas 3 mensagens para evitar loop infinito
⋮----
// workingMemory deve ser vazio
⋮----
// compressionRatio deve ser 1.0
⋮----
// Garantir que nenhuma exceção é lançada
⋮----
// Sem histórico, nada foi comprimido — ratio é 1.0 (sem dados processados)
⋮----
// Campos não-string devem ser tratados como vazios
⋮----
const history = createHistory(4); // 1 para comprimir + 3 para keptMessages

================
File: tests/unit/core/TokenEstimator.test.ts
================
/**
 * Testes unitários para TokenEstimator
 *
 * Critérios de aceitação (TASK 01):
 * 1. string vazia retorna 0
 * 2. string de 400 chars retorna 100
 * 3. array de mensagens soma corretamente
 * 4. modelo desconhecido retorna 4096
 * Zero dependências externas.
 */
⋮----
import { describe, it, expect } from 'vitest';
import { TokenEstimator, DEFAULT_CONTEXT_WINDOWS } from '../../../src/core/TokenEstimator';
import type { ReActMessage } from '../../../src/core/rag/ReActLoop';
⋮----
// ---------------------------------------------------------------------------
// estimate()
// ---------------------------------------------------------------------------
⋮----
expect(TokenEstimator.estimate('   ')).toBe(1); // Math.ceil(3/4) = 1
⋮----
expect(TokenEstimator.estimate(text)).toBe(100); // Math.ceil(400/4) = 100
⋮----
expect(TokenEstimator.estimate('abc')).toBe(1);   // Math.ceil(3/4) = 1
expect(TokenEstimator.estimate('abcd')).toBe(1);  // Math.ceil(4/4) = 1
expect(TokenEstimator.estimate('abcde')).toBe(2); // Math.ceil(5/4) = 2
⋮----
// ---------------------------------------------------------------------------
// estimateMessages()
// ---------------------------------------------------------------------------
⋮----
{ role: 'user', content: 'hello' },         // "userhello" = 9 chars => ceil(9/4)=3
{ role: 'assistant', content: 'world' },     // "assistantworld" = 14 chars => ceil(14/4)=4
{ role: 'system', content: '' },             // "system" = 6 chars => ceil(6/4)=2
⋮----
// 3 + 4 + 2 = 9
⋮----
{ role: 'user', content: 'a'.repeat(400) }, // "user" + 400 = 404 chars => ceil(404/4)=101
⋮----
// ---------------------------------------------------------------------------
// getLimit()
// ---------------------------------------------------------------------------
⋮----
// ---------------------------------------------------------------------------
// DEFAULT_CONTEXT_WINDOWS (constante exportada)
// ---------------------------------------------------------------------------

================
File: tests/unit/core/ToolRegistry.test.ts
================
import { describe, it, expect, vi } from 'vitest';
import { ToolRegistry } from '../../../src/core/ToolRegistry';
⋮----
/**
 * Testes unitários para ToolRegistry.
 *
 * Nenhuma dependência externa — apenas Map interno e handlers mockados.
 * Zero consumo de CPU por Ollama, zero I/O.
 */

================
File: tests/unit/graph/ASTRelationshipExtractor.test.ts
================
/**
 * Testes unitários para ASTRelationshipExtractor.
 *
 * Cobre:
 * - Extração de funções, classes, interfaces do AST
 * - CALLS entre funções no mesmo arquivo
 * - IMPORTS de módulos
 * - EXTENDS e IMPLEMENTS
 * - BELONGS_TO e CONTAINS (arquivo ↔ símbolo)
 * - Limite de MAX_EDGES_PER_FILE (200)
 * - Código vazio, inválido
 * - Extensão não suportada (ex: .md, .json)
 * - Auto-referências e built-ins
 */
⋮----
import { describe, it, expect, vi } from 'vitest';
import type { IASTParser, ASTNode } from '../../../src/core/rag/IASTParser';
import { ASTRelationshipExtractor } from '../../../src/core/rag/graph/ASTRelationshipExtractor';
⋮----
/**
 * Mock de IASTParser que retorna ASTNodes previsíveis.
 * Evita depender do TypescriptASTAdapter real.
 */
function createMockParser(nodes: ASTNode[]): IASTParser
⋮----
// =========================================================================
// Nós e arquivos básicos
// =========================================================================
⋮----
expect(result.nodes.length).toBeGreaterThanOrEqual(2); // file + function
⋮----
// =========================================================================
// Relação BELONGS_TO / CONTAINS
// =========================================================================
⋮----
// BELONGS_TO: símbolo → arquivo
⋮----
// CONTAINS: arquivo → símbolo
⋮----
// =========================================================================
// CALLS
// =========================================================================
⋮----
// A função myFunc() chama helper() que está definida no mesmo arquivo
⋮----
// =========================================================================
// IMPORTS
// =========================================================================
⋮----
// =========================================================================
// EXTENDS
// =========================================================================
⋮----
// =========================================================================
// IMPLEMENTS
// =========================================================================
⋮----
// =========================================================================
// Casos de borda
// =========================================================================
⋮----
// Deve respeitar o limite MAX_EDGES_PER_FILE (200)
⋮----
// refB aparece sem chamada → deve gerar REFERENCES

================
File: tests/unit/graph/GraphBuilder.test.ts
================
/**
 * Testes unitários para GraphBuilder.
 *
 * Cobre:
 * - build() com diretório vazio (sem arquivos)
 * - build() com arquivos .ts e .js
 * - build() incremental (arquivos não modificados são ignorados)
 * - build() detecta arquivos removidos
 * - build() respeita maxFiles
 * - build() ignora node_modules, .git, .soberano, dist
 * - Erro ao ler arquivo não interrompe o processo
 * - Reload do grafo existente
 */
⋮----
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { GraphBuilder } from '../../../src/core/rag/graph/GraphBuilder';
import type { IRelationshipExtractor } from '../../../src/core/rag/graph/IRelationshipExtractor';
import type { KnowledgeGraph, GraphNode, GraphEdge } from '../../../src/core/rag/graph/types';
⋮----
// ----- Mocks globais do fs para evitar tocar no disco real -----
⋮----
/**
 * Mock do FileReader: retorna conteúdo arbitrário ou diretório.
 */
function createMockFileReader()
⋮----
/**
 * Mock do IGraphStore.
 */
function createMockGraphStore()
⋮----
/**
 * Helper para criar um nó de grafo.
 */
function makeNode(id: string, label: string, filePath: string, type: GraphNode['type'] = 'function'): GraphNode
⋮----
/**
 * Helper para criar uma aresta.
 */
function makeEdge(from: string, to: string, type: GraphEdge['type'] = 'CALLS'): GraphEdge
⋮----
// Reseta mocks do fs
⋮----
// =========================================================================
// build - diretório vazio
// =========================================================================
⋮----
// =========================================================================
// build - com arquivos fonte
// =========================================================================
⋮----
// readDir do FileReader retorna lista de entradas
⋮----
// fs.stat retorna que é um arquivo .ts
⋮----
// Cada entrada é um arquivo (não diretório)
⋮----
// =========================================================================
// build - incremental (arquivos não modificados)
// =========================================================================
⋮----
// Mesmo mtime do grafo existente → não modifica
⋮----
// Quando não há nada para processar nem remover, save não é chamado
⋮----
// =========================================================================
// build - detecta arquivos removidos
// =========================================================================
⋮----
// =========================================================================
// build - respeita maxFiles
// =========================================================================
⋮----
// =========================================================================
// build - ignora pastas de sistema
// =========================================================================
⋮----
// Primeiro, FileReader.readDir é chamado para /tmp/proj → contém file.ts + pastas
// Depois, o walk tenta entrar em node_modules, .git, etc — readDir retorna vazio ou são pulados
⋮----
// file.ts é um arquivo
⋮----
// Pastas bloqueadas: node_modules, .git, .soberano, dist
// O GraphBuilder checa o basename, não o stat, então o walk nunca chama stat para essas pastas
⋮----
// =========================================================================
// build - erro ao ler arquivo não interrompe
// =========================================================================
⋮----
// =========================================================================
// build - reload do grafo existente
// =========================================================================
⋮----
// Segundo build: load retorna grafo salvo (mockStore.save salvou internamente)
// Agora o mtime é diferente, então processa novamente

================
File: tests/unit/graph/GraphRAGManager.test.ts
================
/**
 * Testes unitários para GraphRAGManager.
 *
 * Cobre:
 * - search() com grafo vazio (sem nós)
 * - search() expande vizinhança corretamente
 * - search() com profundidade de grafo diferente
 * - search() mescla e re-ranqueia resultados
 * - search() textWeight = 1.0 (apenas textual)
 * - search() textWeight = 0.0 (apenas estrutural)
 * - search() filtra por edgeTypes
 * - formatGraphContext() com e sem nós visitados
 */
⋮----
import { describe, it, expect, beforeEach, vi } from 'vitest';
import {
  GraphRAGManager,
  type GraphSearchOptions,
} from '../../../src/core/rag/graph/GraphRAGManager';
import { Retriever } from '../../../src/core/rag/Retriever';
import type { IGraphQuery, NeighborNode } from '../../../src/core/rag/graph/IGraphQuery';
import type { ChunkEntry } from '../../../src/core/rag/VectorStore';
import type { RelationshipType } from '../../../src/core/rag/graph/types';
⋮----
// ----- helpers de teste -----
⋮----
/** Cria um chunk simples */
function makeChunk(
  id: string,
  filePath: string,
  text: string,
  startLine = 1,
  endLine = 10
): ChunkEntry
⋮----
/** Cria um vizinho do grafo */
function makeNeighbor(
  id: string,
  label: string,
  filePath: string,
  edgeType: RelationshipType,
  distance: number
): NeighborNode
⋮----
// =========================================================================
// search - grafo vazio
// =========================================================================
⋮----
// getNodeByChunkId retorna null para todos (grafo vazio)
⋮----
// =========================================================================
// search - expansão de vizinhança
// =========================================================================
⋮----
// Nó no grafo para c1
⋮----
// Vizinhança de n-a: nó n-b (CALLS)
⋮----
// Deve ter expandido
⋮----
// =========================================================================
// search - profundidade de grafo diferente
// =========================================================================
⋮----
// =========================================================================
// search - mescla e re-ranqueia resultados
// =========================================================================
⋮----
// Chunks com embedding similar ao query
⋮----
// chunk3 está no mesmo arquivo do vizinho de c1
⋮----
// Expandindo n-c1 → encontra vizinho neighbor-file
⋮----
// Deve ter pelo menos 1 match
⋮----
// expandNeighborhood foi chamado
⋮----
// =========================================================================
// search - textWeight = 1.0 (apenas textual)
// =========================================================================
⋮----
// Todos os matches devem ter score textual puro
⋮----
// =========================================================================
// search - textWeight = 0.0 (apenas estrutural)
// =========================================================================
⋮----
// Pode ter encontrado neighbor via expansão
⋮----
// =========================================================================
// search - filtra por edgeTypes
// =========================================================================
⋮----
// =========================================================================
// formatGraphContext
// =========================================================================
⋮----
// Executa busca primeiro para ter um resultado
⋮----
// Deve conter o caminho do arquivo
⋮----
// Deve conter a label do chunk

================
File: tests/unit/graph/JsonGraphStore.test.ts
================
/**
 * Testes unitários para JsonGraphStore.
 *
 * Cobre:
 * - save(), load(), clear()
 * - upsertNodes(), upsertEdges()
 * - getNodeByChunkId(), expandNeighborhood(), getNodesByFile()
 * - Idempotência de upserts (chave composta)
 * - Grafo não carregado (adjacency = null)
 */
⋮----
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
⋮----
import { JsonGraphStore, hashId } from '../../../src/core/rag/graph/JsonGraphStore';
import type { KnowledgeGraph, GraphNode, GraphEdge } from '../../../src/core/rag/graph/types';
⋮----
/**
 * Cria um diretório temporário isolado para cada teste.
 * Garante zero efeito colateral entre testes.
 */
function createTempDir(): string
⋮----
// =========================================================================
// save / load / clear
// =========================================================================
⋮----
// =========================================================================
// upsertNodes
// =========================================================================
⋮----
// =========================================================================
// upsertEdges
// =========================================================================
⋮----
await store.upsertEdges([edge]); // mesmo exato
⋮----
// =========================================================================
// IGraphQuery - getNodeByChunkId
// =========================================================================
⋮----
// =========================================================================
// IGraphQuery - expandNeighborhood
// =========================================================================
⋮----
// a -> b -> c
⋮----
// =========================================================================
// IGraphQuery - getNodesByFile
// =========================================================================
⋮----
// =========================================================================
// hashId
// =========================================================================
⋮----
expect(h1).toBe(h2);   // determinístico
expect(h1).not.toBe(h3); // entradas diferentes → hashes diferentes

================
File: tests/unit/providers/OllamaProvider.test.ts
================
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { OllamaProvider } from '../../../src/providers/OllamaProvider';
import type { ChatRequest } from '../../../src/providers/types';
⋮----
/**
 * Testes unitários para OllamaProvider.
 *
 * Mock do módulo nativo `node:http` → zero requisições reais.
 * Zero consumo de CPU por Ollama, zero I/O de rede.
 * Compatível com hardware de 12GB — sem carregar modelos.
 */
⋮----
// Mock completo do node:http
⋮----
// Função que simula http.request
const request = (
    options: any,
    callback?: (res: any) => void
): any =>
⋮----
// Encontra a resposta mockada baseada no path
⋮----
// Simula IncomingMessage — emite dados como string (não Buffer)
⋮----
// Iterator assíncrono para for await...of (postStream)
⋮----
// Se callback foi passado (modo post tradicional), chama direto
⋮----
// Objeto req com suporte a req.on('response') usado pelo postStream
⋮----
// postStream usa req.on('response') em vez de callback
⋮----
// Verifica se a requisição foi montada corretamente
⋮----
// Verifica a resposta
⋮----
// Consome o generator
⋮----
'',                       // vazia (ignora)
'\n',                     // linha em branco
⋮----
'not json line\n',        // não-JSON (ignora)

================
File: tests/unit/rag/ASTChunkerService.test.ts
================
/**
 * Tests for ASTChunkerService
 *
 * Validações:
 * - Chunk de arquivo .ts usa AST; .md usa fallback textual
 * - AST parse bem-sucedido gera chunks baseados em nós semânticos
 * - Nós pequenos (≤ MAX_CHUNK_SIZE) geram 1 chunk cada
 * - Classe grande é subdividida em signature + métodos
 * - Nó grande não-classe é dividido por linhas respeitando MAX_CHUNK_SIZE
 * - Fallback textual para código inválido ou sem extensão reconhecida
 * - Fallback para parse que retorna array vazio
 * - Respeita MAX_CHUNKS_PER_FILE (não explode com arquivo gigante)
 */
⋮----
import { describe, it, expect, vi } from 'vitest';
import { ASTChunkerService } from '../../../src/core/rag/ASTChunkerService';
import type { IChunker, ChunkResult } from '../../../src/core/rag/IChunker';
import type { IASTParser, ASTNode } from '../../../src/core/rag/IASTParser';
⋮----
// Factory helper
function createService(params?: {
  astParser?: IASTParser;
  fallbackChunker?: IChunker;
}): ASTChunkerService
⋮----
// Classe precisa ter > 2000 chars para entrar no splitLargeClass
⋮----
// Garantir que classe tem > 2000 chars
⋮----
endLine: 2 + methods.length, // 2 = class header + footer
⋮----
// Deve ter pelo menos signature + métodos
⋮----
// Primeiro chunk = assinatura da classe
⋮----
// Deve ter método method0
⋮----
// Gera uma função grande com > 2000 chars
⋮----
// Deve ter dividido em múltiplos chunks
⋮----
// Todos os chunks juntos devem manter o conteúdo original
⋮----
// Gera 60 nós pequenos

================
File: tests/unit/rag/Chunker.test.ts
================
import { describe, it, expect } from 'vitest';
import { Chunker } from '../../../src/core/rag/Chunker';
⋮----
/**
 * Testes unitários para Chunker.
 *
 * Função pura, sem dependências, sem I/O.
 * Zero consumo de CPU além do necessário.
 */
⋮----
expect(chunks[1].startLine).toBe(4); // +1 pelo \n\n
⋮----
// Cria um texto longo com sentenças reais (com pontuação para split funcionar)
⋮----
const longParagraph = sentence.repeat(60); // ~6000 chars, > 2000
⋮----
// Deve ter gerado múltiplos chunks
⋮----
// Cada chunk (exceto talvez o último) deve ter no máximo 2000 chars
⋮----
// Conteúdo com 60 parágrafos

================
File: tests/unit/rag/ReActLoop.test.ts
================
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { ReActLoop, type ReActMessage } from '../../../src/core/rag/ReActLoop';
import { CommandExecutor } from '../../../src/core/CommandExecutor';
import { ToolRegistry } from '../../../src/core/ToolRegistry';
import type { IProvider } from '../../../src/providers/types';
import type { IContextCompressor, CompressedContext } from '../../../src/core/IContextCompressor';
import { assessCompressionNeed, CompressionTrigger } from '../../../src/core/IContextCompressor';
import { TokenEstimator } from '../../../src/core/TokenEstimator';
⋮----
/**
 * Testes unitários para ReActLoop.
 *
 * Mock das dependências IProvider, CommandExecutor e ToolRegistry →
 * zero requisições ao Ollama, zero execução de comandos reais, zero tool calls reais.
 * Zero consumo de CPU, zero I/O.
 *
 * Cobre ambos os modos:
 * - Text mode (ACTION / FINAL_ANSWER) — legado, com CommandExecutor
 * - JSON mode (tool_call / final_response) — novo, com ToolRegistry
 */
⋮----
// ── Helpers de mock ──
⋮----
function createMockProvider(responses: string[]): IProvider
⋮----
/**
 * Cria um mock de provider com streamChat implementado.
 * Usa um AsyncGenerator que itera sobre os tokens da resposta.
 */
function createMockStreamProvider(responseContent: string): IProvider
⋮----
// Yield character by character para simular streaming real
⋮----
function createMockExecutor(): CommandExecutor
⋮----
function createMockToolRegistry(): ToolRegistry
⋮----
// ── Suite de testes ──
⋮----
expect(result.iterations).toBe(5); // MAX_ITERATIONS real
⋮----
// Deve ter passado format: 'json'
⋮----
// ToolRegistry deve ter executado readFile
⋮----
// Mesma tool_call duas vezes consecutivas → detecta loop
⋮----
// Fallback retorna o conteúdo cru
⋮----
// Sempre retorna tool_call (nunca final_response)
⋮----
// Sem Reflector (undefined)
⋮----
// Deve ter recebido tokens via callback (streamChat foi usado)
⋮----
// Sem streamChat — proposital
⋮----
// onToken nunca deve ser chamado porque streamChat não existe
⋮----
// streamChat não deve ser chamado porque stream está undefined
⋮----
// Provider com streamChat: a primeira chamada retorna tool_call, a segunda final_response
// O sendPrompt sempre prefere streamChat sobre chat quando streamOpts está ativo,
// então o streamMock precisa ser configurado para múltiplas chamadas.
⋮----
// streamChat foi chamado duas vezes (tool_call + final_response)
⋮----
// Tokens da última iteração (final_response)
⋮----
// ── Suite de testes: Compressão de Contexto ──
⋮----
/**
   * Cria um compressor mock que retorna um CompressedContext previsível.
   */
function createMockCompressor(): IContextCompressor
⋮----
// Com tinyllama:1b (limit=4096), 70% = ~2867 tokens ≈ ~11468 chars
// Um histórico curto de ~10 mensagens curtas fica bem abaixo disso
⋮----
'Seja breve', // systemPrompt pequeno
⋮----
// tinyllama:1b tem limit=4096. 70% = 2867 tokens ≈ 11468 chars
// Criar um histórico longo o suficiente para estourar 70%
// Cada mensagem: role + content ≈ 200 chars → ~50 tokens cada
// 2900 tokens / 50 tokens/msg ≈ 58 mensagens
// systemPrompt também conta, então vamos usar 60 mensagens de ~230 chars cada
⋮----
// Spy no compress para verificar argumentos
⋮----
// O resultado deve conter FINAL_ANSWER (provido pelo mock)

================
File: tests/unit/rag/Retriever.test.ts
================
import { describe, it, expect } from 'vitest';
import { cosineSimilarity, Retriever } from '../../../src/core/rag/Retriever';
import type { ChunkEntry } from '../../../src/core/rag/VectorStore';
⋮----
/**
 * Testes unitários para Retriever e cosineSimilarity.
 *
 * Nenhuma dependência externa — funções matemáticas/transformação pura.
 * Zero consumo de CPU por Ollama, zero I/O.
 */
⋮----
function unitVector(index: number): number[]
⋮----
// O prefixo "Documentos relevantes:\n\n" é adicionado antes do truncamento
// então o tamanho pode passar ligeiramente de 200, mas deve ficar abaixo do texto completo

================
File: tests/unit/rag/TypescriptASTAdapter.test.ts
================
/**
 * Tests for TypescriptASTAdapter
 *
 * Validações:
 * - Parse de function declaration com nome
 * - Parse de class declaration com métodos
 * - Parse de interface e type alias
 * - Parse de enum
 * - Parse de arrow function em const
 * - Array vazio para código inválido / vazio
 * - Ordenação por startLine
 * - Preservação de texto fonte e line numbers
 * - Métodos dentro de classe (MethodDeclaration, Constructor)
 * - ExportAssignment (export default)
 * - ModuleDeclaration (namespace)
 */
⋮----
import { describe, it, expect } from 'vitest';
import { TypescriptASTAdapter } from '../../../src/core/rag/TypescriptASTAdapter';
⋮----
// TS parser tolera erros, mas não deve crashar

================
File: tests/unit/validation/JsonValidator.test.ts
================
import { describe, it, expect } from 'vitest';
import { JsonValidator, ValidationError } from '../../../src/validation/JsonValidator';
⋮----
/**
 * Testes unitários para JsonValidator.
 *
 * Função pura, sem dependências, sem I/O.
 * Zero consumo de CPU além do necessário.
 */
⋮----
interface MeuTipo {
      nome: string;
      idade: number;
    }

================
File: package.json
================
{
  "name": "@soberano/core",
  "version": "1.0.0",
  "description": "Soberano-Core: Agente IA modular com Core, Providers e CLI",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli/index.ts",
    "start": "node dist/cli/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": ["ai", "agent", "ollama", "cli"],
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "vitest": "^4.1.5"
  }
}

================
File: tsconfig.json
================
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020"],
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "types": ["node"],
    "declarationMap": true,
    "sourceMap": true,
    "composite": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "tests"]
}

================
File: tsconfig.tsbuildinfo
================
{"fileNames":["../../node_modules/typescript/lib/lib.es5.d.ts","../../node_modules/typescript/lib/lib.es2015.d.ts","../../node_modules/typescript/lib/lib.es2016.d.ts","../../node_modules/typescript/lib/lib.es2017.d.ts","../../node_modules/typescript/lib/lib.es2018.d.ts","../../node_modules/typescript/lib/lib.es2019.d.ts","../../node_modules/typescript/lib/lib.es2020.d.ts","../../node_modules/typescript/lib/lib.es2015.core.d.ts","../../node_modules/typescript/lib/lib.es2015.collection.d.ts","../../node_modules/typescript/lib/lib.es2015.generator.d.ts","../../node_modules/typescript/lib/lib.es2015.iterable.d.ts","../../node_modules/typescript/lib/lib.es2015.promise.d.ts","../../node_modules/typescript/lib/lib.es2015.proxy.d.ts","../../node_modules/typescript/lib/lib.es2015.reflect.d.ts","../../node_modules/typescript/lib/lib.es2015.symbol.d.ts","../../node_modules/typescript/lib/lib.es2015.symbol.wellknown.d.ts","../../node_modules/typescript/lib/lib.es2016.array.include.d.ts","../../node_modules/typescript/lib/lib.es2016.intl.d.ts","../../node_modules/typescript/lib/lib.es2017.arraybuffer.d.ts","../../node_modules/typescript/lib/lib.es2017.date.d.ts","../../node_modules/typescript/lib/lib.es2017.object.d.ts","../../node_modules/typescript/lib/lib.es2017.sharedmemory.d.ts","../../node_modules/typescript/lib/lib.es2017.string.d.ts","../../node_modules/typescript/lib/lib.es2017.intl.d.ts","../../node_modules/typescript/lib/lib.es2017.typedarrays.d.ts","../../node_modules/typescript/lib/lib.es2018.asyncgenerator.d.ts","../../node_modules/typescript/lib/lib.es2018.asynciterable.d.ts","../../node_modules/typescript/lib/lib.es2018.intl.d.ts","../../node_modules/typescript/lib/lib.es2018.promise.d.ts","../../node_modules/typescript/lib/lib.es2018.regexp.d.ts","../../node_modules/typescript/lib/lib.es2019.array.d.ts","../../node_modules/typescript/lib/lib.es2019.object.d.ts","../../node_modules/typescript/lib/lib.es2019.string.d.ts","../../node_modules/typescript/lib/lib.es2019.symbol.d.ts","../../node_modules/typescript/lib/lib.es2019.intl.d.ts","../../node_modules/typescript/lib/lib.es2020.bigint.d.ts","../../node_modules/typescript/lib/lib.es2020.date.d.ts","../../node_modules/typescript/lib/lib.es2020.promise.d.ts","../../node_modules/typescript/lib/lib.es2020.sharedmemory.d.ts","../../node_modules/typescript/lib/lib.es2020.string.d.ts","../../node_modules/typescript/lib/lib.es2020.symbol.wellknown.d.ts","../../node_modules/typescript/lib/lib.es2020.intl.d.ts","../../node_modules/typescript/lib/lib.es2020.number.d.ts","../../node_modules/typescript/lib/lib.decorators.d.ts","../../node_modules/typescript/lib/lib.decorators.legacy.d.ts","./src/core/FileReader.ts","./src/core/CommandExecutor.ts","./src/core/ToolRegistry.ts","./src/providers/types.ts","./src/core/Reflector.ts","./src/validation/JsonValidator.ts","./src/core/IContextCompressor.ts","./src/core/rag/ReActLoop.ts","./src/core/TokenEstimator.ts","./src/core/StatefulCompressor.ts","./src/providers/OllamaHttpClient.ts","./src/providers/OllamaProvider.ts","./src/core/ProviderFactory.ts","./src/core/SessionStore.ts","./src/core/SessionManager.ts","../../node_modules/typescript/lib/typescript.d.ts","./src/core/astUtils.ts","./src/core/ASTEditor.ts","./src/core/SearchReplaceEditor.ts","./src/core/AppContext.ts","./src/core/rag/IChunker.ts","./src/core/rag/Chunker.ts","./src/core/rag/Embedder.ts","./src/core/rag/VectorStore.ts","./src/core/rag/Retriever.ts","./src/core/rag/graph/types.ts","./src/core/rag/graph/IRelationshipExtractor.ts","./src/core/rag/graph/IGraphStore.ts","./src/core/rag/graph/GraphBuilder.ts","./src/core/rag/graph/IGraphQuery.ts","./src/core/rag/IASTParser.ts","./src/core/rag/graph/JsonGraphStore.ts","./src/core/rag/graph/ASTRelationshipExtractor.ts","./src/core/rag/TypescriptASTAdapter.ts","./src/core/rag/ASTChunkerService.ts","./src/core/rag/graph/GraphRAGManager.ts","./src/core/rag/RAGManager.ts","./src/core/ErrorJournal.ts","./src/core/index.ts","./src/index.ts","./src/cli/strategies/ChatStrategy.ts","./src/cli/strategies/StreamStrategy.ts","./src/cli/strategies/ReActStrategy.ts","./src/cli/strategies/index.ts","./src/cli/commands.ts","./src/cli/index.ts","./src/core/rag/graph/index.ts","./src/core/rag/index.ts","./src/providers/index.ts","../../node_modules/@types/node/compatibility/disposable.d.ts","../../node_modules/@types/node/compatibility/indexable.d.ts","../../node_modules/@types/node/compatibility/iterators.d.ts","../../node_modules/@types/node/compatibility/index.d.ts","../../node_modules/@types/node/globals.typedarray.d.ts","../../node_modules/@types/node/buffer.buffer.d.ts","../../node_modules/@types/node/globals.d.ts","../../node_modules/@types/node/web-globals/abortcontroller.d.ts","../../node_modules/@types/node/web-globals/domexception.d.ts","../../node_modules/@types/node/web-globals/events.d.ts","../../node_modules/undici-types/header.d.ts","../../node_modules/undici-types/readable.d.ts","../../node_modules/undici-types/file.d.ts","../../node_modules/undici-types/fetch.d.ts","../../node_modules/undici-types/formdata.d.ts","../../node_modules/undici-types/connector.d.ts","../../node_modules/undici-types/client.d.ts","../../node_modules/undici-types/errors.d.ts","../../node_modules/undici-types/dispatcher.d.ts","../../node_modules/undici-types/global-dispatcher.d.ts","../../node_modules/undici-types/global-origin.d.ts","../../node_modules/undici-types/pool-stats.d.ts","../../node_modules/undici-types/pool.d.ts","../../node_modules/undici-types/handlers.d.ts","../../node_modules/undici-types/balanced-pool.d.ts","../../node_modules/undici-types/agent.d.ts","../../node_modules/undici-types/mock-interceptor.d.ts","../../node_modules/undici-types/mock-agent.d.ts","../../node_modules/undici-types/mock-client.d.ts","../../node_modules/undici-types/mock-pool.d.ts","../../node_modules/undici-types/mock-errors.d.ts","../../node_modules/undici-types/proxy-agent.d.ts","../../node_modules/undici-types/env-http-proxy-agent.d.ts","../../node_modules/undici-types/retry-handler.d.ts","../../node_modules/undici-types/retry-agent.d.ts","../../node_modules/undici-types/api.d.ts","../../node_modules/undici-types/interceptors.d.ts","../../node_modules/undici-types/util.d.ts","../../node_modules/undici-types/cookies.d.ts","../../node_modules/undici-types/patch.d.ts","../../node_modules/undici-types/websocket.d.ts","../../node_modules/undici-types/eventsource.d.ts","../../node_modules/undici-types/filereader.d.ts","../../node_modules/undici-types/diagnostics-channel.d.ts","../../node_modules/undici-types/content-type.d.ts","../../node_modules/undici-types/cache.d.ts","../../node_modules/undici-types/index.d.ts","../../node_modules/@types/node/web-globals/fetch.d.ts","../../node_modules/@types/node/assert.d.ts","../../node_modules/@types/node/assert/strict.d.ts","../../node_modules/@types/node/async_hooks.d.ts","../../node_modules/@types/node/buffer.d.ts","../../node_modules/@types/node/child_process.d.ts","../../node_modules/@types/node/cluster.d.ts","../../node_modules/@types/node/console.d.ts","../../node_modules/@types/node/constants.d.ts","../../node_modules/@types/node/crypto.d.ts","../../node_modules/@types/node/dgram.d.ts","../../node_modules/@types/node/diagnostics_channel.d.ts","../../node_modules/@types/node/dns.d.ts","../../node_modules/@types/node/dns/promises.d.ts","../../node_modules/@types/node/domain.d.ts","../../node_modules/@types/node/events.d.ts","../../node_modules/@types/node/fs.d.ts","../../node_modules/@types/node/fs/promises.d.ts","../../node_modules/@types/node/http.d.ts","../../node_modules/@types/node/http2.d.ts","../../node_modules/@types/node/https.d.ts","../../node_modules/@types/node/inspector.generated.d.ts","../../node_modules/@types/node/module.d.ts","../../node_modules/@types/node/net.d.ts","../../node_modules/@types/node/os.d.ts","../../node_modules/@types/node/path.d.ts","../../node_modules/@types/node/perf_hooks.d.ts","../../node_modules/@types/node/process.d.ts","../../node_modules/@types/node/punycode.d.ts","../../node_modules/@types/node/querystring.d.ts","../../node_modules/@types/node/readline.d.ts","../../node_modules/@types/node/readline/promises.d.ts","../../node_modules/@types/node/repl.d.ts","../../node_modules/@types/node/sea.d.ts","../../node_modules/@types/node/stream.d.ts","../../node_modules/@types/node/stream/promises.d.ts","../../node_modules/@types/node/stream/consumers.d.ts","../../node_modules/@types/node/stream/web.d.ts","../../node_modules/@types/node/string_decoder.d.ts","../../node_modules/@types/node/test.d.ts","../../node_modules/@types/node/timers.d.ts","../../node_modules/@types/node/timers/promises.d.ts","../../node_modules/@types/node/tls.d.ts","../../node_modules/@types/node/trace_events.d.ts","../../node_modules/@types/node/tty.d.ts","../../node_modules/@types/node/url.d.ts","../../node_modules/@types/node/util.d.ts","../../node_modules/@types/node/v8.d.ts","../../node_modules/@types/node/vm.d.ts","../../node_modules/@types/node/wasi.d.ts","../../node_modules/@types/node/worker_threads.d.ts","../../node_modules/@types/node/zlib.d.ts","../../node_modules/@types/node/index.d.ts"],"fileIdsList":[[100,143,146],[100,145,146],[146],[100,146,151,179],[100,146,147,152,157,165,176,187],[100,146,147,148,157,165],[100,146],[95,96,97,100,146],[100,146,149,188],[100,146,150,151,158,166],[100,146,151,176,184],[100,146,152,154,157,165],[100,145,146,153],[100,146,154,155],[100,146,156,157],[100,145,146,157],[100,146,157,158,159,176,187],[100,146,157,158,159,172,176,179],[100,146,154,157,160,165,176,187],[100,146,157,158,160,161,165,176,184,187],[100,146,160,162,176,184,187],[98,99,100,101,102,103,104,142,143,144,145,146,147,148,149,150,151,152,153,154,155,156,157,158,159,160,161,162,163,164,165,166,167,168,169,170,171,172,173,174,175,176,177,178,179,180,181,182,183,184,185,186,187,188,189,190,191,192,193],[100,146,157,163],[100,146,164,187,192],[100,146,154,157,165,176],[100,146,166],[100,146,167],[100,145,146,168],[100,143,144,145,146,147,148,149,150,151,152,153,154,155,156,157,158,159,160,161,162,163,164,165,166,167,168,169,170,171,172,173,174,175,176,177,178,179,180,181,182,183,184,185,186,187,188,189,190,191,192,193],[100,146,170],[100,146,171],[100,146,157,172,173],[100,146,172,174,188,190],[100,146,157,176,177,179],[100,146,178,179],[100,146,176,177],[100,146,179],[100,146,180],[100,143,146,176,181],[100,146,157,182,183],[100,146,182,183],[100,146,151,165,176,184],[100,146,185],[100,146,165,186],[100,146,160,171,187],[100,146,151,188],[100,146,176,189],[100,146,164,190],[100,146,191],[100,141,146],[100,141,146,157,159,168,176,179,187,190,192],[100,146,176,193],[100,113,117,146,187],[100,113,146,176,187],[100,108,146],[100,110,113,146,184,187],[100,146,165,184],[100,146,194],[100,108,146,194],[100,110,113,146,165,187],[100,105,106,109,112,146,157,176,187],[100,113,120,146],[100,105,111,146],[100,113,134,135,146],[100,109,113,146,179,187,194],[100,134,146,194],[100,107,108,146,194],[100,113,146],[100,107,108,109,110,111,112,113,114,115,117,118,119,120,121,122,123,124,125,126,127,128,129,130,131,132,133,135,136,137,138,139,140,146],[100,113,128,146],[100,113,120,121,146],[100,111,113,121,122,146],[100,112,146],[100,105,108,113,146],[100,113,117,121,122,146],[100,117,146],[100,111,113,116,146,187],[100,105,110,113,120,146],[100,146,176],[100,108,113,134,146,192,194],[84,89,100,146],[90,100,146],[49,84,100,146],[49,84,86,100,146],[86,100,146],[86,87,88,100,146],[46,61,62,100,146,159],[46,47,48,49,53,58,59,60,63,64,100,146,159,167],[100,146,147],[49,100,146,159,167],[100,146,158,159,167],[53,100,146],[49,57,100,146],[49,100,146],[46,100,146,159],[53,59,100,146,151],[53,100,146,159,167],[49,52,53,100,146],[61,100,146],[46,47,48,50,51,52,53,54,55,58,59,60,63,64,65,66,67,68,69,70,82,83,100,146],[66,76,100,146],[66,100,146],[46,49,66,67,68,69,70,72,73,74,75,77,78,79,80,81,100,146,158,159,167],[47,48,49,50,51,52,54,100,146],[69,100,146],[61,62,76,100,146],[100,146,159,167],[66,71,72,76,77,100,146],[46,71,72,73,100,146,158,159,167],[69,70,75,100,146],[71,100,146],[71,73,75,100,146,151,159,167],[71,72,73,74,75,77,78,81,100,146],[53,66,67,68,69,70,76,79,80,82,92,100,146],[84,100,146],[100,146,160],[49,56,100,146],[49,56,57,100,146]],"fileInfos":[{"version":"c430d44666289dae81f30fa7b2edebf186ecc91a2d4c71266ea6ae76388792e1","affectsGlobalScope":true,"impliedFormat":1},{"version":"45b7ab580deca34ae9729e97c13cfd999df04416a79116c3bfb483804f85ded4","impliedFormat":1},{"version":"3facaf05f0c5fc569c5649dd359892c98a85557e3e0c847964caeb67076f4d75","impliedFormat":1},{"version":"e44bb8bbac7f10ecc786703fe0a6a4b952189f908707980ba8f3c8975a760962","impliedFormat":1},{"version":"5e1c4c362065a6b95ff952c0eab010f04dcd2c3494e813b493ecfd4fcb9fc0d8","impliedFormat":1},{"version":"68d73b4a11549f9c0b7d352d10e91e5dca8faa3322bfb77b661839c42b1ddec7","impliedFormat":1},{"version":"5efce4fc3c29ea84e8928f97adec086e3dc876365e0982cc8479a07954a3efd4","impliedFormat":1},{"version":"c57796738e7f83dbc4b8e65132f11a377649c00dd3eee333f672b8f0a6bea671","affectsGlobalScope":true,"impliedFormat":1},{"version":"dc2df20b1bcdc8c2d34af4926e2c3ab15ffe1160a63e58b7e09833f616efff44","affectsGlobalScope":true,"impliedFormat":1},{"version":"515d0b7b9bea2e31ea4ec968e9edd2c39d3eebf4a2d5cbd04e88639819ae3b71","affectsGlobalScope":true,"impliedFormat":1},{"version":"0559b1f683ac7505ae451f9a96ce4c3c92bdc71411651ca6ddb0e88baaaad6a3","affectsGlobalScope":true,"impliedFormat":1},{"version":"0dc1e7ceda9b8b9b455c3a2d67b0412feab00bd2f66656cd8850e8831b08b537","affectsGlobalScope":true,"impliedFormat":1},{"version":"ce691fb9e5c64efb9547083e4a34091bcbe5bdb41027e310ebba8f7d96a98671","affectsGlobalScope":true,"impliedFormat":1},{"version":"8d697a2a929a5fcb38b7a65594020fcef05ec1630804a33748829c5ff53640d0","affectsGlobalScope":true,"impliedFormat":1},{"version":"4ff2a353abf8a80ee399af572debb8faab2d33ad38c4b4474cff7f26e7653b8d","affectsGlobalScope":true,"impliedFormat":1},{"version":"fb0f136d372979348d59b3f5020b4cdb81b5504192b1cacff5d1fbba29378aa1","affectsGlobalScope":true,"impliedFormat":1},{"version":"d15bea3d62cbbdb9797079416b8ac375ae99162a7fba5de2c6c505446486ac0a","affectsGlobalScope":true,"impliedFormat":1},{"version":"68d18b664c9d32a7336a70235958b8997ebc1c3b8505f4f1ae2b7e7753b87618","affectsGlobalScope":true,"impliedFormat":1},{"version":"eb3d66c8327153d8fa7dd03f9c58d351107fe824c79e9b56b462935176cdf12a","affectsGlobalScope":true,"impliedFormat":1},{"version":"38f0219c9e23c915ef9790ab1d680440d95419ad264816fa15009a8851e79119","affectsGlobalScope":true,"impliedFormat":1},{"version":"69ab18c3b76cd9b1be3d188eaf8bba06112ebbe2f47f6c322b5105a6fbc45a2e","affectsGlobalScope":true,"impliedFormat":1},{"version":"a680117f487a4d2f30ea46f1b4b7f58bef1480456e18ba53ee85c2746eeca012","affectsGlobalScope":true,"impliedFormat":1},{"version":"2f11ff796926e0832f9ae148008138ad583bd181899ab7dd768a2666700b1893","affectsGlobalScope":true,"impliedFormat":1},{"version":"4de680d5bb41c17f7f68e0419412ca23c98d5749dcaaea1896172f06435891fc","affectsGlobalScope":true,"impliedFormat":1},{"version":"954296b30da6d508a104a3a0b5d96b76495c709785c1d11610908e63481ee667","affectsGlobalScope":true,"impliedFormat":1},{"version":"ac9538681b19688c8eae65811b329d3744af679e0bdfa5d842d0e32524c73e1c","affectsGlobalScope":true,"impliedFormat":1},{"version":"0a969edff4bd52585473d24995c5ef223f6652d6ef46193309b3921d65dd4376","affectsGlobalScope":true,"impliedFormat":1},{"version":"9e9fbd7030c440b33d021da145d3232984c8bb7916f277e8ffd3dc2e3eae2bdb","affectsGlobalScope":true,"impliedFormat":1},{"version":"811ec78f7fefcabbda4bfa93b3eb67d9ae166ef95f9bff989d964061cbf81a0c","affectsGlobalScope":true,"impliedFormat":1},{"version":"717937616a17072082152a2ef351cb51f98802fb4b2fdabd32399843875974ca","affectsGlobalScope":true,"impliedFormat":1},{"version":"d7e7d9b7b50e5f22c915b525acc5a49a7a6584cf8f62d0569e557c5cfc4b2ac2","affectsGlobalScope":true,"impliedFormat":1},{"version":"71c37f4c9543f31dfced6c7840e068c5a5aacb7b89111a4364b1d5276b852557","affectsGlobalScope":true,"impliedFormat":1},{"version":"576711e016cf4f1804676043e6a0a5414252560eb57de9faceee34d79798c850","affectsGlobalScope":true,"impliedFormat":1},{"version":"89c1b1281ba7b8a96efc676b11b264de7a8374c5ea1e6617f11880a13fc56dc6","affectsGlobalScope":true,"impliedFormat":1},{"version":"74f7fa2d027d5b33eb0471c8e82a6c87216223181ec31247c357a3e8e2fddc5b","affectsGlobalScope":true,"impliedFormat":1},{"version":"d6d7ae4d1f1f3772e2a3cde568ed08991a8ae34a080ff1151af28b7f798e22ca","affectsGlobalScope":true,"impliedFormat":1},{"version":"063600664504610fe3e99b717a1223f8b1900087fab0b4cad1496a114744f8df","affectsGlobalScope":true,"impliedFormat":1},{"version":"934019d7e3c81950f9a8426d093458b65d5aff2c7c1511233c0fd5b941e608ab","affectsGlobalScope":true,"impliedFormat":1},{"version":"52ada8e0b6e0482b728070b7639ee42e83a9b1c22d205992756fe020fd9f4a47","affectsGlobalScope":true,"impliedFormat":1},{"version":"3bdefe1bfd4d6dee0e26f928f93ccc128f1b64d5d501ff4a8cf3c6371200e5e6","affectsGlobalScope":true,"impliedFormat":1},{"version":"59fb2c069260b4ba00b5643b907ef5d5341b167e7d1dbf58dfd895658bda2867","affectsGlobalScope":true,"impliedFormat":1},{"version":"639e512c0dfc3fad96a84caad71b8834d66329a1f28dc95e3946c9b58176c73a","affectsGlobalScope":true,"impliedFormat":1},{"version":"368af93f74c9c932edd84c58883e736c9e3d53cec1fe24c0b0ff451f529ceab1","affectsGlobalScope":true,"impliedFormat":1},{"version":"8e7f8264d0fb4c5339605a15daadb037bf238c10b654bb3eee14208f860a32ea","affectsGlobalScope":true,"impliedFormat":1},{"version":"782dec38049b92d4e85c1585fbea5474a219c6984a35b004963b00beb1aab538","affectsGlobalScope":true,"impliedFormat":1},{"version":"848e7d5aada893ea17cb67916f8b423ec51470024c917a6ac20b1b4676d2ec5b","signature":"7dd2836965712a75347fdcc17f0f67d61c6e23ca81baadf3adc366c21261a0ff"},{"version":"1c309f6ea3aba1b70c7578e5458e5bad34f55066d43caaa41fcb1e978b70dc43","signature":"6207bb80b5cb593fc3de2a26205f31470a0bd8e3499847b1ed7ec31dcaab4f8f"},{"version":"27c61e6936e991af31a59811c66ffbc84a2032b43d267b5da59a66bceb986ea2","signature":"34864f854d621455261cbdc6ea85c446df03ceb727adae80a9973bb23209a162"},{"version":"92039c1cfec52d8498f55cca9eeeb089b10ed1bd610608fb13d19fb471380efa","signature":"d1e9414dd48a5e60a794d59f9a2b12f7f321f6786f4bcbfdc4c5479b73c3838d"},{"version":"a689ac82941e1512fae8550a25517a910c18ff52984b8ba46af0bba47b5b46e4","signature":"7bc7cf9c0f9c5b90933089789a3a70bdd4f0f24fae5df10437bebcf5d0d862f7"},{"version":"7c1dd9751dedb3e2ceca94cfc5dd243b5068918b5d2370865f3cfec48d1b2bc1","signature":"a91214bdc5b9052ff7e0913970efc44e5735204f642d63d3bb5d7b67385c165e"},{"version":"f62b98ffbcea03940a472bde2e4e8f6f6a82d94bce1e80639441ef4f799b1d1d","signature":"ae660ece86a21f0c044a356f517ecdc0aef33736bf20f4261ab34eb92b78b211"},{"version":"97ca0f8e8d3adec709cb91f2cac24f4fbb6a7c191af8e9b1dc1e757f97f6535d","signature":"f82597dd6692a919a5324f75fef67a7cfce2c95c40369a63c96ba2cea30a0b9f"},{"version":"19d0dd12dfc352d03e42b54e0521c575d1e7615f091f6f63575454bf7ceb7828","signature":"147deb8ef58056b8a12bc2813317a913e944df83ab765f354530f985a69da3e9"},{"version":"0f687235ada00579827c7994c29c2bb87c365f96e0e535edf8169bb9b5cf8482","signature":"c0f36d9595eb631fc76ec068154ef3d272facd9ef2ecff0eec11854da2fffa57"},{"version":"467f006facf41b964035d0a5cb743dd4ef23538d78966903f6f93a2d210cfb50","signature":"a155ffbc77e98379ad64671397d8453819ac1d9c89b941e1d10e48def0b8186f"},{"version":"a52dab30c2f0e77090e4b1adced2b13fc33ed2376b075f87b23d5d9f17d39494","signature":"307b9002f2fb6e1aed9a5d48bceb395d64838b768fdfa6640b5898431a206208"},{"version":"64b7cfa95c5b55e467e006d79c6012ff48cb7d2bed608487264271a7622edda9","signature":"c2100f05825b5d7bbbb04ca45674fc8bcb749dfde01d4eb2a0cc4b517be9573e"},{"version":"ad798369d06e29d7bae58212381357434da45b1fb44bc9da46748f307caea9c1","signature":"f1acd16bd8e76e9e56ad03855ad7f3ed1a1fa169064db6ce33527fd53a09e56f"},{"version":"e44bf13db61811900c2ed9f7f82f033d8f2f8cad7df66b9d7ff1f42b77a9f4a6","signature":"b59a97f2054bcc7fb3356429318306a11371c3ec2d672332db004dc6d103094a"},{"version":"e134052a6b1ded61693b4037f615dc72f14e2881e79c1ddbff6c514c8a516b05","impliedFormat":1},{"version":"a33b4f59305aff86135d2b950f1dec2de399a7cf2be426dd436791a2cdb9bbf1","signature":"b21280b7d0924c74884c14365eb9e5123af4ac0e96034c47c0e0fe65e9e090e0"},{"version":"100f917632debd9d3c0475d78b582bbd1255a5fd51a061220ab880c6b3d0e5d7","signature":"ed3357ac512d6f3cf19a4e93f7879cff14890d65d5b974f38b0e303811910217"},{"version":"4b92094727b7fc828420d8f0b75ee1c5215c6df5970ce462976ef246d39a1142","signature":"aa5dad2eebc5ac21a9c9c0bdc90da10e0d14b82911415c00e587705a95561fff"},{"version":"68384a4d4c54cbe4cf0a1f7ead925fd488a852fb0ffff0041d8f5e082bc8e436","signature":"f7c22450fff4e779b643e218610042932b28343970898423f9a991df8c56443f"},{"version":"88f40f8f527a81efd63855b436ea3d0029295a3917f40867064052dcfde7e910","signature":"73c06431b2266cde2beed9652b14084637a0d522824ebaa7d0ef9eb7950db2a1"},{"version":"e6ef99e836a889b63329276e7ea5356bb333b385659f428b45618909a7d4d115","signature":"f6561cb5a11473cc7a8983f6f059f66c675e198ed20ce6ec2ccdd223fd0b06b4"},{"version":"d541f344f5659b0d0d8139c3ef357fd5ec3ff37e393b6525b00f29eab4563494","signature":"f7e3ee3bda11795dd7776fe49f4427867b0e2717f8c23a12b4ed02458b6ff55f"},{"version":"cbfe24a31b75d93deb2e3b4dffbead60cd9e4b902335aba71ab6deaf59fc0ae2","signature":"65063aa0cdbfe110cffd6ca4c7ba79fd64da3fff95d688013a4100a28bf9a486"},{"version":"20517e36f535e2b085ca7afeff184248848d37844b36b1c12f50e30bd8fe703c","signature":"ec6536698cb9d6cdc5cbb26f681b09beff03b6820bf72cd3e0cc1e244bf8c19f"},{"version":"26085fd92320c826fb1f7b74d37bb7a02686b4fea1feaed5c322c65ec1f0cae9","signature":"cc3d0d21ae81bbeab5188ebd289bca6cadf68d992eeeda2c46a83f6baf5bdf90"},{"version":"08f3ccd1234c794a10a186496487430830f47ddebe403dbc311555426b4f3194","signature":"1181bd895576b30a04429e7dcf3302b56a68aa5c0491655d3e238e45872519ef"},{"version":"a5d491b11582d9bc59cbdd20551835b3dd6cf8646b481b2dcd49ee6804a9acd4","signature":"0a4d450e2b845b981bafaab4542638671d0a24820cd828c860df9602a03ad56c"},{"version":"2861343a840afafa8110a4da7c2242810b297c2f314eecd4e3b08355e5f2f4b0","signature":"d1c34611343be09243842ff46bd00c684cf8be8f21c52ee94cfafd1943e70ee5"},{"version":"91b3484f9f04d995de26080ae332011f55278471e70dc97697413efd5f422118","signature":"b3735d15d1314ed87d9beac1bb381cdeba0a10102b247b76e2491be5a7e9fb8b"},{"version":"98aad174ab124f21e8482b59a64ad367d6980ab8c95fd343990a030042bbdca2","signature":"520697234a92aa69078c48ab3a7fa2bcb58d234fd3e3a231225de8e5a4444d2b"},{"version":"2a787daac6da7a3cb2374fef705414047e58a621dcfe1dc1d1b57dbe9040b24b","signature":"e0508727db8cd98d13d011c4ccec47faf503112b1139a8d11b494dfcee5df1fd"},{"version":"640d22a045c663ab9fd5e07fd82626c9539a46ad265b343798b628f6d98137b0","signature":"b6a3b10ae3c607e254d27724e383c55696324c37ee948b342bb91297e1409813"},{"version":"dbf2958d7bd80d9e513c1ddf3a74b5cf8dd4e495ae77f74faf43b60e314d7640","signature":"51a066518c0b587925a5b200836039871c1c15cb6802147985a3973b100ddbd1"},{"version":"31cb8b590e9a7711049d2f9193e0b864115ae4ff73922be75e7b3f3de3a1add2","signature":"541aa90f9ec49e233f507b9d552f901b079f234a91e2cecdcb818f46db0d07de"},{"version":"5e9ac3bf6f98a37661a24c14ca5f85fcffcb20bfc06b8c97c95809eabce0dbf3","signature":"b2e4bbee28c6e2dcc31a0f96543b3f8613f46e596f7d1b2c0b5423d56c48d901"},{"version":"96d93f25c4b4802560af4a26ca41f8d50ca8770bc8f84232f3a58b7460732703","signature":"aa5f0dd9c6c70b2b6f2d59de4aecc5ed1ced662f162089105f1d8ca1c226da82"},{"version":"01007786f9cf6aa7c0eb42bb203f1ff7787753de66a87fd204ba84ed4b59f848","signature":"e6162f1a79c136843e64fd0200636237e78c492ad576b3744b8f29c1847a3d05"},{"version":"dfffd544e2689877c1394d9fa57cd0a525729214760c0b9a99384ee8e97285dd","signature":"c371dc343bfe097a55fe8190cd2f342bb3e6b1425d4574db67654898fefb4f15"},{"version":"684574d8a066134170406d9b528a8ceb24060f55dbd7eef97222b7d0da3ad3ce","signature":"a0aa31a1e9d69079d949353573fcfb7a8a693fa12147516f7ad01f2fb994f214"},{"version":"7c04e9fbe1f7b8ff8fdbb69bbc4d194f9e40ed9470242b596db2975028a466b0","signature":"161163711b4f25dce9e5b200557e79f56065e764a1d3e7c4f822b25730fe120c"},{"version":"328b5de7c7f37ba3ac5308fbd6d3fea13a1be09d6becf3be5258e314bccf87c5","signature":"9fc6062e2898c02d910f942cda08e4837a60527e9a0c7452c56d10b242f16060"},{"version":"b6a9e237fc00a7eee886d6649676386432d72c97b1c322f5b8fcfd5297a3330d","signature":"8cd08117020d664871368d21cf9698596b4828cb31c62afd038b869ddd522aaf"},{"version":"e3318d2cdfe20ae5dd617d7145acece38d1ba8e1de12c87f416ef2c757eb0095","signature":"c0e4aef52352496bd28626f940fe2bfc0deaf624989e9fdb7c731d45e298777f"},{"version":"7ec857654f1566dcda7c5df087d2472114f677436733969510ff646f0621c541","signature":"415faca9a81558d607c45d861ee34a9c3ae16d775f157fcd8895ee8b18039679"},{"version":"5bc1a95c6b875af11122d03132cffe7499faeaf2ff2cbd2991ce53319536e920","signature":"bca2c9ff0d823a5b0f27fe62e7818e5d1201269938b24298cf45ef5e0d1902d6"},{"version":"d7a224b8efd6addae97a1897c0ece99d46f3e2452b870b71deb981a1822d1baa","signature":"2015dd7b4b61aad0bf6824d76cf9a06317bd7f95a4cffc72368efd3d042c2cb1"},"2dcfcd67775fb2e15d748882fcf58559db0d00332825905bb50c4a7e79e9ed10",{"version":"198199921daa461f48659502464cdd4615a8ca4d550e1f41bd69b440b3ecf73b","signature":"5146a7452654d7c21d1429d56aedb8b4de5113c5b3fc779e59d2e5532978e62f"},{"version":"70521b6ab0dcba37539e5303104f29b721bfb2940b2776da4cc818c07e1fefc1","affectsGlobalScope":true,"impliedFormat":1},{"version":"ab41ef1f2cdafb8df48be20cd969d875602483859dc194e9c97c8a576892c052","affectsGlobalScope":true,"impliedFormat":1},{"version":"d153a11543fd884b596587ccd97aebbeed950b26933ee000f94009f1ab142848","affectsGlobalScope":true,"impliedFormat":1},{"version":"21d819c173c0cf7cc3ce57c3276e77fd9a8a01d35a06ad87158781515c9a438a","impliedFormat":1},{"version":"98cffbf06d6bab333473c70a893770dbe990783904002c4f1a960447b4b53dca","affectsGlobalScope":true,"impliedFormat":1},{"version":"ba481bca06f37d3f2c137ce343c7d5937029b2468f8e26111f3c9d9963d6568d","affectsGlobalScope":true,"impliedFormat":1},{"version":"6d9ef24f9a22a88e3e9b3b3d8c40ab1ddb0853f1bfbd5c843c37800138437b61","affectsGlobalScope":true,"impliedFormat":1},{"version":"1db0b7dca579049ca4193d034d835f6bfe73096c73663e5ef9a0b5779939f3d0","affectsGlobalScope":true,"impliedFormat":1},{"version":"9798340ffb0d067d69b1ae5b32faa17ab31b82466a3fc00d8f2f2df0c8554aaa","affectsGlobalScope":true,"impliedFormat":1},{"version":"f26b11d8d8e4b8028f1c7d618b22274c892e4b0ef5b3678a8ccbad85419aef43","affectsGlobalScope":true,"impliedFormat":1},{"version":"5929864ce17fba74232584d90cb721a89b7ad277220627cc97054ba15a98ea8f","impliedFormat":1},{"version":"763fe0f42b3d79b440a9b6e51e9ba3f3f91352469c1e4b3b67bfa4ff6352f3f4","impliedFormat":1},{"version":"25c8056edf4314820382a5fdb4bb7816999acdcb929c8f75e3f39473b87e85bc","impliedFormat":1},{"version":"c464d66b20788266e5353b48dc4aa6bc0dc4a707276df1e7152ab0c9ae21fad8","impliedFormat":1},{"version":"78d0d27c130d35c60b5e5566c9f1e5be77caf39804636bc1a40133919a949f21","impliedFormat":1},{"version":"c6fd2c5a395f2432786c9cb8deb870b9b0e8ff7e22c029954fabdd692bff6195","impliedFormat":1},{"version":"1d6e127068ea8e104a912e42fc0a110e2aa5a66a356a917a163e8cf9a65e4a75","impliedFormat":1},{"version":"5ded6427296cdf3b9542de4471d2aa8d3983671d4cac0f4bf9c637208d1ced43","impliedFormat":1},{"version":"7f182617db458e98fc18dfb272d40aa2fff3a353c44a89b2c0ccb3937709bfb5","impliedFormat":1},{"version":"cadc8aced301244057c4e7e73fbcae534b0f5b12a37b150d80e5a45aa4bebcbd","impliedFormat":1},{"version":"385aab901643aa54e1c36f5ef3107913b10d1b5bb8cbcd933d4263b80a0d7f20","impliedFormat":1},{"version":"9670d44354bab9d9982eca21945686b5c24a3f893db73c0dae0fd74217a4c219","impliedFormat":1},{"version":"0b8a9268adaf4da35e7fa830c8981cfa22adbbe5b3f6f5ab91f6658899e657a7","impliedFormat":1},{"version":"11396ed8a44c02ab9798b7dca436009f866e8dae3c9c25e8c1fbc396880bf1bb","impliedFormat":1},{"version":"ba7bc87d01492633cb5a0e5da8a4a42a1c86270e7b3d2dea5d156828a84e4882","impliedFormat":1},{"version":"4893a895ea92c85345017a04ed427cbd6a1710453338df26881a6019432febdd","impliedFormat":1},{"version":"c21dc52e277bcfc75fac0436ccb75c204f9e1b3fa5e12729670910639f27343e","impliedFormat":1},{"version":"13f6f39e12b1518c6650bbb220c8985999020fe0f21d818e28f512b7771d00f9","impliedFormat":1},{"version":"9b5369969f6e7175740bf51223112ff209f94ba43ecd3bb09eefff9fd675624a","impliedFormat":1},{"version":"4fe9e626e7164748e8769bbf74b538e09607f07ed17c2f20af8d680ee49fc1da","impliedFormat":1},{"version":"24515859bc0b836719105bb6cc3d68255042a9f02a6022b3187948b204946bd2","impliedFormat":1},{"version":"ea0148f897b45a76544ae179784c95af1bd6721b8610af9ffa467a518a086a43","impliedFormat":1},{"version":"24c6a117721e606c9984335f71711877293a9651e44f59f3d21c1ea0856f9cc9","impliedFormat":1},{"version":"dd3273ead9fbde62a72949c97dbec2247ea08e0c6952e701a483d74ef92d6a17","impliedFormat":1},{"version":"405822be75ad3e4d162e07439bac80c6bcc6dbae1929e179cf467ec0b9ee4e2e","impliedFormat":1},{"version":"0db18c6e78ea846316c012478888f33c11ffadab9efd1cc8bcc12daded7a60b6","impliedFormat":1},{"version":"e61be3f894b41b7baa1fbd6a66893f2579bfad01d208b4ff61daef21493ef0a8","impliedFormat":1},{"version":"bd0532fd6556073727d28da0edfd1736417a3f9f394877b6d5ef6ad88fba1d1a","impliedFormat":1},{"version":"89167d696a849fce5ca508032aabfe901c0868f833a8625d5a9c6e861ef935d2","impliedFormat":1},{"version":"615ba88d0128ed16bf83ef8ccbb6aff05c3ee2db1cc0f89ab50a4939bfc1943f","impliedFormat":1},{"version":"a4d551dbf8746780194d550c88f26cf937caf8d56f102969a110cfaed4b06656","impliedFormat":1},{"version":"8bd86b8e8f6a6aa6c49b71e14c4ffe1211a0e97c80f08d2c8cc98838006e4b88","impliedFormat":1},{"version":"317e63deeb21ac07f3992f5b50cdca8338f10acd4fbb7257ebf56735bf52ab00","impliedFormat":1},{"version":"4732aec92b20fb28c5fe9ad99521fb59974289ed1e45aecb282616202184064f","impliedFormat":1},{"version":"2e85db9e6fd73cfa3d7f28e0ab6b55417ea18931423bd47b409a96e4a169e8e6","impliedFormat":1},{"version":"c46e079fe54c76f95c67fb89081b3e399da2c7d109e7dca8e4b58d83e332e605","impliedFormat":1},{"version":"bf67d53d168abc1298888693338cb82854bdb2e69ef83f8a0092093c2d562107","impliedFormat":1},{"version":"b52476feb4a0cbcb25e5931b930fc73cb6643fb1a5060bf8a3dda0eeae5b4b68","affectsGlobalScope":true,"impliedFormat":1},{"version":"e2677634fe27e87348825bb041651e22d50a613e2fdf6a4a3ade971d71bac37e","impliedFormat":1},{"version":"7394959e5a741b185456e1ef5d64599c36c60a323207450991e7a42e08911419","impliedFormat":1},{"version":"8c0bcd6c6b67b4b503c11e91a1fb91522ed585900eab2ab1f61bba7d7caa9d6f","impliedFormat":1},{"version":"8cd19276b6590b3ebbeeb030ac271871b9ed0afc3074ac88a94ed2449174b776","affectsGlobalScope":true,"impliedFormat":1},{"version":"696eb8d28f5949b87d894b26dc97318ef944c794a9a4e4f62360cd1d1958014b","impliedFormat":1},{"version":"3f8fa3061bd7402970b399300880d55257953ee6d3cd408722cb9ac20126460c","impliedFormat":1},{"version":"35ec8b6760fd7138bbf5809b84551e31028fb2ba7b6dc91d95d098bf212ca8b4","affectsGlobalScope":true,"impliedFormat":1},{"version":"5524481e56c48ff486f42926778c0a3cce1cc85dc46683b92b1271865bcf015a","impliedFormat":1},{"version":"68bd56c92c2bd7d2339457eb84d63e7de3bd56a69b25f3576e1568d21a162398","affectsGlobalScope":true,"impliedFormat":1},{"version":"3e93b123f7c2944969d291b35fed2af79a6e9e27fdd5faa99748a51c07c02d28","impliedFormat":1},{"version":"9d19808c8c291a9010a6c788e8532a2da70f811adb431c97520803e0ec649991","impliedFormat":1},{"version":"87aad3dd9752067dc875cfaa466fc44246451c0c560b820796bdd528e29bef40","impliedFormat":1},{"version":"4aacb0dd020eeaef65426153686cc639a78ec2885dc72ad220be1d25f1a439df","impliedFormat":1},{"version":"f0bd7e6d931657b59605c44112eaf8b980ba7f957a5051ed21cb93d978cf2f45","impliedFormat":1},{"version":"8db0ae9cb14d9955b14c214f34dae1b9ef2baee2fe4ce794a4cd3ac2531e3255","affectsGlobalScope":true,"impliedFormat":1},{"version":"15fc6f7512c86810273af28f224251a5a879e4261b4d4c7e532abfbfc3983134","impliedFormat":1},{"version":"58adba1a8ab2d10b54dc1dced4e41f4e7c9772cbbac40939c0dc8ce2cdb1d442","impliedFormat":1},{"version":"641942a78f9063caa5d6b777c99304b7d1dc7328076038c6d94d8a0b81fc95c1","impliedFormat":1},{"version":"092c1137e31de289f3cc6a57fdccb3cca298d8a680d1e367d206d3318f1394a1","impliedFormat":1},{"version":"855cd5f7eb396f5f1ab1bc0f8580339bff77b68a770f84c6b254e319bbfd1ac7","impliedFormat":1},{"version":"5650cf3dace09e7c25d384e3e6b818b938f68f4e8de96f52d9c5a1b3db068e86","impliedFormat":1},{"version":"1354ca5c38bd3fd3836a68e0f7c9f91f172582ba30ab15bb8c075891b91502b7","affectsGlobalScope":true,"impliedFormat":1},{"version":"7e20d899c28ca26a2a7afc98beaa69e63ff7fba0a8bc47b4e3bf3ede5e09e424","impliedFormat":1},{"version":"2d2fcaab481b31a5882065c7951255703ddbe1c0e507af56ea42d79ac3911201","impliedFormat":1},{"version":"a192fe8ec33f75edbc8d8f3ed79f768dfae11ff5735e7fe52bfa69956e46d78d","impliedFormat":1},{"version":"ca867399f7db82df981d6915bcbb2d81131d7d1ef683bc782b59f71dda59bc85","affectsGlobalScope":true,"impliedFormat":1},{"version":"372413016d17d804e1d139418aca0c68e47a83fb6669490857f4b318de8cccb3","affectsGlobalScope":true,"impliedFormat":1},{"version":"9e043a1bc8fbf2a255bccf9bf27e0f1caf916c3b0518ea34aa72357c0afd42ec","impliedFormat":1},{"version":"b4f70ec656a11d570e1a9edce07d118cd58d9760239e2ece99306ee9dfe61d02","impliedFormat":1},{"version":"3bc2f1e2c95c04048212c569ed38e338873f6a8593930cf5a7ef24ffb38fc3b6","impliedFormat":1},{"version":"6e70e9570e98aae2b825b533aa6292b6abd542e8d9f6e9475e88e1d7ba17c866","impliedFormat":1},{"version":"f9d9d753d430ed050dc1bf2667a1bab711ccbb1c1507183d794cc195a5b085cc","impliedFormat":1},{"version":"9eece5e586312581ccd106d4853e861aaaa1a39f8e3ea672b8c3847eedd12f6e","impliedFormat":1},{"version":"085f552d005479e2e6a7311cdbbe5d8c55c497b4d19274285df161ee9684cd9c","impliedFormat":1},{"version":"37ba7b45141a45ce6e80e66f2a96c8a5ab1bcef0fc2d0f56bb58df96ec67e972","impliedFormat":1},{"version":"45650f47bfb376c8a8ed39d4bcda5902ab899a3150029684ee4c10676d9fbaee","impliedFormat":1},{"version":"007faacc9268357caa21d24169f3f3f2497af3e9241308df2d89f6e6d9bb3f2e","affectsGlobalScope":true,"impliedFormat":1},{"version":"74cf591a0f63db318651e0e04cb55f8791385f86e987a67fd4d2eaab8191f730","impliedFormat":1},{"version":"5eab9b3dc9b34f185417342436ec3f106898da5f4801992d8ff38ab3aff346b5","impliedFormat":1},{"version":"12ed4559eba17cd977aa0db658d25c4047067444b51acfdcbf38470630642b23","affectsGlobalScope":true,"impliedFormat":1},{"version":"f3ffabc95802521e1e4bcba4c88d8615176dc6e09111d920c7a213bdda6e1d65","impliedFormat":1},{"version":"809821b8a065e3234a55b3a9d7846231ed18d66dd749f2494c66288d890daf7f","impliedFormat":1},{"version":"ae56f65caf3be91108707bd8dfbccc2a57a91feb5daabf7165a06a945545ed26","impliedFormat":1},{"version":"a136d5de521da20f31631a0a96bf712370779d1c05b7015d7019a9b2a0446ca9","impliedFormat":1},{"version":"c3b41e74b9a84b88b1dca61ec39eee25c0dbc8e7d519ba11bb070918cfacf656","affectsGlobalScope":true,"impliedFormat":1},{"version":"4737a9dc24d0e68b734e6cfbcea0c15a2cfafeb493485e27905f7856988c6b29","affectsGlobalScope":true,"impliedFormat":1},{"version":"36d8d3e7506b631c9582c251a2c0b8a28855af3f76719b12b534c6edf952748d","impliedFormat":1},{"version":"1ca69210cc42729e7ca97d3a9ad48f2e9cb0042bada4075b588ae5387debd318","impliedFormat":1},{"version":"f5ebe66baaf7c552cfa59d75f2bfba679f329204847db3cec385acda245e574e","impliedFormat":1},{"version":"ed59add13139f84da271cafd32e2171876b0a0af2f798d0c663e8eeb867732cf","affectsGlobalScope":true,"impliedFormat":1},{"version":"b7c5e2ea4a9749097c347454805e933844ed207b6eefec6b7cfd418b5f5f7b28","impliedFormat":1},{"version":"b1810689b76fd473bd12cc9ee219f8e62f54a7d08019a235d07424afbf074d25","impliedFormat":1}],"root":[[46,60],[62,94]],"options":{"composite":true,"declaration":true,"declarationMap":true,"esModuleInterop":true,"module":1,"outDir":"./dist","rootDir":"./src","skipLibCheck":true,"sourceMap":true,"strict":true,"target":7},"referencedMap":[[143,1],[144,1],[145,2],[100,3],[146,4],[147,5],[148,6],[95,7],[98,8],[96,7],[97,7],[149,9],[150,10],[151,11],[152,12],[153,13],[154,14],[155,14],[156,15],[157,16],[158,17],[159,18],[101,7],[99,7],[160,19],[161,20],[162,21],[194,22],[163,23],[164,24],[165,25],[166,26],[167,27],[168,28],[169,29],[170,30],[171,31],[172,32],[173,32],[174,33],[175,7],[176,34],[178,35],[177,36],[179,37],[180,38],[181,39],[182,40],[183,41],[184,42],[185,43],[186,44],[187,45],[188,46],[189,47],[190,48],[191,49],[102,7],[103,7],[104,7],[142,50],[192,51],[193,52],[44,7],[45,7],[9,7],[8,7],[2,7],[10,7],[11,7],[12,7],[13,7],[14,7],[15,7],[16,7],[17,7],[3,7],[18,7],[19,7],[4,7],[20,7],[24,7],[21,7],[22,7],[23,7],[25,7],[26,7],[27,7],[5,7],[28,7],[29,7],[30,7],[31,7],[6,7],[35,7],[32,7],[33,7],[34,7],[36,7],[7,7],[37,7],[42,7],[43,7],[38,7],[39,7],[40,7],[41,7],[1,7],[61,7],[120,53],[130,54],[119,53],[140,55],[111,56],[110,57],[139,58],[133,59],[138,60],[113,61],[127,62],[112,63],[136,64],[108,65],[107,58],[137,66],[109,67],[114,68],[115,7],[118,68],[105,7],[141,69],[131,70],[122,71],[123,72],[125,73],[121,74],[124,75],[134,58],[116,76],[117,77],[126,78],[106,79],[129,70],[128,68],[132,7],[135,80],[90,81],[91,82],[86,83],[88,84],[87,85],[89,86],[63,87],[65,88],[47,89],[83,90],[46,91],[52,92],[58,93],[50,94],[64,95],[60,96],[59,97],[55,98],[54,92],[48,7],[62,99],[84,100],[80,101],[67,102],[68,94],[76,7],[66,7],[82,103],[53,104],[70,105],[79,106],[69,107],[78,108],[74,109],[81,110],[75,111],[73,111],[72,111],[77,112],[92,113],[71,7],[93,114],[85,115],[56,116],[57,117],[94,118],[49,7],[51,7]],"latestChangedDtsFile":"./dist/providers/index.d.ts","version":"5.9.3"}

================
File: vitest.config.ts
================
import { defineConfig } from 'vitest/config';





================================================================
End of Codebase
================================================================

================
File: packages/shell/repomix-output.xml
================
This file is a merged representation of the entire codebase, combined into a single document by Repomix.
The content has been processed where content has been compressed (code blocks are separated by ⋮---- delimiter).

================================================================
File Summary
================================================================

Purpose:
--------
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.

File Format:
------------
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  a. A separator line (================)
  b. The file path (File: path/to/file)
  c. Another separator line
  d. The full contents of the file
  e. A blank line

Usage Guidelines:
-----------------
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.

Notes:
------
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Content has been compressed - code blocks are separated by ⋮---- delimiter
- Files are sorted by Git change count (files with more changes are at the bottom)


================================================================
Directory Structure
================================================================
electron/
  dev-runner.ts
  main.ts
  preload.ts
src/
  App.tsx
  main.tsx
  vite-env.d.ts
index.html
package.json
tsconfig.electron.json
tsconfig.json
vite.config.ts

================================================================
Files
================================================================

================
File: electron/dev-runner.ts
================
/**
 * Dev runner — starts Electron pointing to Vite dev server.
 * Compiles the Electron TypeScript first, then launches Electron on the compiled JS.
 */
import { spawnSync, spawn } from 'child_process';
import { resolve } from 'path';
⋮----
function compileElectron(): void
⋮----
async function waitForVite(url: string, maxRetries = 30): Promise<void>
⋮----
// Server not ready yet
⋮----
async function main(): Promise<void>
⋮----
// eslint-disable-next-line @typescript-eslint/no-var-requires

================
File: electron/main.ts
================
import { app, BrowserWindow, ipcMain } from 'electron';
import path from 'node:path';
import { AppContext, ReActLoop, StatefulCompressor } from '@soberano/core';
import type { ReActMessage } from '@soberano/core';
⋮----
// ── Persistência de Memória (Histórico Multi-turn) ──
// Mantém o histórico entre chamadas IPC, permitindo continuidade
// na conversa com o mesmo agente.
⋮----
function createWindow(): void
⋮----
/**
 * Gera o system prompt dinamicamente a partir do ToolRegistry.
 *
 * Principio DRY: as definições das ferramentas são centralizadas em
 * AppContext.setupDefaultTools() e expostas via toolRegistry.getDefinitions().
 * Este builder converte a saída JSON do registry em instruções legíveis
 * pelo modelo, eliminando a duplicação manual que causava a omissão de
 * ferramentas como readDir.
 *
 * @param toolRegistryJson JSON string com as definições das ferramentas
 * @returns System prompt completo com instruções + ferramentas disponíveis
 */
function buildSystemPrompt(toolRegistryJson: string): string
⋮----
// Parse as definições JSON do ToolRegistry e gera lista legível
⋮----
// Fallback: se o JSON estiver malformado, usa a lista crua
⋮----
// ── Inicializa o AppContext (DIP Container) ──
// O Core não sabe que o Electron existe. A injeção é feita via bridge,
// mantendo o desacoplamento SOLID.
⋮----
// ── Constrói o system prompt dinâmico a partir do ToolRegistry ──
// GARANTE que toda ferramenta registrada (incluindo readDir) seja
// listada para o modelo, sem duplicação manual.
⋮----
// ── IPC Handler: Ponte de Soberania ──
// Renderer → IPC Bridge → Core (ReActLoop) → Resposta → Renderer
⋮----
// ── Health-Check: Verifica se o motor Ollama está respondendo ──
⋮----
// ── Injeção do ReActLoop ──
// StatefulCompressor: compressão inteligente do histórico via LLM
⋮----
// ReActLoop: ciclo Reasoning + Acting com suporte a ferramentas
⋮----
undefined,            // executor não necessário em JSON mode
⋮----
undefined,            // reflector opcional
compressor,           // compressor IContextCompressor
⋮----
// ── Execução do Ciclo ReAct ──
// Adiciona o prompt do utilizador ao histórico persistente
⋮----
// Atualiza o histórico com as mensagens geradas pelo loop
// Adiciona a resposta final como mensagem do assistente
⋮----
// Evita crescimento infinito do histórico: mantém no máximo 50 mensagens

================
File: electron/preload.ts
================
import { contextBridge, ipcRenderer } from 'electron';

================
File: src/App.tsx
================
import React, { useState } from 'react';
⋮----
/**
 * App component — Main entry point for the Electron Shell.
 *
 * This component implements the IPC Bridge (Ponte de Soberania):
 * - Sends prompts to the main process via window.api.askSoberano
 * - Displays the response from the Node.js backend
 */
⋮----
const handleSubmit = async (e: React.FormEvent) =>
⋮----
onChange=

================
File: src/main.tsx
================
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

================
File: src/vite-env.d.ts
================
/// <reference types="vite/client" />
⋮----
export interface SoberanoAPI {
  askSoberano: (prompt: string) => Promise<string>;
}
⋮----
interface Window {
    api: SoberanoAPI;
  }

================
File: index.html
================
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; connect-src 'self'">
    <title>Soberano-Core</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>

================
File: package.json
================
{
  "name": "@soberano/shell",
  "version": "1.0.0",
  "description": "Soberano-Core Electron Shell — Desktop interface for the AI agent",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "concurrently -k \"vite\" \"tsx electron/dev-runner.ts\"",
    "build": "tsc -p tsconfig.electron.json && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@soberano/core": "*",
    "@types/node": "^20.19.40",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "concurrently": "^8.2.0",
    "electron": "^28.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.0"
  }
}

================
File: tsconfig.electron.json
================
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "lib": ["ES2020"],
    "outDir": "dist-electron",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "sourceMap": true
  },
  "include": ["electron"],
  "exclude": ["node_modules", "dist-electron"]
}

================
File: tsconfig.json
================
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "outDir": "dist",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "noEmit": true,
    "declaration": false
  },
  "include": ["src"],
  "references": [
    { "path": "../core" }
  ]
}

================
File: vite.config.ts
================
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'node:path';





================================================================
End of Codebase
================================================================

================
File: packages/core/src/cli/strategies/ChatStrategy.ts
================
/**
 * ChatStrategy — Interface Strategy para modos de chat no CLI.
 *
 * SRP: Cada estratégia encapsula um pipeline de chat diferente:
 *   - StreamStrategy: streaming direto (sem ReAct)
 *   - ReActStrategy: ReAct loop (com ou sem streaming pós-resposta)
 *
 * OCP: Novos modos de chat são adicionados criando novas classes
 * que implementam ChatStrategy, sem modificar as existentes.
 */
⋮----
import { AppContext, ReActLoop, RAGManager } from '../../core';
import type { IProvider } from '../../providers/types';
⋮----
/** Contexto compartilhado entre todas as estratégias de chat */
export interface ChatContext {
  app: AppContext;
  prompt: string;
  streamMode: boolean;
  noThink: boolean;
  /** Se true, aplica Reflector (self-correction) pós-resposta */
  reflectMode: boolean;
}
⋮----
/** Se true, aplica Reflector (self-correction) pós-resposta */
⋮----
/** Interface base para todas as estratégias de chat */
export interface ChatStrategy {
  /** Executa o pipeline de chat e retorna a resposta final formatada */
  execute(ctx: ChatContext): Promise<string>;
}
⋮----
/** Executa o pipeline de chat e retorna a resposta final formatada */
execute(ctx: ChatContext): Promise<string>;
⋮----
/**
 * Constrói o system prompt completo (tool definitions + RAG + JSON rules).
 *
 * Extraída para função pura (SRP) — testável isoladamente.
 *
 * @param app     AppContext (provider, toolRegistry, commandExecutor, etc.)
 * @param prompt  Prompt do usuário
 * @param jsonMode Se true, adiciona regras de resposta em formato JSON
 * @param ragDir  Diretório RAG opcional
 * @param skipPromptSuffix Se true, não adiciona o sufixo "Pergunta do usuário" —
 *                         útil quando o prompt já está no histórico da sessão
 * @returns System prompt completo
 */
export async function buildSystemPrompt(
  app: AppContext,
  prompt: string,
  jsonMode: boolean,
  ragDir?: string,
  skipPromptSuffix?: boolean
): Promise<string>
⋮----
// ── RAG (Retrieval-Augmented Generation) ──
⋮----
// ── System Prompt ──
⋮----
// ── WORKFLOW DE ENGENHARIA OBRIGATÓRIO (sempre presente) ──
⋮----
// ── Bloco condicional de workflow de edição ──
⋮----
// Injeta contexto RAG no system prompt, se houver
⋮----
// Se --json estiver ativo, usa formato tool_call / final_response

================
File: packages/core/src/cli/strategies/index.ts
================


================
File: packages/core/src/cli/strategies/ReActStrategy.ts
================
/**
 * ReActStrategy — Estratégia de chat com ReAct Loop (Reasoning + Acting).
 *
 * Fluxo:
 *   1. buildSystemPrompt() → system prompt completo (com RAG se --rag)
 *   2. Delega para ReActLoop.execute() — loop de raciocínio com ferramentas
 *   3. Se --stream + streamChat disponível: faz streaming da resposta final
 *   4. Se --no-think: suprime o indicador "🤔 Pensando..."
 *
 * SRP: Responsabilidade única — gerenciar o pipeline ReAct.
 * OCP: Não precisa ser modificada para novos modos de chat.
 */
⋮----
import type { ChatStrategy, ChatContext } from './ChatStrategy';
import { buildSystemPrompt } from './ChatStrategy';
import type { ICritiqueProvider } from '../../providers/types';
⋮----
/** Type guard: verifica se provider implementa ICritiqueProvider em runtime */
function isCritiqueProvider(p: unknown): p is ICritiqueProvider
import { ReActLoop, CommandExecutor, Reflector, ErrorJournal, StatefulCompressor } from '../../core';
⋮----
export class ReActStrategy implements ChatStrategy
⋮----
async execute(ctx: ChatContext): Promise<string>
⋮----
// Obtém histórico da sessão ativa (vazio se não houver sessão)
⋮----
// Constrói o system prompt completo (RAG + rules)
// skipPromptSuffix=true quando há histórico, pois o prompt já está lá
⋮----
history.length > 0 // skipPromptSuffix
⋮----
// Cria o Reflector (self-correction) se --reflect estiver ativo
// Type guard verifica se provider implementa ICritiqueProvider em runtime
⋮----
// ErrorJournal: persistência de erros de reflexão (TASK 6)
// Usado internamente para registrar correções; exposto com --verbose
⋮----
// Indicador visual de reflexão (vai para stderr para não poluir stdout)
⋮----
// Cria o ReActLoop com dependências injetadas
// - jsonMode = true: usa toolRegistry (JSON tool calls)
// - jsonMode = false: usa commandExecutor (texto ACTION/FINAL_ANSWER)
// StatefulCompressor: compressão de contexto para evitar estouro de tokens
⋮----
reflector, // injeta o Reflector (undefined se --reflect não ativo)
compressor // injeta o compressor de contexto
⋮----
// Indicador visual de "pensamento" durante o ReAct
⋮----
// Prepara opções de streaming (efeito "máquina de escrever")
⋮----
// Executa o loop ReAct com histórico da sessão multi-turn
⋮----
history, // histórico real da sessão (multi-turn)
⋮----
// ── REGISTRO NO ERRORJOURNAL ──
// Persiste erros de reflexão para auditoria (TASK 6)
⋮----
// ── INDICADOR PÓS-REACT ──
// Mostra iterações e status de correção no stderr (não polui stdout)
⋮----
// Flag --verbose: estatísticas detalhadas do ErrorJournal (TASK 7)
⋮----
// Propaga CorrectionStatus (TASK 4)
⋮----
// ── RESPOSTA FINAL ──
// Se --stream está ativo, o streaming já foi feito inline pelo ReActLoop
// via callback onToken. A resposta final é retornada normalmente.
// Inclui badge de correção se aplicável
⋮----
// Streaming já foi feito pelo ReActLoop durante a execução.
// Só precisamos do \n final e do badge de correção no stderr
⋮----
return ''; // Já escrevemos os tokens no stdout via onToken
⋮----
// Modo normal (sem streaming): retorna a resposta formatada

================
File: packages/core/src/cli/strategies/StreamStrategy.ts
================
/**
 * StreamStrategy — Estratégia de chat com streaming direto (efeito máquina de escrever).
 *
 * Fluxo:
 *   1. buildSystemPrompt() → system prompt completo (com RAG se --rag)
 *   2. Itera sobre provider.streamChat() escrevendo tokens no stdout
 *   3. Se streamChat não estiver disponível ou falhar, faz fallback para chat()
 *
 * SRP: Responsabilidade única — gerenciar o pipeline de streaming puro.
 * OCP: Não precisa ser modificada para novos modos de chat.
 */
⋮----
import type { ChatStrategy, ChatContext } from './ChatStrategy';
import { buildSystemPrompt } from './ChatStrategy';
⋮----
export class StreamStrategy implements ChatStrategy
⋮----
async execute(ctx: ChatContext): Promise<string>
⋮----
// Validação: streaming direto não é compatível com --json
⋮----
// Obtém o histórico multi-turn da sessão ativa
⋮----
// Constrói o system prompt completo (RAG + tools)
// skipPromptSuffix=true quando há histórico, pois o prompt já está lá
⋮----
// Injeta histórico multi-turn no prompt, se houver
// Formato: [USER]/[ASSISTANT] blocks para o modelo entender o contexto
⋮----
// Se não houver histórico, usa systemPrompt direto (já tem o sufixo "Pergunta do usuário")
// Se houver histórico, o buildSystemPrompt pulou o sufixo, mas a mensagem do usuário
// já foi adicionada ao histórico pelo commands.ts ANTES de chamar execute().
// Portanto, o historyBlock já contém a pergunta — adicioná-la novamente duplicaria.
⋮----
// Indica o modelo no início da resposta
⋮----
// Se streamMode está desativado, ou streamChat não está disponível,
// faz uma chamada normal e escreve de uma vez
⋮----
// Streaming real (efeito máquina de escrever)
⋮----
// Fallback para chat normal se streaming falhar

================
File: packages/core/src/cli/commands.ts
================
/**
 * commands.ts — Roteador de comandos CLI com Injeção de Dependência (DIP).
 *
 * Fluxo:
 *   parseArgs (index.ts) → CliArgs → buildConfig() → AppContext
 *   ↓
 *   runCommand() usa AppContext para acessar:
 *     - fileReader, commandExecutor, provider, toolRegistry
 *
 * DIP: Nenhum comando instancia diretamente FileReader, OllamaProvider, etc.
 *       Tudo é injetado via AppContext → ProviderFactory → interfaces.
 */
⋮----
import { AppContext } from '../core';
import { StreamStrategy, ReActStrategy } from './strategies';
⋮----
/**
 * Estrutura que representa os argumentos parseados da linha de comando.
 *
 * Exemplo: `soberano chat "Explique SOLID" --model phi3:3b`
 *   command = "chat"
 *   args    = ["Explique SOLID"]
 *   flags   = { model: "phi3:3b" }
 *
 * Flags booleanas:
 *   --json     → ativa tool_call/final_response em JSON
 *   --stream   → ativa streaming SSE (efeito máquina de escrever)
 *   --no-think → desativa o indicador "pensando..." (útil para scripting)
 *   --reflect  → ativa Reflector (auto-revisão) na resposta final
 */
export interface CliArgs {
  command: string;
  args: string[];
  flags: Record<string, string | boolean>;
}
⋮----
/**
 * Constrói a configuração do AppContext a partir dos argumentos CLI parseados.
 *
 * DIP (Dependency Inversion Principle):
 * - AppContext gerencia a criação e injeção de TODAS as dependências.
 * - O comando não instancia diretamente providers, readers ou executors.
 * - Basta mudar a config para trocar de provider ou config.
 *
 * Flags de sessão:
 *   --session <id>    → carrega uma sessão existente pelo UUID
 *   --new-session     → força a criação de uma nova sessão (ignora --session)
 */
function buildConfig(parsed: CliArgs): import('../core').AppContextConfig
⋮----
/**
 * Cria um novo AppContext a partir dos argumentos parseados.
 *
 * Cada chamada de comando recebe um AppContext limpo (sem cache de execuções
 * anteriores). Isso garante que flags como --json, --stream, --model sejam
 * respeitadas independentemente da ordem de execução.
 *
 * @param parsed Argumentos parseados da linha de comando
 * @returns AppContext configurado e inicializado
 */
async function createContext(parsed: CliArgs): Promise<AppContext>
⋮----
/**
 * Função central de roteamento da CLI.
 *
 * Fluxo geral de dados:
 * ┌──────────┐    CliArgs     ┌──────────────────┐    comando     ┌─────────────────┐
 * │  index.ts│ ── parse ──▶  │  runCommand()     │ ── switch ──▶ │  FileReader /   │
 * │ (argv)   │              │                    │              │  CmdExecutor /  │
 * └──────────┘              └──────────────────┘              │  OllamaProvider │
 *                                    │                        └─────────────────┘
 *                                    │ string (output)
 *                                    ▼
 *                              ┌──────────┐
 *                              │  console │
 *                              └──────────┘
 *
 * @param parsed Argumentos parseados da linha de comando
 * @returns String formatada para exibição no terminal
 */
export async function runCommand(parsed: CliArgs): Promise<string>
⋮----
/**
     * Comando: read <file>
     * Exemplo: soberano read package.json
     */
⋮----
/**
     * Comando: dir <path>
     * Exemplo: soberano dir src
     */
⋮----
/**
     * Comando: search <dir> <pattern>
     * Exemplo: soberano search src "export"
     */
⋮----
/**
     * Comando: exec <cmd>
     * Exemplo: soberano exec "ls -la"
     *
     * ⚠️ Segurança: o comando é dividido em executável + argumentos,
     * e o CommandExecutor usa spawn com shell:false, prevenindo injeção.
     */
⋮----
/**
     * Comando: chat <prompt>
     *
     * Pipeline com suporte a dois modos:
     *   - StreamStrategy: streaming direto (efeito máquina de escrever, sem ReAct)
     *   - ReActStrategy: ReAct Loop (com ou sem streaming pós-resposta)
     *
     * OCP (Open-Closed Principle): Novos modos de chat são adicionados criando
     * novas classes que implementam ChatStrategy, sem modificar este case.
     *
     * Flags:
     *   --stream   → ativa streaming SSE (efeito máquina de escrever)
     *   --json     → ativa tool_call/final_response em JSON (força ReAct)
     *   --no-think → desativa o indicador "pensando..." (útil para scripting)
     *
     * Exemplos:
     *   soberano chat "Explique SOLID" --model phi3:3b --stream
     *   soberano chat "Qual o conteúdo do package.json?" --json
     *   soberano chat "Liste os arquivos" --stream --no-think
     */
⋮----
// Adiciona a mensagem do usuário ao histórico da sessão
⋮----
// Estratégia: decide qual pipeline executar
// - streaming direto: quando --stream está ativo, --json NÃO está, e provider suporta
// - ReAct: quando --json está ativo OU streaming não é suportado
⋮----
// Adiciona a resposta do assistente ao histórico e persiste
// Apenas salva se houver conteúdo real (evita sessões fantasmas)
⋮----
/**
     * Comando: sessions
     * Exemplo: soberano sessions
     *
     * Lista todas as sessões de conversa salvas, ordenadas por
     * última atualização (mais recentes primeiro).
     */
⋮----
/** Fallback: comando não reconhecido */

================
File: packages/core/src/cli/index.ts
================
import { runCommand, CliArgs } from './commands';
⋮----
/**
 * Parser simples de argumentos de linha de comando.
 *
 * Fluxo de parsing:
 * ┌────────────┐                         ┌─────────────────┐
 * │ process    │  Ex: ['ts-node',        │  parseArgs()    │
 * │ .argv      │       'chat',           │                 │
 * │            │       'Explique SOLID', │  ┌─ positional ─▶ ['chat', 'Explique SOLID']
 * │            │       '--model',        │  │               
 * │            │       'phi3:3b']        │  └─ flags ──────▶ { model: 'phi3:3b' }
 * └────────────┘                         └──────┬──────────┘
 *                                               │
 *                                     ┌─────────▼──────────┐
 *                                     │ CliArgs {           │
 *                                     │   command: 'chat',  │
 *                                     │   args: ['Explique  │
 *                                     │          SOLID'],   │
 *                                     │   flags: {          │
 *                                     │     model:'phi3:3b' │
 *                                     │   }                 │
 *                                     │ }                   │
 *                                     └─────────────────────┘
 *
 * Convenção:
 *   - Tudo que começa com -- é flag
 *   - --flag valor → flags.flag = valor (consome o próximo token)
 *   - --flag (sem valor seguinte) → flags.flag = true (booleano)
 *   - Todo o resto é argumento posicional
 *   - O primeiro argumento posicional é o nome do comando
 *
 * @param raw Array cru de strings (process.argv)
 * @returns Objeto estruturado com comando, argumentos e flags
 */
function parseArgs(raw: string[]): CliArgs
⋮----
// raw = ['ts-node', 'src/cli/index.ts', 'read', 'file.txt', '--model', 'tinyllama:1b']
⋮----
// Descarta os dois primeiros (interpretador + script)
// Ex: ['ts-node', 'src/cli/index.ts', ...] → sliced = ['read', 'file.txt', ...]
⋮----
// Remove o prefixo '--' para obter o nome da flag
const key = token.slice(2); // '--model' → 'model'
⋮----
// Se o próximo token existe e não é outra flag, é o valor desta flag
⋮----
i++; // Consome o valor (avança um token extra)
⋮----
// Flag booleana (ex: --verbose)
⋮----
// Argumento posicional (não começa com --)
⋮----
// Primeiro posicional = comando; se ausente, default é 'help'
⋮----
// Demais posicionais = argumentos do comando
⋮----
/**
 * Entrypoint principal da CLI Soberano-Core.
 *
 * Fluxo completo da aplicação (do terminal ao output):
 *
 * Terminal
 *    │
 *    │ $ npm run dev -- chat "Explique SOLID" --model phi3:3b
 *    ▼
 * process.argv
 *    │
 *    ▼
 * parseArgs() ──▶ CliArgs { command, args, flags }
 *    │
 *    ├── command === 'help' ? ──▶ Exibe tela de ajuda e encerra
 *    │
 *    ▼
 * runCommand(parsed) ──▶ Promise<string>
 *    │
 *    ├── Sucesso ──▶ console.log(output)
 *    │
 *    └── Erro ──────▶ console.error() + process.exit(1)
 *
 * Tratamento de erros:
 *   - Erros de uso (args faltando) → mensagem + exit code 1
 *   - Erros de sistema (arquivo não encontrado) → mensagem + exit code 1
 *   - Erros de rede (Ollama offline) → mensagem + exit code 1
 */
async function main(): Promise<void>
⋮----
// Comando help: exibe documentação e encerra sem erro
⋮----
// Executa o comando e exibe o resultado
⋮----
// Qualquer erro é exibido no stderr e o processo termina com código 1
⋮----
// Executa a função principal

================
File: packages/core/src/core/rag/graph/ASTRelationshipExtractor.ts
================
/**
 * ASTRelationshipExtractor: Extrai relações semânticas do código fonte via AST.
 *
 * Responsabilidade Única (SRP):
 * - Consumir IASTParser (já existente) para obter ASTNode[]
 * - Detectar CALLS, IMPORTS, EXTENDS, IMPLEMENTS, CONTAINS, BELONGS_TO
 * - Gerar GraphNode para cada símbolo
 * - Gerar GraphEdge para cada relação descoberta
 *
 * DIP: Depende de IASTParser (abstração), não de TypescriptASTAdapter (concreto).
 *
 * Validações:
 * - Ignora chamadas a built-ins (console, process, require, import)
 * - Ignora auto-referências
 * - Limite de 200 arestas por arquivo
 */
⋮----
import type { IASTParser } from '../IASTParser';
import type { IRelationshipExtractor, ExtractionResult } from './IRelationshipExtractor';
import type { GraphNode, GraphEdge } from './types';
import { hashId } from './JsonGraphStore';
import type { IChunker } from '../IChunker';
⋮----
// Built-ins do Node.js/JS que não devem gerar arestas
⋮----
// Extensões suportadas para extração
⋮----
export class ASTRelationshipExtractor implements IRelationshipExtractor
⋮----
constructor(astParser: IASTParser, chunkIdMapper?: IChunker)
⋮----
extract(filePath: string, source: string): ExtractionResult
⋮----
// Valida se o arquivo é suportado
⋮----
// Se código vazio, retorna vazio
⋮----
// Se não conseguiu parsear, retorna vazio
⋮----
// 1. Nó do arquivo
⋮----
// 2. Nós dos símbolos (funções, classes, métodos, interfaces)
⋮----
// Pré-calcula chunkIds se chunkIdMapper estiver disponível
⋮----
// Determina chunkId: se temos o mapeamento, usa a startLine do símbolo
⋮----
// Procura o chunk que contém a startLine do símbolo
⋮----
// Fallback: usa o índice do nó como parte do chunkId (formato: path#indice)
⋮----
// Aresta BELONGS_TO: símbolo → arquivo
⋮----
// Aresta CONTAINS: arquivo → símbolo (inverso)
⋮----
// 3. Extrair relações a partir do texto de cada nó
⋮----
// CALLS: detectar chamadas de função
⋮----
// Ignorar built-ins
⋮----
// Ignorar auto-chamadas
⋮----
// Verificar se a chamada é para um símbolo conhecido no mesmo arquivo
⋮----
// Não criar aresta para si mesmo
⋮----
// Método de objeto (ex: this.save()) ou lib nativa — ignorar
⋮----
// IMPORTS: extrair importações
⋮----
// Ignorar módulos core do Node
⋮----
// Criar nó para o módulo (se não existir)
⋮----
// EXTENDS: herança de classe
⋮----
// IMPLEMENTS: implementação de interface
⋮----
// REFERENCES: referências a símbolos sem chamada explícita
⋮----
// Pré-compila todas as regexes de nome para evitar new RegExp() no loop duplo
⋮----
// Pular auto-referência
⋮----
// Verificar se o nome aparece no texto do nó (excluindo chamadas já detectadas)
⋮----
// Verificar se não é uma chamada (já coberta por CALLS)
⋮----
// Verificar se já existe aresta CALLS entre os mesmos nós
⋮----
/**
   * Verifica se a extensão do arquivo é suportada para extração.
   */
private isSupportedFile(filePath: string): boolean
⋮----
/**
   * Mapeia kind do AST para tipo do GraphNode.
   */
private mapKindToType(kind: string): GraphNode['type']
⋮----
/**
   * Constrói um mapa de linha inicial → chunkId usando o chunkIdMapper (IChunker).
   * Útil para que os GraphNode tenham chunkId preenchido, populando nodesByChunkId.
   */
private buildChunkIdMap(source: string, filePath: string): Map<number, string>
⋮----
// Mapeia cada linha do chunk para o chunkId
⋮----
/**
   * Encontra o chunkId que contém uma determinada linha.
   * Busca exata primeiro, depois a linha mais próxima anterior.
   */
private findChunkIdForLine(chunkIdByLine: Map<number, string>, line: number): string | undefined
⋮----
// Fallback: encontra a linha anterior mais próxima que tem chunkId

================
File: packages/core/src/core/rag/graph/GraphBuilder.ts
================
/**
 * GraphBuilder: Orquestrador de construção do KnowledgeGraph.
 *
 * Responsabilidade Única (SRP):
 * - Percorrer diretório de código fonte
 * - Para cada arquivo .ts/.js, delegar extração ao IRelationshipExtractor
 * - Acumular todos os nós e arestas
 * - Persistir o grafo via IGraphStore
 *
 * DIP: Depende de IRelationshipExtractor e IGraphStore (abstrações).
 */
⋮----
import { FileReader } from '../../FileReader';
import type { IRelationshipExtractor } from './IRelationshipExtractor';
import type { IGraphStore } from './IGraphStore';
import type { KnowledgeGraph, GraphNode, GraphEdge } from './types';
⋮----
// Extensões de código-fonte relevantes para o grafo
⋮----
export interface GraphBuildOptions {
  /** Diretório raiz para indexar */
  rootDir: string;
  /** Máximo de arquivos a processar (default 200) */
  maxFiles?: number;
  /** Versão do grafo (default 1) */
  version?: number;
}
⋮----
/** Diretório raiz para indexar */
⋮----
/** Máximo de arquivos a processar (default 200) */
⋮----
/** Versão do grafo (default 1) */
⋮----
export interface GraphBuildResult {
  /** Total de nós no grafo final */
  totalNodes: number;
  /** Total de arestas no grafo final */
  totalEdges: number;
  /** Número de arquivos processados */
  filesProcessed: number;
  /** Se o grafo foi recriado do zero ou atualizado incrementalmente */
  rebuilt: boolean;
}
⋮----
/** Total de nós no grafo final */
⋮----
/** Total de arestas no grafo final */
⋮----
/** Número de arquivos processados */
⋮----
/** Se o grafo foi recriado do zero ou atualizado incrementalmente */
⋮----
export class GraphBuilder
⋮----
constructor(
    fileReader: FileReader,
    relationshipExtractor: IRelationshipExtractor,
    graphStore: IGraphStore
)
⋮----
/**
   * Constrói ou atualiza o KnowledgeGraph para o diretório informado.
   *
   * Estratégia incremental:
   * - Carrega grafo existente (se houver)
   * - Processa apenas arquivos modificados (por mtime)
   * - Remove nós/arestas de arquivos removidos
   * - Mescla com grafo existente
   */
async build(options: GraphBuildOptions): Promise<GraphBuildResult>
⋮----
// 1. Carrega grafo existente (se houver)
⋮----
// 2. Coleta arquivos fonte
⋮----
// 3. Determina arquivos modificados e removidos
⋮----
continue; // não modificado
⋮----
// Arquivos que existiam antes mas não existem mais → remover
⋮----
// Se não há nada para processar, retorna estado atual
⋮----
// 4. Processa arquivos modificados/novos
⋮----
// Helper para chave única de aresta (dedup)
const edgeKey = (e: GraphEdge): string => `$
⋮----
// Pré-carrega nós/arestas existentes (exceto de arquivos removidos)
⋮----
// Verificar se a aresta envolve algum nó de arquivo removido
⋮----
// Preserva mtimes de arquivos não modificados
⋮----
// Processa arquivos modificados — paralelizado
⋮----
// Remove nós/arestas antigos deste arquivo (se estava no grafo anterior)
⋮----
// Remove arestas conectadas a nós antigos
⋮----
// Adiciona novos nós/arestas
⋮----
// 5. Monta grafo final
⋮----
// 6. Persiste
⋮----
/**
   * Coleta arquivos fonte recursivamente.
   */
private async collectSourceFiles(
    rootDir: string,
    maxFiles: number
): Promise<SourceFile[]>
⋮----
const walk = async (dir: string): Promise<void> =>
⋮----
// Paraleliza fs.stat em todas as entradas do diretório
⋮----
interface SourceFile {
  fullPath: string;
  relPath: string;
  mtime: number;
}

================
File: packages/core/src/core/rag/graph/GraphRAGManager.ts
================
/**
 * GraphRAGManager: Busca híbrida que combina similaridade vetorial (RAG clássico)
 * com navegação em grafo de conhecimento (GraphRAG).
 *
 * Responsabilidade Única (SRP):
 * - Receber uma consulta de busca
 * - Executar retrieve vetorial (via Retriever)
 * - Expandir os top-N resultados com vizinhança no grafo (via IGraphQuery)
 * - Mesclar e ranquear resultados combinados
 * - Formatar resultado para prompt
 *
 * DIP: Depende de Retriever e IGraphQuery (abstrações).
 * OCP: Novas estratégias de ranqueamento podem ser adicionadas sem modificar esta classe.
 */
⋮----
import { Retriever, type SearchMatch } from '../Retriever';
import type { IGraphQuery, NeighborNode, ExpandOptions } from './IGraphQuery';
import type { ChunkEntry } from '../VectorStore';
⋮----
/**
 * Opções de busca híbrida.
 */
export interface GraphSearchOptions {
  /** Número de resultados vetoriais iniciais (default 5) */
  topK?: number;
  /** Profundidade de expansão no grafo (default 2) */
  graphDepth?: number;
  /** Tipos de aresta para filtrar expansão (default: todos) */
  edgeTypes?: ExpandOptions['edgeTypes'];
  /** Peso da relevância textual vs estrutural (0..1, default 0.7) */
  textWeight?: number;
}
⋮----
/** Número de resultados vetoriais iniciais (default 5) */
⋮----
/** Profundidade de expansão no grafo (default 2) */
⋮----
/** Tipos de aresta para filtrar expansão (default: todos) */
⋮----
/** Peso da relevância textual vs estrutural (0..1, default 0.7) */
⋮----
/**
 * Resultado da busca híbrida.
 */
export interface GraphSearchResult {
  /** Resultados combinados e ranqueados */
  matches: SearchMatch[];
  /** Nós do grafo visitados durante a expansão */
  visitedNodes: NeighborNode[];
  /** Total de chunks antes da expansão */
  baseChunks: number;
  /** Total de chunks após expansão */
  expandedChunks: number;
}
⋮----
/** Resultados combinados e ranqueados */
⋮----
/** Nós do grafo visitados durante a expansão */
⋮----
/** Total de chunks antes da expansão */
⋮----
/** Total de chunks após expansão */
⋮----
export class GraphRAGManager
⋮----
constructor(retriever: Retriever, graphQuery: IGraphQuery)
⋮----
/**
   * Busca híbrida: similaridade vetorial + expansão em grafo.
   *
   * Algoritmo:
   * 1. Retrieve vetorial (top-K chunks por similaridade de cosseno)
   * 2. Para cada chunk nos top resultados, encontra nó do grafo via chunk.id
   * 3. Expande vizinhança de cada nó (profundidade configurável)
   * 4. Coleta filePaths dos nós vizinhos
   * 5. Adiciona chunks dos arquivos vizinhos ao resultado (se não duplicados)
   * 6. Re-ranqueia por score combinado (textual * textWeight + estrutural * (1-textWeight))
   * 7. Retorna top-K final
   */
async search(
    queryEmbedding: number[],
    chunks: ChunkEntry[],
    options: GraphSearchOptions = {}
): Promise<GraphSearchResult>
⋮----
// 1. Retrieve vetorial
⋮----
// 2. Para cada match, encontra nó no grafo e expande
⋮----
// Expande vizinhança
⋮----
// 3. Adiciona chunks dos arquivos vizinhos (não duplicados)
⋮----
let structuralScore = 0.3; // score base para arquivo vizinho
⋮----
// Verifica se o nó está nos vizinhos expandidos
⋮----
// Score inversamente proporcional à distância
⋮----
// 4. Mescla e re-ranqueia
⋮----
// Matches vetoriais mantêm seu score original * textWeight
⋮----
// Matches estruturais recebem (1 - textWeight) * structuralScore
⋮----
// Ordena por score descendente
⋮----
/**
   * Formata resultado híbrido para inclusão no prompt.
   * Inclui contexto textual e informações estruturais do grafo.
   */
formatGraphContext(result: GraphSearchResult): string
⋮----
// Resultados textuais
⋮----
// Informações estruturais

================
File: packages/core/src/core/rag/graph/IGraphQuery.ts
================
/**
 * IGraphQuery: Interface para consultas no KnowledgeGraph (DIP, CQRS).
 *
 * Responsabilidade Única (SRP): Definir o contrato de consulta (read-only)
 * do grafo de conhecimento. Separada de IGraphStore (Command) por ISP.
 *
 * A implementação concreta (JsonGraphStore) implementa ambas as interfaces
 * mas o consumidor (GraphRAGManager) depende apenas desta interface
 * para não ter acesso acidental a operações de escrita.
 */
⋮----
import type { GraphNode, RelationshipType } from './types';
⋮----
/**
 * Nó vizinho com metadados da relação.
 */
export interface NeighborNode extends GraphNode {
  /** Tipo da aresta que conecta ao nó de origem */
  edgeType: RelationshipType;
  /** Distância no grafo a partir do nó de origem (1 = direto) */
  distance: number;
}
⋮----
/** Tipo da aresta que conecta ao nó de origem */
⋮----
/** Distância no grafo a partir do nó de origem (1 = direto) */
⋮----
/**
 * Opções para expansão de vizinhança no grafo.
 */
export interface ExpandOptions {
  /** Profundidade da BFS (1 = apenas vizinhos diretos). Default: 1 */
  depth: number;
  /** Filtro opcional por tipos de aresta (se omitido, retorna todos) */
  edgeTypes?: RelationshipType[];
}
⋮----
/** Profundidade da BFS (1 = apenas vizinhos diretos). Default: 1 */
⋮----
/** Filtro opcional por tipos de aresta (se omitido, retorna todos) */
⋮----
export interface IGraphQuery {
  /**
   * Busca um nó do grafo pelo ID do chunk correspondente.
   *
   * @param chunkId ID do ChunkEntry (ex: "src/core/RAGManager.ts#3")
   * @returns GraphNode ou null se não encontrado
   */
  getNodeByChunkId(chunkId: string): GraphNode | null;

  /**
   * Expande a vizinhança de um nó usando BFS limitada por profundidade.
   *
   * @param nodeId ID do nó de origem
   * @param options Opções de expansão (profundidade, filtro de arestas)
   * @returns Array de nós vizinhos (sem incluir o nó de origem)
   */
  expandNeighborhood(nodeId: string, options: ExpandOptions): NeighborNode[];

  /**
   * Retorna todos os nós pertencentes a um arquivo.
   *
   * @param filePath Caminho relativo do arquivo
   * @returns Array de nós do arquivo (vazio se nenhum)
   */
  getNodesByFile(filePath: string): GraphNode[];
}
⋮----
/**
   * Busca um nó do grafo pelo ID do chunk correspondente.
   *
   * @param chunkId ID do ChunkEntry (ex: "src/core/RAGManager.ts#3")
   * @returns GraphNode ou null se não encontrado
   */
getNodeByChunkId(chunkId: string): GraphNode | null;
⋮----
/**
   * Expande a vizinhança de um nó usando BFS limitada por profundidade.
   *
   * @param nodeId ID do nó de origem
   * @param options Opções de expansão (profundidade, filtro de arestas)
   * @returns Array de nós vizinhos (sem incluir o nó de origem)
   */
expandNeighborhood(nodeId: string, options: ExpandOptions): NeighborNode[];
⋮----
/**
   * Retorna todos os nós pertencentes a um arquivo.
   *
   * @param filePath Caminho relativo do arquivo
   * @returns Array de nós do arquivo (vazio se nenhum)
   */
getNodesByFile(filePath: string): GraphNode[];

================
File: packages/core/src/core/rag/graph/IGraphStore.ts
================
/**
 * IGraphStore: Interface para persistência do KnowledgeGraph (DIP).
 *
 * Responsabilidade Única (SRP): Definir o contrato de armazenamento
 * do grafo de conhecimento. A implementação concreta (JsonGraphStore)
 * cuida da serialização em disco.
 *
 * ISP: Métodos segregados — CRUD básico de nós/arestas, sem lógica de busca.
 * A busca fica em IGraphQuery.
 */
⋮----
import type { KnowledgeGraph, GraphNode, GraphEdge } from './types';
⋮----
export interface IGraphStore {
  /**
   * Persiste o grafo completo em disco.
   * Substitui completamente o arquivo anterior.
   */
  save(graph: KnowledgeGraph): Promise<void>;

  /**
   * Carrega o grafo do disco.
   * @returns KnowledgeGraph ou null se o arquivo não existir.
   */
  load(): Promise<KnowledgeGraph | null>;

  /**
   * Insere ou atualiza nós no grafo.
   * Usa `id` como chave: se o nó já existe, substitui (merge).
   */
  upsertNodes(nodes: GraphNode[]): Promise<void>;

  /**
   * Insere ou atualiza arestas no grafo.
   * Usa a chave composta `from|to|type` para deduplicação.
   */
  upsertEdges(edges: GraphEdge[]): Promise<void>;

  /**
   * Remove o arquivo de grafo do disco.
   * Usado para resetar o cache.
   */
  clear(): Promise<void>;
}
⋮----
/**
   * Persiste o grafo completo em disco.
   * Substitui completamente o arquivo anterior.
   */
save(graph: KnowledgeGraph): Promise<void>;
⋮----
/**
   * Carrega o grafo do disco.
   * @returns KnowledgeGraph ou null se o arquivo não existir.
   */
load(): Promise<KnowledgeGraph | null>;
⋮----
/**
   * Insere ou atualiza nós no grafo.
   * Usa `id` como chave: se o nó já existe, substitui (merge).
   */
upsertNodes(nodes: GraphNode[]): Promise<void>;
⋮----
/**
   * Insere ou atualiza arestas no grafo.
   * Usa a chave composta `from|to|type` para deduplicação.
   */
upsertEdges(edges: GraphEdge[]): Promise<void>;
⋮----
/**
   * Remove o arquivo de grafo do disco.
   * Usado para resetar o cache.
   */
clear(): Promise<void>;

================
File: packages/core/src/core/rag/graph/index.ts
================
/**
 * GraphRAG — barrel exports
 */

================
File: packages/core/src/core/rag/graph/IRelationshipExtractor.ts
================
/**
 * IRelationshipExtractor: Interface para extração de relações entre símbolos (DIP).
 *
 * Responsabilidade Única (SRP): Extrair relações semânticas (arestas)
 * de um arquivo de código fonte. A implementação concreta
 * (ASTRelationshipExtractor) analisa o AST para descobrir chamadas,
 * heranças, imports, etc.
 *
 * ISP: Segregada do armazenamento — esta interface não sabe onde
 * o grafo é persistido.
 */
⋮----
import type { GraphNode, GraphEdge } from './types';
⋮----
/**
 * Resultado da extração de relações de um único arquivo.
 * Contém apenas os novos nós e arestas descobertos.
 */
export interface ExtractionResult {
  /** Novos nós descobertos (funções, classes, interfaces) */
  nodes: GraphNode[];
  /** Novas arestas descobertas (CALLS, IMPORTS, EXTENDS, etc.) */
  edges: GraphEdge[];
}
⋮----
/** Novos nós descobertos (funções, classes, interfaces) */
⋮----
/** Novas arestas descobertas (CALLS, IMPORTS, EXTENDS, etc.) */
⋮----
export interface IRelationshipExtractor {
  /**
   * Extrai relações semânticas do código fonte de um arquivo.
   *
   * @param filePath Caminho relativo do arquivo (usado para namespacing dos IDs)
   * @param source Conteúdo completo do arquivo
   * @returns ExtractionResult com os nós e arestas descobertos.
   *          Retorna `{ nodes: [], edges: [] }` se o código for inválido
   *          ou se o arquivo não for suportado.
   */
  extract(filePath: string, source: string): ExtractionResult;
}
⋮----
/**
   * Extrai relações semânticas do código fonte de um arquivo.
   *
   * @param filePath Caminho relativo do arquivo (usado para namespacing dos IDs)
   * @param source Conteúdo completo do arquivo
   * @returns ExtractionResult com os nós e arestas descobertos.
   *          Retorna `{ nodes: [], edges: [] }` se o código for inválido
   *          ou se o arquivo não for suportado.
   */
extract(filePath: string, source: string): ExtractionResult;

================
File: packages/core/src/core/rag/graph/JsonGraphStore.ts
================
/**
 * JsonGraphStore: Implementação concreta de IGraphStore + IGraphQuery
 * usando JSON em disco (SRP, DIP, CQRS).
 *
 * Responsabilidade Única:
 * - Persistir o KnowledgeGraph em .soberano/graph.json
 * - Manter índice de adjacência em memória para O(1) em consultas
 * - Implementar IGraphStore (escrita) e IGraphQuery (leitura)
 *
 * Soberania Local: 100% node:fs/promises. Zero dependências externas.
 */
⋮----
import type { KnowledgeGraph, GraphNode, GraphEdge, RelationshipType } from './types';
import type { IGraphStore } from './IGraphStore';
import type { IGraphQuery, NeighborNode, ExpandOptions } from './IGraphQuery';
⋮----
/**
 * Índice de adjacência: nodeId → lista de arestas que saem do nó.
 * Construído uma vez no load() para consultas O(1).
 */
interface AdjacencyIndex {
  edgesBySource: Map<string, GraphEdge[]>;
  nodesById: Map<string, GraphNode>;
  nodesByChunkId: Map<string, GraphNode>;
  nodesByFile: Map<string, GraphNode[]>;
}
⋮----
export class JsonGraphStore implements IGraphStore, IGraphQuery
⋮----
constructor(rootDir: string)
⋮----
// =========================================================================
// IGraphStore — Escrita
// =========================================================================
⋮----
async save(graph: KnowledgeGraph): Promise<void>
⋮----
async load(): Promise<KnowledgeGraph | null>
⋮----
async upsertNodes(nodes: GraphNode[]): Promise<void>
⋮----
async upsertEdges(edges: GraphEdge[]): Promise<void>
⋮----
const edgeKey = (e: GraphEdge) => `$
⋮----
async clear(): Promise<void>
⋮----
// Arquivo não existe — ignorar
⋮----
// =========================================================================
// IGraphQuery — Leitura
// =========================================================================
⋮----
getNodeByChunkId(chunkId: string): GraphNode | null
⋮----
expandNeighborhood(nodeId: string, options: ExpandOptions): NeighborNode[]
⋮----
getNodesByFile(filePath: string): GraphNode[]
⋮----
// =========================================================================
// Privados
// =========================================================================
⋮----
private getGraphPath(): string
⋮----
private async getOrCreateGraph(): Promise<KnowledgeGraph>
⋮----
/**
   * Constrói índices de adjacência a partir do grafo carregado.
   * Executado uma vez no load()/save().
   */
private buildAdjacency(graph: KnowledgeGraph): void
⋮----
// Indexar nós
⋮----
// Indexar arestas (direcionais, apenas saída)
⋮----
/**
 * Utilitário para gerar IDs hash determinísticos.
 * Usa SHA-256 truncado para 12 caracteres hexadecimais.
 */
export function hashId(input: string): string

================
File: packages/core/src/core/rag/graph/types.ts
================
/**
 * GraphRAG Local — Tipos de domínio do KnowledgeGraph.
 *
 * Define as estruturas de dados para representar o grafo de conhecimento
 * que mapeia relações entre chunks, funções, classes, interfaces e arquivos.
 *
 * ISP: Interfaces segregadas — cada tipo tem uma responsabilidade única.
 */
⋮----
/**
 * Relação semântica entre dois nós do grafo.
 */
export type RelationshipType =
  | 'CALLS'         // Função A chama Função B (detectado via AST: CallExpression)
  | 'IMPORTS'       // Arquivo X importa Módulo Y (AST: ImportDeclaration)
  | 'EXTENDS'       // Classe X herda de Classe Y (AST: HeritageClause)
  | 'IMPLEMENTS'    // Classe X implementa Interface Y (AST: HeritageClause)
  | 'BELONGS_TO'    // Chunk/Função pertence a um Arquivo
  | 'CONTAINS'      // Arquivo/Classe contém Função/Método (inverso de BELONGS_TO)
  | 'REFERENCES';   // Referência genérica (fallback para export/require)
⋮----
| 'CALLS'         // Função A chama Função B (detectado via AST: CallExpression)
| 'IMPORTS'       // Arquivo X importa Módulo Y (AST: ImportDeclaration)
| 'EXTENDS'       // Classe X herda de Classe Y (AST: HeritageClause)
| 'IMPLEMENTS'    // Classe X implementa Interface Y (AST: HeritageClause)
| 'BELONGS_TO'    // Chunk/Função pertence a um Arquivo
| 'CONTAINS'      // Arquivo/Classe contém Função/Método (inverso de BELONGS_TO)
| 'REFERENCES';   // Referência genérica (fallback para export/require)
⋮----
/**
 * Nó do grafo de conhecimento.
 * Representa um símbolo do código fonte (função, classe, chunk, arquivo).
 */
export interface GraphNode {
  /** Identificador único do nó (hash determinístico) */
  id: string;
  /** Nome legível — ex: "RAGManager.ensureIndex()" */
  label: string;
  /** Tipo do nó */
  type: 'chunk' | 'function' | 'method' | 'class' | 'interface' | 'file';
  /** ID do ChunkEntry correspondente (apenas para type='chunk') */
  chunkId?: string;
  /** Caminho relativo do arquivo de origem */
  filePath: string;
  /** Linha inicial no arquivo (1-based) */
  startLine: number;
  /** Linha final no arquivo (1-based) */
  endLine: number;
}
⋮----
/** Identificador único do nó (hash determinístico) */
⋮----
/** Nome legível — ex: "RAGManager.ensureIndex()" */
⋮----
/** Tipo do nó */
⋮----
/** ID do ChunkEntry correspondente (apenas para type='chunk') */
⋮----
/** Caminho relativo do arquivo de origem */
⋮----
/** Linha inicial no arquivo (1-based) */
⋮----
/** Linha final no arquivo (1-based) */
⋮----
/**
 * Aresta direcionada entre dois nós do grafo.
 */
export interface GraphEdge {
  /** ID do nó de origem */
  from: string;
  /** ID do nó de destino */
  to: string;
  /** Tipo da relação semântica */
  type: RelationshipType;
  /** Metadados opcionais — ex: { lineNumber: 42 } */
  metadata?: Record<string, unknown>;
}
⋮----
/** ID do nó de origem */
⋮----
/** ID do nó de destino */
⋮----
/** Tipo da relação semântica */
⋮----
/** Metadados opcionais — ex: { lineNumber: 42 } */
⋮----
/**
 * Grafo de conhecimento completo.
 * Persistido em .soberano/graph.json.
 */
export interface KnowledgeGraph {
  /** Versão da estrutura do grafo (para migrações futuras) */
  version: number;
  /** Todos os nós do grafo */
  nodes: GraphNode[];
  /** Todas as arestas do grafo */
  edges: GraphEdge[];
  /** Mapeamento de filePath → mtimeMs para detecção incremental */
  fileMtimes: Record<string, number>;
  /** Timestamp ISO de criação */
  createdAt: string;
  /** Timestamp ISO da última atualização */
  updatedAt: string;
}
⋮----
/** Versão da estrutura do grafo (para migrações futuras) */
⋮----
/** Todos os nós do grafo */
⋮----
/** Todas as arestas do grafo */
⋮----
/** Mapeamento de filePath → mtimeMs para detecção incremental */
⋮----
/** Timestamp ISO de criação */
⋮----
/** Timestamp ISO da última atualização */

================
File: packages/core/src/core/rag/ASTChunkerService.ts
================
/**
 * ASTChunkerService: Chunking estrutural baseado em AST (SRP, DIP).
 *
 * Responsabilidade Única:
 * - Decidir se usa AST ou fallback textual baseado na extensão do arquivo
 * - Delegar parse para IASTParser (abstração — DIP)
 * - Transformar ASTNode[] em ChunkResult[] com respeito a limites de tamanho
 * - Subdividir nós grandes (ex: classes com muitos métodos) preservando
 *   blocos sintáticos completos
 *
 * Depende de IASTParser (interface) e IChunker (interface para fallback).
 * NÃO depende de nenhuma implementação concreta de parser.
 */
⋮----
import type { IChunker, ChunkResult } from './IChunker';
import type { IASTParser, ASTNode } from './IASTParser';
⋮----
const MAX_CHUNK_SIZE = 2000;         // caracteres máximos por chunk
const MAX_CHUNKS_PER_FILE = 50;      // limite máximo de chunks por arquivo
⋮----
// Extensões que suportam parseamento AST
⋮----
export class ASTChunkerService implements IChunker
⋮----
constructor(
⋮----
/**
   * Divide o conteúdo usando AST para arquivos de código,
   * ou fallback textual para os demais formatos.
   *
   * @param content Conteúdo do arquivo
   * @param filePath Caminho (opcional — usado para detectar extensão)
   * @returns Array de chunks semanticamente coesos
   */
chunk(content: string, filePath?: string): ChunkResult[]
⋮----
// Se não tem filePath ou não é arquivo de código, usa fallback textual
⋮----
// Tenta parse AST
⋮----
// Se o parse falhou (código inválido), usa fallback
⋮----
// Converte nós AST em chunks, respeitando limites
⋮----
/**
   * Verifica se a extensão do arquivo é de código-processável por AST.
   */
private isCodeFile(filePath: string): boolean
⋮----
/**
   * Extrai extensão do arquivo (incluindo o ponto).
   */
private getExtension(filePath: string): string
⋮----
// Pega a extensão: ".ts", ".js", etc.
⋮----
/**
   * Converte nós AST em ChunkResult[] com as regras:
   * 1. Nó pequeno (≤ MAX_CHUNK_SIZE) → 1 chunk
   * 2. Nó grande do tipo ClassDeclaration → subdivide por métodos
   * 3. Outros nós grandes → chunk por linhas respeitando MAX_CHUNK_SIZE
   * 4. Respeita MAX_CHUNKS_PER_FILE
   */
private nodesToChunks(nodes: ASTNode[], _source: string): ChunkResult[]
⋮----
// Para classes grandes: chunk da classe + chunk de cada método
⋮----
// Para outros nós grandes: divide por linhas
⋮----
/**
   * Subdivide uma classe grande: primeiro a assinatura da classe,
   * depois cada método como chunk independente.
   */
private splitLargeClass(node: ASTNode, chunks: ChunkResult[]): void
⋮----
// Extrai a assinatura da classe (até a primeira {)
⋮----
// Encontra métodos dentro do texto da classe usando regex simples
⋮----
// Acha o fim do método contando chaves aninhadas
⋮----
/**
   * Subdivide um nó grande que não é classe
   * em chunks menores respeitando MAX_CHUNK_SIZE.
   */
private splitLargeNode(node: ASTNode, chunks: ChunkResult[]): void

================
File: packages/core/src/core/rag/Chunker.ts
================
/**
 * Chunker: Responsável apenas por dividir textos em chunks textuais (SRP).
 *
 * Estratégias de chunking:
 * - Por parágrafo (\n\n): divisão semântica padrão
 * - Por sentença (. ? !): fallback para parágrafos longos
 * - Com overlap entre chunks consecutivos
 *
 * Implementa IChunker — funciona como fallback textual para o ASTChunkerService.
 * É uma função pura — sem estado, sem I/O. Ideal para testes.
 */
⋮----
import type { IChunker, ChunkResult } from './IChunker';
⋮----
const MAX_CHUNK_SIZE = 2000;        // caracteres (~500 tokens)
const OVERLAP = 200;                 // sobreposição entre chunks
const MAX_CHUNKS_PER_FILE = 50;     // limite por arquivo
⋮----
export class Chunker implements IChunker
⋮----
/**
   * Divide o conteúdo de um arquivo em chunks semanticamente coerentes.
   *
   * Algoritmo:
   * 1. Divide o texto em parágrafos (separados por \n\n)
   * 2. Se o parágrafo cabe em MAX_CHUNK_SIZE, usa como chunk
   * 3. Se não, divide por sentenças
   * 4. Aplica overlap de OVERLAP caracteres entre chunks consecutivos
   * 5. Respeita MAX_CHUNKS_PER_FILE
   *
   * @param content Conteúdo completo do arquivo
   * @param _filePath Caminho do arquivo (ignorado — chunking sempre textual)
   * @returns Array de chunks com metadados de linha
   */
chunk(content: string, _filePath?: string): ChunkResult[]
⋮----
// Se o parágrafo cabe em um chunk
⋮----
currentLine += paraLines.length + 1; // +1 pelo \n\n
⋮----
// Parágrafo muito grande: divide por sentenças
⋮----
// Overlap: mantém os últimos OVERLAP caracteres

================
File: packages/core/src/core/rag/Embedder.ts
================
/**
 * Embedder: Camada de abstração sobre o provider de embeddings (SRP).
 *
 * Responsabilidade Única:
 * - Recebe um IEmbedProvider e gerencia a chamada de embedding
 * - Controla keep_alive para gerenciamento de RAM
 * - Trata erros de rede/timeout silenciosamente
 *
 * Não faz chunking, não busca no índice, não formata contexto.
 * Apenas embeddings.
 */
⋮----
import type { IEmbedProvider } from '../../providers/types';
⋮----
export class Embedder
⋮----
constructor(provider: IEmbedProvider, defaultModel: string = 'all-minilm')
⋮----
/**
   * Gera embedding para um texto.
   *
   * @param text Texto a ser embedado
   * @param model Nome do modelo (usa default se omitido)
   * @param keepAlive Tempo de keep_alive (padrão "30s" para indexação)
   * @returns Vetor de floats 384-dim
   */
async embed(text: string, model?: string, keepAlive: string = '30s'): Promise<number[]>
⋮----
/**
   * Descarrega o modelo de embedding da RAM.
   * Usado após indexação para liberar recursos (12GB RAM limitada).
   */
async unload(): Promise<void>
⋮----
// Não falha se o descarregamento não funcionar

================
File: packages/core/src/core/rag/IASTParser.ts
================
/**
 * IASTParser: Interface para parseamento de AST (DIP).
 *
 * Permite que o ASTChunkerService dependa de uma abstração em vez de
 * uma implementação concreta de parser AST. O Adapter concreto
 * (TypescriptASTAdapter) isola a dependência do compilador TypeScript.
 */
⋮----
/**
 * Nó AST extraído do código fonte.
 */
export interface ASTNode {
  /** Nome do nó: nome da função, classe, interface, etc. */
  name: string;
  /** Tipo do nó: 'FunctionDeclaration' | 'ClassDeclaration' | 'MethodDeclaration' | ... */
  kind: string;
  /** Código fonte completo do nó (assinatura + corpo) */
  text: string;
  /** Linha inicial (1-based) */
  startLine: number;
  /** Linha final (1-based) */
  endLine: number;
}
⋮----
/** Nome do nó: nome da função, classe, interface, etc. */
⋮----
/** Tipo do nó: 'FunctionDeclaration' | 'ClassDeclaration' | 'MethodDeclaration' | ... */
⋮----
/** Código fonte completo do nó (assinatura + corpo) */
⋮----
/** Linha inicial (1-based) */
⋮----
/** Linha final (1-based) */
⋮----
export interface IASTParser {
  /**
   * Extrai nós semânticos (funções, classes, etc.) do código fonte.
   *
   * @param source Código fonte TypeScript/JavaScript completo
   * @returns Array de nós AST ordenados por startLine.
   *          Retorna array vazio se o código não for TypeScript/JavaScript válido.
   */
  parse(source: string): ASTNode[];
}
⋮----
/**
   * Extrai nós semânticos (funções, classes, etc.) do código fonte.
   *
   * @param source Código fonte TypeScript/JavaScript completo
   * @returns Array de nós AST ordenados por startLine.
   *          Retorna array vazio se o código não for TypeScript/JavaScript válido.
   */
parse(source: string): ASTNode[];

================
File: packages/core/src/core/rag/IChunker.ts
================
/**
 * IChunker: Interface para o contrato de chunking (DIP).
 *
 * Permite que o RAGManager dependa de uma abstração em vez de
 * uma implementação concreta de chunking. Suporta tanto chunking
 * textual (por parágrafo/sentença) quanto estrutural (AST-aware).
 *
 * O parâmetro opcional `filePath` permite que implementações
 * adaptem a estratégia com base na extensão do arquivo.
 */
⋮----
export interface ChunkResult {
  text: string;
  startLine: number;
  endLine: number;
}
⋮----
export interface IChunker {
  /**
   * Divide o conteúdo em chunks semanticamente coerentes.
   *
   * @param content Conteúdo completo do arquivo
   * @param filePath Caminho do arquivo (opcional, usado para detectar
   *                 extensão e adaptar estratégia de chunking)
   * @returns Array de chunks com metadados de linha
   */
  chunk(content: string, filePath?: string): ChunkResult[];
}
⋮----
/**
   * Divide o conteúdo em chunks semanticamente coerentes.
   *
   * @param content Conteúdo completo do arquivo
   * @param filePath Caminho do arquivo (opcional, usado para detectar
   *                 extensão e adaptar estratégia de chunking)
   * @returns Array de chunks com metadados de linha
   */
chunk(content: string, filePath?: string): ChunkResult[];

================
File: packages/core/src/core/rag/index.ts
================


================
File: packages/core/src/core/rag/RAGManager.ts
================
/**
 * RAGManager: Orquestrador do pipeline RAG + GraphRAG (SRP — orquestração pura).
 *
 * Responsabilidade Única: Coordenar as etapas do pipeline RAG:
 * 1. Coletar arquivos texto do diretório
 * 2. Chunking (delega para Chunker)
 * 3. Embedding (delega para Embedder)
 * 4. Cache em disco (delega para VectorStore)
 * 5. Busca semântica (delega para Retriever)
 * 6. Construção do KnowledgeGraph (delega para GraphBuilder)
 *
 * Esta classe NÃO implementa chunking, NÃO gera embeddings,
 * NÃO salva arquivos, NÃO constrói grafos — ela apenas coordena as peças.
 */
⋮----
import { FileReader } from '../FileReader';
import { Chunker } from './Chunker';
import type { IChunker } from './IChunker';
import { Embedder } from './Embedder';
import { VectorStore } from './VectorStore';
import { Retriever } from './Retriever';
import type { SearchMatch } from './Retriever';
import type { ChunkEntry } from './VectorStore';
import type { IEmbedProvider } from '../../providers/types';
import { GraphBuilder } from './graph/GraphBuilder';
import type { IGraphStore } from './graph/IGraphStore';
import type { IGraphQuery } from './graph/IGraphQuery';
import type { IRelationshipExtractor } from './graph/IRelationshipExtractor';
import { ASTRelationshipExtractor } from './graph/ASTRelationshipExtractor';
import { JsonGraphStore } from './graph/JsonGraphStore';
import { TypescriptASTAdapter } from './TypescriptASTAdapter';
import { ASTChunkerService } from './ASTChunkerService';
import { GraphRAGManager } from './graph/GraphRAGManager';
⋮----
// Extensões de arquivos texto para indexar
⋮----
export class RAGManager
⋮----
constructor(
    fileReader: FileReader,
    chunker: IChunker,
    embedder: Embedder,
    vectorStore: VectorStore,
    retriever: Retriever,
    graphBuilder?: GraphBuilder | null,
    graphRAGManager?: GraphRAGManager | null
)
⋮----
/**
   * Factory method: cria RAGManager completo com instâncias default.
   * Útil para consumo externo (ex: CLI) onde não se quer instanciar
   * cada dependência manualmente.
   *
   * @param fileReader FileReader para ler arquivos
   * @param embedProvider Provider de embeddings (ex: OllamaProvider)
   * @param graphStorePath Caminho para persistir o grafo (opcional, ex: ".soberano/graph.json")
   */
static create(
    fileReader: FileReader,
    embedProvider: IEmbedProvider,
    chunker?: IChunker,
    graphStorePath?: string
): RAGManager
⋮----
// Default: ASTChunkerService com fallback textual
⋮----
/**
   * Verifica se um arquivo é texto (vs binário) baseado nos primeiros bytes.
   */
private isTextContent(content: string): boolean
⋮----
/**
   * Coleta arquivos texto de um diretório recursivamente.
   */
private async collectTextFiles(
    rootDir: string,
    maxFiles: number = 200
): Promise<string[]>
⋮----
const walk = async (dir: string): Promise<void> =>
⋮----
// Paraleliza fs.stat em todas as entradas do diretório
⋮----
/**
   * Garante que o índice existe para o diretório informado.
   * Se o cache estiver atualizado, reutiliza.
   * Indexa apenas arquivos novos/modificados.
   */
async ensureIndex(rootDir: string): Promise<void>
⋮----
// 1. Coleta arquivos
⋮----
// 2. Monta lista com DOCUMENTACAO_PROJETO.md sempre primeiro
⋮----
// 3. Verifica quais precisam ser (re)indexados — paralelizado
⋮----
// Se não há nada para processar, usa índice existente
⋮----
// 4. Indexa arquivos novos/modificados — paralelizado com allSettled
⋮----
// Gera embeddings em paralelo para cada chunk deste arquivo
⋮----
// resultados rejected (erro de leitura/embed) são ignorados
⋮----
// 5. Descarrega modelo de embedding
⋮----
// 6. Mescla com índice existente
⋮----
// Paraleliza fs.stat para todos os arquivos processados
⋮----
// 7. Salva índice
⋮----
// 8. Constrói/atualiza o KnowledgeGraph (se habilitado)
⋮----
/**
   * Busca os chunks mais similares à consulta.
   * Se o GraphRAGManager estiver disponível, enriquece os resultados
   * com expansão de vizinhança no grafo de conhecimento.
   */
async retrieve(query: string, rootDir: string): Promise<SearchMatch[]>
⋮----
// Carrega índice
⋮----
// Busca vetorial base
⋮----
// Busca híbrida: vetorial + grafo de conhecimento
⋮----
/**
   * Formata os resultados em contexto.
   */
formatContext(matches: SearchMatch[]): string

================
File: packages/core/src/core/rag/ReActLoop.ts
================
/**
 * ReActLoop: Execução do ciclo Reasoning + Acting (SRP).
 *
 * Responsabilidade Única: Gerenciar o loop de interação ReAct:
 * 1. Concatena histórico de mensagens em um prompt único
 * 2. Envia para o provider e interpreta ações vs resposta final
 * 3. Se for JSON mode (--json), usa ToolRegistry + formato tool_call/final_response
 * 4. Se for modo texto, usa ACTION/FINAL_ANSWER + CommandExecutor
 * 5. Se streamMode=true, usa provider.streamChat() com callback onToken
 *
 * Compatível com IProvider.chat(request: ChatRequest): ChatResponse
 * e opcionalmente provider.streamChat(request: ChatRequest): AsyncIterable<string>
 * (prompt único, não array de mensagens — formata tudo inline)
 */
⋮----
import type { IProvider } from '../../providers/types';
import type { CommandExecutor } from '../CommandExecutor';
import type { ToolRegistry } from '../ToolRegistry';
import type { Reflector, ReflectionResult, ReflectionError } from '../Reflector';
import { JsonValidator } from '../../validation/JsonValidator';
import { assessCompressionNeed, CompressionTrigger } from '../IContextCompressor';
import type { IContextCompressor } from '../IContextCompressor';
import { TokenEstimator } from '../TokenEstimator';
⋮----
export interface ReActMessage {
  role: 'user' | 'assistant' | 'system';
  content: string;
}
⋮----
export interface ReActResult {
  finalAnswer: string;
  iterations: number;
  /** Status da correção do Reflector */
  correctionStatus?: 'stable' | 'suspicious' | 'rejected';
  /** Erros encontrados pelo Reflector (vazio se não houve reflexão) */
  errors?: ReflectionError[];
  /** Indica se a compressão de contexto foi aplicada no histórico */
  wasCompressed?: boolean;
  /** Taxa de compressão (0.0 a 1.0), presente apenas se wasCompressed for true */
  compressionRatio?: number;
}
⋮----
/** Status da correção do Reflector */
⋮----
/** Erros encontrados pelo Reflector (vazio se não houve reflexão) */
⋮----
/** Indica se a compressão de contexto foi aplicada no histórico */
⋮----
/** Taxa de compressão (0.0 a 1.0), presente apenas se wasCompressed for true */
⋮----
/** Opções de streaming para o ReActLoop */
export interface StreamOptions {
  /** Se true, ativa streaming das respostas do modelo */
  enabled: boolean;
  /**
   * Callback chamado para cada token recebido durante o streaming.
   * Usado para efeito "máquina de escrever" no terminal.
   */
  onToken: (token: string) => void;
}
⋮----
/** Se true, ativa streaming das respostas do modelo */
⋮----
/**
   * Callback chamado para cada token recebido durante o streaming.
   * Usado para efeito "máquina de escrever" no terminal.
   */
⋮----
export class ReActLoop
⋮----
constructor(
    provider: IProvider,
    executor?: CommandExecutor,
    toolRegistry?: ToolRegistry,
    reflector?: Reflector,
    compressor?: IContextCompressor
)
⋮----
/**
   * Concatena o histórico de mensagens em um único prompt texto,
   * no formato esperado pelo Ollama (system + user/assistant turns).
   */
private buildPrompt(
    systemPrompt: string,
    history: ReActMessage[]
): string
⋮----
/**
   * Envia um prompt para o provider e retorna a resposta completa.
   * Se streamMode estiver ativo e o provider suportar streamChat,
   * faz streaming dos tokens + acumula o buffer para o parser.
   */
private async sendPrompt(
    prompt: string,
    model: string,
    temperature: number,
    format?: 'json',
    streamOpts?: StreamOptions
): Promise<string>
⋮----
// Modo streaming: usa streamChat + acumula buffer
⋮----
// Modo normal: usa chat() padrão
⋮----
/**
   * Executa o loop ReAct no modo JSON (tool_call / final_response).
   * Usado quando --json está ativo. Usa ToolRegistry + detecção de loop.
   * Suporta streaming se streamOpts for fornecido.
   */
private async executeJsonMode(
    systemPrompt: string,
    history: ReActMessage[],
    model: string,
    streamOpts?: StreamOptions
): Promise<ReActResult>
⋮----
streamOpts // streaming para todas iterações; sendPrompt decide
⋮----
// Usa JsonValidator para parsear
⋮----
// Se não for JSON, retorna cru como fallback
⋮----
// tool_call → executa e continua
⋮----
// Detecta chamadas repetidas consecutivas (loop infinito)
⋮----
// ── Prevenção de Context Overflow ──
⋮----
// Trunca o toolResult para caber no limite seguro
⋮----
const truncated = toolResult.slice(0, maxToolTokens * 4); // 4 chars ≈ 1 token
⋮----
// final_response → retorna
⋮----
// Formato desconhecido → fallback
⋮----
// Loop esgotado
⋮----
/**
   * Executa o loop ReAct no modo texto (ACTION / FINAL_ANSWER).
   * Formato legado, sem JSON.
   * Suporta streaming se streamOpts for fornecido.
   */
private async executeTextMode(
    systemPrompt: string,
    history: ReActMessage[],
    model: string,
    streamOpts?: StreamOptions
): Promise<ReActResult>
⋮----
streamOpts // streaming para todas iterações; sendPrompt decide
⋮----
/**
   * Aplica o Reflector na resposta final, se disponível e solicitado.
   */
private async applyReflection(
    result: ReActResult,
    model: string,
    reflectFlag: boolean
): Promise<ReActResult>
⋮----
// Só aplica reflexão se:
// 1. A flag --reflect está ativa
// 2. O Reflector foi injetado
// 3. Há uma resposta real (não vazia)
⋮----
/**
   * Ponto de entrada: executa o loop ReAct no modo apropriado.
   *
   * @param systemPrompt Prompt de sistema
   * @param history Histórico de mensagens
   * @param model Modelo (default: tinyllama:1b)
   * @param options.jsonMode Se true, usa JSON mode (tool_call/final_response)
   * @param options.reflect Se true, aplica Reflector pós-resposta (self-correction)
   * @param options.stream Opções de streaming (se undefined, usa chat() normal)
   * @returns Resultado com resposta final e número de iterações
   */
async execute(
    systemPrompt: string,
    history: ReActMessage[],
    model?: string,
    options?: { jsonMode?: boolean; reflect?: boolean; stream?: StreamOptions }
): Promise<ReActResult>
⋮----
// ── Compressão de contexto (pré-buildPrompt) ──
⋮----
// Só adiciona workingMemory ao histórico se tiver conteúdo real
// Isso evita mensagens [SYSTEM]: vazias no SessionStore (sessões fantasmas)
⋮----
// Aplica reflexão após a resposta final (ambos os modos)

================
File: packages/core/src/core/rag/Retriever.ts
================
/**
 * Retriever: Busca semântica por cosine similarity (SRP).
 *
 * Responsabilidade Única:
 * - Calcular similaridade entre vetores (cosine similarity)
 * - Filtrar por threshold
 * - Ordenar e retornar top-K
 *
 * Função pura — sem estado, sem I/O. Ideal para testes unitários.
 */
⋮----
import type { ChunkEntry } from './VectorStore';
⋮----
export interface SearchMatch {
  chunk: ChunkEntry;
  score: number; // 0.0 a 1.0
}
⋮----
score: number; // 0.0 a 1.0
⋮----
/**
 * Cosine Similarity — implementação nativa TypeScript.
 * cos(θ) = (A·B) / (||A|| × ||B||)
 */
export function cosineSimilarity(a: number[], b: number[]): number
⋮----
export class Retriever
⋮----
/**
   * Busca os chunks mais similares a um vetor de consulta.
   *
   * @param queryEmbedding Vetor da consulta (384-dim)
   * @param chunks Lista de chunks candidatos
   * @returns Top-K matches ordenados por similaridade (decrescente)
   */
retrieve(
    queryEmbedding: number[],
    chunks: ChunkEntry[]
): SearchMatch[]
⋮----
/**
   * Formata os matches em um bloco de contexto para o prompt.
   *
   * @param matches Resultados da busca
   * @param maxChars Limite de caracteres (padrão 4000)
   * @returns String formatada com citação de fonte [arquivo:linha]
   */
formatContext(matches: SearchMatch[], maxChars: number = 4000): string

================
File: packages/core/src/core/rag/TypescriptASTAdapter.ts
================
/**
 * TypescriptASTAdapter: Adaptador para o compilador TypeScript (DIP).
 *
 * Implementa IASTParser usando a API nativa do TypeScript (ts.createSourceFile).
 * Aproveita o pacote 'typescript' já instalado como devDependency — sem
 * dependências externas adicionais (regra Zero Dependências).
 *
 * Extrai nós semânticos do código fonte:
 * - FunctionDeclaration, ClassDeclaration, MethodDeclaration
 * - InterfaceDeclaration, EnumDeclaration, TypeAliasDeclaration
 * - VariableStatement (const/let/var com arrow functions)
 * - ExportAssignment (export default)
 */
⋮----
import type { IASTParser, ASTNode } from './IASTParser';
import { getNodeName as sharedGetNodeName } from '../astUtils';
⋮----
export class TypescriptASTAdapter implements IASTParser
⋮----
/**
   * Extrai nós semânticos do código fonte TypeScript/JavaScript.
   *
   * Percorre a árvore sintática e extrai declarações de alto nível que
   * representam blocos semânticos completos (funções, classes, etc.).
   *
   * @param source Código fonte completo
   * @returns Array de nós AST ordenados por startLine.
   *          Array vazio se o parse falhar ou código for inválido.
   */
parse(source: string): ASTNode[]
⋮----
// Ordena por startLine
⋮----
/**
   * Percorre recursivamente os nós filhos e extrai os nós semânticos.
   *
   * @param node Nó atual da AST
   * @param source Código fonte original
   * @param sourceFile SourceFile para calcular line/character
   * @param nodes Array acumulador de nós encontrados
   */
private extractNodes(
    node: ts.Node,
    source: string,
    sourceFile: ts.SourceFile,
    nodes: ASTNode[]
): void
⋮----
// Só processa nós de alto nível (statements) exceto para métodos de classe
⋮----
// Percorre filhos — para MethodDeclaration dentro de ClassDeclaration,
// isso captura os métodos como nós independentes
⋮----
/**
   * Verifica se um nó da AST deve ser extraído como chunk semântico.
   */
private shouldExtractNode(node: ts.Node): boolean
⋮----
// Declarações de alto nível
⋮----
// ExportAssignment: export default <expression>
// NOTA: export default class/function gera ClassDeclaration/FunctionDeclaration
// com modificador, NÃO ExportAssignment.
⋮----
// Métodos dentro de classe (MethodDeclaration, Constructor, GetAccessor, SetAccessor)
⋮----
// VariableStatement com arrow function ou declaração relevante
⋮----
/**
   * Extrai o nome do nó.
   * Delega para a função compartilhada em astUtils.ts.
   */
private getNodeName(node: ts.Node, _source: string, sourceFile: ts.SourceFile): string
⋮----
/**
   * Retorna o tipo do nó como string legível.
   */
private getNodeKind(node: ts.Node): string

================
File: packages/core/src/core/rag/VectorStore.ts
================
/**
 * VectorStore: Persistência do índice de embeddings em disco (SRP).
 *
 * Responsabilidade Única:
 * - Salvar/carregar o índice de chunks + embeddings em JSON
 * - Gerenciar metadados de arquivos (mtime para cache)
 * - Criar diretório de cache se necessário
 *
 * Não gera embeddings, não faz chunking, não busca.
 * Apenas I/O em disco.
 */
⋮----
/**
 * Um chunk de texto com metadados e embedding
 */
export interface ChunkEntry {
  id: string;
  text: string;
  filePath: string;
  startLine: number;
  endLine: number;
  embedding: number[];
  indexedAt: number;
}
⋮----
interface FileMeta {
  mtime: number;
  chunks: number;
}
⋮----
interface IndexStore {
  formatVersion: 1;
  embedModel: string;
  dimensions: number;
  indexedAt: number;
  files: Record<string, FileMeta>;
  chunks: ChunkEntry[];
}
⋮----
export class VectorStore
⋮----
/**
   * Salva o índice em disco.
   * Cria o diretório de cache automaticamente.
   */
async save(rootDir: string, store: IndexStore): Promise<void>
⋮----
/**
   * Carrega o índice do disco.
   * Retorna null se não existir (primeira execução).
   */
async load(rootDir: string): Promise<IndexStore | null>
⋮----
private getCacheDir(rootDir: string): string
⋮----
private getIndexPath(rootDir: string): string

================
File: packages/core/src/core/AppContext.ts
================
/**
 * AppContext: Container de injeção de dependência (DIP).
 *
 * Dependency Inversion Principle (DIP):
 * - Módulos de ALTO nível (CLI, comandos) NÃO dependem de módulos de BAIXO nível
 *   (OllamaProvider, FileReader concretos).
 * - Ambos dependem de abstrações (IProvider, FileReader, CommandExecutor).
 * - AppContext é o "ponto de cola" onde as implementações concretas são injetadas.
 *
 * Benefícios:
 * 1. Testabilidade: nos testes unitários, podemos passar mocks/stubs
 *    no lugar das implementações reais.
 * 2. Flexibilidade: para trocar de provider (ex: Ollama → OpenRouter),
 *    basta mudar a fábrica em um único lugar.
 * 3. Configuração por flags CLI: os parâmetros (host, porta) são passados
 *    pelo AppContext, não lidos diretamente dentro do comando.
 */
⋮----
import type { IProvider, IEmbedProvider } from '../providers/types';
import { FileReader } from './FileReader';
import { CommandExecutor } from './CommandExecutor';
import { ToolRegistry } from './ToolRegistry';
import { ProviderFactory } from './ProviderFactory';
import type { ProviderConfig } from './ProviderFactory';
import { SessionManager } from './SessionManager';
import { SessionStore } from './SessionStore';
import type { ReActMessage } from './rag/ReActLoop';
import { ASTEditor } from './ASTEditor';
import { SearchReplaceEditor } from './SearchReplaceEditor';
⋮----
export interface AppContextConfig {
  provider: ProviderConfig;
  model?: string;
  jsonMode?: boolean;
  ragDir?: string;
  sessionId?: string;
  newSession?: boolean;
}
⋮----
export class AppContext
⋮----
constructor(config: AppContextConfig)
⋮----
// Utilitários base — sempre os mesmos
⋮----
// Provider via fábrica (OCP) — fácil de estender para novos providers
⋮----
// ToolRegistry — configurado com as tools padrão
⋮----
// Configurações de execução
⋮----
// SessionManager — gerencia histórico da conversa multi-turn
⋮----
// NOTA: initSession NÃO é chamado aqui no construtor para evitar race condition.
// Chame app.initialize() após construir o AppContext para carregar/criar a sessão.
⋮----
/**
   * Inicializa a sessão de conversa (carrega existente ou cria nova).
   *
   * Deve ser chamado após a construção do AppContext, antes de usar o sessionManager.
   * Separar do construtor evita race condition entre initSession() assíncrono
   * e addMessage() síncrono.
   *
   * @param config Configuração original passada no construtor
   */
async initialize(config?: AppContextConfig): Promise<void>
⋮----
if (!sessionId && !newSession) return; // Nada a inicializar
⋮----
private async initSession(sessionId?: string, newSession?: boolean): Promise<void>
⋮----
/**
   * Helper para obter o histórico de mensagens da sessão ativa.
   * Retorna array vazio se não houver sessão ativa.
   */
getSessionHistory(): ReActMessage[]
⋮----
/**
   * Registra as tools padrão disponíveis para o ReAct loop.
   * São injetadas via DI, mas configuradas com as dependências reais.
   */
private setupDefaultTools(): void
⋮----
// Tool: readFile
⋮----
// Tool: readDir
⋮----
// Tool: execute
⋮----
// Tool: editSymbol — edição estrutural de símbolos TypeScript via AST
⋮----
// Tool: searchReplace — edição textual por bloco exato
⋮----
// Tool: writeFile — criação/atualização de ficheiros
⋮----
// Tool: readFileForEdit — leitura com numeração de linhas

================
File: packages/core/src/core/ASTEditor.ts
================
/**
 * ASTEditor: Edição estrutural de arquivos TypeScript por nome de símbolo.
 *
 * Permite substituir o corpo de uma declaração (função, classe, interface,
 * variável, etc.) em arquivos .ts mantendo o restante do arquivo intacto.
 *
 * Dependências (DIP):
 * - FileReader: leitura do arquivo fonte
 * - fs.writeFile (nativo): escrita do arquivo modificado
 *
 * Uso:
 *   const editor = new ASTEditor(fileReader);
 *   const result = await editor.replaceSymbol('src/foo.ts', 'myFunction', '...new code...');
 */
⋮----
import { FileReader } from './FileReader';
import { findTopLevelSymbol } from './astUtils';
⋮----
/**
 * Resultado de uma operação de substituição de símbolo.
 */
export interface ASTEditResult {
  /** true se a operação foi concluída sem erros */
  success: boolean;
  /** true se o símbolo foi encontrado no arquivo */
  symbolFound: boolean;
  /** Caminho do arquivo alvo */
  filePath: string;
  /** Nome do símbolo procurado */
  symbolName: string;
  /** Mensagem de erro, se houver */
  error?: string;
}
⋮----
/** true se a operação foi concluída sem erros */
⋮----
/** true se o símbolo foi encontrado no arquivo */
⋮----
/** Caminho do arquivo alvo */
⋮----
/** Nome do símbolo procurado */
⋮----
/** Mensagem de erro, se houver */
⋮----
export class ASTEditor
⋮----
/**
   * @param fileReader Instância de FileReader para ler arquivos do disco
   */
constructor(private readonly fileReader: FileReader)
⋮----
/**
   * Substitui o código de um símbolo top-level em um arquivo TypeScript.
   *
   * Pipeline:
   *   1. Lê o arquivo via FileReader.readFile()
   *   2. Parseia com ts.createSourceFile()
   *   3. Busca o nó top-level cujo nome corresponda a symbolName
   *   4. Se encontrado, substitui o trecho (node.getFullStart() a node.getEnd())
   *      por newCode
   *   5. Escreve o resultado de volta no arquivo via fs.writeFile()
   *
   * @param filePath Caminho do arquivo .ts a editar
   * @param symbolName Nome do símbolo top-level a substituir
   * @param newCode Novo código fonte para substituir o símbolo
   * @returns ASTEditResult indicando sucesso/falha
   */
async replaceSymbol(
    filePath: string,
    symbolName: string,
    newCode: string
): Promise<ASTEditResult>
⋮----
// 1. Ler o arquivo
⋮----
// 2. Parsear com TypeScript
⋮----
// 3. Buscar o símbolo top-level
⋮----
// 4. Calcular offsets e construir novo conteúdo
const fullStart = node.getFullStart(); // includes leading comments/whitespace
⋮----
// 5. Validar caminho (Path Traversal) e escrever o arquivo modificado

================
File: packages/core/src/core/astUtils.ts
================
/**
 * astUtils: Funções utilitárias compartilhadas para manipulação da AST do TypeScript.
 *
 * Extraída do TypescriptASTAdapter para reuso no ASTEditor.
 * Mantida aqui como módulo independente (sem dependências externas além do 'typescript').
 */
⋮----
/**
 * Extrai o nome de um nó da AST (top-level symbol).
 *
 * Lógica idêntica à usada no TypescriptASTAdapter.getNodeName().
 *
 * @param node Nó da AST do TypeScript
 * @param sourceFile SourceFile de referência para obter text spans
 * @returns Nome do símbolo ou '<anonymous>' se não for nomeável
 */
export function getNodeName(node: ts.Node, sourceFile: ts.SourceFile): string
⋮----
// Para VariableStatement, tenta extrair o nome da variável
⋮----
// Para constructor sem nome explícito
⋮----
/**
 * Busca um símbolo top-level pelo nome no SourceFile.
 *
 * Percorre os nós filhos do SourceFile (apenas nível superior) e retorna
 * o primeiro nó cujo nome corresponda exatamente a `symbolName`.
 *
 * @param sourceFile SourceFile parseado
 * @param symbolName Nome do símbolo a buscar
 * @returns O nó encontrado, ou undefined se não existir
 */
export function findTopLevelSymbol(
  sourceFile: ts.SourceFile,
  symbolName: string
): ts.Node | undefined
⋮----
if (found) return; // já encontrou

================
File: packages/core/src/core/CommandExecutor.ts
================
/**
 * Resultado da execução de um comando shell.
 * Contém as saídas stdout/stderr, código de saída e sinal (se houve).
 */
export interface CommandResult {
  stdout: string;
  stderr: string;
  exitCode: number | null;
  signal: NodeJS.Signals | null;
}
⋮----
/**
 * CommandExecutor: Camada de abstração sobre child_process.spawn do Node.js.
 *
 * Fluxo de dados:
 * CLI ──(cmd+args)──▶ execute() ──(spawn)──▶ processo filho ──(stdout/stderr streams)──▶ CommandResult
 *
 * Diferente de exec() que bufferiza tudo em memória, spawn() trabalha com streams,
 * o que é mais seguro para comandos com saída longa e permite timeout nativo.
 *
 * shell: false → previne injeção de comandos (não passa pelo interpretador shell).
 */
export class CommandExecutor
⋮----
/**
   * Executa um comando de forma segura e assíncrona.
   *
   * Pipeline:
   *   1. cp.spawn(command, args, options) → cria processo filho
   *   2. Escuta stdout (child.stdout.on('data')) → acumula chunks Buffer
   *   3. Escuta stderr (child.stderr.on('data')) → acumula chunks Buffer
   *   4. child.on('error') → rejeita Promise se o spawn falhar
   *   5. child.on('close') → resolve Promise com stdout/stderr/exitCode
   *   6. Timeout (padrão 60s) → encerra o processo automaticamente
   *
   * Segurança:
   *   - shell: false → o comando é executado diretamente, sem interpretador.
   *     Ex: execute('ls', ['-la']) → spawn('ls', ['-la'])
   *     Isso evita vulnerabilidades como injeção de shell (ex: 'file; rm -rf /').
   *   - timeout → evita processos zumbi.
   *
   * @param command  Nome do executável (ex: 'ls', 'git', 'node')
   * @param args     Argumentos do comando como array de strings
   * @param options  Opções: cwd (working directory), timeout (ms)
   * @returns Promise com stdout, stderr, exitCode e signal
   */
execute(
    command: string,
    args: string[] = [],
    options?: { cwd?: string; timeout?: number }
): Promise<CommandResult>
⋮----
// spawn: cria o processo filho sem shell intermediário
⋮----
shell: false, // Sem shell → sem injeção de comandos
timeout: options?.timeout ?? 60_000, // 60 segundos padrão
⋮----
// stdout: saída padrão do processo
⋮----
// stderr: saída de erro do processo (logs, erros, etc.)
⋮----
// 'error': falha ao criar o processo (comando inexistente, permissão, etc.)
⋮----
// 'close': processo terminou (normalmente, por timeout ou por sinal)

================
File: packages/core/src/core/ErrorJournal.ts
================
/**
 * ErrorJournal: Persistência de erros de reflexão em disco.
 *
 * SRP: Responsabilidade única — salvar e recuperar registros de erros
 * encontrados pelo Reflector durante a auto-correção.
 *
 * Sem dependências externas: usa apenas node:fs/promises nativo.
 * Operações assíncronas para não bloquear o Event Loop.
 *
 * Os registros são salvos em .soberano/error-journal.json na raiz do projeto
 *
 * Formato do arquivo:
 * ```json
 * {
 *   "version": 1,
 *   "entries": [
 *     {
 *       "timestamp": "2026-05-09T10:30:00.000Z",
 *       "model": "llama3.2:1b",
 *       "type": "hallucination",
 *       "description": "Menção a função inexistente 'fs.readJsonSync'",
 *       "correctionStatus": "stable",
 *       "originalLength": 245,
 *       "correctedLength": 260
 *     }
 *   ]
 * }
 * ```
 */
⋮----
import type { CorrectionStatus } from '../providers/types';
⋮----
/** Um único registro de erro no journal */
export interface ErrorJournalEntry {
  timestamp: string;
  model: string;
  type: 'hallucination' | 'syntax' | 'inconsistency' | 'logic';
  description: string;
  correctionStatus: CorrectionStatus;
  originalLength: number;
  correctedLength: number;
}
⋮----
/** Estrutura completa do journal */
export interface ErrorJournalData {
  version: number;
  entries: ErrorJournalEntry[];
}
⋮----
export class ErrorJournal
⋮----
/**
   * @param filePath Caminho do arquivo JSON (opcional, default .soberano/error-journal.json na raiz do projeto)
   * @param maxEntries Número máximo de entradas (default 1000)
   */
constructor(filePath?: string, maxEntries: number = 1000)
⋮----
/**
   * Carrega o journal do disco (assíncrono).
   * Se o arquivo não existir, retorna um journal vazio.
   */
private async load(): Promise<ErrorJournalData>
⋮----
/**
   * Salva o journal no disco (assíncrono).
   */
private async save(data: ErrorJournalData): Promise<void>
⋮----
// Falha silenciosa — o journal é auxiliar, não deve quebrar o fluxo
⋮----
/**
   * Adiciona um entry ao journal e persiste.
   *
   * @param entry Dados do erro a ser registrado
   */
async addEntry(entry: ErrorJournalEntry): Promise<void>
⋮----
// Limita o número de entradas (FIFO)
⋮----
/**
   * Retorna todos os entries do journal (mais recentes primeiro).
   */
async getEntries(): Promise<ErrorJournalEntry[]>
⋮----
/**
   * Retorna os últimos N entries, filtrados por tipo (opcional).
   *
   * @param limit Número máximo de entries
   * @param type Filter por tipo de erro (opcional)
   */
async getRecentEntries(limit: number = 10, type?: string): Promise<ErrorJournalEntry[]>
⋮----
/**
   * Retorna estatísticas agregadas do journal.
   */
async getStats(): Promise<

================
File: packages/core/src/core/FileReader.ts
================
import type { Dirent } from 'node:fs';
⋮----
/**
 * Resultado de uma busca textual em arquivos.
 * Cada ocorrência contém o caminho do arquivo, número da linha e o texto encontrado.
 */
export interface SearchResult {
  file: string;
  line: number;
  content: string;
}
⋮----
/**
 * FileReader: Camada de abstração sobre o sistema de arquivos do Node.js.
 *
 * Fluxo de dados:
 * CLI ──(path:string)──▶ readFile() ──(buffer)──▶ utf-8 string ──▶ console
 * CLI ──(dir:string)──▶ readDir() ──(Dirent[])──▶ string[] (nomes) ──▶ console
 * CLI ──(root+pattern)──▶ searchFiles() ──(walk recursivo)──▶ SearchResult[] ──▶ console
 *
 * Toda operação é assíncrona (Promise-based) para não bloquear o event loop.
 *
 * Segurança: Todos os métodos que acessam o sistema de arquivos usam
 * `resolveSecurePath` para prevenir Path Traversal (escapar do diretório do projeto).
 */
export class FileReader
⋮----
/**
   * Resolve um caminho de usuário e valida que ele está dentro do diretório do projeto.
   *
   * Segurança contra Path Traversal (SEC-01, SEC-02) e Symlink Attacks (C-1):
   * 1. Resolve o caminho absoluto via path.resolve()
   * 2. Resolve symlinks via fs.promises.realpath() para evitar que atalhos maliciosos
   *    desviem do diretório do projeto (C-1)
   * 3. Também resolve symlinks do rootDir (caso o projeto em si seja um link)
   * 4. Verifica se o caminho real começa com o diretório raiz real do projeto
   * 5. Se estiver fora (ex: ../../.ssh/id_rsa via symlink), lança erro
   *
   * @param userPath Caminho fornecido pelo usuário/agente (absoluto ou relativo)
   * @returns Caminho absoluto seguro dentro do projeto (com symlinks resolvidos)
   * @throws Error se o caminho resolvido estiver fora do diretório do projeto
   */
static async resolveSecurePath(userPath: string): Promise<string>
⋮----
// Tenta resolver symlinks do caminho completo.
// Se o ficheiro não existir (ENOENT), resolve pelo diretório pai
// para ainda validar que está dentro do projeto.
⋮----
// Caminho não existe no disco → resolve pelo diretório pai
⋮----
throw err; // Outro erro (permissão, etc) — relança
⋮----
/**
   * Lê o conteúdo completo de um arquivo em disco.
   *
   * Pipeline:
   *   1. resolveSecurePath() → validação anti Path Traversal
   *   2. fsp.readFile(path, 'utf-8') → buffer binário decodificado como string
   *   3. Retorna a string diretamente
   *   4. Em caso de erro (arquivo inexistente, permissão), lança exceção
   *
   * @param filePath Caminho absoluto ou relativo do arquivo
   * @returns Conteúdo textual do arquivo
   */
async readFile(filePath: string): Promise<string>
⋮----
/**
   * Lista as entradas (arquivos e subdiretórios) de um diretório.
   *
   * Pipeline:
   *   1. resolveSecurePath() → validação anti Path Traversal
   *   2. fsp.readdir(path) → array de nomes (somente strings, sem metadados)
   *   3. Retorna o array diretamente
   *
   * @param dirPath Caminho do diretório
   * @returns Array com os nomes dos arquivos/diretórios dentro de dirPath
   */
async readDir(dirPath: string): Promise<string[]>
⋮----
/**
   * Busca recursiva em arquivos: percorre uma árvore de diretórios e encontra
   * linhas que casam com um padrão (string ou RegExp).
   *
   * Fluxo detalhado da recursão:
   *   1. Converte `pattern` string em RegExp com flag 'g' (global)
   *   2. `walk(dir)` é chamada recursivamente:
   *      a) fsp.readdir(dir, { withFileTypes: true }) → Dirent[] com metadados
   *      b) Para cada entry:
   *         - Se for diretório → walk(entry) (recursão)
   *         - Se for arquivo → lê conteúdo como utf-8, quebra em linhas,
   *           testa cada linha contra o regex
   *   3. Resultados são acumulados no array `results`
   *   4. Para quando atinge `maxResults` (padrão 100) para evitar consumo excessivo
   *
   * Tratamento de erros silencioso:
   *   - Diretórios sem permissão → ignorados (não interrompe a busca)
   *   - Arquivos binários (não UTF-8) → ignorados
   *
   * @param rootDir  Diretório raiz da busca
   * @param pattern  Padrão a buscar (string literal ou regex)
   * @param maxResults  Limite de ocorrências (padrão 100)
   * @returns Array de SearchResult ordenado por profundidade de descoberta
   */
async searchFiles(
    rootDir: string,
    pattern: string | RegExp,
    maxResults: number = 100
): Promise<SearchResult[]>
⋮----
// Converte string em RegExp — cada linha é testada individualmente.
// Sem flag 'g' para evitar falsos negativos por lastIndex (RegExp global mantém estado interno).
⋮----
/**
     * Função interna recursiva que percorre a árvore de diretórios.
     * Captura `results` e `maxResults` do closure externo via closure.
     */
const walk = async (dir: string): Promise<void> =>
⋮----
// Early return: já atingimos o limite de resultados
⋮----
// withFileTypes: true → retorna objetos Dirent em vez de strings,
// permitindo verificar isDirectory() / isFile() sem stat extra
⋮----
return; // Silencia diretórios sem permissão de leitura
⋮----
if (results.length >= maxResults) break; // Early break
⋮----
// Constrói caminho absoluto juntando dir + nome da entrada
⋮----
// Ramo recursivo: desce um nível na árvore
⋮----
// Lê o arquivo inteiro como string UTF-8
⋮----
// Divide em linhas (LF ou CRLF)
⋮----
// Itera cada linha testando o regex
⋮----
line: i + 1,      // Linhas são 1-indexed
content: lines[i].trim(), // Remove espaços extras
⋮----
// Ignora arquivos binários (não decodificáveis como UTF-8)
// ou arquivos sem permissão de leitura
⋮----
// Inicia a recursão a partir do diretório raiz

================
File: packages/core/src/core/IContextCompressor.ts
================
/**
 * IContextCompressor: Contrato para compressão de contexto (ISP + DIP).
 *
 * Responsabilidade Única: Definir o contrato de compressão de histórico
 * de mensagens ReAct, sem qualquer lógica de I/O.
 *
 * - workingMemory: bloco consolidado do histórico (sumarização/compressão)
 * - keptMessages: últimas N mensagens preservadas intactas (mínimo última resposta do assistente)
 * - compressionRatio: 0.0 (sem compressão) a 1.0 (100% comprimido), para logging
 */
⋮----
import type { ReActMessage } from './rag/ReActLoop';
⋮----
/**
 * Resultado da compressão de contexto.
 */
export interface CompressedContext {
  /** Bloco consolidado com o histórico comprimido/sumarizado */
  workingMemory: string;
  /** Últimas N mensagens preservadas intactas (mínimo última resposta do assistente) */
  keptMessages: ReActMessage[];
  /** Taxa de compressão: 0.0 (sem compressão) a 1.0 (100% comprimido) */
  compressionRatio: number;
}
⋮----
/** Bloco consolidado com o histórico comprimido/sumarizado */
⋮----
/** Últimas N mensagens preservadas intactas (mínimo última resposta do assistente) */
⋮----
/** Taxa de compressão: 0.0 (sem compressão) a 1.0 (100% comprimido) */
⋮----
/**
 * Gatilhos de compressão baseados na porcentagem de uso do contexto.
 */
export enum CompressionTrigger {
  /** Nenhuma compressão necessária */
  NONE = 'NONE',
  /** Compressão suave: >= 70% do contexto usado */
  SOFT = 'SOFT',
  /** Compressão agressiva: >= 85% do contexto usado */
  HARD = 'HARD',
}
⋮----
/** Nenhuma compressão necessária */
⋮----
/** Compressão suave: >= 70% do contexto usado */
⋮----
/** Compressão agressiva: >= 85% do contexto usado */
⋮----
/**
 * Avalia a necessidade de compressão baseada na proporção de tokens estimados
 * versus o limite máximo de contexto do modelo.
 *
 * @param estimatedTokens - Quantidade estimada de tokens no histórico atual
 * @param contextLimit - Limite máximo de tokens de contexto do modelo
 * @returns CompressionTrigger indicando o nível de compressão necessário
 *
 * @example
 * ```ts
 * assessCompressionNeed(690, 1000) // 69% → CompressionTrigger.NONE
 * assessCompressionNeed(700, 1000) // 70% → CompressionTrigger.SOFT
 * assessCompressionNeed(850, 1000) // 85% → CompressionTrigger.HARD
 * ```
 */
export function assessCompressionNeed(
  estimatedTokens: number,
  contextLimit: number,
): CompressionTrigger
⋮----
/**
 * Interface para implementadores do compressor de contexto (DIP).
 */
export interface IContextCompressor {
  /**
   * Comprime o histórico de mensagens ReAct.
   *
   * @param history - Array completo de mensagens do histórico
   * @param model - Identificador do modelo sendo usado (para limites de contexto)
   * @param systemPrompt - Prompt de sistema original
   * @returns Promise com o contexto comprimido
   */
  compress(
    history: ReActMessage[],
    model: string,
    systemPrompt: string,
  ): Promise<CompressedContext>;
}
⋮----
/**
   * Comprime o histórico de mensagens ReAct.
   *
   * @param history - Array completo de mensagens do histórico
   * @param model - Identificador do modelo sendo usado (para limites de contexto)
   * @param systemPrompt - Prompt de sistema original
   * @returns Promise com o contexto comprimido
   */
compress(
    history: ReActMessage[],
    model: string,
    systemPrompt: string,
  ): Promise<CompressedContext>;

================
File: packages/core/src/core/index.ts
================
/**
 * Core module index — barrel file que expõe toda a API pública do core.
 *
 * Separação clara entre:
 * - Utilitários base (FileReader, CommandExecutor, ToolRegistry)
 * - Fábrica de providers (ProviderFactory)
 * - Pipeline RAG (RAGManager, Chunker, Embedder, VectorStore, Retriever)
 * - Loop ReAct + PromptBuilder (ReActLoop, PromptBuilder)
 * - Validação JSON (JsonValidator)
 */
⋮----
// Utilitários base
⋮----
// Context Compressor (ISP + DIP)
⋮----
// Fábrica de providers (OCP)
⋮----
// Container de injeção de dependência (DIP)
⋮----
// Pipeline RAG
⋮----
// ReAct Loop + Prompt Builder (Strategy pattern)
⋮----
// Reflector (self-correction)
⋮----
// ErrorJournal (persistência de erros de reflexão)
⋮----
// Session Store (Memória Episódica / Multi-turn)
⋮----
// Session Manager (orquestração de sessão ativa)
⋮----
// AST Editor (edição estrutural de código)
⋮----
// SearchReplace Editor (edição por bloco exato)
⋮----
// Validação JSON

================
File: packages/core/src/core/ProviderFactory.ts
================
/**
 * ProviderFactory: Fábrica de provedores de linguagem (OCP).
 *
 * Open/Closed Principle: Esta classe está ABERTA para extensão
 * (novos provedores podem ser adicionados sem modificar código existente)
 * e FECHADA para modificação (a lógica de seleção é genérica).
 *
 * Para adicionar um novo provider:
 * 1. Crie a classe que implementa IProvider (ex: OpenRouterProvider)
 * 2. Adicione o case no switch ou no map
 * 3. Pronto — nenhuma outra parte do sistema precisa mudar
 */
⋮----
import type { IProvider, IEmbedProvider } from '../providers/types';
import { OllamaProvider } from '../providers/OllamaProvider';
⋮----
export type ProviderType = 'ollama';
⋮----
export interface ProviderConfig {
  type: ProviderType;
  host?: string;
  port?: number;
}
⋮----
export class ProviderFactory
⋮----
/**
   * Cria um provider baseado na configuração.
   *
   * @param config Configuração do provider (tipo, host, porta)
   * @returns Instância de IProvider
   */
static createProvider(config: ProviderConfig): IProvider
⋮----
/**
   * Cria um provider que também suporta embeddings (IEmbedProvider).
   * Útil para garantia em tempo de compilação de que o provider
   * implementa o método embed().
   */
static createEmbedProvider(config: ProviderConfig): IEmbedProvider

================
File: packages/core/src/core/Reflector.ts
================
/**
 * Reflector: Módulo de auto-correção (Self-Correction Layer).
 *
 * Responsabilidade Única (SRP):
 * - Receber uma resposta gerada pelo modelo
 * - Submetê-la a um sistema de crítica (segunda chamada ao provider)
 * - Retornar a versão corrigida, se necessário
 *
 * O Reflector NÃO modifica o ReActLoop — ele é um step opcional
 * acionado APÓS a resposta final ser obtida.
 *
 * Zero dependências externas — usa apenas ICritiqueProvider injetado.
 * Compatível com recursos limitados (12GB RAM): reusa o mesmo modelo,
 * sem duplicar contexto do histórico ReAct.
 */
⋮----
import type { ICritiqueProvider, CorrectionStatus } from '../providers/types';
⋮----
// ── Interfaces públicas ──
⋮----
/** Erro individual encontrado pelo crítico */
export interface ReflectionError {
  type: 'hallucination' | 'syntax' | 'inconsistency' | 'logic';
  description: string;
}
⋮----
/** Resultado completo da reflexão */
export interface ReflectionResult {
  /** Conteúdo final (corrigido ou original) */
  finalContent: string;
  /** Status da correção ('stable' | 'suspicious' | 'rejected') */
  correctionStatus: CorrectionStatus;
  /** Lista de erros encontrados (vazia se nenhum) */
  errors: ReflectionError[];
}
⋮----
/** Conteúdo final (corrigido ou original) */
⋮----
/** Status da correção ('stable' | 'suspicious' | 'rejected') */
⋮----
/** Lista de erros encontrados (vazia se nenhum) */
⋮----
// ── System Prompt de Crítica ──
⋮----
/**
 * Prompt de crítica otimizado (TASK 9).
 *
 * Otimizações:
 * - Reduzido de ~40 linhas para ~20 linhas (menos tokens consumidos)
 * - Prioriza exemplos concretos vs regras genéricas
 * - Usa verbos imperativos no início de cada seção
 * - Formato JSON simplificado
 * - Removeu seções redundantes (contradições lógicas fundidas com alucinações)
 */
⋮----
/**
 * Reflector: Executa uma chamada de crítica sobre a resposta gerada.
 *
 * Fluxo:
 *   1. Monta prompt de crítica com as tools disponíveis
 *   2. Chama critiqueProvider.critique() (via ICritiqueProvider)
 *   3. Parseia o JSON de retorno
 *   4. Decide se houve correção e retorna ReflectionResult
 *
 * SRP: Apenas reflexão crítica. Não gerencia histórico, não executa ferramentas.
 * LSP: Usa ICritiqueProvider em vez de IProvider — o Reflector não precisa
 *      de chat() genérico, apenas do método critique() especializado.
 */
export class Reflector
⋮----
/**
   * @param critiqueProvider Provider especializado para crítica de respostas
   * @param toolRegistry Registro de ferramentas (para listar tools disponíveis)
   * @throws Error se critiqueProvider não for fornecido
   */
constructor(
    private readonly critiqueProvider: ICritiqueProvider,
    private readonly toolRegistry: { getToolNames(): string[] }
)
⋮----
private readonly toolRegistry:
⋮----
/**
   * Submete a resposta do modelo à crítica e retorna o resultado.
   *
   * @param rawAnswer Resposta original gerada pelo modelo
   * @param model     Nome do modelo (mesmo usado na geração)
   * @returns         Resultado da reflexão (corrigido ou original)
   */
async reflect(rawAnswer: string, model: string): Promise<ReflectionResult>
⋮----
// Se a resposta é vazia ou só espaços, não perde tempo com crítica
⋮----
// Monta o prompt de crítica com a lista de tools disponíveis
⋮----
// Usa critique() em vez de chat() — mais específico e com melhor controle
⋮----
temperature: 0.1, // Baixa temperatura = mais determinístico
⋮----
// ── CIRCUIT BREAKER (TASK 3) ──
// Valida a qualidade da correção antes de aceitá-la
⋮----
// Se a correção é quase idêntica ao original (>90% similaridade),
// o crítico provavelmente gerou um falso positivo
⋮----
finalContent: rawAnswer, // Mantém original
correctionStatus: 'suspicious', // Sinaliza suspeita
errors, // Mantém erros reportados para transparência
⋮----
// Se a correção mudou significativamente (<20% similaridade),
// pode ser alucinação do crítico — descarta a correção
⋮----
finalContent: rawAnswer, // Mantém original
⋮----
// Correção válida: mudança moderada e com erros reportados
⋮----
// Sem erros → mantém original (stable)
⋮----
// Timeout ou erro de conexão → retorna original sem reflexão
⋮----
/**
   * Calcula similaridade entre duas strings usando Dice Coefficient
   * sobre bigramas — leve, rápido e eficaz para detectar mudanças reais.
   *
   * Circuit Breaker: evita que falsos positivos do crítico (hasError=true
   * com mudança mínima) ou alucinações (mudança drástica) contaminem
   * a resposta final.
   *
   * @returns Valor entre 0 (completamente diferente) e 1 (idêntico)
   */
private computeSimilarity(a: string, b: string): number
⋮----
const bigrams = (s: string): Set<string> =>
⋮----
/**
   * Parseia o array de erros do JSON, validando o formato.
   */
private parseErrors(raw: unknown): ReflectionError[]

================
File: packages/core/src/core/SearchReplaceEditor.ts
================
/**
 * SearchReplaceEditor — Edição por bloco exato com normalização.
 *
 * Estratégia:
 * 1. Lê o arquivo via FileReader
 * 2. Divide o conteúdo e o searchBlock em linhas
 * 3. Normaliza linha a linha (CRLF→LF, trimEnd)
 * 4. Busca um bloco contíguo de linhas normalizadas que case exatamente
 * 5. Se match único: substitui as linhas correspondentes no conteúdo original
 * 6. Se 0 matches: retorna falha (matchCount: 0)
 * 7. Se >1 matches: retorna falha (matchCount: N) sem substituir
 * 8. Escreve o arquivo modificado no disco
 */
⋮----
import { FileReader } from './FileReader';
⋮----
/**
 * Resultado da operação de busca e substituição.
 */
export interface SearchReplaceResult {
  /** true se a substituição foi bem-sucedida */
  success: boolean;
  /** Número de ocorrências do bloco de busca encontradas */
  matchCount: number;
  /** Caminho do arquivo alvo */
  filePath: string;
  /** Mensagem de erro em caso de falha */
  error?: string;
}
⋮----
/** true se a substituição foi bem-sucedida */
⋮----
/** Número de ocorrências do bloco de busca encontradas */
⋮----
/** Caminho do arquivo alvo */
⋮----
/** Mensagem de erro em caso de falha */
⋮----
export class SearchReplaceEditor
⋮----
constructor(fileReader: FileReader)
⋮----
/**
   * Normaliza uma única linha:
   * - Converte \r\n para \n (remove \r do final)
   * - Remove trailing whitespace
   * - Preserva indentação inicial
   */
private normalizeLine(line: string): string
⋮----
/**
   * Normaliza o texto completo para comparação.
   */
private normalize(text: string): string
⋮----
/**
   * Aplica uma substituição de bloco exato no arquivo.
   *
   * A busca é feita no conteúdo normalizado (CRLF→LF, trimEnd por linha),
   * mas a substituição ocorre no conteúdo original, preservando
   * terminações de linha originais e whitespace.
   *
   * @param filePath    Caminho do arquivo a ser editado
   * @param searchBlock Bloco de texto a ser encontrado
   * @param replaceBlock Bloco de texto substituto
   * @returns SearchReplaceResult com o resultado da operação
   */
async apply(
    filePath: string,
    searchBlock: string,
    replaceBlock: string
): Promise<SearchReplaceResult>
⋮----
// 1. Ler o conteúdo original do arquivo
⋮----
// 2. Dividir original e search em linhas
⋮----
// 3. Se searchBlock vazio ou sem linhas, retorna falha
⋮----
// 4. Encontrar todas as ocorrências do bloco normalizado
⋮----
// 5. Validar número de matches
⋮----
// 6. Match único — substituir as linhas no conteúdo original
⋮----
// Constrói o novo conteúdo:
// - Linhas antes do match (preservando originais)
// - Bloco de substituição (texto exato fornecido pelo usuário)
// - Linhas após o match (preservando originais)
⋮----
// 7. Validar caminho (Path Traversal) e escrever o arquivo

================
File: packages/core/src/core/SessionManager.ts
================
/**
 * SessionManager: Orquestração da sessão de conversa ativa.
 *
 * Responsabilidade Única (SRP):
 * - Gerenciar a sessão ativa (carregar, iniciar nova, adicionar mensagens).
 * - Orquestrar a persistência via ISessionStore.
 * - Fornecer o histórico formatado (ReActMessage[]) para o ReActLoop.
 * - Extrair título automaticamente da primeira mensagem do usuário.
 *
 * DIP (Dependency Inversion Principle):
 * - Depende da abstração ISessionStore, não da implementação concreta.
 * - Pode ser testado com mock de ISessionStore.
 */
⋮----
import { randomUUID } from 'node:crypto';
import type { ISessionStore, Session } from './SessionStore';
import type { ReActMessage } from './rag/ReActLoop';
⋮----
/**
 * SessionManager: interface pública para gerenciar sessões.
 */
export class SessionManager
⋮----
constructor(store: ISessionStore)
⋮----
/**
   * Retorna o ID da sessão ativa, ou null se nenhuma foi iniciada.
   */
getCurrentSessionId(): string | null
⋮----
/**
   * Retorna o título da sessão ativa, ou null.
   */
getCurrentTitle(): string | null
⋮----
/**
   * Indica se há uma sessão ativa com mensagens.
   */
isActive(): boolean
⋮----
/**
   * Retorna cópia das mensagens da sessão ativa.
   * Útil para o ReActLoop consumir como histórico.
   */
getHistory(): ReActMessage[]
⋮----
/**
   * Contagem de mensagens na sessão ativa.
   */
getMessageCount(): number
⋮----
/**
   * Carrega uma sessão existente pelo ID.
   * @param sessionId UUID da sessão a carregar
   * @returns true se a sessão foi carregada, false se não existir
   */
async loadSession(sessionId: string): Promise<boolean>
⋮----
/**
   * Cria uma nova sessão com ID único e timestamp atual.
   * Se já houver uma sessão ativa, ela é salva automaticamente antes.
   */
async newSession(model?: string): Promise<string>
⋮----
// Salva sessão anterior se existir
⋮----
/**
   * Adiciona uma mensagem ao histórico da sessão ativa.
   * Se a sessão não foi iniciada, cria uma automaticamente.
   * Se for a primeira mensagem do usuário, extrai título.
   *
   * @param message Mensagem para adicionar (role + content)
   * @param model Modelo usado (opcional, para metadado)
   */
async addMessage(message: ReActMessage, model?: string): Promise<void>
⋮----
// Garante sessão ativa
⋮----
// Extrai título da primeira mensagem do usuário (máx 80 chars)
⋮----
/**
   * Persiste a sessão ativa em disco via ISessionStore.
   * Chamado automaticamente ao trocar de sessão; também exposto
   * para ser chamado explicitamente após cada turno de conversa.
   */
async flush(): Promise<void>
⋮----
/**
   * Lista todas as sessões disponíveis (delega ao store).
   */
listSessions()
⋮----
/**
   * Deleta uma sessão específica (delega ao store).
   * Se for a sessão ativa, limpa o estado atual.
   */
async deleteSession(sessionId: string): Promise<void>
⋮----
/**
   * Extrai um título legível da primeira pergunta do usuário.
   * Pega as primeiras N palavras, no máximo 80 caracteres.
   */
private extractTitle(content: string): string
⋮----
// Remove quebras de linha e espaços extras
⋮----
// Pega primeiras 8 palavras ou até 80 chars

================
File: packages/core/src/core/SessionStore.ts
================
/**
 * SessionStore: Persistência de sessões de conversa em disco.
 *
 * Responsabilidade Única (SRP):
 * - Ler, escrever, listar e deletar arquivos de sessão (.json)
 *   no diretório .soberano/sessions/.
 * - Zero conhecimento do que é uma conversa ou ReActLoop.
 * - Usa apenas módulos nativos do Node.js (node:fs/promises).
 *
 * DIP (Dependency Inversion Principle):
 * - A interface ISessionStore é a abstração que o SessionManager
 *   e os testes usam, nunca a classe concreta diretamente.
 */
⋮----
import { mkdir, readFile, writeFile, unlink, readdir } from 'node:fs/promises';
import { join, extname } from 'node:path';
import type { ReActMessage } from './rag/ReActLoop';
⋮----
/**
 * Representa uma sessão de conversa completa, persistida em disco.
 */
export interface Session {
  /** UUID v4 — identificador único da sessão */
  id: string;
  /** ISO 8601 — timestamp de criação */
  createdAt: string;
  /** ISO 8601 — timestamp da última atualização */
  updatedAt: string;
  /** Array de mensagens da conversa (em ordem cronológica) */
  messages: ReActMessage[];
  /** Metadados opcionais (modelo usado, título, etc.) */
  metadata?: {
    /** Modelo utilizado na sessão (ex: "llama3.2:1b") */
    model?: string;
    /** Título auto-extraído da primeira pergunta (máx 80 chars) */
    title?: string;
  };
}
⋮----
/** UUID v4 — identificador único da sessão */
⋮----
/** ISO 8601 — timestamp de criação */
⋮----
/** ISO 8601 — timestamp da última atualização */
⋮----
/** Array de mensagens da conversa (em ordem cronológica) */
⋮----
/** Metadados opcionais (modelo usado, título, etc.) */
⋮----
/** Modelo utilizado na sessão (ex: "llama3.2:1b") */
⋮----
/** Título auto-extraído da primeira pergunta (máx 80 chars) */
⋮----
/**
 * Resumo leve de uma sessão para listagem (sem mensagens).
 */
export interface SessionSummary {
  id: string;
  createdAt: string;
  updatedAt: string;
  messageCount: number;
  title?: string;
}
⋮----
/** Interface para erros com código (Enoent, EACCES, etc.) */
interface FileSystemError extends Error {
  code?: string;
}
⋮----
/**
 * ISessionStore: Abstração para persistência de sessões.
 *
 * DIP (Dependency Inversion Principle):
 * - SessionManager depende desta interface, não da implementação concreta.
 * - Testes podem mockar facilmente.
 */
export interface ISessionStore {
  /** Persiste uma sessão em disco (cria ou sobrescreve) */
  save(session: Session): Promise<void>;
  /** Carrega uma sessão pelo ID. Retorna null se não existir ou corrompida. */
  load(sessionId: string): Promise<Session | null>;
  /** Lista todas as sessões disponíveis (apenas resumo). */
  list(): Promise<SessionSummary[]>;
  /** Remove uma sessão do disco. Idempotente (não erra se não existir). */
  delete(sessionId: string): Promise<void>;
}
⋮----
/** Persiste uma sessão em disco (cria ou sobrescreve) */
save(session: Session): Promise<void>;
/** Carrega uma sessão pelo ID. Retorna null se não existir ou corrompida. */
load(sessionId: string): Promise<Session | null>;
/** Lista todas as sessões disponíveis (apenas resumo). */
list(): Promise<SessionSummary[]>;
/** Remove uma sessão do disco. Idempotente (não erra se não existir). */
delete(sessionId: string): Promise<void>;
⋮----
/**
 * SessionStore: Implementação concreta de ISessionStore.
 *
 * Salva arquivos .json no diretório especificado (padrão: .soberano/sessions).
 * Usa exclusivamente módulos nativos do Node.js.
 */
export class SessionStore implements ISessionStore
⋮----
/**
   * @param baseDir Diretório base para armazenar as sessões.
   *                Padrão: '.soberano/sessions'
   */
constructor(baseDir?: string)
⋮----
/**
   * Retorna o caminho completo para o arquivo de uma sessão.
   */
private sessionPath(sessionId: string): string
⋮----
async save(session: Session): Promise<void>
⋮----
// Garante que o diretório existe
⋮----
// Atualiza o timestamp antes de salvar
⋮----
async load(sessionId: string): Promise<Session | null>
⋮----
// Arquivo não encontrado — retorna null (caso normal)
⋮----
// JSON inválido — retorna null (tolerância a corrupção)
⋮----
// Outros erros (permissão, etc.) — relança
⋮----
async list(): Promise<SessionSummary[]>
⋮----
// Diretório não existe — retorna lista vazia
⋮----
// Filtra apenas arquivos .json e paraleliza a leitura
⋮----
// Arquivos corrompidos ou ausentes são ignorados
⋮----
// Ordena por updatedAt decrescente (mais recentes primeiro)
⋮----
async delete(sessionId: string): Promise<void>
⋮----
// Se o arquivo não existe, resolve silenciosamente (idempotente)

================
File: packages/core/src/core/StatefulCompressor.ts
================
/**
 * StatefulCompressor: Implementação do IContextCompressor que usa o próprio
 * modelo de linguagem para condensar o histórico (SRP + DIP).
 *
 * Responsabilidade Única: Pegar um histórico longo de mensagens ReAct e
 * pedir ao modelo que extraia três blocos essenciais (objective, completed,
 * critical_rules), descartando o lixo da conversa e preservando o essencial.
 *
 * ISP (Interface Segregation): Depende apenas de IContextCompressor e IProvider.
 * DIP (Dependency Inversion): Recebe IProvider via construtor, não instancia nada.
 */
⋮----
import type { IContextCompressor, CompressedContext } from './IContextCompressor';
import type { IProvider } from '../providers/types';
import type { ReActMessage } from './rag/ReActLoop';
⋮----
/** Número de mensagens finais mantidas intactas após compressão */
⋮----
/**
 * Sistema de prompt usado para instruir o modelo a extrair os três blocos.
 */
⋮----
/**
 * Formata o histórico como texto para enviar ao modelo.
 */
function formatHistoryForCompression(history: ReActMessage[]): string
⋮----
/**
 * Formata o JSON parseado como Markdown estruturado para workingMemory.
 */
function formatWorkingMemory(
  objective: string,
  completed: string,
  criticalRules: string,
): string
⋮----
/**
 * Calcula a taxa de compressão: 0.0 (sem compressão) a 1.0 (100% comprimido).
 *
 * @param originalLength - Tamanho total do histórico original em caracteres
 * @param compressedLength - Tamanho do workingMemory em caracteres
 * @returns Taxa de compressão entre 0.0 e 1.0
 */
function computeCompressionRatio(originalLength: number, compressedLength: number): number
⋮----
// Clamp between 0.0 and 1.0
⋮----
export class StatefulCompressor implements IContextCompressor
⋮----
constructor(provider: IProvider)
⋮----
/**
   * Comprime o histórico de mensagens ReAct usando o modelo de linguagem.
   *
   * @param history - Array completo de mensagens do histórico
   * @param _model - Identificador do modelo sendo usado (não usado diretamente, delegado ao provider)
   * @param _systemPrompt - Prompt de sistema original (usado como contexto adicional)
   * @returns Promise com o contexto comprimido
   */
async compress(
    history: ReActMessage[],
    _model: string,
    _systemPrompt: string,
): Promise<CompressedContext>
⋮----
// Sempre preservar as últimas KEEP_LAST mensagens intactas
⋮----
// Se não há histórico suficiente para comprimir, retorna sem compressão
⋮----
// Prompt completo: system prompt + histórico + instrução de formato
⋮----
// Parse da resposta JSON
⋮----
// Falha no parse JSON — loga erro e retorna sem compressão
⋮----
// Extrair campos (com fallback para string vazia)
⋮----
// Provider lançou exceção (ex: limite de contexto) — loga erro e
// trunca mecanicamente para evitar loop infinito no ReAct.

================
File: packages/core/src/core/TokenEstimator.ts
================
/**
 * TokenEstimator — Estimador de tokens nativo e sem dependências.
 *
 * Heurística: Math.ceil(text.length / 4) ≈ 4 caracteres por token (média
 * conservadora para modelos Flash como Llama 3.2 e Phi-3).
 *
 * SRP: Responsabilidade única de estimar consumo de tokens.
 * Zero dependências externas.
 */
⋮----
import type { ReActMessage } from './rag/ReActLoop';
⋮----
/**
 * Constante com os limites de contexto conhecidos dos modelos suportados.
 * Chave = nome do modelo (tag), Valor = limite máximo de tokens.
 */
⋮----
/**
 * Limite conservador usado como fallback quando o modelo não está mapeado.
 */
⋮----
export class TokenEstimator
⋮----
/**
   * Estima o número de tokens em um texto usando a heurística Math.ceil(text.length / 4).
   *
   * @param text  Texto de entrada (pode ser vazio).
   * @returns     Número estimado de tokens (0 para string vazia).
   */
static estimate(text: string): number
⋮----
/**
   * Estima o total de tokens de um array de mensagens ReAct.
   * Soma `estimate(role + content)` para cada mensagem.
   *
   * @param messages  Array de mensagens no formato ReAct.
   * @returns         Soma total estimada de tokens.
   */
static estimateMessages(messages: ReActMessage[]): number
⋮----
/**
   * Retorna o limite de contexto (context window) para um modelo específico.
   * Se o modelo não estiver mapeado em DEFAULT_CONTEXT_WINDOWS, retorna
   * 4096 como fallback conservador.
   *
   * @param model  Nome/tag do modelo (ex.: "llama3.2:1b").
   * @returns      Limite máximo de tokens.
   */
static getLimit(model: string): number

================
File: packages/core/src/core/ToolRegistry.ts
================
/**
 * ToolRegistry: Registro de ferramentas (tools) no formato JSON Schema
 * compatível com Function Calling / Tool Use do Ollama.
 *
 * Arquitetura:
 * ┌─────────────────────────────────────────────────────────┐
 * │                   ToolRegistry                          │
 * │                                                         │
 * │  tools: Map<                                               │
 * │    string,                // nome da tool               │
 * │    {                                                    │
 * │      definition: {...},   // JSON Schema (LLM-readable) │
 * │      handler: Function    // executa a tool de fato     │
 * │    }                                                    │
 * │  >                                                      │
 * │                                                         │
 * │  + register(name, description, paramsSchema, handler)   │
 * │  + getDefinitions(): string   // JSON string p/ prompt  │
 * │  + execute(name, args): Promise    // chama handler     │
 * │  + hasTool(name): boolean                               │
 * │  + getToolNames(): string[]                              │
 * └─────────────────────────────────────────────────────────┘
 *
 * Segurança:
 * - execute() verifica se a tool existe antes de chamar o handler,
 *   prevenindo execução de funções arbitrárias.
 * - Os handlers são registrados explicitamente via register(), nunca
 *   expostos dinamicamente a partir de input do usuário/LLM.
 */
⋮----
/**
 * Definição de uma ferramenta no formato JSON Schema (padrão OpenAI/Function Calling).
 * É o que o LLM recebe para decidir qual tool chamar e com quais argumentos.
 */
export interface ToolDefinition {
  type: 'function';
  function: {
    name: string;
    description: string;
    parameters: {
      type: 'object';
      properties: Record<string, { type: string; description: string }>;
      required: string[];
    };
  };
}
⋮----
/**
 * Entrada completa de uma tool no registro: definição + handler.
 */
interface ToolEntry {
  definition: ToolDefinition;
  handler: (args: Record<string, unknown>) => Promise<string>;
}
⋮----
export class ToolRegistry
⋮----
/**
   * Registra uma nova ferramenta no registro.
   *
   * @param name        Nome único da tool (ex: 'readFile', 'execute')
   * @param description Descrição legível para o LLM
   * @param paramsSchema Schema dos parâmetros: { prop: { type, description } }
   * @param handler     Função que executa a tool. Recebe args e retorna string.
   */
register(
    name: string,
    description: string,
    paramsSchema: Record<string, { type: string; description: string }>,
    handler: (args: Record<string, unknown>) => Promise<string>
): void
⋮----
/**
   * Retorna a lista de definições de todas as tools registradas
   * serializada como JSON string, pronta para ser injetada no system prompt.
   *
   * Formato:
   * [
   *   {
   *     "type": "function",
   *     "function": { "name": "readFile", "description": "...", "parameters": {...} }
   *   },
   *   ...
   * ]
   */
getDefinitions(): string
⋮----
/**
   * Retorna a lista de nomes de tools registradas.
   * Útil para validação e para o system prompt.
   */
getToolNames(): string[]
⋮----
/**
   * Verifica se uma tool com o nome informado existe no registro.
   * Usado para validação de segurança antes de executar.
   */
hasTool(name: string): boolean
⋮----
/**
   * Executa uma tool pelo nome, passando os argumentos.
   *
   * Pipeline de segurança:
   *   1. Verifica se a tool existe no registro
   *   2. Se não existe → lança erro (previne execução arbitrária)
   *   3. Chama o handler registrado com os args fornecidos
   *   4. Retorna o resultado como string
   *
   * @param name Nome da tool a executar
   * @param args Argumentos para o handler (validados pelo JSON Schema no LLM)
   * @returns Resultado da execução como string
   */
async execute(name: string, args: Record<string, unknown>): Promise<string>

================
File: packages/core/src/providers/index.ts
================


================
File: packages/core/src/providers/OllamaHttpClient.ts
================
/**
 * OllamaHttpClient: Cliente HTTP de baixo nível para comunicação com a API Ollama.
 *
 * Responsabilidade única (SRP):
 * - Gerenciar conexões TCP, timeouts e parsing de respostas HTTP
 * - Post comum (post) — retorna Promise do JSON parseado
 * - Post com streaming (postStream) — retorna AsyncGenerator de tokens
 *
 * Zero dependências externas — usa exclusivamente node:http.
 */
export class OllamaHttpClient
⋮----
constructor(
⋮----
/**
   * Realiza uma requisição HTTP POST e retorna o corpo parseado como JSON.
   *
   * @param path  Caminho da API (ex: '/api/generate')
   * @param body  Corpo JSON já stringificado
   * @returns Promise que resolve com o objeto JSON parseado
   */
post(path: string, body: string): Promise<any>
⋮----
/**
   * Realiza uma requisição HTTP POST com streaming habilitado e
   * retorna um AsyncGenerator que emite tokens linha por linha.
   *
   * Fluxo:
   * - Conecta via http.request
   * - Aguarda req.on('response') em vez de callback (para suportar async generator)
   * - Processa chunks TCP linha a linha com buffer de linha parcial
   * - Cada linha completa é parseada como JSON e extrai o campo "response"
   * - O loop termina ao receber um objeto com "done": true
   *
   * @param path  Caminho da API (ex: '/api/generate')
   * @param body  Corpo JSON já stringificado (com stream: true)
   * @returns AsyncGenerator que emite strings (tokens individuais)
   */
async *postStream(path: string, body: string): AsyncGenerator<string>
⋮----
// Promise que resolve quando a resposta HTTP começa a chegar
⋮----
// Envia o corpo e finaliza a requisição
⋮----
// Aguarda a resposta HTTP
⋮----
// Verifica status code antes de processar streaming
⋮----
// Buffer que acumula dados até encontrar uma quebra de linha
⋮----
// Processa os chunks TCP conforme chegam
⋮----
// Processa todas as linhas completas no buffer
⋮----
// Linha vazia (ex: keep-alive ou separador SSE) → ignora
⋮----
// Tenta parsear cada linha como JSON individual
⋮----
// Extrai o token de resposta
⋮----
// Se done === true, encerra o streaming
⋮----
// Linha não-JSON (ex: espaços, comentários SSE) → ignora

================
File: packages/core/src/providers/OllamaProvider.ts
================
import type {
  ChatRequest,
  ChatResponse,
  CritiqueRequest,
  CritiqueResponse,
  EmbedResponse,
  ICritiqueProvider,
  IProvider,
} from './types';
import { OllamaHttpClient } from './OllamaHttpClient';
⋮----
/**
 * OllamaProvider: Implementação concreta do IProvider que se comunica
 * com uma instância local do Ollama via HTTP.
 *
 * Fluxo de dados completo (chat):
 * ┌──────────┐    ChatRequest     ┌──────────────────┐   JSON body    ┌─────────────────┐
 * │  CLI     │ ── {model,        │  OllamaProvider   │ ── POST ──▶  │  Ollama Server  │
 * │ (usuário)│    prompt, temp}──▶│  .chat()          │              │  localhost:11434 │
 * └──────────┘                   │  └─ post()        │ ◀── JSON ──  │  /api/generate   │
 *                                │     └─ http.request│              └─────────────────┘
 *                                └──────────────────┘
 *                                       │
 *                                  ChatResponse
 *                                  {response, model, done}
 *                                       │
 *                                       ▼
 *                                  ┌──────────┐
 *                                  │  console │
 *                                  └──────────┘
 *
 * A classe usa exclusivamente o módulo nativo `node:http` (zero dependências externas).
 * O método `chat()` é a interface pública; `post()` é o método privado que gerencia
 * a conexão TCP, timeout e parsing da resposta.
 */
export class OllamaProvider implements IProvider, ICritiqueProvider
⋮----
/** Host onde o Ollama está rodando (padrão: localhost) */
⋮----
/** Porta da API do Ollama (padrão: 11434) */
⋮----
/**
   * @param host Endereço do servidor Ollama
   * @param port Porta do servidor Ollama
   */
constructor(
    host: string = 'localhost',
    port: number = 11434,
    private readonly http: OllamaHttpClient = new OllamaHttpClient(host, port)
)
⋮----
/**
   * Envia um prompt ao Ollama e retorna a resposta gerada pelo modelo.
   *
   * Pipeline:
   *   1. Constrói o JSON body conforme a API do Ollama:
   *      - model: identificador do modelo (ex: "phi3:3b", "tinyllama:1b")
   *      - prompt: texto do usuário
   *      - stream: false → resposta única, sem streaming SSE
   *      - options.temperature: criatividade (0 = determinístico, 1 = criativo)
   *      - options.num_predict: limite de tokens na resposta
   *   2. Chama this.post('/api/generate', body) → requisição HTTP POST
   *   3. Extrai data.response, data.model, data.done da resposta JSON
   *   4. Retorna ChatResponse padronizado
   *
   * @param request Dados da requisição (modelo, prompt, parâmetros)
   * @returns Resposta do modelo encapsulada em ChatResponse
   */
async chat(request: ChatRequest): Promise<ChatResponse>
⋮----
// Monta o objeto do body de forma incremental para suportar format opcional
⋮----
// temperature: controla aleatoriedade (0-1). Padrão 0.7 se não informado.
⋮----
// num_predict: máximo de tokens a gerar. undefined = sem limite.
⋮----
// Quando format='json', ativa o Grammar Restraint nativo do Ollama
// A API /api/generate aceita "format": "json" para forçar resposta em JSON
⋮----
// Validação de robustez: se o contrato exigia JSON, verifica se a resposta
// do modelo é realmente parseável. Isso previne que alucinações ou falhas
// do modelo cheguem ao CLI como dados inválidos.
⋮----
/**
   * streamChat: Envia um prompt ao Ollama com streaming habilitado e
   * retorna um AsyncIterable que emite cada token individualmente.
   *
   * Pipeline:
   *   1. Constrói o JSON body com stream: true (SSE habilitado)
   *   2. Chama this.postStream('/api/generate', body) → AsyncGenerator
   *   3. Cada token é emitido via yield conforme chega do servidor
   *
   * O cliente (CLI) pode iterar com `for await...of` e escrever os tokens
   * no stdout em tempo real, criando o efeito "máquina de escrever".
   *
   * @param request Dados da requisição (modelo, prompt, parâmetros)
   * @returns AsyncIterable que emite strings (tokens individuais)
   */
async *streamChat(request: ChatRequest): AsyncIterable<string>
⋮----
// Delega para o gerador de streaming que lida com a resposta HTTP
⋮----
/**
   * Gera um embedding (vetor de floats) para um texto usando o modelo all-minilm.
   *
   * Pipeline:
   *   1. Envia POST /api/embed com { model, input, keep_alive }
   *   2. Extrai embeddings[0] do response
   *   3. Retorna o vetor de números
   *
   * O parâmetro keep_alive controla quanto tempo o modelo fica carregado na RAM:
   *   - "0s": descarrega imediatamente (economiza RAM)
   *   - "30s": mantém carregado por 30s (útil para indexação em lote)
   *   - "-1": mantém indefinidamente (não recomendado com 12GB RAM)
   *
   * @param text  O texto a ser embedado
   * @param embedModel  Nome do modelo de embedding (padrão: all-minilm)
   * @param keepAlive  Tempo de vida do modelo na RAM (padrão: "30s")
   * @returns Vetor de floats representando o embedding
   */
async embed(
    text: string,
    embedModel: string = 'all-minilm',
    keepAlive: string = '30s'
): Promise<number[]>
⋮----
/**
   * critique: Submete um prompt de crítica ao modelo com formatação JSON.
   *
   * Diferenças do chat() normal:
   * - stream: false (sempre resposta completa, não streaming)
   * - format: 'json' (força o Ollama a retornar JSON válido)
   * - temperature: 0.1 (baixa para ser mais determinístico)
   * - num_predict: 1024 (limita tokens da crítica)
   *
   * @param request Prompt de crítica + resposta a analisar
   * @returns CritiqueResponse com JSON parseado e rawText
   */
async critique(request: CritiqueRequest): Promise<CritiqueResponse>
⋮----
// Fallback seguro: se o modelo não retornar JSON válido, devolve vazio
⋮----
// post() e postStream() foram extraídos para OllamaHttpClient (SRP)
// Veja src/providers/OllamaHttpClient.ts

================
File: packages/core/src/providers/types.ts
================
export interface ChatRequest {
  /** Modelo Ollama a ser usado (ex: "tinyllama:1b", "phi3:3b") */
  model: string;
  /** Prompt do usuário */
  prompt: string;
  /** Temperatura para criatividade (0-1), padrão 0.7 */
  temperature?: number;
  /** Máximo de tokens na resposta */
  max_tokens?: number;
  /** Força o modelo a responder em JSON estrito (ativa format: 'json' no Ollama) */
  format?: 'json';
}
⋮----
/** Modelo Ollama a ser usado (ex: "tinyllama:1b", "phi3:3b") */
⋮----
/** Prompt do usuário */
⋮----
/** Temperatura para criatividade (0-1), padrão 0.7 */
⋮----
/** Máximo de tokens na resposta */
⋮----
/** Força o modelo a responder em JSON estrito (ativa format: 'json' no Ollama) */
⋮----
export interface ChatResponse {
  /** Resposta completa gerada pelo modelo */
  response: string;
  /** Modelo utilizado */
  model: string;
  /** Se a resposta foi truncada */
  done: boolean;
}
⋮----
/** Resposta completa gerada pelo modelo */
⋮----
/** Modelo utilizado */
⋮----
/** Se a resposta foi truncada */
⋮----
export interface EmbedResponse {
  /** Array de embeddings gerados (cada input gera um vetor) */
  embeddings: number[][];
}
⋮----
/** Array de embeddings gerados (cada input gera um vetor) */
⋮----
/**
 * IProvider: Contrato base para provedores de linguagem.
 * 
 * LSP (Liskov Substitution): Qualquer classe que implemente IProvider
 * pode ser usada no lugar de OllamaProvider sem quebrar o sistema.
 * 
 * O método embed() é opcional na interface base (para providers
 * que não suportam embeddings, como APIs de chat puro).
 */
export interface IProvider {
  /** Nome do provider para logging */
  readonly name: string;
  /** Envia um prompt e recebe a resposta do modelo */
  chat(request: ChatRequest): Promise<ChatResponse>;
  /**
   * Gera embedding vetorial para um texto.
   * Opcional por padrão — providers que não suportam embedding
   * podem deixar sem implementação.
   */
  embed?(text: string, embedModel?: string, keepAlive?: string): Promise<number[]>;
  /**
   * Streaming: retorna tokens incrementalmente via AsyncIterable.
   * Opcional — providers que não suportam streaming ou modo legado
   * podem deixar sem implementação (fallback para chat()).
   */
  streamChat?(request: ChatRequest): AsyncIterable<string>;
}
⋮----
/** Nome do provider para logging */
⋮----
/** Envia um prompt e recebe a resposta do modelo */
chat(request: ChatRequest): Promise<ChatResponse>;
/**
   * Gera embedding vetorial para um texto.
   * Opcional por padrão — providers que não suportam embedding
   * podem deixar sem implementação.
   */
embed?(text: string, embedModel?: string, keepAlive?: string): Promise<number[]>;
/**
   * Streaming: retorna tokens incrementalmente via AsyncIterable.
   * Opcional — providers que não suportam streaming ou modo legado
   * podem deixar sem implementação (fallback para chat()).
   */
streamChat?(request: ChatRequest): AsyncIterable<string>;
⋮----
/**
 * IEmbedProvider: Extensão de IProvider para provedores que
 * suportam embeddings (RAG, busca semântica, etc.).
 * 
 * Ao contrário de IProvider.embed() que é opcional, esta interface
 * garante que o método existe em tempo de compilação.
 */
export interface IEmbedProvider extends IProvider {
  embed(text: string, embedModel?: string, keepAlive?: string): Promise<number[]>;
}
⋮----
embed(text: string, embedModel?: string, keepAlive?: string): Promise<number[]>;
⋮----
// ── Interfaces de Crítica (Self-Correction) ──
⋮----
export interface CritiqueRequest {
  /** Modelo a ser usado para a crítica */
  model: string;
  /** Prompt completo (system prompt + resposta a analisar) */
  prompt: string;
  /** Temperatura para a crítica (baixa = mais determinístico) */
  temperature?: number;
}
⋮----
/** Modelo a ser usado para a crítica */
⋮----
/** Prompt completo (system prompt + resposta a analisar) */
⋮----
/** Temperatura para a crítica (baixa = mais determinístico) */
⋮----
export interface CritiqueResponse {
  /** JSON parseado da resposta do crítico */
  parsedJson: Record<string, unknown>;
  /** Texto cru antes do parse (para debug/fallback) */
  rawText: string;
}
⋮----
/** JSON parseado da resposta do crítico */
⋮----
/** Texto cru antes do parse (para debug/fallback) */
⋮----
/**
 * Status da correção aplicada pelo Reflector:
 * - 'stable'   → correção confiável, aplicada com sucesso
 * - 'suspicious' → correção aplicada mas com ressalvas (ex: tamanho muito reduzido)
 * - 'rejected'  → correção rejeitada (ex: similaridade muito baixa com original)
 */
export type CorrectionStatus = 'stable' | 'suspicious' | 'rejected';
⋮----
/**
 * ICritiqueProvider: Interface para provedores que suportam crítica de respostas.
 *
 * LSP (Liskov Substitution): Cada provider implementa o método critique()
 * da sua própria forma (Ollama usa format:'json', OpenAI usaria response_format,
 * Anthropic usaria prompt engineering), sem vazar detalhes de implementação
 * para o Reflector.
 *
 * SRP: Responsabilidade única — submeter um prompt de crítica e retornar JSON.
 */
export interface ICritiqueProvider {
  /** Nome do provider para logging */
  readonly name: string;
  /**
   * Submete um prompt de crítica e retorna o JSON parseado.
   *
   * @param request Prompt completo de crítica + resposta a analisar
   * @returns Resposta parseada (parsedJson + rawText para fallback)
   */
  critique(request: CritiqueRequest): Promise<CritiqueResponse>;
}
⋮----
/** Nome do provider para logging */
⋮----
/**
   * Submete um prompt de crítica e retorna o JSON parseado.
   *
   * @param request Prompt completo de crítica + resposta a analisar
   * @returns Resposta parseada (parsedJson + rawText para fallback)
   */
critique(request: CritiqueRequest): Promise<CritiqueResponse>;

================
File: packages/core/src/validation/JsonValidator.ts
================
/**
 * JsonValidator: Validação de respostas JSON de modelos de linguagem.
 *
 * Responsabilidade Única (SRP): Esta classe existe APENAS para validar
 * se uma string é um JSON válido. Ela não formata prompts, não gerencia
 * conexões HTTP, não lida com provedores de linguagem.
 *
 * Uso típico:
 *   const validator = new JsonValidator();
 *   const parsed = validator.validate(rawResponse);
 *   // Se falhar, lança ValidationError com preview do conteúdo inválido
 */
⋮----
export class ValidationError extends Error
⋮----
constructor(message: string, raw: string)
⋮----
export class JsonValidator
⋮----
/**
   * Valida se uma string é um JSON válido e retorna o objeto parseado.
   *
   * @param raw String bruta recebida do modelo
   * @param contextLabel Rótulo para mensagem de erro (ex: "Ollama response")
   * @returns Objeto parseado
   * @throws ValidationError se o JSON for inválido
   */
validate<T = Record<string, unknown>>(raw: string, contextLabel: string = 'response'): T
⋮----
/**
   * Valida que a resposta do modelo, quando solicitado format:'json',
   * é realmente um JSON válido. Se falhar, retorna null em vez de lançar
   * exceção (útil para fallbacks).
   */
tryValidate<T = Record<string, unknown>>(raw: string): T | null

================
File: packages/core/src/index.ts
================
/**
 * Root barrel file for @soberano/core package.
 *
 * Re-exports everything from src/core/index.ts so that
 * `import { AppContext, ReActLoop, ... } from '@soberano/core'`
 * resolves correctly to dist/index.js (as declared in package.json).
 */

================
File: packages/core/tests/integration/editing-pipeline.test.ts
================
/**
 * editing-pipeline.test.ts — Teste de Integração do Pipeline Completo de Edição.
 *
 * ═══════════════════════════════════════════════════════════════════════════
 * AVISO: Este é o ÚNICO conjunto de testes neste projeto que acessa o
 * filesystem real (cria, lê, edita e deleta arquivos em disco).
 * Todos os demais testes usam mocks para FileReader / fs.
 * ═══════════════════════════════════════════════════════════════════════════
 *
 * Pipeline simulado:
 * 1. Cria arquivo TypeScript temporário em tests/integration/.tmp/
 * 2. ASTEditor.replaceSymbol() → substitui função por nome de símbolo
 * 3. Verifica que o arquivo foi alterado corretamente
 * 4. Cria arquivo não-TypeScript (texto) temporário
 * 5. SearchReplaceEditor.apply() → substitui bloco exato
 * 6. Verifica que o arquivo foi alterado corretamente
 * 7. Limpeza: afterEach deleta todos os arquivos temporários
 */
⋮----
import { describe, it, expect, afterEach, beforeAll, afterAll } from 'vitest';
⋮----
import { FileReader } from '../../src/core/FileReader';
import { ASTEditor } from '../../src/core/ASTEditor';
import { SearchReplaceEditor } from '../../src/core/SearchReplaceEditor';
⋮----
// ── Helpers ──────────────────────────────────────────────────────
⋮----
/** Diretório temporário dentro do projeto (respeita FileReader.resolveSecurePath) */
⋮----
/** Cria um arquivo temporário com conteúdo e registra para limpeza */
async function createTempFile(filename: string, content: string): Promise<string>
⋮----
/** Lê arquivo como string */
async function readFile(filePath: string): Promise<string>
⋮----
// ── Setup / Teardown ─────────────────────────────────────────────
⋮----
// Remove diretório temporário e todo o seu conteúdo
⋮----
// Ignora se já foi removido
⋮----
// Ignora se já foi deletado
⋮----
// ── Testes ───────────────────────────────────────────────────────
⋮----
// ──────────────────────────────────────────────────────────────────
// ASTEditor.replaceSymbol() em arquivo TypeScript
// ──────────────────────────────────────────────────────────────────
⋮----
// Substituir a função "add" por uma nova implementação
⋮----
// Verificar conteúdo do arquivo pós-edição
⋮----
// A função 'add' original foi substituída — verificar que o código antigo sumiu
⋮----
// ──────────────────────────────────────────────────────────────────
// SearchReplaceEditor.apply() em arquivo não-TypeScript
// ──────────────────────────────────────────────────────────────────
⋮----
// O erro será sobre bloco não encontrado, não sobre caminho negado
⋮----
// ──────────────────────────────────────────────────────────────────
// Pipeline Completo (TypeScript + texto em sequência)
// ──────────────────────────────────────────────────────────────────
⋮----
// ── Etapa 1: Criar um arquivo .ts com função + comentário ──
⋮----
// ── Etapa 2: ASTEditor.replaceSymbol() em "divide" ──
⋮----
// ── Etapa 3: Verificar alteração AST ──
⋮----
// '// TODO: documentar' era leading trivia de 'divide', então foi substituído junto
⋮----
// ── Etapa 4: SearchReplaceEditor.apply() no mesmo arquivo .ts ──
// NOTA: '// TODO: documentar' foi consumido como leading trivia do símbolo 'divide'
// no passo AST. Vamos substituir o cabeçalho do arquivo que ainda está intacto.
⋮----
// ── Etapa 5: Verificar edições combinadas ──

================
File: packages/core/tests/unit/cli/ChatStrategy.test.ts
================
/**
 * ChatStrategy.test.ts — Testes para buildSystemPrompt()
 *
 * Cobertura:
 * - Bloco de workflow de edição aparece quando tools de edição estão registradas
 * - Bloco NÃO aparece sem as tools
 * - Bloco tem no máximo 15 linhas
 */
⋮----
import { describe, it, expect, vi } from 'vitest';
import { buildSystemPrompt } from '../../../src/cli/strategies/ChatStrategy';
import type { AppContext } from '../../../src/core/AppContext';
⋮----
// ── Factories ────────────────────────────────────────────────────
⋮----
function makeToolRegistry(toolNames: string[])
⋮----
function makeMockApp(toolRegistry: ReturnType<typeof makeToolRegistry>): AppContext
⋮----
// ── Testes ───────────────────────────────────────────────────────
⋮----
// Extrai o bloco de workflow

================
File: packages/core/tests/unit/cli/commands.test.ts
================
/**
 * commands.test.ts — Testes unitários para o roteador de comandos CLI.
 *
 * Cobertura:
 * - Comandos individuais: read, dir, search, exec, chat
 * - Modos: normal e stream
 * - Erros: prompt vazio, comando desconhecido, falta de argumentos
 * - Sessões: listagem, addMessage, flush (multi-turn)
 *
 * Arquitetura:
 *   runCommand() recebe CliArgs parseados e usa AppContext.
 *   Para testar, mockamos AppContext com um provider mock que suporta streamChat.
 *
 * IMPORTANTE:
 *   vi.mock() é HOISTED para o topo do arquivo. Tudo que as factories precisam
 *   referenciar deve ser criado via vi.hoisted().
 */
import { describe, it, expect, vi, beforeEach } from 'vitest';
⋮----
// ═══════════════════════════════════════════════════════════════════════
// Estado compartilhado (vi.hoisted → NÃO é hoisted, executado antes)
// ═══════════════════════════════════════════════════════════════════════
⋮----
// ── Objetos mock ──
⋮----
// streamChat como async generator
⋮----
// ── Mock SessionManager ──
⋮----
// ── Classes mock construtíveis ──
class MockCtx
⋮----
get fileReader()
get commandExecutor()
get toolRegistry()
get provider()
get embedProvider()
get model()
get jsonMode()
get ragDir()
get sessionManager()
getSessionHistory()
initialize()
⋮----
class MockStrat
⋮----
class MockReAct
⋮----
// ═══════════════════════════════════════════════════════════════════════
// Mocks (hoisted — podem referenciar vi.hoisted vars)
// ═══════════════════════════════════════════════════════════════════════
⋮----
// ═══════════════════════════════════════════════════════════════════════
// Imports (depois dos mocks)
// ═══════════════════════════════════════════════════════════════════════
⋮----
import { runCommand } from '../../../src/cli/commands';
import type { CliArgs } from '../../../src/cli/commands';
⋮----
// ═══════════════════════════════════════════════════════════════════════
// Testes
// ═══════════════════════════════════════════════════════════════════════
⋮----
// Reseta estado entre testes
⋮----
// Garante streamChat existe (pode ter sido deletado)
⋮----
// ── Helpers ──
function makeArgs(
    command: string,
    args: string[],
    flags: Record<string, string | boolean> = {}
): CliArgs
⋮----
// ── read ──
⋮----
// ── dir ──
⋮----
// ── search ──
⋮----
// ── exec ──
⋮----
// ── chat ──
⋮----
// Reseta contadores dos mocks de sessão entre testes de chat
⋮----
// Remove streamChat dinamicamente
⋮----
// ── sessions ──
⋮----
// ── Comando desconhecido ──

================
File: packages/core/tests/unit/core/ASTEditor.test.ts
================
/**
 * Tests for ASTEditor — Edição estrutural por nome de símbolo.
 *
 * Cenários:
 * ✅ Sucesso: substituição de função mantém resto do arquivo intacto
 * ✅ Símbolo não encontrado: success: false, symbolFound: false
 * ✅ Arquivo inexistente: success: false, mensagem de erro clara
 * ✅ Substituição de classe, interface, variável, type alias
 * ✅ getFullStart() usado corretamente (preserva comentários/whitespace anteriores)
 * ✅ Path traversal bloqueado: caminhos fora do projeto são rejeitados
 */
⋮----
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { ASTEditor } from '../../../src/core/ASTEditor';
import type { FileReader } from '../../../src/core/FileReader';
import { FileReader as FileReaderImpl } from '../../../src/core/FileReader';
⋮----
// Mock do fs nativo
⋮----
// getFullStart() inclui whitespace/trivia anteriores ao nó,
// então o conteúdo ANTES do nó termina em 'from 'bar';\n'
// e o newCode precisa incluir o whitespace que estava antes
⋮----
// getFullStart() consome o '\n\n' antes de 'function', então a saída fica colada no ';'
⋮----
// getFullStart() consome '// top comment\n\n' junto com o nó
⋮----
// getFullStart() includes leading comments, so they get replaced along with the node
⋮----
// Arquivo não deve ter sido escrito
⋮----
// getFullStart() consome o '\n' antes de 'export default'

================
File: packages/core/tests/unit/core/CommandExecutor.test.ts
================
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { CommandExecutor } from '../../../src/core/CommandExecutor';
⋮----
/**
 * Testes unitários para CommandExecutor.
 *
 * Mock completo do módulo nativo `node:child_process` → zero execuções reais.
 * Zero consumo de CPU, zero I/O, zero risco de segurança.
 */
⋮----
// Mock dos eventos do child_process
type EventHandler = (...args: any[]) => void;
⋮----
function emit(event: string, ...args: any[])
⋮----
const spawn = (_cmd: string, _args: string[], _opts: any)
⋮----
// Simula dados no stdout
⋮----
// Simula fechamento do processo com exitCode 0
⋮----
// Emite stderr
⋮----
// Fecha com exitCode 1
⋮----
// Emite erro de spawn

================
File: packages/core/tests/unit/core/ErrorJournal.test.ts
================
/**
 * Testes unitários do ErrorJournal (TASK 6, ARCH-04).
 *
 * Cobertura:
 * - addEntry e persistência assíncrona
 * - getEntries ordenação FIFO reversa
 * - getRecentEntries com filtro
 * - getStats agregação
 * - Carregamento de arquivo corrompido
 */
⋮----
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { ErrorJournal } from '../../../src/core';
⋮----
// Limpa resíduos de testes anteriores
try { fs.rmSync(testDir, { recursive: true }); } catch { /* ok */ }
⋮----
try { fs.rmSync(testDir, { recursive: true }); } catch { /* ok */ }
⋮----
// Verifica que foi persistido em disco
⋮----
expect(entries[0].type).toBe('logic'); // Mais recente primeiro
⋮----
expect(entries).toHaveLength(3); // Apenas os 3 últimos
expect(entries[0].description).toBe('Erro 5'); // Mais recente
expect(entries[2].description).toBe('Erro 3'); // Mais antigo dos 3
⋮----
// Cria arquivo inválido

================
File: packages/core/tests/unit/core/IContextCompressor.test.ts
================
import { describe, it, expect } from 'vitest';
import { assessCompressionNeed, CompressionTrigger } from '../../../src/core/IContextCompressor';
⋮----
// 69% → NONE
⋮----
// 70% → SOFT
⋮----
// 85% → HARD

================
File: packages/core/tests/unit/core/Reflector.test.ts
================
/**
 * Reflector.test.ts — Testes unitários para o Reflector (self-correction).
 *
 * Cobertura:
 * 1. Instanciação com critiqueProvider válido
 * 2. Instanciação com critiqueProvider inválido (lança erro)
 * 3. reflect() com resposta correta (stable)
 * 4. reflect() com resposta incorreta (corrected → stable)
 * 5. reflect() com resposta vazia (stable)
 * 6. reflect() com resposta só com espaços (stable)
 * 7. reflect() com JSON inválido do provider (fallback → rejected)
 * 8. reflect() com múltiplos erros
 * 9. Integração com toolRegistry
 * 10. Fallback em caso de erro de rede (rejected)
 * 11. correctedOutput vazio com hasError=true (mantém original)
 */
⋮----
import { describe, it, expect, vi } from 'vitest';
import { Reflector } from '../../../src/core/Reflector';
import type { ICritiqueProvider } from '../../../src/providers/types';
⋮----
// ── Helpers ──
⋮----
/** ToolRegistry mock padrão */
⋮----
/** Cria um critiqueProvider mock que retorna respostas previsíveis */
function createMockCritiqueProvider(
  responses: Record<string, { parsedJson: Record<string, unknown>; rawText: string }>
): ICritiqueProvider
⋮----
// ── Testes ──
⋮----
// eslint-disable-next-line @typescript-eslint/no-explicit-any
⋮----
// eslint-disable-next-line @typescript-eslint/no-explicit-any
⋮----
// Provider que retorna parsedJson vazio (simula JSON inválido)
⋮----
// Fallback: retorna o conteúdo original sem reflexão
⋮----
// hasError=true mas correctedOutput vazio → mantém original

================
File: packages/core/tests/unit/core/SearchReplaceEditor.test.ts
================
/**
 * Tests for SearchReplaceEditor — Edição por bloco exato com normalização.
 *
 * Cenários (Critério de Aceitação):
 * ✅ Match exato (sucesso): substituição ocorre, arquivo é escrito
 * ✅ Zero matches (falha): matchCount: 0, arquivo não escrito
 * ✅ Múltiplos matches (falha): matchCount: N, mensagem pedindo bloco mais específico
 * ✅ \r\n vs \n (sucesso): normalização trata diferença de CRLF
 * ✅ Trailing whitespace diferente (sucesso): trimEnd normaliza
 * ✅ Indentação diferente (falha): indentação é significativa
 * ✅ Path traversal bloqueado: caminhos fora do projeto são rejeitados
 */
⋮----
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { SearchReplaceEditor } from '../../../src/core/SearchReplaceEditor';
import type { FileReader } from '../../../src/core/FileReader';
⋮----
// Mock do fs nativo
⋮----
// Conteúdo com LF, search com CRLF
⋮----
// A busca encontra 'a\nb\nc' (normalizado), e substitui no original
⋮----
// search tem trailing whitespace em uma linha
⋮----
// search com indentação diferente (3 espaços vs 2)
⋮----
// fs.writeFile will still be called, but with the outside path SearchReplaceEditor
// will catch the error from resolveSecurePath
// We simulate this by checking the path validation in SearchReplaceEditor
⋮----
// Path outside project: should be blocked by resolveSecurePath
⋮----
expect(result.matchCount).toBe(1); // match encontrado, mas escrita falhou

================
File: packages/core/tests/unit/core/SessionManager.test.ts
================
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { SessionManager } from '../../../src/core/SessionManager';
import type { ISessionStore, Session } from '../../../src/core/SessionStore';
⋮----
// ── Mock do ISessionStore ──
⋮----
function createMockStore(): ISessionStore
⋮----
// ── Estado inicial ──
⋮----
// ── addMessage (criação automática) ──
⋮----
// Título continua sendo o da primeira
⋮----
const longMessage = 'a '.repeat(50); // 100 chars
⋮----
// ── newSession ──
⋮----
expect(store.save).toHaveBeenCalledTimes(0); // flush implícito no newSession
⋮----
// save deve ter sido chamado após newSession (flush)
⋮----
// Acessa internamente via addMessage depois
⋮----
// ── loadSession ──
⋮----
// ── flush ──
⋮----
// ── listSessions ──
⋮----
// ── deleteSession ──

================
File: packages/core/tests/unit/core/SessionStore.test.ts
================
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { SessionStore, Session } from '../../../src/core/SessionStore';
⋮----
// ── Mocks do node:fs/promises ──
⋮----
// Reseta chamadas e implementações de todos os mocks
⋮----
// ── save() ──
⋮----
// Verifica que mkdir foi chamado com recursive true
⋮----
// Verifica que writeFile foi chamado com o caminho correto
⋮----
// Extrai o JSON passado para writeFile e verifica o conteúdo
⋮----
// updatedAt deve ter sido atualizado (não é mais o original)
⋮----
// ── load() ──
⋮----
// ── list() ──
⋮----
// Deve vir primeiro o mais recente (b)
⋮----
// Isola completamente este teste: redefine o mock
⋮----
// Força a ordem: primeiro invalido.json, depois valido.json
⋮----
// ── delete() ──
⋮----
// Deve resolver sem lançar

================
File: packages/core/tests/unit/core/StatefulCompressor.test.ts
================
import { describe, it, expect, vi } from 'vitest';
import { StatefulCompressor } from '../../../src/core/StatefulCompressor';
import type { IProvider, ChatRequest, ChatResponse } from '../../../src/providers/types';
import type { ReActMessage } from '../../../src/core/rag/ReActLoop';
⋮----
/**
 * Cria um mock de IProvider que pode ser configurado por teste.
 */
function createMockProvider(
  response?: string,
  shouldThrow = false,
  throwMessage = 'Provider error',
): IProvider
⋮----
/**
 * Cria um array de mensagens ReAct para teste.
 */
function createHistory(count: number): ReActMessage[]
⋮----
// Verificar workingMemory contém os três campos formatados
⋮----
// Verificar keptMessages tem exatamente as últimas 3 mensagens
⋮----
// Verificar compressionRatio é menor que 1.0
⋮----
// Verificar que o provider foi chamado com os parâmetros corretos
⋮----
// Apenas campos não-vazios devem aparecer
⋮----
// Resposta não-JSON do modelo
⋮----
// Deve retornar o histórico completo em keptMessages
⋮----
// workingMemory deve ser vazio
⋮----
// compressionRatio deve ser 1.0 (100% comprimido = falhou)
⋮----
// JSON válido mas sem os campos esperados
⋮----
// Campos vazios → workingMemory com placeholder
⋮----
const history = createHistory(3); // Apenas 3 mensagens
⋮----
// Com 3 mensagens ou menos, histórico vai inteiro pra keptMessages
⋮----
// Provider não deve ser chamado quando não há o que comprimir
⋮----
// Deve retornar apenas as últimas 3 mensagens para evitar loop infinito
⋮----
// workingMemory deve ser vazio
⋮----
// compressionRatio deve ser 1.0
⋮----
// Garantir que nenhuma exceção é lançada
⋮----
// Sem histórico, nada foi comprimido — ratio é 1.0 (sem dados processados)
⋮----
// Campos não-string devem ser tratados como vazios
⋮----
const history = createHistory(4); // 1 para comprimir + 3 para keptMessages

================
File: packages/core/tests/unit/core/TokenEstimator.test.ts
================
/**
 * Testes unitários para TokenEstimator
 *
 * Critérios de aceitação (TASK 01):
 * 1. string vazia retorna 0
 * 2. string de 400 chars retorna 100
 * 3. array de mensagens soma corretamente
 * 4. modelo desconhecido retorna 4096
 * Zero dependências externas.
 */
⋮----
import { describe, it, expect } from 'vitest';
import { TokenEstimator, DEFAULT_CONTEXT_WINDOWS } from '../../../src/core/TokenEstimator';
import type { ReActMessage } from '../../../src/core/rag/ReActLoop';
⋮----
// ---------------------------------------------------------------------------
// estimate()
// ---------------------------------------------------------------------------
⋮----
expect(TokenEstimator.estimate('   ')).toBe(1); // Math.ceil(3/4) = 1
⋮----
expect(TokenEstimator.estimate(text)).toBe(100); // Math.ceil(400/4) = 100
⋮----
expect(TokenEstimator.estimate('abc')).toBe(1);   // Math.ceil(3/4) = 1
expect(TokenEstimator.estimate('abcd')).toBe(1);  // Math.ceil(4/4) = 1
expect(TokenEstimator.estimate('abcde')).toBe(2); // Math.ceil(5/4) = 2
⋮----
// ---------------------------------------------------------------------------
// estimateMessages()
// ---------------------------------------------------------------------------
⋮----
{ role: 'user', content: 'hello' },         // "userhello" = 9 chars => ceil(9/4)=3
{ role: 'assistant', content: 'world' },     // "assistantworld" = 14 chars => ceil(14/4)=4
{ role: 'system', content: '' },             // "system" = 6 chars => ceil(6/4)=2
⋮----
// 3 + 4 + 2 = 9
⋮----
{ role: 'user', content: 'a'.repeat(400) }, // "user" + 400 = 404 chars => ceil(404/4)=101
⋮----
// ---------------------------------------------------------------------------
// getLimit()
// ---------------------------------------------------------------------------
⋮----
// ---------------------------------------------------------------------------
// DEFAULT_CONTEXT_WINDOWS (constante exportada)
// ---------------------------------------------------------------------------

================
File: packages/core/tests/unit/core/ToolRegistry.test.ts
================
import { describe, it, expect, vi } from 'vitest';
import { ToolRegistry } from '../../../src/core/ToolRegistry';
⋮----
/**
 * Testes unitários para ToolRegistry.
 *
 * Nenhuma dependência externa — apenas Map interno e handlers mockados.
 * Zero consumo de CPU por Ollama, zero I/O.
 */

================
File: packages/core/tests/unit/graph/ASTRelationshipExtractor.test.ts
================
/**
 * Testes unitários para ASTRelationshipExtractor.
 *
 * Cobre:
 * - Extração de funções, classes, interfaces do AST
 * - CALLS entre funções no mesmo arquivo
 * - IMPORTS de módulos
 * - EXTENDS e IMPLEMENTS
 * - BELONGS_TO e CONTAINS (arquivo ↔ símbolo)
 * - Limite de MAX_EDGES_PER_FILE (200)
 * - Código vazio, inválido
 * - Extensão não suportada (ex: .md, .json)
 * - Auto-referências e built-ins
 */
⋮----
import { describe, it, expect, vi } from 'vitest';
import type { IASTParser, ASTNode } from '../../../src/core/rag/IASTParser';
import { ASTRelationshipExtractor } from '../../../src/core/rag/graph/ASTRelationshipExtractor';
⋮----
/**
 * Mock de IASTParser que retorna ASTNodes previsíveis.
 * Evita depender do TypescriptASTAdapter real.
 */
function createMockParser(nodes: ASTNode[]): IASTParser
⋮----
// =========================================================================
// Nós e arquivos básicos
// =========================================================================
⋮----
expect(result.nodes.length).toBeGreaterThanOrEqual(2); // file + function
⋮----
// =========================================================================
// Relação BELONGS_TO / CONTAINS
// =========================================================================
⋮----
// BELONGS_TO: símbolo → arquivo
⋮----
// CONTAINS: arquivo → símbolo
⋮----
// =========================================================================
// CALLS
// =========================================================================
⋮----
// A função myFunc() chama helper() que está definida no mesmo arquivo
⋮----
// =========================================================================
// IMPORTS
// =========================================================================
⋮----
// =========================================================================
// EXTENDS
// =========================================================================
⋮----
// =========================================================================
// IMPLEMENTS
// =========================================================================
⋮----
// =========================================================================
// Casos de borda
// =========================================================================
⋮----
// Deve respeitar o limite MAX_EDGES_PER_FILE (200)
⋮----
// refB aparece sem chamada → deve gerar REFERENCES

================
File: packages/core/tests/unit/graph/GraphBuilder.test.ts
================
/**
 * Testes unitários para GraphBuilder.
 *
 * Cobre:
 * - build() com diretório vazio (sem arquivos)
 * - build() com arquivos .ts e .js
 * - build() incremental (arquivos não modificados são ignorados)
 * - build() detecta arquivos removidos
 * - build() respeita maxFiles
 * - build() ignora node_modules, .git, .soberano, dist
 * - Erro ao ler arquivo não interrompe o processo
 * - Reload do grafo existente
 */
⋮----
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { GraphBuilder } from '../../../src/core/rag/graph/GraphBuilder';
import type { IRelationshipExtractor } from '../../../src/core/rag/graph/IRelationshipExtractor';
import type { KnowledgeGraph, GraphNode, GraphEdge } from '../../../src/core/rag/graph/types';
⋮----
// ----- Mocks globais do fs para evitar tocar no disco real -----
⋮----
/**
 * Mock do FileReader: retorna conteúdo arbitrário ou diretório.
 */
function createMockFileReader()
⋮----
/**
 * Mock do IGraphStore.
 */
function createMockGraphStore()
⋮----
/**
 * Helper para criar um nó de grafo.
 */
function makeNode(id: string, label: string, filePath: string, type: GraphNode['type'] = 'function'): GraphNode
⋮----
/**
 * Helper para criar uma aresta.
 */
function makeEdge(from: string, to: string, type: GraphEdge['type'] = 'CALLS'): GraphEdge
⋮----
// Reseta mocks do fs
⋮----
// =========================================================================
// build - diretório vazio
// =========================================================================
⋮----
// =========================================================================
// build - com arquivos fonte
// =========================================================================
⋮----
// readDir do FileReader retorna lista de entradas
⋮----
// fs.stat retorna que é um arquivo .ts
⋮----
// Cada entrada é um arquivo (não diretório)
⋮----
// =========================================================================
// build - incremental (arquivos não modificados)
// =========================================================================
⋮----
// Mesmo mtime do grafo existente → não modifica
⋮----
// Quando não há nada para processar nem remover, save não é chamado
⋮----
// =========================================================================
// build - detecta arquivos removidos
// =========================================================================
⋮----
// =========================================================================
// build - respeita maxFiles
// =========================================================================
⋮----
// =========================================================================
// build - ignora pastas de sistema
// =========================================================================
⋮----
// Primeiro, FileReader.readDir é chamado para /tmp/proj → contém file.ts + pastas
// Depois, o walk tenta entrar em node_modules, .git, etc — readDir retorna vazio ou são pulados
⋮----
// file.ts é um arquivo
⋮----
// Pastas bloqueadas: node_modules, .git, .soberano, dist
// O GraphBuilder checa o basename, não o stat, então o walk nunca chama stat para essas pastas
⋮----
// =========================================================================
// build - erro ao ler arquivo não interrompe
// =========================================================================
⋮----
// =========================================================================
// build - reload do grafo existente
// =========================================================================
⋮----
// Segundo build: load retorna grafo salvo (mockStore.save salvou internamente)
// Agora o mtime é diferente, então processa novamente

================
File: packages/core/tests/unit/graph/GraphRAGManager.test.ts
================
/**
 * Testes unitários para GraphRAGManager.
 *
 * Cobre:
 * - search() com grafo vazio (sem nós)
 * - search() expande vizinhança corretamente
 * - search() com profundidade de grafo diferente
 * - search() mescla e re-ranqueia resultados
 * - search() textWeight = 1.0 (apenas textual)
 * - search() textWeight = 0.0 (apenas estrutural)
 * - search() filtra por edgeTypes
 * - formatGraphContext() com e sem nós visitados
 */
⋮----
import { describe, it, expect, beforeEach, vi } from 'vitest';
import {
  GraphRAGManager,
  type GraphSearchOptions,
} from '../../../src/core/rag/graph/GraphRAGManager';
import { Retriever } from '../../../src/core/rag/Retriever';
import type { IGraphQuery, NeighborNode } from '../../../src/core/rag/graph/IGraphQuery';
import type { ChunkEntry } from '../../../src/core/rag/VectorStore';
import type { RelationshipType } from '../../../src/core/rag/graph/types';
⋮----
// ----- helpers de teste -----
⋮----
/** Cria um chunk simples */
function makeChunk(
  id: string,
  filePath: string,
  text: string,
  startLine = 1,
  endLine = 10
): ChunkEntry
⋮----
/** Cria um vizinho do grafo */
function makeNeighbor(
  id: string,
  label: string,
  filePath: string,
  edgeType: RelationshipType,
  distance: number
): NeighborNode
⋮----
// =========================================================================
// search - grafo vazio
// =========================================================================
⋮----
// getNodeByChunkId retorna null para todos (grafo vazio)
⋮----
// =========================================================================
// search - expansão de vizinhança
// =========================================================================
⋮----
// Nó no grafo para c1
⋮----
// Vizinhança de n-a: nó n-b (CALLS)
⋮----
// Deve ter expandido
⋮----
// =========================================================================
// search - profundidade de grafo diferente
// =========================================================================
⋮----
// =========================================================================
// search - mescla e re-ranqueia resultados
// =========================================================================
⋮----
// Chunks com embedding similar ao query
⋮----
// chunk3 está no mesmo arquivo do vizinho de c1
⋮----
// Expandindo n-c1 → encontra vizinho neighbor-file
⋮----
// Deve ter pelo menos 1 match
⋮----
// expandNeighborhood foi chamado
⋮----
// =========================================================================
// search - textWeight = 1.0 (apenas textual)
// =========================================================================
⋮----
// Todos os matches devem ter score textual puro
⋮----
// =========================================================================
// search - textWeight = 0.0 (apenas estrutural)
// =========================================================================
⋮----
// Pode ter encontrado neighbor via expansão
⋮----
// =========================================================================
// search - filtra por edgeTypes
// =========================================================================
⋮----
// =========================================================================
// formatGraphContext
// =========================================================================
⋮----
// Executa busca primeiro para ter um resultado
⋮----
// Deve conter o caminho do arquivo
⋮----
// Deve conter a label do chunk

================
File: packages/core/tests/unit/graph/JsonGraphStore.test.ts
================
/**
 * Testes unitários para JsonGraphStore.
 *
 * Cobre:
 * - save(), load(), clear()
 * - upsertNodes(), upsertEdges()
 * - getNodeByChunkId(), expandNeighborhood(), getNodesByFile()
 * - Idempotência de upserts (chave composta)
 * - Grafo não carregado (adjacency = null)
 */
⋮----
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
⋮----
import { JsonGraphStore, hashId } from '../../../src/core/rag/graph/JsonGraphStore';
import type { KnowledgeGraph, GraphNode, GraphEdge } from '../../../src/core/rag/graph/types';
⋮----
/**
 * Cria um diretório temporário isolado para cada teste.
 * Garante zero efeito colateral entre testes.
 */
function createTempDir(): string
⋮----
// =========================================================================
// save / load / clear
// =========================================================================
⋮----
// =========================================================================
// upsertNodes
// =========================================================================
⋮----
// =========================================================================
// upsertEdges
// =========================================================================
⋮----
await store.upsertEdges([edge]); // mesmo exato
⋮----
// =========================================================================
// IGraphQuery - getNodeByChunkId
// =========================================================================
⋮----
// =========================================================================
// IGraphQuery - expandNeighborhood
// =========================================================================
⋮----
// a -> b -> c
⋮----
// =========================================================================
// IGraphQuery - getNodesByFile
// =========================================================================
⋮----
// =========================================================================
// hashId
// =========================================================================
⋮----
expect(h1).toBe(h2);   // determinístico
expect(h1).not.toBe(h3); // entradas diferentes → hashes diferentes

================
File: packages/core/tests/unit/providers/OllamaProvider.test.ts
================
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { OllamaProvider } from '../../../src/providers/OllamaProvider';
import type { ChatRequest } from '../../../src/providers/types';
⋮----
/**
 * Testes unitários para OllamaProvider.
 *
 * Mock do módulo nativo `node:http` → zero requisições reais.
 * Zero consumo de CPU por Ollama, zero I/O de rede.
 * Compatível com hardware de 12GB — sem carregar modelos.
 */
⋮----
// Mock completo do node:http
⋮----
// Função que simula http.request
const request = (
    options: any,
    callback?: (res: any) => void
): any =>
⋮----
// Encontra a resposta mockada baseada no path
⋮----
// Simula IncomingMessage — emite dados como string (não Buffer)
⋮----
// Iterator assíncrono para for await...of (postStream)
⋮----
// Se callback foi passado (modo post tradicional), chama direto
⋮----
// Objeto req com suporte a req.on('response') usado pelo postStream
⋮----
// postStream usa req.on('response') em vez de callback
⋮----
// Verifica se a requisição foi montada corretamente
⋮----
// Verifica a resposta
⋮----
// Consome o generator
⋮----
'',                       // vazia (ignora)
'\n',                     // linha em branco
⋮----
'not json line\n',        // não-JSON (ignora)

================
File: packages/core/tests/unit/rag/ASTChunkerService.test.ts
================
/**
 * Tests for ASTChunkerService
 *
 * Validações:
 * - Chunk de arquivo .ts usa AST; .md usa fallback textual
 * - AST parse bem-sucedido gera chunks baseados em nós semânticos
 * - Nós pequenos (≤ MAX_CHUNK_SIZE) geram 1 chunk cada
 * - Classe grande é subdividida em signature + métodos
 * - Nó grande não-classe é dividido por linhas respeitando MAX_CHUNK_SIZE
 * - Fallback textual para código inválido ou sem extensão reconhecida
 * - Fallback para parse que retorna array vazio
 * - Respeita MAX_CHUNKS_PER_FILE (não explode com arquivo gigante)
 */
⋮----
import { describe, it, expect, vi } from 'vitest';
import { ASTChunkerService } from '../../../src/core/rag/ASTChunkerService';
import type { IChunker, ChunkResult } from '../../../src/core/rag/IChunker';
import type { IASTParser, ASTNode } from '../../../src/core/rag/IASTParser';
⋮----
// Factory helper
function createService(params?: {
  astParser?: IASTParser;
  fallbackChunker?: IChunker;
}): ASTChunkerService
⋮----
// Classe precisa ter > 2000 chars para entrar no splitLargeClass
⋮----
// Garantir que classe tem > 2000 chars
⋮----
endLine: 2 + methods.length, // 2 = class header + footer
⋮----
// Deve ter pelo menos signature + métodos
⋮----
// Primeiro chunk = assinatura da classe
⋮----
// Deve ter método method0
⋮----
// Gera uma função grande com > 2000 chars
⋮----
// Deve ter dividido em múltiplos chunks
⋮----
// Todos os chunks juntos devem manter o conteúdo original
⋮----
// Gera 60 nós pequenos

================
File: packages/core/tests/unit/rag/Chunker.test.ts
================
import { describe, it, expect } from 'vitest';
import { Chunker } from '../../../src/core/rag/Chunker';
⋮----
/**
 * Testes unitários para Chunker.
 *
 * Função pura, sem dependências, sem I/O.
 * Zero consumo de CPU além do necessário.
 */
⋮----
expect(chunks[1].startLine).toBe(4); // +1 pelo \n\n
⋮----
// Cria um texto longo com sentenças reais (com pontuação para split funcionar)
⋮----
const longParagraph = sentence.repeat(60); // ~6000 chars, > 2000
⋮----
// Deve ter gerado múltiplos chunks
⋮----
// Cada chunk (exceto talvez o último) deve ter no máximo 2000 chars
⋮----
// Conteúdo com 60 parágrafos

================
File: packages/core/tests/unit/rag/ReActLoop.test.ts
================
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { ReActLoop, type ReActMessage } from '../../../src/core/rag/ReActLoop';
import { CommandExecutor } from '../../../src/core/CommandExecutor';
import { ToolRegistry } from '../../../src/core/ToolRegistry';
import type { IProvider } from '../../../src/providers/types';
import type { IContextCompressor, CompressedContext } from '../../../src/core/IContextCompressor';
import { assessCompressionNeed, CompressionTrigger } from '../../../src/core/IContextCompressor';
import { TokenEstimator } from '../../../src/core/TokenEstimator';
⋮----
/**
 * Testes unitários para ReActLoop.
 *
 * Mock das dependências IProvider, CommandExecutor e ToolRegistry →
 * zero requisições ao Ollama, zero execução de comandos reais, zero tool calls reais.
 * Zero consumo de CPU, zero I/O.
 *
 * Cobre ambos os modos:
 * - Text mode (ACTION / FINAL_ANSWER) — legado, com CommandExecutor
 * - JSON mode (tool_call / final_response) — novo, com ToolRegistry
 */
⋮----
// ── Helpers de mock ──
⋮----
function createMockProvider(responses: string[]): IProvider
⋮----
/**
 * Cria um mock de provider com streamChat implementado.
 * Usa um AsyncGenerator que itera sobre os tokens da resposta.
 */
function createMockStreamProvider(responseContent: string): IProvider
⋮----
// Yield character by character para simular streaming real
⋮----
function createMockExecutor(): CommandExecutor
⋮----
function createMockToolRegistry(): ToolRegistry
⋮----
// ── Suite de testes ──
⋮----
expect(result.iterations).toBe(5); // MAX_ITERATIONS real
⋮----
// Deve ter passado format: 'json'
⋮----
// ToolRegistry deve ter executado readFile
⋮----
// Mesma tool_call duas vezes consecutivas → detecta loop
⋮----
// Fallback retorna o conteúdo cru
⋮----
// Sempre retorna tool_call (nunca final_response)
⋮----
// Sem Reflector (undefined)
⋮----
// Deve ter recebido tokens via callback (streamChat foi usado)
⋮----
// Sem streamChat — proposital
⋮----
// onToken nunca deve ser chamado porque streamChat não existe
⋮----
// streamChat não deve ser chamado porque stream está undefined
⋮----
// Provider com streamChat: a primeira chamada retorna tool_call, a segunda final_response
// O sendPrompt sempre prefere streamChat sobre chat quando streamOpts está ativo,
// então o streamMock precisa ser configurado para múltiplas chamadas.
⋮----
// streamChat foi chamado duas vezes (tool_call + final_response)
⋮----
// Tokens da última iteração (final_response)
⋮----
// ── Suite de testes: Compressão de Contexto ──
⋮----
/**
   * Cria um compressor mock que retorna um CompressedContext previsível.
   */
function createMockCompressor(): IContextCompressor
⋮----
// Com tinyllama:1b (limit=4096), 70% = ~2867 tokens ≈ ~11468 chars
// Um histórico curto de ~10 mensagens curtas fica bem abaixo disso
⋮----
'Seja breve', // systemPrompt pequeno
⋮----
// tinyllama:1b tem limit=4096. 70% = 2867 tokens ≈ 11468 chars
// Criar um histórico longo o suficiente para estourar 70%
// Cada mensagem: role + content ≈ 200 chars → ~50 tokens cada
// 2900 tokens / 50 tokens/msg ≈ 58 mensagens
// systemPrompt também conta, então vamos usar 60 mensagens de ~230 chars cada
⋮----
// Spy no compress para verificar argumentos
⋮----
// O resultado deve conter FINAL_ANSWER (provido pelo mock)

================
File: packages/core/tests/unit/rag/Retriever.test.ts
================
import { describe, it, expect } from 'vitest';
import { cosineSimilarity, Retriever } from '../../../src/core/rag/Retriever';
import type { ChunkEntry } from '../../../src/core/rag/VectorStore';
⋮----
/**
 * Testes unitários para Retriever e cosineSimilarity.
 *
 * Nenhuma dependência externa — funções matemáticas/transformação pura.
 * Zero consumo de CPU por Ollama, zero I/O.
 */
⋮----
function unitVector(index: number): number[]
⋮----
// O prefixo "Documentos relevantes:\n\n" é adicionado antes do truncamento
// então o tamanho pode passar ligeiramente de 200, mas deve ficar abaixo do texto completo

================
File: packages/core/tests/unit/rag/TypescriptASTAdapter.test.ts
================
/**
 * Tests for TypescriptASTAdapter
 *
 * Validações:
 * - Parse de function declaration com nome
 * - Parse de class declaration com métodos
 * - Parse de interface e type alias
 * - Parse de enum
 * - Parse de arrow function em const
 * - Array vazio para código inválido / vazio
 * - Ordenação por startLine
 * - Preservação de texto fonte e line numbers
 * - Métodos dentro de classe (MethodDeclaration, Constructor)
 * - ExportAssignment (export default)
 * - ModuleDeclaration (namespace)
 */
⋮----
import { describe, it, expect } from 'vitest';
import { TypescriptASTAdapter } from '../../../src/core/rag/TypescriptASTAdapter';
⋮----
// TS parser tolera erros, mas não deve crashar

================
File: packages/core/tests/unit/validation/JsonValidator.test.ts
================
import { describe, it, expect } from 'vitest';
import { JsonValidator, ValidationError } from '../../../src/validation/JsonValidator';
⋮----
/**
 * Testes unitários para JsonValidator.
 *
 * Função pura, sem dependências, sem I/O.
 * Zero consumo de CPU além do necessário.
 */
⋮----
interface MeuTipo {
      nome: string;
      idade: number;
    }

================
File: packages/core/package.json
================
{
  "name": "@soberano/core",
  "version": "1.0.0",
  "description": "Soberano-Core: Agente IA modular com Core, Providers e CLI",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli/index.ts",
    "start": "node dist/cli/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": ["ai", "agent", "ollama", "cli"],
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "vitest": "^4.1.5"
  }
}

================
File: packages/core/tsconfig.json
================
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020"],
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "types": ["node"],
    "declarationMap": true,
    "sourceMap": true,
    "composite": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "tests"]
}

================
File: packages/core/tsconfig.tsbuildinfo
================
{"fileNames":["../../node_modules/typescript/lib/lib.es5.d.ts","../../node_modules/typescript/lib/lib.es2015.d.ts","../../node_modules/typescript/lib/lib.es2016.d.ts","../../node_modules/typescript/lib/lib.es2017.d.ts","../../node_modules/typescript/lib/lib.es2018.d.ts","../../node_modules/typescript/lib/lib.es2019.d.ts","../../node_modules/typescript/lib/lib.es2020.d.ts","../../node_modules/typescript/lib/lib.es2015.core.d.ts","../../node_modules/typescript/lib/lib.es2015.collection.d.ts","../../node_modules/typescript/lib/lib.es2015.generator.d.ts","../../node_modules/typescript/lib/lib.es2015.iterable.d.ts","../../node_modules/typescript/lib/lib.es2015.promise.d.ts","../../node_modules/typescript/lib/lib.es2015.proxy.d.ts","../../node_modules/typescript/lib/lib.es2015.reflect.d.ts","../../node_modules/typescript/lib/lib.es2015.symbol.d.ts","../../node_modules/typescript/lib/lib.es2015.symbol.wellknown.d.ts","../../node_modules/typescript/lib/lib.es2016.array.include.d.ts","../../node_modules/typescript/lib/lib.es2016.intl.d.ts","../../node_modules/typescript/lib/lib.es2017.arraybuffer.d.ts","../../node_modules/typescript/lib/lib.es2017.date.d.ts","../../node_modules/typescript/lib/lib.es2017.object.d.ts","../../node_modules/typescript/lib/lib.es2017.sharedmemory.d.ts","../../node_modules/typescript/lib/lib.es2017.string.d.ts","../../node_modules/typescript/lib/lib.es2017.intl.d.ts","../../node_modules/typescript/lib/lib.es2017.typedarrays.d.ts","../../node_modules/typescript/lib/lib.es2018.asyncgenerator.d.ts","../../node_modules/typescript/lib/lib.es2018.asynciterable.d.ts","../../node_modules/typescript/lib/lib.es2018.intl.d.ts","../../node_modules/typescript/lib/lib.es2018.promise.d.ts","../../node_modules/typescript/lib/lib.es2018.regexp.d.ts","../../node_modules/typescript/lib/lib.es2019.array.d.ts","../../node_modules/typescript/lib/lib.es2019.object.d.ts","../../node_modules/typescript/lib/lib.es2019.string.d.ts","../../node_modules/typescript/lib/lib.es2019.symbol.d.ts","../../node_modules/typescript/lib/lib.es2019.intl.d.ts","../../node_modules/typescript/lib/lib.es2020.bigint.d.ts","../../node_modules/typescript/lib/lib.es2020.date.d.ts","../../node_modules/typescript/lib/lib.es2020.promise.d.ts","../../node_modules/typescript/lib/lib.es2020.sharedmemory.d.ts","../../node_modules/typescript/lib/lib.es2020.string.d.ts","../../node_modules/typescript/lib/lib.es2020.symbol.wellknown.d.ts","../../node_modules/typescript/lib/lib.es2020.intl.d.ts","../../node_modules/typescript/lib/lib.es2020.number.d.ts","../../node_modules/typescript/lib/lib.decorators.d.ts","../../node_modules/typescript/lib/lib.decorators.legacy.d.ts","./src/core/FileReader.ts","./src/core/CommandExecutor.ts","./src/core/ToolRegistry.ts","./src/providers/types.ts","./src/core/Reflector.ts","./src/validation/JsonValidator.ts","./src/core/IContextCompressor.ts","./src/core/rag/ReActLoop.ts","./src/core/TokenEstimator.ts","./src/core/StatefulCompressor.ts","./src/providers/OllamaHttpClient.ts","./src/providers/OllamaProvider.ts","./src/core/ProviderFactory.ts","./src/core/SessionStore.ts","./src/core/SessionManager.ts","../../node_modules/typescript/lib/typescript.d.ts","./src/core/astUtils.ts","./src/core/ASTEditor.ts","./src/core/SearchReplaceEditor.ts","./src/core/AppContext.ts","./src/core/rag/IChunker.ts","./src/core/rag/Chunker.ts","./src/core/rag/Embedder.ts","./src/core/rag/VectorStore.ts","./src/core/rag/Retriever.ts","./src/core/rag/graph/types.ts","./src/core/rag/graph/IRelationshipExtractor.ts","./src/core/rag/graph/IGraphStore.ts","./src/core/rag/graph/GraphBuilder.ts","./src/core/rag/graph/IGraphQuery.ts","./src/core/rag/IASTParser.ts","./src/core/rag/graph/JsonGraphStore.ts","./src/core/rag/graph/ASTRelationshipExtractor.ts","./src/core/rag/TypescriptASTAdapter.ts","./src/core/rag/ASTChunkerService.ts","./src/core/rag/graph/GraphRAGManager.ts","./src/core/rag/RAGManager.ts","./src/core/ErrorJournal.ts","./src/core/index.ts","./src/index.ts","./src/cli/strategies/ChatStrategy.ts","./src/cli/strategies/StreamStrategy.ts","./src/cli/strategies/ReActStrategy.ts","./src/cli/strategies/index.ts","./src/cli/commands.ts","./src/cli/index.ts","./src/core/rag/graph/index.ts","./src/core/rag/index.ts","./src/providers/index.ts","../../node_modules/@types/node/compatibility/disposable.d.ts","../../node_modules/@types/node/compatibility/indexable.d.ts","../../node_modules/@types/node/compatibility/iterators.d.ts","../../node_modules/@types/node/compatibility/index.d.ts","../../node_modules/@types/node/globals.typedarray.d.ts","../../node_modules/@types/node/buffer.buffer.d.ts","../../node_modules/@types/node/globals.d.ts","../../node_modules/@types/node/web-globals/abortcontroller.d.ts","../../node_modules/@types/node/web-globals/domexception.d.ts","../../node_modules/@types/node/web-globals/events.d.ts","../../node_modules/undici-types/header.d.ts","../../node_modules/undici-types/readable.d.ts","../../node_modules/undici-types/file.d.ts","../../node_modules/undici-types/fetch.d.ts","../../node_modules/undici-types/formdata.d.ts","../../node_modules/undici-types/connector.d.ts","../../node_modules/undici-types/client.d.ts","../../node_modules/undici-types/errors.d.ts","../../node_modules/undici-types/dispatcher.d.ts","../../node_modules/undici-types/global-dispatcher.d.ts","../../node_modules/undici-types/global-origin.d.ts","../../node_modules/undici-types/pool-stats.d.ts","../../node_modules/undici-types/pool.d.ts","../../node_modules/undici-types/handlers.d.ts","../../node_modules/undici-types/balanced-pool.d.ts","../../node_modules/undici-types/agent.d.ts","../../node_modules/undici-types/mock-interceptor.d.ts","../../node_modules/undici-types/mock-agent.d.ts","../../node_modules/undici-types/mock-client.d.ts","../../node_modules/undici-types/mock-pool.d.ts","../../node_modules/undici-types/mock-errors.d.ts","../../node_modules/undici-types/proxy-agent.d.ts","../../node_modules/undici-types/env-http-proxy-agent.d.ts","../../node_modules/undici-types/retry-handler.d.ts","../../node_modules/undici-types/retry-agent.d.ts","../../node_modules/undici-types/api.d.ts","../../node_modules/undici-types/interceptors.d.ts","../../node_modules/undici-types/util.d.ts","../../node_modules/undici-types/cookies.d.ts","../../node_modules/undici-types/patch.d.ts","../../node_modules/undici-types/websocket.d.ts","../../node_modules/undici-types/eventsource.d.ts","../../node_modules/undici-types/filereader.d.ts","../../node_modules/undici-types/diagnostics-channel.d.ts","../../node_modules/undici-types/content-type.d.ts","../../node_modules/undici-types/cache.d.ts","../../node_modules/undici-types/index.d.ts","../../node_modules/@types/node/web-globals/fetch.d.ts","../../node_modules/@types/node/assert.d.ts","../../node_modules/@types/node/assert/strict.d.ts","../../node_modules/@types/node/async_hooks.d.ts","../../node_modules/@types/node/buffer.d.ts","../../node_modules/@types/node/child_process.d.ts","../../node_modules/@types/node/cluster.d.ts","../../node_modules/@types/node/console.d.ts","../../node_modules/@types/node/constants.d.ts","../../node_modules/@types/node/crypto.d.ts","../../node_modules/@types/node/dgram.d.ts","../../node_modules/@types/node/diagnostics_channel.d.ts","../../node_modules/@types/node/dns.d.ts","../../node_modules/@types/node/dns/promises.d.ts","../../node_modules/@types/node/domain.d.ts","../../node_modules/@types/node/events.d.ts","../../node_modules/@types/node/fs.d.ts","../../node_modules/@types/node/fs/promises.d.ts","../../node_modules/@types/node/http.d.ts","../../node_modules/@types/node/http2.d.ts","../../node_modules/@types/node/https.d.ts","../../node_modules/@types/node/inspector.generated.d.ts","../../node_modules/@types/node/module.d.ts","../../node_modules/@types/node/net.d.ts","../../node_modules/@types/node/os.d.ts","../../node_modules/@types/node/path.d.ts","../../node_modules/@types/node/perf_hooks.d.ts","../../node_modules/@types/node/process.d.ts","../../node_modules/@types/node/punycode.d.ts","../../node_modules/@types/node/querystring.d.ts","../../node_modules/@types/node/readline.d.ts","../../node_modules/@types/node/readline/promises.d.ts","../../node_modules/@types/node/repl.d.ts","../../node_modules/@types/node/sea.d.ts","../../node_modules/@types/node/stream.d.ts","../../node_modules/@types/node/stream/promises.d.ts","../../node_modules/@types/node/stream/consumers.d.ts","../../node_modules/@types/node/stream/web.d.ts","../../node_modules/@types/node/string_decoder.d.ts","../../node_modules/@types/node/test.d.ts","../../node_modules/@types/node/timers.d.ts","../../node_modules/@types/node/timers/promises.d.ts","../../node_modules/@types/node/tls.d.ts","../../node_modules/@types/node/trace_events.d.ts","../../node_modules/@types/node/tty.d.ts","../../node_modules/@types/node/url.d.ts","../../node_modules/@types/node/util.d.ts","../../node_modules/@types/node/v8.d.ts","../../node_modules/@types/node/vm.d.ts","../../node_modules/@types/node/wasi.d.ts","../../node_modules/@types/node/worker_threads.d.ts","../../node_modules/@types/node/zlib.d.ts","../../node_modules/@types/node/index.d.ts"],"fileIdsList":[[100,143,146],[100,145,146],[146],[100,146,151,179],[100,146,147,152,157,165,176,187],[100,146,147,148,157,165],[100,146],[95,96,97,100,146],[100,146,149,188],[100,146,150,151,158,166],[100,146,151,176,184],[100,146,152,154,157,165],[100,145,146,153],[100,146,154,155],[100,146,156,157],[100,145,146,157],[100,146,157,158,159,176,187],[100,146,157,158,159,172,176,179],[100,146,154,157,160,165,176,187],[100,146,157,158,160,161,165,176,184,187],[100,146,160,162,176,184,187],[98,99,100,101,102,103,104,142,143,144,145,146,147,148,149,150,151,152,153,154,155,156,157,158,159,160,161,162,163,164,165,166,167,168,169,170,171,172,173,174,175,176,177,178,179,180,181,182,183,184,185,186,187,188,189,190,191,192,193],[100,146,157,163],[100,146,164,187,192],[100,146,154,157,165,176],[100,146,166],[100,146,167],[100,145,146,168],[100,143,144,145,146,147,148,149,150,151,152,153,154,155,156,157,158,159,160,161,162,163,164,165,166,167,168,169,170,171,172,173,174,175,176,177,178,179,180,181,182,183,184,185,186,187,188,189,190,191,192,193],[100,146,170],[100,146,171],[100,146,157,172,173],[100,146,172,174,188,190],[100,146,157,176,177,179],[100,146,178,179],[100,146,176,177],[100,146,179],[100,146,180],[100,143,146,176,181],[100,146,157,182,183],[100,146,182,183],[100,146,151,165,176,184],[100,146,185],[100,146,165,186],[100,146,160,171,187],[100,146,151,188],[100,146,176,189],[100,146,164,190],[100,146,191],[100,141,146],[100,141,146,157,159,168,176,179,187,190,192],[100,146,176,193],[100,113,117,146,187],[100,113,146,176,187],[100,108,146],[100,110,113,146,184,187],[100,146,165,184],[100,146,194],[100,108,146,194],[100,110,113,146,165,187],[100,105,106,109,112,146,157,176,187],[100,113,120,146],[100,105,111,146],[100,113,134,135,146],[100,109,113,146,179,187,194],[100,134,146,194],[100,107,108,146,194],[100,113,146],[100,107,108,109,110,111,112,113,114,115,117,118,119,120,121,122,123,124,125,126,127,128,129,130,131,132,133,135,136,137,138,139,140,146],[100,113,128,146],[100,113,120,121,146],[100,111,113,121,122,146],[100,112,146],[100,105,108,113,146],[100,113,117,121,122,146],[100,117,146],[100,111,113,116,146,187],[100,105,110,113,120,146],[100,146,176],[100,108,113,134,146,192,194],[84,89,100,146],[90,100,146],[49,84,100,146],[49,84,86,100,146],[86,100,146],[86,87,88,100,146],[46,61,62,100,146,159],[46,47,48,49,53,58,59,60,63,64,100,146,159,167],[100,146,147],[49,100,146,159,167],[100,146,158,159,167],[53,100,146],[49,57,100,146],[49,100,146],[46,100,146,159],[53,59,100,146,151],[53,100,146,159,167],[49,52,53,100,146],[61,100,146],[46,47,48,50,51,52,53,54,55,58,59,60,63,64,65,66,67,68,69,70,82,83,100,146],[66,76,100,146],[66,100,146],[46,49,66,67,68,69,70,72,73,74,75,77,78,79,80,81,100,146,158,159,167],[47,48,49,50,51,52,54,100,146],[69,100,146],[61,62,76,100,146],[100,146,159,167],[66,71,72,76,77,100,146],[46,71,72,73,100,146,158,159,167],[69,70,75,100,146],[71,100,146],[71,73,75,100,146,151,159,167],[71,72,73,74,75,77,78,81,100,146],[53,66,67,68,69,70,76,79,80,82,92,100,146],[84,100,146],[100,146,160],[49,56,100,146],[49,56,57,100,146]],"fileInfos":[{"version":"c430d44666289dae81f30fa7b2edebf186ecc91a2d4c71266ea6ae76388792e1","affectsGlobalScope":true,"impliedFormat":1},{"version":"45b7ab580deca34ae9729e97c13cfd999df04416a79116c3bfb483804f85ded4","impliedFormat":1},{"version":"3facaf05f0c5fc569c5649dd359892c98a85557e3e0c847964caeb67076f4d75","impliedFormat":1},{"version":"e44bb8bbac7f10ecc786703fe0a6a4b952189f908707980ba8f3c8975a760962","impliedFormat":1},{"version":"5e1c4c362065a6b95ff952c0eab010f04dcd2c3494e813b493ecfd4fcb9fc0d8","impliedFormat":1},{"version":"68d73b4a11549f9c0b7d352d10e91e5dca8faa3322bfb77b661839c42b1ddec7","impliedFormat":1},{"version":"5efce4fc3c29ea84e8928f97adec086e3dc876365e0982cc8479a07954a3efd4","impliedFormat":1},{"version":"c57796738e7f83dbc4b8e65132f11a377649c00dd3eee333f672b8f0a6bea671","affectsGlobalScope":true,"impliedFormat":1},{"version":"dc2df20b1bcdc8c2d34af4926e2c3ab15ffe1160a63e58b7e09833f616efff44","affectsGlobalScope":true,"impliedFormat":1},{"version":"515d0b7b9bea2e31ea4ec968e9edd2c39d3eebf4a2d5cbd04e88639819ae3b71","affectsGlobalScope":true,"impliedFormat":1},{"version":"0559b1f683ac7505ae451f9a96ce4c3c92bdc71411651ca6ddb0e88baaaad6a3","affectsGlobalScope":true,"impliedFormat":1},{"version":"0dc1e7ceda9b8b9b455c3a2d67b0412feab00bd2f66656cd8850e8831b08b537","affectsGlobalScope":true,"impliedFormat":1},{"version":"ce691fb9e5c64efb9547083e4a34091bcbe5bdb41027e310ebba8f7d96a98671","affectsGlobalScope":true,"impliedFormat":1},{"version":"8d697a2a929a5fcb38b7a65594020fcef05ec1630804a33748829c5ff53640d0","affectsGlobalScope":true,"impliedFormat":1},{"version":"4ff2a353abf8a80ee399af572debb8faab2d33ad38c4b4474cff7f26e7653b8d","affectsGlobalScope":true,"impliedFormat":1},{"version":"fb0f136d372979348d59b3f5020b4cdb81b5504192b1cacff5d1fbba29378aa1","affectsGlobalScope":true,"impliedFormat":1},{"version":"d15bea3d62cbbdb9797079416b8ac375ae99162a7fba5de2c6c505446486ac0a","affectsGlobalScope":true,"impliedFormat":1},{"version":"68d18b664c9d32a7336a70235958b8997ebc1c3b8505f4f1ae2b7e7753b87618","affectsGlobalScope":true,"impliedFormat":1},{"version":"eb3d66c8327153d8fa7dd03f9c58d351107fe824c79e9b56b462935176cdf12a","affectsGlobalScope":true,"impliedFormat":1},{"version":"38f0219c9e23c915ef9790ab1d680440d95419ad264816fa15009a8851e79119","affectsGlobalScope":true,"impliedFormat":1},{"version":"69ab18c3b76cd9b1be3d188eaf8bba06112ebbe2f47f6c322b5105a6fbc45a2e","affectsGlobalScope":true,"impliedFormat":1},{"version":"a680117f487a4d2f30ea46f1b4b7f58bef1480456e18ba53ee85c2746eeca012","affectsGlobalScope":true,"impliedFormat":1},{"version":"2f11ff796926e0832f9ae148008138ad583bd181899ab7dd768a2666700b1893","affectsGlobalScope":true,"impliedFormat":1},{"version":"4de680d5bb41c17f7f68e0419412ca23c98d5749dcaaea1896172f06435891fc","affectsGlobalScope":true,"impliedFormat":1},{"version":"954296b30da6d508a104a3a0b5d96b76495c709785c1d11610908e63481ee667","affectsGlobalScope":true,"impliedFormat":1},{"version":"ac9538681b19688c8eae65811b329d3744af679e0bdfa5d842d0e32524c73e1c","affectsGlobalScope":true,"impliedFormat":1},{"version":"0a969edff4bd52585473d24995c5ef223f6652d6ef46193309b3921d65dd4376","affectsGlobalScope":true,"impliedFormat":1},{"version":"9e9fbd7030c440b33d021da145d3232984c8bb7916f277e8ffd3dc2e3eae2bdb","affectsGlobalScope":true,"impliedFormat":1},{"version":"811ec78f7fefcabbda4bfa93b3eb67d9ae166ef95f9bff989d964061cbf81a0c","affectsGlobalScope":true,"impliedFormat":1},{"version":"717937616a17072082152a2ef351cb51f98802fb4b2fdabd32399843875974ca","affectsGlobalScope":true,"impliedFormat":1},{"version":"d7e7d9b7b50e5f22c915b525acc5a49a7a6584cf8f62d0569e557c5cfc4b2ac2","affectsGlobalScope":true,"impliedFormat":1},{"version":"71c37f4c9543f31dfced6c7840e068c5a5aacb7b89111a4364b1d5276b852557","affectsGlobalScope":true,"impliedFormat":1},{"version":"576711e016cf4f1804676043e6a0a5414252560eb57de9faceee34d79798c850","affectsGlobalScope":true,"impliedFormat":1},{"version":"89c1b1281ba7b8a96efc676b11b264de7a8374c5ea1e6617f11880a13fc56dc6","affectsGlobalScope":true,"impliedFormat":1},{"version":"74f7fa2d027d5b33eb0471c8e82a6c87216223181ec31247c357a3e8e2fddc5b","affectsGlobalScope":true,"impliedFormat":1},{"version":"d6d7ae4d1f1f3772e2a3cde568ed08991a8ae34a080ff1151af28b7f798e22ca","affectsGlobalScope":true,"impliedFormat":1},{"version":"063600664504610fe3e99b717a1223f8b1900087fab0b4cad1496a114744f8df","affectsGlobalScope":true,"impliedFormat":1},{"version":"934019d7e3c81950f9a8426d093458b65d5aff2c7c1511233c0fd5b941e608ab","affectsGlobalScope":true,"impliedFormat":1},{"version":"52ada8e0b6e0482b728070b7639ee42e83a9b1c22d205992756fe020fd9f4a47","affectsGlobalScope":true,"impliedFormat":1},{"version":"3bdefe1bfd4d6dee0e26f928f93ccc128f1b64d5d501ff4a8cf3c6371200e5e6","affectsGlobalScope":true,"impliedFormat":1},{"version":"59fb2c069260b4ba00b5643b907ef5d5341b167e7d1dbf58dfd895658bda2867","affectsGlobalScope":true,"impliedFormat":1},{"version":"639e512c0dfc3fad96a84caad71b8834d66329a1f28dc95e3946c9b58176c73a","affectsGlobalScope":true,"impliedFormat":1},{"version":"368af93f74c9c932edd84c58883e736c9e3d53cec1fe24c0b0ff451f529ceab1","affectsGlobalScope":true,"impliedFormat":1},{"version":"8e7f8264d0fb4c5339605a15daadb037bf238c10b654bb3eee14208f860a32ea","affectsGlobalScope":true,"impliedFormat":1},{"version":"782dec38049b92d4e85c1585fbea5474a219c6984a35b004963b00beb1aab538","affectsGlobalScope":true,"impliedFormat":1},{"version":"848e7d5aada893ea17cb67916f8b423ec51470024c917a6ac20b1b4676d2ec5b","signature":"7dd2836965712a75347fdcc17f0f67d61c6e23ca81baadf3adc366c21261a0ff"},{"version":"1c309f6ea3aba1b70c7578e5458e5bad34f55066d43caaa41fcb1e978b70dc43","signature":"6207bb80b5cb593fc3de2a26205f31470a0bd8e3499847b1ed7ec31dcaab4f8f"},{"version":"27c61e6936e991af31a59811c66ffbc84a2032b43d267b5da59a66bceb986ea2","signature":"34864f854d621455261cbdc6ea85c446df03ceb727adae80a9973bb23209a162"},{"version":"92039c1cfec52d8498f55cca9eeeb089b10ed1bd610608fb13d19fb471380efa","signature":"d1e9414dd48a5e60a794d59f9a2b12f7f321f6786f4bcbfdc4c5479b73c3838d"},{"version":"a689ac82941e1512fae8550a25517a910c18ff52984b8ba46af0bba47b5b46e4","signature":"7bc7cf9c0f9c5b90933089789a3a70bdd4f0f24fae5df10437bebcf5d0d862f7"},{"version":"7c1dd9751dedb3e2ceca94cfc5dd243b5068918b5d2370865f3cfec48d1b2bc1","signature":"a91214bdc5b9052ff7e0913970efc44e5735204f642d63d3bb5d7b67385c165e"},{"version":"f62b98ffbcea03940a472bde2e4e8f6f6a82d94bce1e80639441ef4f799b1d1d","signature":"ae660ece86a21f0c044a356f517ecdc0aef33736bf20f4261ab34eb92b78b211"},{"version":"97ca0f8e8d3adec709cb91f2cac24f4fbb6a7c191af8e9b1dc1e757f97f6535d","signature":"f82597dd6692a919a5324f75fef67a7cfce2c95c40369a63c96ba2cea30a0b9f"},{"version":"19d0dd12dfc352d03e42b54e0521c575d1e7615f091f6f63575454bf7ceb7828","signature":"147deb8ef58056b8a12bc2813317a913e944df83ab765f354530f985a69da3e9"},{"version":"0f687235ada00579827c7994c29c2bb87c365f96e0e535edf8169bb9b5cf8482","signature":"c0f36d9595eb631fc76ec068154ef3d272facd9ef2ecff0eec11854da2fffa57"},{"version":"467f006facf41b964035d0a5cb743dd4ef23538d78966903f6f93a2d210cfb50","signature":"a155ffbc77e98379ad64671397d8453819ac1d9c89b941e1d10e48def0b8186f"},{"version":"a52dab30c2f0e77090e4b1adced2b13fc33ed2376b075f87b23d5d9f17d39494","signature":"307b9002f2fb6e1aed9a5d48bceb395d64838b768fdfa6640b5898431a206208"},{"version":"64b7cfa95c5b55e467e006d79c6012ff48cb7d2bed608487264271a7622edda9","signature":"c2100f05825b5d7bbbb04ca45674fc8bcb749dfde01d4eb2a0cc4b517be9573e"},{"version":"ad798369d06e29d7bae58212381357434da45b1fb44bc9da46748f307caea9c1","signature":"f1acd16bd8e76e9e56ad03855ad7f3ed1a1fa169064db6ce33527fd53a09e56f"},{"version":"e44bf13db61811900c2ed9f7f82f033d8f2f8cad7df66b9d7ff1f42b77a9f4a6","signature":"b59a97f2054bcc7fb3356429318306a11371c3ec2d672332db004dc6d103094a"},{"version":"e134052a6b1ded61693b4037f615dc72f14e2881e79c1ddbff6c514c8a516b05","impliedFormat":1},{"version":"a33b4f59305aff86135d2b950f1dec2de399a7cf2be426dd436791a2cdb9bbf1","signature":"b21280b7d0924c74884c14365eb9e5123af4ac0e96034c47c0e0fe65e9e090e0"},{"version":"100f917632debd9d3c0475d78b582bbd1255a5fd51a061220ab880c6b3d0e5d7","signature":"ed3357ac512d6f3cf19a4e93f7879cff14890d65d5b974f38b0e303811910217"},{"version":"4b92094727b7fc828420d8f0b75ee1c5215c6df5970ce462976ef246d39a1142","signature":"aa5dad2eebc5ac21a9c9c0bdc90da10e0d14b82911415c00e587705a95561fff"},{"version":"68384a4d4c54cbe4cf0a1f7ead925fd488a852fb0ffff0041d8f5e082bc8e436","signature":"f7c22450fff4e779b643e218610042932b28343970898423f9a991df8c56443f"},{"version":"88f40f8f527a81efd63855b436ea3d0029295a3917f40867064052dcfde7e910","signature":"73c06431b2266cde2beed9652b14084637a0d522824ebaa7d0ef9eb7950db2a1"},{"version":"e6ef99e836a889b63329276e7ea5356bb333b385659f428b45618909a7d4d115","signature":"f6561cb5a11473cc7a8983f6f059f66c675e198ed20ce6ec2ccdd223fd0b06b4"},{"version":"d541f344f5659b0d0d8139c3ef357fd5ec3ff37e393b6525b00f29eab4563494","signature":"f7e3ee3bda11795dd7776fe49f4427867b0e2717f8c23a12b4ed02458b6ff55f"},{"version":"cbfe24a31b75d93deb2e3b4dffbead60cd9e4b902335aba71ab6deaf59fc0ae2","signature":"65063aa0cdbfe110cffd6ca4c7ba79fd64da3fff95d688013a4100a28bf9a486"},{"version":"20517e36f535e2b085ca7afeff184248848d37844b36b1c12f50e30bd8fe703c","signature":"ec6536698cb9d6cdc5cbb26f681b09beff03b6820bf72cd3e0cc1e244bf8c19f"},{"version":"26085fd92320c826fb1f7b74d37bb7a02686b4fea1feaed5c322c65ec1f0cae9","signature":"cc3d0d21ae81bbeab5188ebd289bca6cadf68d992eeeda2c46a83f6baf5bdf90"},{"version":"08f3ccd1234c794a10a186496487430830f47ddebe403dbc311555426b4f3194","signature":"1181bd895576b30a04429e7dcf3302b56a68aa5c0491655d3e238e45872519ef"},{"version":"a5d491b11582d9bc59cbdd20551835b3dd6cf8646b481b2dcd49ee6804a9acd4","signature":"0a4d450e2b845b981bafaab4542638671d0a24820cd828c860df9602a03ad56c"},{"version":"2861343a840afafa8110a4da7c2242810b297c2f314eecd4e3b08355e5f2f4b0","signature":"d1c34611343be09243842ff46bd00c684cf8be8f21c52ee94cfafd1943e70ee5"},{"version":"91b3484f9f04d995de26080ae332011f55278471e70dc97697413efd5f422118","signature":"b3735d15d1314ed87d9beac1bb381cdeba0a10102b247b76e2491be5a7e9fb8b"},{"version":"98aad174ab124f21e8482b59a64ad367d6980ab8c95fd343990a030042bbdca2","signature":"520697234a92aa69078c48ab3a7fa2bcb58d234fd3e3a231225de8e5a4444d2b"},{"version":"2a787daac6da7a3cb2374fef705414047e58a621dcfe1dc1d1b57dbe9040b24b","signature":"e0508727db8cd98d13d011c4ccec47faf503112b1139a8d11b494dfcee5df1fd"},{"version":"640d22a045c663ab9fd5e07fd82626c9539a46ad265b343798b628f6d98137b0","signature":"b6a3b10ae3c607e254d27724e383c55696324c37ee948b342bb91297e1409813"},{"version":"dbf2958d7bd80d9e513c1ddf3a74b5cf8dd4e495ae77f74faf43b60e314d7640","signature":"51a066518c0b587925a5b200836039871c1c15cb6802147985a3973b100ddbd1"},{"version":"31cb8b590e9a7711049d2f9193e0b864115ae4ff73922be75e7b3f3de3a1add2","signature":"541aa90f9ec49e233f507b9d552f901b079f234a91e2cecdcb818f46db0d07de"},{"version":"5e9ac3bf6f98a37661a24c14ca5f85fcffcb20bfc06b8c97c95809eabce0dbf3","signature":"b2e4bbee28c6e2dcc31a0f96543b3f8613f46e596f7d1b2c0b5423d56c48d901"},{"version":"96d93f25c4b4802560af4a26ca41f8d50ca8770bc8f84232f3a58b7460732703","signature":"aa5f0dd9c6c70b2b6f2d59de4aecc5ed1ced662f162089105f1d8ca1c226da82"},{"version":"01007786f9cf6aa7c0eb42bb203f1ff7787753de66a87fd204ba84ed4b59f848","signature":"e6162f1a79c136843e64fd0200636237e78c492ad576b3744b8f29c1847a3d05"},{"version":"dfffd544e2689877c1394d9fa57cd0a525729214760c0b9a99384ee8e97285dd","signature":"c371dc343bfe097a55fe8190cd2f342bb3e6b1425d4574db67654898fefb4f15"},{"version":"684574d8a066134170406d9b528a8ceb24060f55dbd7eef97222b7d0da3ad3ce","signature":"a0aa31a1e9d69079d949353573fcfb7a8a693fa12147516f7ad01f2fb994f214"},{"version":"7c04e9fbe1f7b8ff8fdbb69bbc4d194f9e40ed9470242b596db2975028a466b0","signature":"161163711b4f25dce9e5b200557e79f56065e764a1d3e7c4f822b25730fe120c"},{"version":"328b5de7c7f37ba3ac5308fbd6d3fea13a1be09d6becf3be5258e314bccf87c5","signature":"9fc6062e2898c02d910f942cda08e4837a60527e9a0c7452c56d10b242f16060"},{"version":"b6a9e237fc00a7eee886d6649676386432d72c97b1c322f5b8fcfd5297a3330d","signature":"8cd08117020d664871368d21cf9698596b4828cb31c62afd038b869ddd522aaf"},{"version":"e3318d2cdfe20ae5dd617d7145acece38d1ba8e1de12c87f416ef2c757eb0095","signature":"c0e4aef52352496bd28626f940fe2bfc0deaf624989e9fdb7c731d45e298777f"},{"version":"7ec857654f1566dcda7c5df087d2472114f677436733969510ff646f0621c541","signature":"415faca9a81558d607c45d861ee34a9c3ae16d775f157fcd8895ee8b18039679"},{"version":"5bc1a95c6b875af11122d03132cffe7499faeaf2ff2cbd2991ce53319536e920","signature":"bca2c9ff0d823a5b0f27fe62e7818e5d1201269938b24298cf45ef5e0d1902d6"},{"version":"d7a224b8efd6addae97a1897c0ece99d46f3e2452b870b71deb981a1822d1baa","signature":"2015dd7b4b61aad0bf6824d76cf9a06317bd7f95a4cffc72368efd3d042c2cb1"},"2dcfcd67775fb2e15d748882fcf58559db0d00332825905bb50c4a7e79e9ed10",{"version":"198199921daa461f48659502464cdd4615a8ca4d550e1f41bd69b440b3ecf73b","signature":"5146a7452654d7c21d1429d56aedb8b4de5113c5b3fc779e59d2e5532978e62f"},{"version":"70521b6ab0dcba37539e5303104f29b721bfb2940b2776da4cc818c07e1fefc1","affectsGlobalScope":true,"impliedFormat":1},{"version":"ab41ef1f2cdafb8df48be20cd969d875602483859dc194e9c97c8a576892c052","affectsGlobalScope":true,"impliedFormat":1},{"version":"d153a11543fd884b596587ccd97aebbeed950b26933ee000f94009f1ab142848","affectsGlobalScope":true,"impliedFormat":1},{"version":"21d819c173c0cf7cc3ce57c3276e77fd9a8a01d35a06ad87158781515c9a438a","impliedFormat":1},{"version":"98cffbf06d6bab333473c70a893770dbe990783904002c4f1a960447b4b53dca","affectsGlobalScope":true,"impliedFormat":1},{"version":"ba481bca06f37d3f2c137ce343c7d5937029b2468f8e26111f3c9d9963d6568d","affectsGlobalScope":true,"impliedFormat":1},{"version":"6d9ef24f9a22a88e3e9b3b3d8c40ab1ddb0853f1bfbd5c843c37800138437b61","affectsGlobalScope":true,"impliedFormat":1},{"version":"1db0b7dca579049ca4193d034d835f6bfe73096c73663e5ef9a0b5779939f3d0","affectsGlobalScope":true,"impliedFormat":1},{"version":"9798340ffb0d067d69b1ae5b32faa17ab31b82466a3fc00d8f2f2df0c8554aaa","affectsGlobalScope":true,"impliedFormat":1},{"version":"f26b11d8d8e4b8028f1c7d618b22274c892e4b0ef5b3678a8ccbad85419aef43","affectsGlobalScope":true,"impliedFormat":1},{"version":"5929864ce17fba74232584d90cb721a89b7ad277220627cc97054ba15a98ea8f","impliedFormat":1},{"version":"763fe0f42b3d79b440a9b6e51e9ba3f3f91352469c1e4b3b67bfa4ff6352f3f4","impliedFormat":1},{"version":"25c8056edf4314820382a5fdb4bb7816999acdcb929c8f75e3f39473b87e85bc","impliedFormat":1},{"version":"c464d66b20788266e5353b48dc4aa6bc0dc4a707276df1e7152ab0c9ae21fad8","impliedFormat":1},{"version":"78d0d27c130d35c60b5e5566c9f1e5be77caf39804636bc1a40133919a949f21","impliedFormat":1},{"version":"c6fd2c5a395f2432786c9cb8deb870b9b0e8ff7e22c029954fabdd692bff6195","impliedFormat":1},{"version":"1d6e127068ea8e104a912e42fc0a110e2aa5a66a356a917a163e8cf9a65e4a75","impliedFormat":1},{"version":"5ded6427296cdf3b9542de4471d2aa8d3983671d4cac0f4bf9c637208d1ced43","impliedFormat":1},{"version":"7f182617db458e98fc18dfb272d40aa2fff3a353c44a89b2c0ccb3937709bfb5","impliedFormat":1},{"version":"cadc8aced301244057c4e7e73fbcae534b0f5b12a37b150d80e5a45aa4bebcbd","impliedFormat":1},{"version":"385aab901643aa54e1c36f5ef3107913b10d1b5bb8cbcd933d4263b80a0d7f20","impliedFormat":1},{"version":"9670d44354bab9d9982eca21945686b5c24a3f893db73c0dae0fd74217a4c219","impliedFormat":1},{"version":"0b8a9268adaf4da35e7fa830c8981cfa22adbbe5b3f6f5ab91f6658899e657a7","impliedFormat":1},{"version":"11396ed8a44c02ab9798b7dca436009f866e8dae3c9c25e8c1fbc396880bf1bb","impliedFormat":1},{"version":"ba7bc87d01492633cb5a0e5da8a4a42a1c86270e7b3d2dea5d156828a84e4882","impliedFormat":1},{"version":"4893a895ea92c85345017a04ed427cbd6a1710453338df26881a6019432febdd","impliedFormat":1},{"version":"c21dc52e277bcfc75fac0436ccb75c204f9e1b3fa5e12729670910639f27343e","impliedFormat":1},{"version":"13f6f39e12b1518c6650bbb220c8985999020fe0f21d818e28f512b7771d00f9","impliedFormat":1},{"version":"9b5369969f6e7175740bf51223112ff209f94ba43ecd3bb09eefff9fd675624a","impliedFormat":1},{"version":"4fe9e626e7164748e8769bbf74b538e09607f07ed17c2f20af8d680ee49fc1da","impliedFormat":1},{"version":"24515859bc0b836719105bb6cc3d68255042a9f02a6022b3187948b204946bd2","impliedFormat":1},{"version":"ea0148f897b45a76544ae179784c95af1bd6721b8610af9ffa467a518a086a43","impliedFormat":1},{"version":"24c6a117721e606c9984335f71711877293a9651e44f59f3d21c1ea0856f9cc9","impliedFormat":1},{"version":"dd3273ead9fbde62a72949c97dbec2247ea08e0c6952e701a483d74ef92d6a17","impliedFormat":1},{"version":"405822be75ad3e4d162e07439bac80c6bcc6dbae1929e179cf467ec0b9ee4e2e","impliedFormat":1},{"version":"0db18c6e78ea846316c012478888f33c11ffadab9efd1cc8bcc12daded7a60b6","impliedFormat":1},{"version":"e61be3f894b41b7baa1fbd6a66893f2579bfad01d208b4ff61daef21493ef0a8","impliedFormat":1},{"version":"bd0532fd6556073727d28da0edfd1736417a3f9f394877b6d5ef6ad88fba1d1a","impliedFormat":1},{"version":"89167d696a849fce5ca508032aabfe901c0868f833a8625d5a9c6e861ef935d2","impliedFormat":1},{"version":"615ba88d0128ed16bf83ef8ccbb6aff05c3ee2db1cc0f89ab50a4939bfc1943f","impliedFormat":1},{"version":"a4d551dbf8746780194d550c88f26cf937caf8d56f102969a110cfaed4b06656","impliedFormat":1},{"version":"8bd86b8e8f6a6aa6c49b71e14c4ffe1211a0e97c80f08d2c8cc98838006e4b88","impliedFormat":1},{"version":"317e63deeb21ac07f3992f5b50cdca8338f10acd4fbb7257ebf56735bf52ab00","impliedFormat":1},{"version":"4732aec92b20fb28c5fe9ad99521fb59974289ed1e45aecb282616202184064f","impliedFormat":1},{"version":"2e85db9e6fd73cfa3d7f28e0ab6b55417ea18931423bd47b409a96e4a169e8e6","impliedFormat":1},{"version":"c46e079fe54c76f95c67fb89081b3e399da2c7d109e7dca8e4b58d83e332e605","impliedFormat":1},{"version":"bf67d53d168abc1298888693338cb82854bdb2e69ef83f8a0092093c2d562107","impliedFormat":1},{"version":"b52476feb4a0cbcb25e5931b930fc73cb6643fb1a5060bf8a3dda0eeae5b4b68","affectsGlobalScope":true,"impliedFormat":1},{"version":"e2677634fe27e87348825bb041651e22d50a613e2fdf6a4a3ade971d71bac37e","impliedFormat":1},{"version":"7394959e5a741b185456e1ef5d64599c36c60a323207450991e7a42e08911419","impliedFormat":1},{"version":"8c0bcd6c6b67b4b503c11e91a1fb91522ed585900eab2ab1f61bba7d7caa9d6f","impliedFormat":1},{"version":"8cd19276b6590b3ebbeeb030ac271871b9ed0afc3074ac88a94ed2449174b776","affectsGlobalScope":true,"impliedFormat":1},{"version":"696eb8d28f5949b87d894b26dc97318ef944c794a9a4e4f62360cd1d1958014b","impliedFormat":1},{"version":"3f8fa3061bd7402970b399300880d55257953ee6d3cd408722cb9ac20126460c","impliedFormat":1},{"version":"35ec8b6760fd7138bbf5809b84551e31028fb2ba7b6dc91d95d098bf212ca8b4","affectsGlobalScope":true,"impliedFormat":1},{"version":"5524481e56c48ff486f42926778c0a3cce1cc85dc46683b92b1271865bcf015a","impliedFormat":1},{"version":"68bd56c92c2bd7d2339457eb84d63e7de3bd56a69b25f3576e1568d21a162398","affectsGlobalScope":true,"impliedFormat":1},{"version":"3e93b123f7c2944969d291b35fed2af79a6e9e27fdd5faa99748a51c07c02d28","impliedFormat":1},{"version":"9d19808c8c291a9010a6c788e8532a2da70f811adb431c97520803e0ec649991","impliedFormat":1},{"version":"87aad3dd9752067dc875cfaa466fc44246451c0c560b820796bdd528e29bef40","impliedFormat":1},{"version":"4aacb0dd020eeaef65426153686cc639a78ec2885dc72ad220be1d25f1a439df","impliedFormat":1},{"version":"f0bd7e6d931657b59605c44112eaf8b980ba7f957a5051ed21cb93d978cf2f45","impliedFormat":1},{"version":"8db0ae9cb14d9955b14c214f34dae1b9ef2baee2fe4ce794a4cd3ac2531e3255","affectsGlobalScope":true,"impliedFormat":1},{"version":"15fc6f7512c86810273af28f224251a5a879e4261b4d4c7e532abfbfc3983134","impliedFormat":1},{"version":"58adba1a8ab2d10b54dc1dced4e41f4e7c9772cbbac40939c0dc8ce2cdb1d442","impliedFormat":1},{"version":"641942a78f9063caa5d6b777c99304b7d1dc7328076038c6d94d8a0b81fc95c1","impliedFormat":1},{"version":"092c1137e31de289f3cc6a57fdccb3cca298d8a680d1e367d206d3318f1394a1","impliedFormat":1},{"version":"855cd5f7eb396f5f1ab1bc0f8580339bff77b68a770f84c6b254e319bbfd1ac7","impliedFormat":1},{"version":"5650cf3dace09e7c25d384e3e6b818b938f68f4e8de96f52d9c5a1b3db068e86","impliedFormat":1},{"version":"1354ca5c38bd3fd3836a68e0f7c9f91f172582ba30ab15bb8c075891b91502b7","affectsGlobalScope":true,"impliedFormat":1},{"version":"7e20d899c28ca26a2a7afc98beaa69e63ff7fba0a8bc47b4e3bf3ede5e09e424","impliedFormat":1},{"version":"2d2fcaab481b31a5882065c7951255703ddbe1c0e507af56ea42d79ac3911201","impliedFormat":1},{"version":"a192fe8ec33f75edbc8d8f3ed79f768dfae11ff5735e7fe52bfa69956e46d78d","impliedFormat":1},{"version":"ca867399f7db82df981d6915bcbb2d81131d7d1ef683bc782b59f71dda59bc85","affectsGlobalScope":true,"impliedFormat":1},{"version":"372413016d17d804e1d139418aca0c68e47a83fb6669490857f4b318de8cccb3","affectsGlobalScope":true,"impliedFormat":1},{"version":"9e043a1bc8fbf2a255bccf9bf27e0f1caf916c3b0518ea34aa72357c0afd42ec","impliedFormat":1},{"version":"b4f70ec656a11d570e1a9edce07d118cd58d9760239e2ece99306ee9dfe61d02","impliedFormat":1},{"version":"3bc2f1e2c95c04048212c569ed38e338873f6a8593930cf5a7ef24ffb38fc3b6","impliedFormat":1},{"version":"6e70e9570e98aae2b825b533aa6292b6abd542e8d9f6e9475e88e1d7ba17c866","impliedFormat":1},{"version":"f9d9d753d430ed050dc1bf2667a1bab711ccbb1c1507183d794cc195a5b085cc","impliedFormat":1},{"version":"9eece5e586312581ccd106d4853e861aaaa1a39f8e3ea672b8c3847eedd12f6e","impliedFormat":1},{"version":"085f552d005479e2e6a7311cdbbe5d8c55c497b4d19274285df161ee9684cd9c","impliedFormat":1},{"version":"37ba7b45141a45ce6e80e66f2a96c8a5ab1bcef0fc2d0f56bb58df96ec67e972","impliedFormat":1},{"version":"45650f47bfb376c8a8ed39d4bcda5902ab899a3150029684ee4c10676d9fbaee","impliedFormat":1},{"version":"007faacc9268357caa21d24169f3f3f2497af3e9241308df2d89f6e6d9bb3f2e","affectsGlobalScope":true,"impliedFormat":1},{"version":"74cf591a0f63db318651e0e04cb55f8791385f86e987a67fd4d2eaab8191f730","impliedFormat":1},{"version":"5eab9b3dc9b34f185417342436ec3f106898da5f4801992d8ff38ab3aff346b5","impliedFormat":1},{"version":"12ed4559eba17cd977aa0db658d25c4047067444b51acfdcbf38470630642b23","affectsGlobalScope":true,"impliedFormat":1},{"version":"f3ffabc95802521e1e4bcba4c88d8615176dc6e09111d920c7a213bdda6e1d65","impliedFormat":1},{"version":"809821b8a065e3234a55b3a9d7846231ed18d66dd749f2494c66288d890daf7f","impliedFormat":1},{"version":"ae56f65caf3be91108707bd8dfbccc2a57a91feb5daabf7165a06a945545ed26","impliedFormat":1},{"version":"a136d5de521da20f31631a0a96bf712370779d1c05b7015d7019a9b2a0446ca9","impliedFormat":1},{"version":"c3b41e74b9a84b88b1dca61ec39eee25c0dbc8e7d519ba11bb070918cfacf656","affectsGlobalScope":true,"impliedFormat":1},{"version":"4737a9dc24d0e68b734e6cfbcea0c15a2cfafeb493485e27905f7856988c6b29","affectsGlobalScope":true,"impliedFormat":1},{"version":"36d8d3e7506b631c9582c251a2c0b8a28855af3f76719b12b534c6edf952748d","impliedFormat":1},{"version":"1ca69210cc42729e7ca97d3a9ad48f2e9cb0042bada4075b588ae5387debd318","impliedFormat":1},{"version":"f5ebe66baaf7c552cfa59d75f2bfba679f329204847db3cec385acda245e574e","impliedFormat":1},{"version":"ed59add13139f84da271cafd32e2171876b0a0af2f798d0c663e8eeb867732cf","affectsGlobalScope":true,"impliedFormat":1},{"version":"b7c5e2ea4a9749097c347454805e933844ed207b6eefec6b7cfd418b5f5f7b28","impliedFormat":1},{"version":"b1810689b76fd473bd12cc9ee219f8e62f54a7d08019a235d07424afbf074d25","impliedFormat":1}],"root":[[46,60],[62,94]],"options":{"composite":true,"declaration":true,"declarationMap":true,"esModuleInterop":true,"module":1,"outDir":"./dist","rootDir":"./src","skipLibCheck":true,"sourceMap":true,"strict":true,"target":7},"referencedMap":[[143,1],[144,1],[145,2],[100,3],[146,4],[147,5],[148,6],[95,7],[98,8],[96,7],[97,7],[149,9],[150,10],[151,11],[152,12],[153,13],[154,14],[155,14],[156,15],[157,16],[158,17],[159,18],[101,7],[99,7],[160,19],[161,20],[162,21],[194,22],[163,23],[164,24],[165,25],[166,26],[167,27],[168,28],[169,29],[170,30],[171,31],[172,32],[173,32],[174,33],[175,7],[176,34],[178,35],[177,36],[179,37],[180,38],[181,39],[182,40],[183,41],[184,42],[185,43],[186,44],[187,45],[188,46],[189,47],[190,48],[191,49],[102,7],[103,7],[104,7],[142,50],[192,51],[193,52],[44,7],[45,7],[9,7],[8,7],[2,7],[10,7],[11,7],[12,7],[13,7],[14,7],[15,7],[16,7],[17,7],[3,7],[18,7],[19,7],[4,7],[20,7],[24,7],[21,7],[22,7],[23,7],[25,7],[26,7],[27,7],[5,7],[28,7],[29,7],[30,7],[31,7],[6,7],[35,7],[32,7],[33,7],[34,7],[36,7],[7,7],[37,7],[42,7],[43,7],[38,7],[39,7],[40,7],[41,7],[1,7],[61,7],[120,53],[130,54],[119,53],[140,55],[111,56],[110,57],[139,58],[133,59],[138,60],[113,61],[127,62],[112,63],[136,64],[108,65],[107,58],[137,66],[109,67],[114,68],[115,7],[118,68],[105,7],[141,69],[131,70],[122,71],[123,72],[125,73],[121,74],[124,75],[134,58],[116,76],[117,77],[126,78],[106,79],[129,70],[128,68],[132,7],[135,80],[90,81],[91,82],[86,83],[88,84],[87,85],[89,86],[63,87],[65,88],[47,89],[83,90],[46,91],[52,92],[58,93],[50,94],[64,95],[60,96],[59,97],[55,98],[54,92],[48,7],[62,99],[84,100],[80,101],[67,102],[68,94],[76,7],[66,7],[82,103],[53,104],[70,105],[79,106],[69,107],[78,108],[74,109],[81,110],[75,111],[73,111],[72,111],[77,112],[92,113],[71,7],[93,114],[85,115],[56,116],[57,117],[94,118],[49,7],[51,7]],"latestChangedDtsFile":"./dist/providers/index.d.ts","version":"5.9.3"}

================
File: packages/core/vitest.config.ts
================
import { defineConfig } from 'vitest/config';

================
File: packages/shell/electron/dev-runner.ts
================
/**
 * Dev runner — starts Electron pointing to Vite dev server.
 * Compiles the Electron TypeScript first, then launches Electron on the compiled JS.
 */
import { spawnSync, spawn } from 'child_process';
import { resolve } from 'path';
⋮----
function compileElectron(): void
⋮----
async function waitForVite(url: string, maxRetries = 30): Promise<void>
⋮----
// Server not ready yet
⋮----
async function main(): Promise<void>
⋮----
// eslint-disable-next-line @typescript-eslint/no-var-requires

================
File: packages/shell/src/main.tsx
================
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

================
File: packages/shell/index.html
================
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; connect-src 'self'">
    <title>Soberano-Core</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>

================
File: packages/shell/package.json
================
{
  "name": "@soberano/shell",
  "version": "1.0.0",
  "description": "Soberano-Core Electron Shell — Desktop interface for the AI agent",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "concurrently -k \"vite\" \"tsx electron/dev-runner.ts\"",
    "build": "tsc -p tsconfig.electron.json && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@soberano/core": "*",
    "@types/node": "^20.19.40",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "concurrently": "^8.2.0",
    "electron": "^28.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.0"
  }
}

================
File: packages/shell/tsconfig.electron.json
================
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "lib": ["ES2020"],
    "outDir": "dist-electron",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "sourceMap": true
  },
  "include": ["electron"],
  "exclude": ["node_modules", "dist-electron"]
}

================
File: packages/shell/tsconfig.json
================
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "outDir": "dist",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "noEmit": true,
    "declaration": false
  },
  "include": ["src"],
  "references": [
    { "path": "../core" }
  ]
}

================
File: packages/shell/vite.config.ts
================
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'node:path';

================
File: .clinerules
================
# Constituição Soberano-Core

Você é o CTO (Cline) deste projeto. Sua missão é manter a integridade técnica do Soberano-Core, um framework de agentes IA focado em hardware limitado e soberania digital.

## 1. Princípios Inegociáveis
- **Zero Dependências:** É terminantemente proibido instalar pacotes externos no ambiente de produção. Use apenas módulos nativos do Node.js (`node:http`, `node:fs`, `node:path`, `node:child_process`).
- **SOLID Estrito:** Toda refatoração deve seguir os princípios SOLID. Evite "God Objects" e funções monolíticas.
- **TypeScript Puro:** Mantenha o código tipado e utilize as interfaces definidas em `src/types/`.

## 2. Regras de Execução (Foco no Modelo Flash)
- **Não Ignore o Core:** O arquivo `src/core/ReActLoop.ts` é a única implementação permitida para o loop de raciocínio. Qualquer lógica de loop duplicada em `src/cli/commands.ts` deve ser removida e delegada a esta classe.
- **Código Completo:** Nunca entregue implementações parciais com comentários como "// ... resto do código". O Flash deve escrever o arquivo completo ou admitir que a tarefa é grande demais e pedir para fatiar.
- **Segurança de Comando:** Sempre use `shell: false` e `spawn` no CommandExecutor para prevenir injeções de código.

## 3. Fluxo de Trabalho Atomizado
- Antes de iniciar qualquer refatoração, leia o `@ARCHITECTURE_CHECKLIST.md` (se disponível) ou `@DOCUMENTACAO_PROJETO.md`.
- Após cada alteração em arquivos de lógica (`src/core/` ou `src/providers/`), você deve tentar rodar os testes unitários correspondentes se a infraestrutura estiver configurada.
- **Testes 100% Antes de Commit:** Nenhum commit será aceito com testes falhando. `npm test` deve retornar 0 falhas (exit code 0). Testes de integração quebrados são bloqueadores de merge, não dívida técnica tolerável. Se um teste existente falhar, o desenvolvedor deve corrigir o código ou o teste antes de prosseguir.

## 4. Memória de Contexto
- Se o contexto ficar muito grande, resuma suas ações no log e foque apenas nos arquivos mencionados pelo usuário com o símbolo `@`.
- Sua prioridade atual é o **Passo 1: Estabilização**. Não proponha novas features (como streaming ou docker) até que o SOLID e os Testes Unitários estejam 100% concluídos.

================
File: packages/shell/electron/main.ts
================
import { app, BrowserWindow, ipcMain } from 'electron';
import path from 'node:path';
import { AppContext, ReActLoop, StatefulCompressor } from '@soberano/core';
import type { ReActMessage } from '@soberano/core';
⋮----
// ── Persistência de Memória (Histórico Multi-turn) ──
// Mantém o histórico entre chamadas IPC, permitindo continuidade
// na conversa com o mesmo agente.
⋮----
function createWindow(): void
⋮----
/**
 * Gera o system prompt dinamicamente a partir do ToolRegistry.
 *
 * Principio DRY: as definições das ferramentas são centralizadas em
 * AppContext.setupDefaultTools() e expostas via toolRegistry.getDefinitions().
 * Este builder converte a saída JSON do registry em instruções legíveis
 * pelo modelo, eliminando a duplicação manual que causava a omissão de
 * ferramentas como readDir.
 *
 * @param toolRegistryJson JSON string com as definições das ferramentas
 * @returns System prompt completo com instruções + ferramentas disponíveis
 */
function buildSystemPrompt(toolRegistryJson: string): string
⋮----
// Parse as definições JSON do ToolRegistry e gera lista legível
⋮----
// Fallback: se o JSON estiver malformado, usa a lista crua
⋮----
// ── Inicializa o AppContext (DIP Container) ──
// O Core não sabe que o Electron existe. A injeção é feita via bridge,
// mantendo o desacoplamento SOLID.
⋮----
// ── Constrói o system prompt dinâmico a partir do ToolRegistry ──
// GARANTE que toda ferramenta registrada (incluindo readDir) seja
// listada para o modelo, sem duplicação manual.
⋮----
// ── IPC Handler: Ponte de Soberania ──
// Renderer → IPC Bridge → Core (ReActLoop) → Resposta → Renderer
⋮----
// ── Health-Check: Verifica se o motor Ollama está respondendo ──
⋮----
// ── Injeção do ReActLoop ──
// StatefulCompressor: compressão inteligente do histórico via LLM
⋮----
// ReActLoop: ciclo Reasoning + Acting com suporte a ferramentas
⋮----
undefined,            // executor não necessário em JSON mode
⋮----
undefined,            // reflector opcional
compressor,           // compressor IContextCompressor
⋮----
// ── Execução do Ciclo ReAct ──
// Adiciona o prompt do utilizador ao histórico persistente
⋮----
// Atualiza o histórico com as mensagens geradas pelo loop
// Adiciona a resposta final como mensagem do assistente
⋮----
// Evita crescimento infinito do histórico: mantém no máximo 50 mensagens

================
File: packages/shell/electron/preload.ts
================
import { contextBridge, ipcRenderer } from 'electron';

================
File: packages/shell/src/App.tsx
================
import React, { useState } from 'react';
⋮----
/**
 * App component — Main entry point for the Electron Shell.
 *
 * This component implements the IPC Bridge (Ponte de Soberania):
 * - Sends prompts to the main process via window.api.askSoberano
 * - Displays the response from the Node.js backend
 */
⋮----
const handleSubmit = async (e: React.FormEvent) =>
⋮----
onChange=

================
File: packages/shell/src/vite-env.d.ts
================
/// <reference types="vite/client" />
⋮----
export interface SoberanoAPI {
  askSoberano: (prompt: string) => Promise<string>;
}
⋮----
interface Window {
    api: SoberanoAPI;
  }

================
File: .gitignore
================
node_modules/
dist/
dist-electron/
.soberano/
*.log
.DS_Store

================
File: package.json
================
{
  "name": "soberano-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Soberano-Core Monorepo — Agente IA modular com Core, CLI e Shell Desktop",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core",
    "test": "npm run test -w packages/core",
    "test:watch": "npm run test:watch -w packages/core",
    "dev": "npm run dev -w packages/shell",
    "dev:core": "npm run dev -w packages/core"
  },
  "keywords": ["ai", "agent", "ollama", "cli", "electron"],
  "license": "MIT"
}

================
File: DOCUMENTACAO_PROJETO.md
================
# 📄 Documentação do Projeto: Soberano-Core

## 📋 Visão Geral

**Soberano-Core** é um **Agente IA modular** construído em **TypeScript/Node.js** que integra um **Core** de utilitários (leitura de arquivos, execução de comandos), **Providers** (conexão com modelos de IA como Ollama) e uma **CLI** para interação via terminal.

- **Nome do pacote:** `soberano-core`
- **Versão:** `2.0.0-alpha`
- **Licença:** MIT
- **Linguagem:** TypeScript (target ES2020)
- **Gerenciador de pacotes:** npm
- **Dependências:** Nenhuma dependência externa em produção (apenas `@types/node`, `ts-node` e `typescript` como devDependencies)

---

## 🗂️ Estrutura do Projeto

```
AgenteIA/
├── package.json                # Configuração do npm + scripts
├── tsconfig.json               # Configuração do TypeScript
├── .gitignore                  # Arquivos ignorados pelo Git
├── DOCUMENTACAO_PROJETO.md     # Esta documentação
├── tests/
│   ├── unit/                   # Testes unitários (mockados, zero I/O real)
│   └── integration/            # Testes de integração (filesystem real)
│       └── editing-pipeline.test.ts  # Pipeline ASTEditor + SearchReplaceEditor
├── src/
│   ├── cli/
│   │   ├── index.ts            # Entrypoint da CLI + parser de argumentos
│   │   ├── commands.ts         # Roteamento e implementação dos comandos (DIP)
│   │   └── strategies/         # Estratégias de chat (Strategy Pattern)
│   │       ├── index.ts        # Re-exports
│   │       ├── ChatStrategy.ts # Interface + buildSystemPrompt() + editing workflow
│   │       ├── StreamStrategy.ts # Streaming direto (SSE)
│   │       └── ReActStrategy.ts  # ReAct loop com ou sem streaming
│   ├── core/
│   │   ├── index.ts            # Re-exports públicos do módulo core
│   │   ├── FileReader.ts       # Abstração do sistema de arquivos
│   │   ├── CommandExecutor.ts  # Execução segura de comandos shell
│   │   ├── ToolRegistry.ts     # Registro de tools com JSON Schema + handlers
│   │   ├── AppContext.ts       # Container DI com todas as dependências (DIP)
│   │   ├── ProviderFactory.ts  # Factory para criar providers (OCP)
│   │   ├── SessionStore.ts     # Persistência de conversas em disco (JSON)
│   │   ├── SessionManager.ts   # Orquestração da sessão ativa + histórico
│   │   ├── TokenEstimator.ts   # Estimador de tokens para compressão de contexto
│   │   ├── IContextCompressor.ts # Interface do compressor de contexto (ISP)
│   │   ├── StatefulCompressor.ts # Compressor com fallback + logging (SRP)
│   │   ├── astUtils.ts         # Utilitários de AST (parsing + análise)
│   │   ├── ASTEditor.ts        # Edição de código-fonte via AST (substituição por símbolo)
│   │   ├── SearchReplaceEditor.ts # Edição de arquivos por busca/substituição de blocos
│   │   └── rag/
│   │       ├── index.ts        # Re-exports do módulo RAG
│   │       ├── IChunker.ts     # Interface do Chunker (ISP/DIP)
│   │       ├── Chunker.ts      # Divisão de texto em chunks (SRP, implementa IChunker)
│   │       ├── IASTParser.ts   # Interface para parseadores AST (ISP)
│   │       ├── TypescriptASTAdapter.ts # Parseador AST concreto (Adapter Pattern)
│   │       ├── ASTChunkerService.ts # Chunking AST-aware com fallback (Decorator)
│   │       ├── Embedder.ts     # Geração de embeddings (SRP)
│   │       ├── VectorStore.ts  # Cache de embeddings em disco (SRP)
│   │       ├── Retriever.ts    # Busca por similaridade (SRP)
│   │       ├── RAGManager.ts   # Orquestrador do pipeline RAG (DI via construtor)
│   │       ├── ReActLoop.ts    # Loop Reasoning + Acting (Strategy)
│   │       └── graph/
│   │           ├── index.ts               # Re-exports do módulo GraphRAG
│   │           ├── types.ts               # KnowledgeGraph, GraphNode, GraphEdge
│   │           ├── IGraphStore.ts         # Interface de persistência (ISP/DIP)
│   │           ├── IRelationshipExtractor.ts # Interface de extração (ISP/DIP)
│   │           ├── IGraphQuery.ts         # Interface de consulta (ISP/DIP)
│   │           ├── JsonGraphStore.ts      # Persistência JSON (SRP)
│   │           ├── ASTRelationshipExtractor.ts # Extração via AST (SRP)
│   │           ├── GraphBuilder.ts        # Construtor do grafo (SRP)
│   │           └── GraphRAGManager.ts     # Busca híbrida vetorial + grafo (SRP)
│   ├── providers/
│   │   ├── index.ts            # Re-exports públicos do módulo providers
│   │   ├── types.ts            # Interfaces: IProvider, IEmbedProvider, ChatRequest, etc.
│   │   ├── OllamaProvider.ts   # Provider Ollama (chat + streamChat + embed)
│   │   └── OllamaHttpClient.ts # Cliente HTTP de baixo nível (post + postStream)
│   └── validation/
│       └── JsonValidator.ts    # Validador JSON puro (SRP, zero dependências)
```

---

## ⚙️ Módulo Core (`src/core/`)

### FileReader (`FileReader.ts`)

Abstração sobre o sistema de arquivos do Node.js (`fs/promises`). Toda operação é assíncrona (Promise-based).

**Métodos estáticos:**
| Método | Descrição |
|--------|-----------|
| `resolveSecurePath(userPath)` | **(async)** Resolve caminho e valida contra Path Traversal + Symlink Attacks (C-1). Usa `fs.realpath()` no caminho do usuário e no rootDir para prevenir ataques via symlinks maliciosos |

**Métodos de instância:**
| Método | Descrição |
|--------|-----------|
| `readFile(filePath)` | Lê conteúdo completo de um arquivo (UTF-8) |
| `readDir(dirPath)` | Lista entradas (arquivos/diretórios) de um diretório |
| `searchFiles(rootDir, pattern, maxResults?)` | Busca recursiva por padrão textual em arquivos; retorna `SearchResult[]` |

**Tipos exportados:**
- `SearchResult`: `{ file: string; line: number; content: string }`

> **Nota:** `resolveSecurePath` foi tornado **assíncrono** (retorna `Promise<string>`) para suportar `fs.promises.realpath()` — mitigação contra Symlink Attacks (C-1). Todos os métodos de instância e consumidores (`ASTEditor`, `SearchReplaceEditor`) usam `await`.

---

### CommandExecutor (`CommandExecutor.ts`)

Camada segura sobre `child_process.spawn` do Node.js.

**Métodos:**
| Método | Descrição |
|--------|-----------|
| `execute(command, args?, options?)` | Executa comando shell com `shell: false` (previne injeção). Suporta timeout (padrão 60s) e cwd customizado |

**Tipos exportados:**
- `CommandResult`: `{ stdout: string; stderr: string; exitCode: number \| null; signal: NodeJS.Signals \| null }`

---

### RAGManager (`RAGManager.ts`)

Gerencia o pipeline de Retrieval-Augmented Generation: chunking de arquivos, geração de embeddings, busca por similaridade de cosseno, cache em disco e GraphRAG. Zero dependências externas (usa apenas `node:fs/promises`, `node:path`, `node:crypto`).

**Injeção de Dependências (DIP):** O `RAGManager` recebe todas as suas dependências via construtor (`IChunker`, `Embedder`, `Retriever`, `VectorStore`, `GraphBuilder`, `IGraphStore`, `IRelationshipExtractor`, `IGraphQuery`). Nenhuma instância é criada internamente — facilitando testes e substituição de implementações.

**Métodos:**
| Método | Descrição |
|--------|-----------|
| `ensureIndex(dir)` | Indexa diretório (chunks → embeddings → cache + GraphBuilder). Só reindexa se houver mudanças |
| `retrieve(query, dir)` | Busca semântica + GraphRAG enhancement: top 5 chunks por cosine similarity, expandidos com adjacências do grafo |
| `formatContext(matches)` | Formata chunks como `[arquivo:linha]` para injeção no prompt |
| `connectProvider(provider)` | Conecta ao OllamaProvider para gerar embeddings via all-minilm |

**Tipos exportados:**
- `ChunkEntry`: `{ text: string; file: string; line: number; embedding: number[] }`
- `SearchMatch`: `{ file: string; line: number; content: string; score: number }`

---

## 🔌 Módulo Providers (`src/providers/`)

Responsável pela comunicação com modelos de IA. Arquitetura baseada em interfaces para permitir múltiplos providers no futuro.

### Types (`types.ts`)

Define os contratos da API:

| Interface | Descrição |
|-----------|-----------|
| `ChatRequest` | `{ model, prompt, temperature?, max_tokens?, format? }` — requisição para o modelo. `format: 'json'` ativa Grammar Restraint |
| `ChatResponse` | `{ response, model, done }` — resposta do modelo |
| `EmbedRequest` | `{ model, prompt }` — requisição de embedding para o Ollama |
| `EmbedResponse` | `{ embedding: number[] }` — resposta de embedding (vetor 384-dim do all-minilm) |
| `IProvider` | `{ readonly name, chat(request): Promise<ChatResponse>, embed(request): Promise<EmbedResponse> }` — interface que todo provider deve implementar |

### OllamaProvider (`OllamaProvider.ts`)

Implementação concreta do `IProvider` para comunicação com instância local do **Ollama** via HTTP (sem dependências externas, usa apenas `node:http`).

**Funcionalidades:**
- Conexão com servidor Ollama em `host:port` configurável (padrão: `localhost:11434`)
- Envio de prompts via POST para `/api/generate` com suporte a `temperature`, `num_predict`, `stream: false` e `format: "json"`
- **Grammar Restraint nativo:** quando `format: 'json'` é ativado, o body inclui `"format": "json"` — o Ollama força o modelo a responder em JSON estrito
- **Embeddings:** método `embed(request)` que envia POST para `/api/embed` com `keep_alive` configurável
- **Vetores 384-dim:** retorna `number[]` (float32) padrão do modelo all-minilm
- **Streaming SSE:** método `streamChat()` retorna `AsyncIterable<string>` que emite tokens um a um conforme chegam do servidor. O ReActLoop consome este stream nativamente via `for await...of` quando `stream.enabled = true` e `stream.onToken` é fornecido
- **Validação de robustez:** se `format: 'json'` foi solicitado, a resposta é validada com `JSON.parse()` dentro de `try/catch`. Se o modelo alucinar JSON inválido, um erro claro é lançado protegendo o CLI
- Timeout de 300 segundos para respostas de modelos grandes
- Tratamento de erros de rede, parsing e status HTTP

### OllamaHttpClient (`OllamaHttpClient.ts`)

Cliente HTTP de baixo nível extraído do `OllamaProvider` (SRP — Single Responsibility Principle). Responsável exclusivamente por gerenciar conexões TCP, timeouts e parsing de respostas HTTP.

**Métodos:**
| Método | Descrição |
|--------|-----------|
| `post(path, body)` | POST comum — retorna Promise do JSON parseado |
| `postStream(path, body)` | POST com streaming — retorna `AsyncGenerator<string>` que processa chunks TCP linha a linha |

**Fluxo do `postStream`:**
1. Conecta via `http.request`
2. Aguarda `req.on('response')` em vez de callback (para suportar async generator)
3. Processa chunks TCP linha a linha com buffer de linha parcial
4. Cada linha completa é parseada como JSON e extrai o campo `"response"`
5. O loop termina ao receber um objeto com `"done": true`

Zero dependências externas — usa exclusivamente `node:http`.

---

## 🖥️ CLI (`src/cli/`)

### Estratégias de Chat (`src/cli/strategies/`)

O comando `chat` usa o **Strategy Pattern** (OCP) para suportar múltiplos pipelines de execução:

| Estratégia | Arquivo | Quando usada |
|------------|---------|--------------|
| `StreamStrategy` | `StreamStrategy.ts` | `--stream` ativo, `--json` desligado, e provider suporta `streamChat` |
| `ReActStrategy` | `ReActStrategy.ts` | Padrão (fallback) — quando `--json` está ativo ou streaming não é suportado |

**StreamStrategy** — Streaming direto (efeito máquina de escrever):
1. Constrói system prompt completo (com RAG se `--rag`)
2. Itera sobre `provider.streamChat()` escrevendo cada token em `process.stdout`
3. Se `streamChat` falhar, faz fallback para `chat()` normal
4. Exibe `[modelo]` como prefixo da resposta

**ReActStrategy** — ReAct Loop (Reasoning + Acting):
1. Constrói system prompt completo (RAG + tools)
2. Executa `ReActLoop.execute()` — loop de raciocínio com ferramentas
3. Se `--stream` estiver ativo, faz streaming da resposta final pós-ReAct
4. Exibe indicador `🤔 Pensando...` no stderr (desativável com `--no-think`)
5. Se streaming falhar, mostra resposta completa de uma vez


Interface de linha de comando que orquestra Core + Providers.

### Entrypoint (`index.ts`)

- Faz o parsing de `process.argv`
- Separa argumentos posicionais de flags (`--flag valor`)
- Roteia para o comando apropriado via `runCommand()`
- Exibe output no console ou mensagem de erro com `process.exit(1)`

### Comandos (`commands.ts`)

| Comando | Sintaxe | Descrição |
|---------|---------|-----------|
| `help` | `soberano help` | Exibe tela de ajuda com todos os comandos disponíveis |
| `read` | `soberano read <file>` | Lê conteúdo de um arquivo |
| `dir` | `soberano dir <path>` | Lista conteúdo de um diretório |
| `search` | `soberano search <dir> <pattern>` | Busca recursiva por padrão textual |
| `exec` | `soberano exec <cmd>` | Executa comando shell (com `shell: false` por segurança) |
| `chat` | `soberano chat <prompt> [--model] [--ollama] [--ollama-port] [--json] [--rag <dir>] [--session <id>] [--new-session]` | Envia prompt para modelo Ollama com suporte a RAG e multi-turn |
| `sessions` | `soberano sessions` | Lista sessões de conversa salvas |

**Flags do comando `chat`:**
- `--model <name>` — Modelo Ollama (padrão: `llama3.2:1b`)
- `--ollama <host>` — Host do servidor Ollama (padrão: `localhost`)
- `--ollama-port <port>` — Porta do Ollama (padrão: `11434`)
- `--json` — Ativa Grammar Restraint: força resposta em JSON estrito e injeta system prompt `"Responda estritamente em formato JSON válido."`
- `--rag <dir>` — Ativa RAG: indexa diretório e injeta chunks relevantes no contexto
- `--session <id>` — Carrega uma sessão existente pelo UUID (retoma contexto da conversa anterior)
- `--new-session` — Força a criação de uma nova sessão (ignora `--session`)

---

## 🚀 Scripts npm

| Script | Comando | Descrição |
|--------|---------|-----------|
| `build` | `tsc` | Compila TypeScript para JS na pasta `dist/` |
| `dev` | `ts-node src/cli/index.ts` | Executa em modo desenvolvimento |
| `start` | `node dist/cli/index.js` | Executa versão compilada |

**Exemplos de uso:**
```bash
npm run dev -- chat "Explique o que é SOLID" --model phi3:3b
npm run dev -- chat "Give me JSON with name and age" --json
npm run dev -- read package.json
npm run dev -- search src "export"
npm run dev -- exec "ls -la"
npm run dev -- chat "Como instalar o projeto?" --rag .
```

---

## 🏗️ Arquitetura Geral

```
Terminal (usuário)
    │
    ▼
┌──────────────────────────┐
│       CLI (index.ts)      │  ← parseArgs()
│   parser de argumentos    │
└────────┬─────────────────┘
         │ CliArgs { command, args, flags }
         ▼
┌──────────────────────────┐
│    commands.ts           │  ← runCommand()
│  roteador de comandos    │
└───┬───────┬───────┬─────┘
    │       │       │
    ▼       ▼       ▼
┌────────┐┌────────┐┌──────────────┐
│Core    ││Core    ││ providers/   │
│FileReader││Command ││ OllamaProvider│
│(FS ops)││Executor││ (HTTP Ollama)│
└────────┘└────────┘└──────────────┘
    │
    ▼
┌──────────────┐
│  RAGManager  │  ← embeddings + busca semântica
│(all-minilm)  │
└──────────────┘
```

---

## 🔒 Segurança

- `CommandExecutor` usa `spawn` com `shell: false` — previne injeção de comandos shell
- `FileReader` silencia erros em diretórios/arquivos sem permissão durante buscas recursivas
- `RAGManager` só reindexa se houver mudanças (hash dos arquivos) — evita I/O desnecessário
- Sem dependências externas em produção (apenas módulos nativos do Node.js)

---

## 🔧 Tópico 11 — Function Calling / Tool Use (ReAct Loop)

### Arquitetura do ToolRegistry

O `ToolRegistry` (`src/core/ToolRegistry.ts`) implementa um registro de ferramentas (tools) no formato JSON Schema, compatível com o padrão OpenAI/Function Calling.

```
┌─────────────────────────────────────────────────────────┐
│                   ToolRegistry                          │
│  tools: Map<                                            │
│    string,                // nome da tool               │
│    { definition, handler }                              │
│  >                                                      │
│                                                         │
│  + register(name, description, paramsSchema, handler)   │
│  + getDefinitions(): string   // JSON Schema p/ prompt  │
│  + execute(name, args): Promise    // chama handler     │
│  + hasTool(name): boolean                               │
└─────────────────────────────────────────────────────────┘
```

**Tools registradas:**
| Tool | Descrição | Parâmetros |
|------|-----------|------------|
| `readFile` | Lê conteúdo completo de um arquivo | `filePath: string` |
| `readDir` | Lista entradas de um diretório | `dirPath: string` |
| `execute` | Executa comando shell (seguro: `shell:false`) | `command: string`, `args: string[]` |

### ReAct Loop (Reasoning + Acting)

O comando `chat` com `--json` implementa o padrão ReAct:

```
Usuário: "Qual o conteúdo do package.json?" --json

1. System Prompt → envia definições JSON Schema das tools +
   regras de resposta: tool_call + args OU final_response
   
2. Modelo responde:
   {"tool_call": "readFile", "args": {"filePath": "package.json"}}

3. ToolRegistry.execute("readFile", {filePath: "package.json"})
   → lê o arquivo real → resultado alimentado de volta no prompt

4. Modelo responde:
   {"final_response": "O package.json contém..."}
   
5. CLI exibe a resposta final
```

**Mecanismos de segurança e robustez:**
| Mecanismo | Descrição |
|-----------|-----------|
| `ToolRegistry.execute()` | Verifica se a tool existe antes de executar — nunca expõe handlers dinamicamente |
| `CommandExecutor.execute()` | Usa `spawn` com `shell: false` — previne injeção de comandos |
| Limite de iterações | Máximo 5 iterações no ReAct Loop (evita loops infinitos) |
| Detecção de loop | Rastreia chamadas repetidas da mesma tool com mesmos args; força `final_response` |
| Última iteração forçada | Na 5ª iteração, injeta instrução para o modelo sintetizar resposta final |
| `JSON.parse()` + `try/catch` | Toda resposta do modelo é validada como JSON antes de ser processada |

### Exemplo de uso

```bash
# Modo ReAct: o agente decide quais ferramentas usar
npm run dev -- chat "Qual o conteúdo do package.json?" --json --model llama3.2:1b

# Modo texto normal (sem ferramentas)
npm run dev -- chat "Explique o que é SOLID" --model llama3.2:3b
```

### Limitações conhecidas

- **Modelos 1B–3B** podem não seguir o schema JSON perfeitamente. O sistema inclui fallbacks (resposta crua se JSON inválido, força de resposta final na última iteração).
- **Modelos 7B+ são recomendados** para uso consistente do ReAct Loop em produção.
- O formato `final_response` pode conter JSON parcial/alucinado em modelos muito pequenos.

---

## 🔧 Tópico 12 — Self-Correction / Reflection Layer (Reflector)

### Visão Geral

O **Reflector** (`src/core/Reflector.ts`) é uma camada opcional de auto-correção que entra em ação **após** a resposta final ser gerada pelo ReAct Loop. Ele submete a resposta a um sistema de crítica (segunda chamada ao mesmo modelo, com baixa temperatura, via `ICritiqueProvider`), e retorna uma versão corrigida se problemas forem detectados.

### Arquitetura SOLID

```
┌──────────────────────────────────────────────────────────────────┐
│                         ReActLoop                                 │
│                                                                  │
│  1. execute() → modo texto ou JSON                               │
│  2. Obtém finalAnswer + iterations                               │
│  3. Se --reflect=true → applyReflection()                        │
│       └── Reflector.reflect(finalAnswer, model)                  │
│       └── Retorna ReflectionResult                               │
│          { finalContent, correctionStatus, errors }              │
│  4. ReActLoop mescla resultado no ReActResult                    │
│                                                                  │
│  Propriedades no resultado:                                      │
│  - correctionStatus: 'stable' | 'suspicious' | 'rejected'       │
│  - errors: ReflectionError[]                                     │
└──────────────────────────────────────────────────────────────────┘
```

### Interfaces

```typescript
// ICritiqueProvider — ISP: especializado para crítica (não IProvider genérico)
interface ICritiqueProvider {
  readonly name: string;
  critique(request: CritiqueRequest): Promise<CritiqueResponse>;
}

// CorrectionStatus — três estados (substitui wasCorrected: boolean)
type CorrectionStatus = 'stable' | 'suspicious' | 'rejected';

interface ReflectionError {
  type: 'hallucination' | 'syntax' | 'inconsistency' | 'logic';
  description: string;
}

interface ReflectionResult {
  finalContent: string;         // Conteúdo corrigido (ou original)
  correctionStatus: CorrectionStatus; // Status da correção
  errors: ReflectionError[];    // Erros encontrados (vazio se nenhum)
}

// IReflectionContext — DIP: contexto formal de reflexão
interface IReflectionContext {
  enabled: boolean;
  model: string;
  temperature?: number;  // default 0.1
  verbose?: boolean;
}
```

### Prompt de Crítica (CRITICAL_SYSTEM_PROMPT) — Otimizado (TASK 9)

Reduzido de ~40 para ~20 linhas, consumindo menos tokens:

```
Analise criticamente a resposta abaixo.

REGRAS:
- Alucinação: API/biblioteca/função que não existe no Node.js nativo
- Erro sintaxe: JSON inválido, await sem async, chaves desbalanceadas
- Ferramenta inválida: usar {TOOLS_LIST} com nome ou parâmetro errado

FORMATO RESPOSTA (JSON puro):
{"hasError":bool,"errors":[...],"correctedOutput":"string"}

REGRAS EXTRAS:
- Se não houver erro: hasError=false, correctedOutput=resposta original
- NÃO invente erros nem adicione info nova na correção
```

### Circuit Breaker (TASK 3) — Dice Coefficient

O Circuit Breaker valida a qualidade da correção antes de aceitá-la:

1. **Se similaridade > 90%** entre original e corrigido → `correctionStatus: 'suspicious'` (falso positivo — mantém original)
2. **Se similaridade < 20%** → `correctionStatus: 'suspicious'` (alucinação do crítico — descarta correção)
3. **Entre 20% e 90%** → `correctionStatus: 'stable'` (correção válida — aceita)

O algoritmo usa **Dice Coefficient sobre bigramas** — leve, rápido e eficaz para detectar mudanças reais.

### ErrorJournal (TASK 6) — Persistência

O `ErrorJournal` (`src/core/ErrorJournal.ts`) persiste erros de reflexão em disco:

```typescript
class ErrorJournal {
  addEntry(entry: Omit<ErrorJournalEntry, 'timestamp'>): void;
  getRecentEntries(options?: { limit?: number; type?: string }): ErrorJournalEntry[];
  getStats(): { total: number; byType: Record<string, number> };
}
```

- Formato JSON versionado (`version: 1`)
- Arquivo: `.soberano/error-journal.json`
- FIFO: máximo 1024 entradas
- Tolerante a falhas: arquivo corrompido ou versão inválida → retorna vazio

### Mecanismos de Robustez

| Mecanismo | Descrição |
|-----------|-----------|
| **Fallback silencioso** | Se o JSON de retorno for inválido ou timeout, retorna original com `correctionStatus: 'rejected'` |
| **Flag `--reflect`** | Só ativa se explicitamente solicitado pelo usuário |
| **String vazia** | Se `finalAnswer` for vazio, não perde tempo com crítica |
| **Baixa temperatura** | `temperature: 0.1` → resposta mais determinística |
| **Circuit Breaker** | Dice Coefficient 0.2–0.9 previne falsos positivos e alucinações do crítico |
| **ErrorJournal** | Persistência de erros com limite FIFO e tolerância a corrupção |
| **Flag `--verbose`** | Exibe detalhes da correção no stderr |
| **Sem dependências** | Zero dependências externas — apenas `ICritiqueProvider` injetado |

### Exemplo de uso

```bash
# Chat com auto-correção ativada
npm run dev -- chat "Explique SOLID" --reflect

# ReAct + JSON + auto-correção + verbose
npm run dev -- chat "Qual o conteúdo do package.json?" --json --reflect --verbose

# Chat normal (sem reflexão)
npm run dev -- chat "Explique SOLID"
```

> **Nota:** A reflexão dobra o número de chamadas ao modelo (1 para gerar + 1 para criticar). Em hardware de 12GB RAM, o overhead é de ~2-5 segundos adicionais por resposta. Recomendado para respostas importantes ou quando a precisão é crítica.

---

## 🔧 Tópico 13 — Retrieval-Augmented Generation (RAG)

### Visão Geral

O RAG (Retrieval-Augmented Generation) permite que o modelo responda perguntas com base no conteúdo real de arquivos do projeto. O pipeline funciona em 3 etapas:

1. **Indexação:** arquivos `.ts`, `.js`, `.json`, `.md`, `.txt` são divididos em chunks de 512 caracteres (com overlap de 64) e cada chunk é convertido em um vetor de embedding 384-dim usando o modelo `all-minilm`
2. **Busca semântica:** a pergunta do usuário é convertida no mesmo espaço vetorial. Usamos **cosine similarity** para encontrar os 5 chunks mais relevantes
3. **Injeção no contexto:** os chunks são formatados como `[arquivo:linha]` e injetados no system prompt do modelo, que responde com base nesse contexto

### Arquivo: `src/core/RAGManager.ts`

```
┌──────────────────────────────────────────────────────────────────┐
│                       RAGManager                                  │
│                                                                   │
│  + ensureIndex(dir): Promise<void>                                │
│    └── percorre arquivos → chunk (512 chars, overlap 64)          │
│    └── gera hash dos arquivos → detecta mudanças                  │
│    └── só reindexa se houver modificação                          │
│    └── salva cache em .soberano/index.json                        │
│                                                                   │
│  + retrieve(query, dir): Promise<SearchMatch[]>                   │
│    └── gera embedding da query (all-minilm)                       │
│    └── calcula cosine similarity contra todos os chunks           │
│    └── retorna top 5 com score + arquivo + linha + conteúdo       │
│                                                                   │
│  + formatContext(matches): string                                 │
│    └── formata como: "--- [arquivo:linha] ---\nconteúdo"          │
│                                                                   │
│  Interface SearchMatch:                                           │
│    { file, line, content, score: number }                         │
│                                                                   │
│  Interface ChunkEntry (cache):                                    │
│    { text, file, line, embedding: number[] }                      │
└──────────────────────────────────────────────────────────────────┘
```

### Pipeline RAG no comando `chat`

```
Usuário: "Como instalar o projeto?" --rag .

1. RAGManager.ensureIndex(".")
   └── indexa .ts, .js, .json, .md, .txt → chunks → embeddings → cache

2. RAGManager.retrieve("Como instalar o projeto?", ".")
   └── embedding da query → cosine similarity → top 5 chunks

3. RAGManager.formatContext(top5)
   └── "[DOCUMENTACAO_PROJETO.md:138] npm run dev -- chat..."

4. System Prompt injetado:
   ────────────────────────────────────────────
   DOCUMENTOS RELEVANTES PARA A PERGUNTA:
   [DOCUMENTACAO_PROJETO.md:138] npm run dev -- chat...
   ...
   ────────────────────────────────────────────

5. Modelo responde com base nos documentos reais
```

### Mecanismos de robustez

| Mecanismo | Descrição |
|-----------|-----------|
| Cache inteligente | `.soberano/index.json` armazena embeddings + hash dos arquivos. Reindexa apenas se houver mudanças |
| DOCUMENTACAO_PROJETO.md prioritário | O arquivo de documentação é indexado primeiro, garantindo que esteja sempre presente |
| Fallback silencioso | Se o embedding falhar ou diretório não existir, o chat continua sem contexto RAG |
| Zero dependências | Embeddings usam `node:http` + `JSON.parse` — sem bibliotecas externas |
| Chunking com overlap | 512 chars com 64 de overlap evita perda de contexto entre chunks |

### Exemplo de uso

```bash
# Indexa o diretório atual e responde com base na documentação real
npm run dev -- chat "Como instalar o projeto?" --rag .

# Indexa um diretório específico
npm run dev -- chat "Qual a estrutura do código?" --rag ./src

# Funciona com ou sem --json (modo RAG puro ou ReAct + RAG)
npm run dev -- chat "Explique a arquitetura" --rag . --json
```

### Requisitos

- Modelo **all-minilm** instalado no Ollama (baixado automaticamente no primeiro uso)
- Modelo de chat (ex: `llama3.2:1b`, `phi3:3b`) para gerar respostas
- Diretório com arquivos de texto `.ts`, `.js`, `.json`, `.md`, `.txt`

---

## 🔧 Tópico 14 — Memória Episódica / Multi-turn (Sessões de Conversa)

### Visão Geral

A **Memória Episódica** permite que o Soberano-Core mantenha o contexto de conversas entre múltiplas execuções do terminal. Cada sessão de chat é salva em disco como um arquivo `.json` dentro do diretório `.soberano/sessions/`, e pode ser retomada em qualquer momento usando a flag `--session <id>`.

### Arquitetura SOLID

A feature foi implementada com duas classes de responsabilidade única:

```
┌───────────────────────────────────────────────────────────────┐
│                      SessionStore                              │
│  (Persistência — SRP: ler/escrever arquivos .json em disco)    │
│                                                               │
│  Diretório: .soberano/sessions/<uuid>.json                    │
│                                                               │
│  + save(session): Promise<void>                               │
│  + load(sessionId): Promise<Session | null>                   │
│  + list(): Promise<SessionSummary[]>                          │
│  + delete(sessionId): Promise<void>                           │
└───────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌───────────────────────────────────────────────────────────────┐
│                      SessionManager                            │
│  (Orquestração — SRP: gerenciar sessão ativa em memória)      │
│                                                               │
│  + addMessage(msg, model): Promise<void>                      │
│  + getHistory(): ChatMessage[]                                │
│  + newSession(model): void                                    │
│  + loadSession(sessionId): Promise<boolean>                   │
│  + flush(): Promise<void>                                     │
│  + listSessions(): Promise<SessionSummary[]>                  │
│  + deleteSession(sessionId): Promise<void>                    │
│  + getSessionId(): string | null                              │
└───────────────────────────────────────────────────────────────┘
```

### Interface SessionStore (`ISessionStore` — DIP)

A `SessionStore` agora implementa a interface `ISessionStore`, seguindo o **Dependency Inversion Principle**. O `SessionManager` depende da abstração (`ISessionStore`), não da implementação concreta.

```typescript
interface ISessionStore {
  save(session: Session): Promise<void>;
  load(sessionId: string): Promise<Session | null>;
  list(): Promise<SessionSummary[]>;
  delete(sessionId: string): Promise<void>;
}

interface Session {
  id: string;                    // UUID v4
  createdAt: string;             // ISO 8601
  updatedAt: string;             // ISO 8601
  messages: ReActMessage[];      // Array de mensagens (tipo ReActMessage do ReActLoop)
  metadata?: {                   // Metadados opcionais agrupados
    model?: string;              // Modelo utilizado (ex: "llama3.2:1b")
    title?: string;              // Título auto-extraído da 1ª mensagem (máx 80 chars)
  };
}

interface SessionSummary {
  id: string;
  createdAt: string;
  updatedAt: string;
  messageCount: number;
  title?: string;
}
```

> **Mudanças:** `messages` agora usa `ReActMessage[]` (tipo compartilhado com `ReActLoop`), `model` e `title` foram movidos para `metadata` aninhado, `title` pode ser `undefined` em vez de `null`, e a listagem usa `Promise.allSettled` para paralelismo com tolerância a falhas.

### Fluxo de uso (exemplo real)

```bash
# Primeira interação → cria sessão automaticamente
npm run dev -- chat "Qual a capital do Brasil?"

# O SessionManager cria a sessão, adiciona a mensagem,
# executa o chat, adiciona a resposta, faz flush().
# Arquivo gerado: .soberano/sessions/<uuid>.json

# Segunda interação: retomar a conversa
npm run dev -- chat "E a população dela?" --session <uuid>

# O SessionManager carrega o histórico (role: 'user'/'assistant'),
# o ReActStrategy/StreamStrategy chama getHistory() e
# constrói o prompt completo com todo o contexto anterior.

# Forçar nova sessão (ignorar --session)
npm run dev -- chat "Nova conversa" --new-session

# Listar todas as sessões
npm run dev -- sessions
# Saída:
#   abc12345...  09/05/2026 14:30:00  [3 msgs]  Qual a capital do Brasil?
```

### Integração com o fluxo de chat

No `commands.ts`, o fluxo do comando `chat` agora inclui:

1. **Antes da execução:** `sessionManager.addMessage({ role: 'user', content: prompt })` — adiciona a pergunta do usuário ao histórico
2. **Injeção de contexto:** A estratégia (ReActStrategy/StreamStrategy) chama `sessionManager.getHistory()` para obter todas as mensagens anteriores e as converte no formato `role: "user"/"assistant"` para o prompt multi-turn
3. **Após a execução:** `sessionManager.addMessage({ role: 'assistant', content: result })` — adiciona a resposta ao histórico
4. **Persistência:** `sessionManager.flush()` — salva o arquivo `.json` em disco

### Mecanismos de robustez

| Mecanismo | Descrição |
|-----------|-----------|
| **Criação automática** | `addMessage()` sem sessão ativa → cria nova sessão automaticamente com UUID v4 |
| **Extração de título** | A primeira mensagem do usuário vira o título (truncado em 80 caracteres) |
| **Flush explícito** | A sessão só é persistida quando `flush()` é chamado — evita I/O em cada mensagem |
| **Criação de diretório** | `mkdir({ recursive: true })` garante que `.soberano/sessions/` exista |
| **Arquivo corrompido** | `SessionStore.load()` retorna `null` se JSON for inválido (não quebra o CLI) |
| **Sessão inexistente** | `loadSession()` retorna `false` se não encontrar o arquivo |
| **Zero dependências** | UUID v4 gerado com `crypto.randomUUID()` (nativo do Node.js 19+) |
| **Data ISO 8601** | Timestamps gerados com `new Date().toISOString()` |

### Exemplos de uso

```bash
# Fluxo multi-turn completo
npm run dev -- chat "Explique o que é SOLID" --model phi3:3b
# → Sessão criada automaticamente (mostra o UUID no stderr)

npm run dev -- chat "Dê um exemplo de cada princípio" --session <uuid>
# → Contexto preservado da conversa anterior

# Listar sessões
npm run dev -- sessions

# Forçar nova sessão
npm run dev -- chat "Nova conversa" --new-session
```

---

## 🔧 Tópico 15 — GraphRAG: Busca Híbrida Vetorial + Grafo de Conhecimento

### Visão Geral

O **GraphRAG** estende o RAG clássico com um **grafo de conhecimento** que mapeia relações estruturais entre arquivos do projeto: imports, exports, herança de classes e chamadas de funções. A busca híbrida combina similaridade vetorial (chunks semânticos) com navegação no grafo (relações topológicas) para resultados mais precisos.

### Arquitetura SOLID

```
┌──────────────────────────────────────────────────────────────────┐
│                    RAGManager (orquestrador)                       │
│                                                                   │
│  ensureIndex(dir)                                                 │
│    └── Chunker → chunks                                           │
│    └── Embedder → embeddings                                      │
│    └── GraphBuilder.build(chunks) → KnowledgeGraph                │
│    └── JsonGraphStore.save(graph)                                 │
│    └── VectorStore.save(embeddings)                               │
│                                                                   │
│  retrieve(query, dir)                                             │
│    └── Retriever → top 5 chunks (vetorial)                        │
│    └── GraphRAGManager.enhance(query, top5, graph) → merged       │
│    └── Retorna SearchMatch[] combinado                            │
└──────────────────────────────────────────────────────────────────┘
```

### Interfaces e Classes (SRP + ISP)

| Interface/Classe | Arquivo | Responsabilidade |
|-----------------|---------|-----------------|
| `KnowledgeGraph`, `GraphNode`, `GraphEdge` | `types.ts` | Tipos de domínio do grafo |
| `IGraphStore` | `IGraphStore.ts` | Contrato de persistência do grafo |
| `IRelationshipExtractor` | `IRelationshipExtractor.ts` | Contrato de extração de relações de texto |
| `IGraphQuery` | `IGraphQuery.ts` | Contrato de consulta (busca por similaridade + adjacência) |
| `JsonGraphStore` | `JsonGraphStore.ts` | Persistência do grafo em `.soberano/graph.json` (SRP) |
| `ASTRelationshipExtractor` | `ASTRelationshipExtractor.ts` | Extrai imports/exports/herança do AST TypeScript (SRP) |
| `GraphBuilder` | `GraphBuilder.ts` | Constrói o grafo a partir de chunks usando extractors (SRP) |
| `GraphRAGManager` | `GraphRAGManager.ts` | Busca híbrida: vetorial + adjacência no grafo (SRP) |

### KnowledgeGraph — Estrutura de Dados

```typescript
interface GraphNode {
  id: string;              // "src/core/ReActLoop.ts"
  label: string;           // "ReActLoop"
  type: 'file' | 'class' | 'function' | 'interface' | 'variable';
  filePath: string;
  line: number;
}

interface GraphEdge {
  source: string;          // node id de origem
  target: string;          // node id de destino
  relationship: 'imports' | 'exports' | 'extends' | 'implements' | 'calls';
}

interface KnowledgeGraph {
  nodes: Map<string, GraphNode>;
  edges: GraphEdge[];
}
```

### Pipeline GraphRAG no RAGManager.ensureIndex()

1. **Chunking:** Arquivos são divididos em chunks (512 chars, overlap 64)
2. **Embeddings:** Cada chunk é convertido em vetor 384-dim (all-minilm)
3. **GraphBuilder.build(chunks):**
   - Para cada chunk, extrai relações via `ASTRelationshipExtractor.extract(chunk.text)`
   - Converte relações em nós e arestas do grafo
   - Deduplica nós e arestas por ID único
4. **Persistência:** Grafo salvo em `.soberano/graph.json`, embeddings em `.soberano/index.json`

### Pipeline de Busca Híbrida (GraphRAGManager.enhance())

```
Usuário: "Como o ReActLoop chama o ToolRegistry?"

1. Retriever.query → top 5 chunks por cosine similarity
2. GraphRAGManager.enhance(query, top5, graph):
   a. Para cada chunk nos top5, busca nós adjacentes no grafo
   b. Adiciona chunks dos nós vizinhos (com decay de score * 0.8)
   c. Limita a no máximo 8 chunks no total
   d. Reordena por score descendente
3. Retorna top 5 (ou menos) da lista mesclada
4. PromptBuilder injeta no contexto
```

### Mecanismos de robustez

| Mecanismo | Descrição |
|-----------|-----------|
| **Fallback vetorial puro** | Se o grafo não existir ou `enhance()` lançar erro, cai para busca vetorial clássica |
| **Sem dependências** | O AST extractor usa `node:fs` + regex para detectar imports/exports (sem parser TypeScript real) |
| **Cache do grafo** | `JsonGraphStore.save()` só persiste se houve mudanças (hash dos chunks) |
| **Decay de score** | Chunks do grafo entram com score * 0.8 para não dominarem os puramente vetoriais |
| **Limite de expansão** | Máximo 8 chunks no total pós-expansão (evita lentidão em projetos grandes) |
| **Arquivo corrompido** | `load()` retorna `null` se JSON for inválido → `GraphRAGManager` usa fallback |

### Exemplo de uso

```bash
# RAG clássico + GraphRAG (automático, sem flag extra)
npm run dev -- chat "Como o ToolRegistry registra ferramentas?" --rag .

# O GraphRAGManager.enhance() expande os top5 chunks com
# nós vizinhos no grafo (arquivos que importam ToolRegistry)
```

---

## 🧪 Testes Unitários

A suíte de testes usa **Vitest** (v4.1.5) e está organizada em `tests/unit/`, espelhando a estrutura do `src/`.

### Cobertura atual

| Módulo | Arquivo | Testes | O que cobre |
|--------|---------|--------|-------------|
| **Core** | `tests/unit/core/ToolRegistry.test.ts` | 9 | Registro de tools, execução com/sem parâmetros, validação de existência |
| **Core** | `tests/unit/core/CommandExecutor.test.ts` | 3 | Execução de comandos, captura stderr, erro de spawn |
| **Core** | `tests/unit/core/Reflector.test.ts` | 12 | Instanciação, resposta correta (sem correção), resposta incorreta (com correção), resposta vazia/só espaços, JSON inválido, múltiplos erros, integração com toolRegistry, fallback de rede, Circuit Breaker (similaridade >90%, <20%), ICritiqueProvider |
| **Core** | `tests/unit/core/ErrorJournal.test.ts` | 8 | Criação, persistência, ordenação, filtro por tipo, limite FIFO, stats, arquivo corrompido, versão inválida |
| **Providers** | `tests/unit/providers/OllamaProvider.test.ts` | 12 | Chat com parâmetros, format=json, erro HTTP, embed. **Streaming:** tokens individuais, body com stream:true, erro HTTP no stream, linhas vazias/não-JSON, chunks TCP quebrados |
| **RAG** | `tests/unit/rag/Retriever.test.ts` | 13 | Cosine similarity, rankeamento, ordenação, busca vazia |
| **RAG** | `tests/unit/rag/ReActLoop.test.ts` | 26 | Text Mode, JSON Mode, Reflector, Streaming. Loop com mensagens tipadas via ReActMessage |
| **RAG** | `tests/unit/rag/Chunker.test.ts` | 8 | Chunking por parágrafo, sentença, overlap, limite de chunks |
| **RAG** | `tests/unit/rag/TypescriptASTAdapter.test.ts` | 19 | Roteamento de extensões (.ts, .js, .tsx, .jsx, .mjs, .cjs), fallback para não-código, parse de classes/funções/imports/exports, ASTNode structure, empty file handling |
| **Graph** | `tests/unit/graph/JsonGraphStore.test.ts` | 20 | CRUD do grafo, persistência JSON, operações de escrita (IGraphStore) |
| **Graph** | `tests/unit/graph/ASTRelationshipExtractor.test.ts` | 17 | Extração de relações de imports/exports/herança via AST |
| **Graph** | `tests/unit/graph/GraphBuilder.test.ts` | 9 | Construção do grafo a partir de chunks, extração cíclica de relações |
| **Graph** | `tests/unit/graph/GraphRAGManager.test.ts` | 12 | Busca híbrida (vetorial + grafo), fallback para vetorial puro, convergência de resultados |
| **RAG** | `tests/unit/rag/ASTChunkerService.test.ts` | 9 | Extension routing (AST vs fallback), AST parse results, large class subdivision (métodos como chunks individuais), large non-class node subdivision, MAX_CHUNKS_PER_FILE limit |
| **Validation** | `tests/unit/validation/JsonValidator.test.ts` | 13 | validate(), tryValidate(), ValidationError |
| **CLI** | `tests/unit/cli/commands.test.ts` | 15 | Comandos read/dir/search/exec/chat, flags --stream/--json/--no-think, fallback streaming direto, erro de comando faltando |

**Total: 308 testes, todos passando.**

### Estratégia de Mocks (zero I/O real)

| Módulo mockado | Técnica | Classe testada |
|----------------|---------|----------------|
| `node:http` | `vi.mock('node:http', ...)` — intercepta `http.request()` | `OllamaProvider` |
| `node:child_process` | `vi.mock('node:child_process', ...)` — intercepta `cp.spawn()` | `CommandExecutor` |
| `IProvider` + `CommandExecutor` + `ToolRegistry` | Mock de interface/classes reais injetadas no construtor com `vi.fn()` | `ReActLoop` |

Nenhum teste faz chamadas reais ao Ollama, executa comandos shell reais ou acessa o sistema de arquivos além do necessário para importar módulos TypeScript. **Consumo de hardware: ~50MB RAM, 0% CPU para modelos.**

### Scripts npm

| Script | Comando | Descrição |
|--------|---------|-----------|
| `test` | `vitest run` | Executa todos os testes uma vez |
| `test:watch` | `vitest` | Executa testes em modo watch (desenvolvimento) |

### Executando

```bash
npm test              # Executa todos os testes
npx vitest run        # Equivalente
npx vitest run tests/unit/providers/  # Apenas providers
npx vitest            # Modo watch (recarrega automático)
```

---

## 🚀 Soberano OS (v2.0.0)

### Visão Geral

O **Soberano OS** é a evolução do Soberano-Core para uma arquitetura **Monorepo** multiplataforma. Agora dividido em dois pacotes principais — `packages/core` e `packages/shell` — o projeto expande seu alcance do terminal para o desktop, mantendo os princípios de **zero dependências externas** e **soberania digital**.

### Fase 1: Encapsulamento Desktop ✅

A Fase 1 foi iniciada com sucesso, estabelecendo a base para uma aplicação desktop completa:

| Componente | Tecnologia | Descrição |
|------------|-----------|-----------|
| **Monorepo** | NPM Workspaces | Gerenciamento unificado de dependências e scripts entre `core` e `shell` |
| **Core (IA Engine)** | TypeScript + Vitest | Motor de IA encapsulado como pacote independente com 308 testes unitários |
| **Shell Desktop** | Electron + Vite + React + TypeScript | Interface gráfica desktop que consome o core via Node.js bindings |

### Estrutura de Pastas (v2.0.0)

```
AgenteIA/
├── package.json                   # Monorepo root (NPM Workspaces)
├── .gitignore                     # Arquivos ignorados pelo Git
├── DOCUMENTACAO_PROJETO.md        # Esta documentação
├── packages/
│   ├── core/                      # Motor de IA (zero dependências externas)
│   │   ├── package.json           # @soberano/core
│   │   ├── tsconfig.json          # TypeScript config para core
│   │   ├── vitest.config.ts       # Configuração do Vitest
│   │   ├── src/                   # Código-fonte do core
│   │   │   ├── cli/               # CLI (comandos + estratégias)
│   │   │   ├── core/              # Core (FileReader, CommandExecutor, RAG, etc.)
│   │   │   ├── providers/         # Providers (Ollama, etc.)
│   │   │   └── validation/        # Validação JSON
│   │   └── tests/                 # Testes unitários e de integração
│   │       ├── unit/              # 308 testes unitários (Vitest)
│   │       └── integration/       # Testes de integração
│   │
│   └── shell/                     # Desktop Shell (Electron + React)
│       ├── package.json           # @soberano/shell
│       ├── index.html             # Entrypoint HTML do Vite
│       ├── vite.config.ts         # Configuração do Vite
│       ├── tsconfig.json          # TypeScript config (React)
│       ├── tsconfig.electron.json # TypeScript config (Electron main process)
│       ├── electron/              # Processo principal do Electron
│       │   ├── main.ts            # Main process (BrowserWindow + IPC)
│       │   ├── preload.ts         # Preload script (contextBridge)
│       │   └── dev-runner.ts      # Runner para desenvolvimento (concurrently)
│       └── src/                   # Renderer process (React)
│           ├── main.tsx           # Entrypoint React (DOM mount)
│           ├── App.tsx            # Componente principal App
│           └── vite-env.d.ts      # Declarações de tipo Vite
```

### Arquitetura do Soberano OS

```
                         ┌──────────────────────────────────┐
                         │         Monorepo Root             │
                         │    (NPM Workspaces)               │
                         │    scripts: build, dev, test      │
                         └──────────┬───────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
                    ▼                               ▼
        ┌──────────────────────┐       ┌──────────────────────────┐
        │   @soberano/core     │       │    @soberano/shell       │
        │   (Node.js Library)  │──────▶│  (Electron + React)      │
        │                      │       │                          │
        │  - FileReader        │       │  - Electron Main Process │
        │  - CommandExecutor   │       │  - IPC Bridge            │
        │  - RAGManager        │       │  - React UI (Vite)       │
        │  - ReActLoop         │       │  - Preload Security      │
        │  - ToolRegistry      │       │                          │
        │  - Providers         │       │  Depende de:             │
        │  - CLI               │       │  @soberano/core (local)  │
        └──────────────────────┘       └──────────────────────────┘
```

### Por que Monorepo?

| Benefício | Descrição |
|-----------|-----------|
| **Código compartilhado** | O `shell` importa `@soberano/core` como dependência local via NPM Workspaces — sem necessidade de publicar pacote |
| **Build unificado** | `npm run build` na raiz compila ambos os pacotes com um único comando |
| **Testes centralizados** | `npm test` executa os 308 testes do core + futuros testes do shell |
| **Versionamento atômico** | Um único commit versiona mudanças no core e no shell simultaneamente |
| **Zero dependências externas no core** | O pacote `@soberano/core` continua sem dependências externas em produção |

### Status da Fase 1

- [x] Setup do Monorepo (NPM Workspaces) com `packages/core` e `packages/shell`
- [x] Integração Electron + Vite + React + TypeScript no `packages/shell`
- [x] Core refatorado para pacote independente (`@soberano/core`)
- [x] 308 testes unitários do core migrados e passando
- [ ] **Próximo passo:** Implementar IPC Bridge entre Electron e Core

---

## 📌 Status Atual

✅ Projeto estruturalmente completo com:
- Core funcional (leitura de arquivos, busca textual, execução de comandos)
- **ToolRegistry** com 3 tools registradas (readFile, readDir, execute)
- **ReAct Loop** — agente decide automaticamente quando usar ferramentas
- **RAG (Retrieval-Augmented Generation)** — indexação de diretórios com embeddings + busca semântica + injeção de contexto
- Integração com Ollama via HTTP (chat + embeddings)
- CLI funcional com 6 comandos (read, dir, search, exec, chat, help)
- Grammar Restraint / Structured Outputs — força modelos a responderem em JSON estrito via `--json`
- Validação de robustez com `JSON.parse()` + `try/catch` para prevenir alucinações
- Detecção de loops com força de resposta final na última iteração
- Injeção automática de system prompt com definições JSON Schema das tools
- Arquitetura modular e extensível (interface `IProvider` permite novos providers)
- Zero dependências externas em produção (apenas `node:http`, `node:fs/promises`, `node:child_process`)
- TypeScript configurado com strict mode
- ✅ **SOLID implementado** — SRP (classes coesas), OCP (ProviderFactory), LSP (IProvider), ISP (interfaces enxutas), DIP (AppContext + injeção de dependências + ISessionStore + RAGManager DI)
- ✅ **FileReader.resolveSecurePath()** tornado assíncrono com `fs.realpath()` — mitigação contra Symlink Attacks (C-1)
- ✅ **SessionStore** refatorada com interface `ISessionStore` (DIP) e `ReActMessage` tipada do `ReActLoop`
- ✅ **RAGManager** refatorado com injeção de dependências via construtor (DIP) — removeu `PromptBuilder.ts` (arquivo deletado, duplicado)
- ✅ **ASTEditor e SearchReplaceEditor** corrigidos para usar `await` em `resolveSecurePath()`
- ✅ **308 testes unitários** passando com Vitest (25 arquivos de teste: ToolRegistry, CommandExecutor, Reflector, ErrorJournal, SessionStore, SessionManager, OllamaProvider, Retriever, ReActLoop, Chunker, TypescriptASTAdapter, ASTChunkerService, JsonValidator, commands, JsonGraphStore, ASTRelationshipExtractor, GraphBuilder, GraphRAGManager, ChatStrategy, ASTEditor, SearchReplaceEditor, TokenEstimator, StatefulCompressor, IContextCompressor, GraphRAGManager)

📝 **Possíveis próximos passos (não implementados):**
- [já implementado] ~~Adicionar streaming de respostas do Ollama (SSE)~~
- [já implementado] ~~Adicionar camada de auto-correção (Reflector + --reflect)~~
- [já implementado] ~~Adicionar suporte a sessões/conversa com histórico (multi-turn)~~
- [já implementado] ~~Adicionar GraphRAG — busca híbrida vetorial + grafo de conhecimento~~
- Implementar novos providers (OpenAI, Anthropic, etc.)
- Expandir ToolRegistry com mais ferramentas (writeFile, searchFiles, etc.)
- Melhorar chunking com overlap adaptativo por estrutura (AST-aware)
- Adicionar reranking multi-stage para melhorar precisão da busca
- Suporte a PDF, DOCX e outros formatos no RAG





================================================================
End of Codebase
================================================================
